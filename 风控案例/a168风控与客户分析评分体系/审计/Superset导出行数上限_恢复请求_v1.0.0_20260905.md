# Superset SQL Lab 导出行数上限 — 恢复请求

**日期**：2026-09-05　**申请人**：Ryo Eng（风控与客户分析）　**账号角色**：`sr_superset`

---

## 一、事由

数日前 Superset 宕机；恢复后，SQL Lab 之导出行数上限由 **100,000 降为 10,000**。

本部门正在进行 `R03b_player_dealer_daily` 之全量导出，此变更使同一件工作的**人工操作次数增加十倍**。

---

## 二、量化影响（皆为实测，非估计）

| 项 | 值 | 来源 |
|---|---:|---|
| 该表总行数 `T_true` | **18,139,550** | 2026-09-05 在库实测（7.800 s 之聚合查询） |
| 已导出 | 417 批 ／ 4,170,000 行（22.99%） | 现档清点 |
| 单批查询耗时 | 48.062 s | 2026-09-05 实测（第 100 批） |

| | 上限 10,000（现况） | 上限 100,000（宕机前） |
|---|---:|---:|
| 尚余批数 | **1,397** | **140** |
| 纯查询时长 | 18.7 小时 | 18.7 小时（**不变**） |
| 含人工操作（90 秒/批） | **34.9 小时** | 3.5 小时 |
| 含人工操作（120 秒/批） | **46.6 小时** | 4.7 小时 |

> **⇒ 多出 1,257 次人工操作、多耗 31 ~ 42 小时人力。**
> 请留意：**纯查询时长完全不变** —— 数据库端的负载与上限无关，增加的纯粹是「点击、等待、下载、归档」的重复劳动。

---

## 三、本部门已自行做的减负工作（供参考，非邀功）

在提出本请求之前，已先自行把能优化的都优化了，且每一项皆经**逐字节验收**：

1. **排序键裁剪**：`audit_rn` 之排序键由 26 键裁为 5 键，经在库实测证明全域安全（`n_dup_groups = 0`），并以第 100 批导出与既有档 **MD5 逐字节比对通过**（`e1503f6e29f189f9676fd0f7a7ee580f`）。此项使单次排序缓冲减少约 **7.16 GB**。
2. **落盘（spill）**：已查明会话预设 `enable_spill = false`，改以单句提示 `/*+ SET_VAR(enable_spill=true,...) */` 开启，亦经逐字节验收通过。

**即：数据库侧能做的已经做尽，剩下的瓶颈只在导出上限这一项。**

---

## 四、请求（三择一即可，按优先次序）

### 甲、恢复 `SQL_MAX_ROW` 至 100,000（原状）

若当初调低是宕机恢复时的临时保护措施，恳请评估是否可恢复。

若顾虑「大结果集会再压垮实例」，谨附一点说明：本部门的查询**每一批都以 `WHERE audit_rn > A AND <= B` 严格框定区间**，返回行数恒定、可预期，不存在无界扫描；且已实测确认数据库端负载与导出上限无关。

### 乙、仅对本账号／本用途放宽

若不宜全局恢复，可否仅对 `sr_superset` 或特定角色放宽至 100,000。

### 丙、启用异步查询 ＋ Results Backend（最稳妥之作法）

此为架构上最正确的解：大结果集不再经 Web worker 内存，而由后端落地后再供下载。既可支持大导出，又不致重演宕机。若贵部门有此规划，本部门愿配合测试。

---

## 五、若三者皆不可行

本部门将循现况以 10,000 行分批完成（尚余 1,397 批），并研究以 Superset REST API 将下载环节脚本化，以减少人工看守。

惟若可行，仍恳请优先考虑**丙**——它同时解决「大导出」与「防宕机」两件事。

---

**附**：本文所引一切数字皆有实测记录可查，如需原始查询与验收脚本，本部门可随时提供。
