# v10-REDTEAM · 审计基线 v1.0.0

**性质**：对 `a168_取数与核验_SQL总包_v10.sql` 之审计结论登记册。
**边界**：本档**不改 v10 一字**。v10 是证据基线，本档是对它的认知；二者分层，永不互写。
**建立日期**：2026-08-22
**状态**：🟢 当前工作基线（v10 本体 🔒 永久冻结）

---

## 〇 · 受审本体身份（六元组）

| 项 | 值 |
|---|---|
| 文件名 | `a168_取数与核验_SQL总包_v10.sql` |
| 行数 | 9,475 |
| 字节数 | 1,195,130 |
| MD5（文件） | `62985e9b2932bd7ab70fba7ee9c01cb8` |
| MD5（LF 归一） | `4b3aaa0120aee27b17bc539fe2317189` |
| 换行 | CRLF（9,475/9,475 行实测） |
| 编码 | UTF-8 |

> 双 MD5 并登之由：文本模式读取会把 CRLF 归一为 LF，致 MD5 与文件真值不符。
> 二者相异非异常，是换行符差异之必然；引用时须指明取哪一个。

---

## 一 · 证据分层（本项目唯一体系）

**OBSERVED ／ INFERRED ／ UNKNOWN ／ CONDITIONAL** 四级。

外部六级体系（PROVEN／STRONGLY SUPPORTED／HYPOTHESIS）仅可作工作草稿，
落册前须映射回四级：PROVEN→OBSERVED，STRONGLY SUPPORTED→INFERRED，HYPOTHESIS→UNKNOWN。

**证据等级不因重新计算而自动升级。**

---

## 二 · 集合身份登记（数字不是身份，集合才是身份）

`IDENTITY = 版本/集合名 + COUNT + 成员指纹`

| 集合身份 | COUNT | MD5-12 | SHA256-12 | 状态 |
|---|---:|---|---|---|
| `v10-93` | 93 | `6c14ab6f69a8` | `f461e90b5cee` | 🔒 冻结 |
| `v11-91` | 91 | `ee9908310e58` | `04188edc0005` | 🟡 CONDITIONAL（未执行） |
| `Z13-14` | 14 | `a4b14d9f465a` | `79595cf0f996` | 🔒 历史截面·SUPERSEDED |
| `GMv1-14` | 14 | `f8429826b52c` | `c5f55727a1aa` | 🟢 本轮真 FAIL 候选 |

成员全表见 `审计/登记/a168_集合身份登记_v1.json`。

### 已登记之撞数陷阱（三例）

| 陷阱 | 内容 |
|---|---|
| 93 vs 93 | `v10-93` 与「v11＋P14/P15」曾同为 93 而集合相异；后因 P14/P15 降 R 侧而自动消解 |
| 14 vs 14 | `Z13-14` 与 `GMv1-14` 同为 14，**交集仅 7**，各独有 7 |
| 53→59 | `.GL` 术语库理论目标 59 未经去重，不得称 OBSERVED |

> 凡引裸数字（93／14／59／91）而不带集合身份者，一律不予采认。

---

## 三 · 交付件结构（OBSERVED）

| 项 | 数量 |
|---|---:|
| SQL `CREATE FUNCTION/PROCEDURE` | **0** |
| 独立导出语句 | 92 |
| 派生复制件（`P11_雷欧名单` ← §R01） | 1 |
| **唯一物理交付件** | **93** |
| ⛔ 墓碑（C-06 原版、S-04a） | 2 |
| ⚠ 备用回退（§E02 回退版，**不属墓碑**） | 1 |

**三态永不合并**：⛔ 已摘除（禁跑）／⚠ 备用回退（可跑非正版）／✅ 正版。

---

## 四 · Gate 事实层（OBSERVED · raw gate coverage）

92 条交付件语句中，**63 条直取 `ods_mariadb_2b.ods_a168_bet02`**；五闸约束仅及此 63 条。

| Gate | 有 | 无 |
|---|---:|---:|
| `bet02='101'` | 52 | 11 |
| `rn=1`（版本去重） | 40 | 23 |
| `age022='1'`（测试线） | 35 | 28 |
| `category='1'` | 42 | 21 |
| `bet38='N'` | 42 | 21 |
| `dt` 窗口字面量 | 58 | 5 |
| `ORDER BY` | 57 | 6 |
| `bet05>0` | 37 | 26 |
| `bet11>0` | 36 | 27 |
| `bet04>0` | 5 | 58 |
| `bet08` 非空（Event-Time Presence） | 19 | 44 |

> **Gate 缺失 ≠ Gate FAIL。** 此表是代码形态事实，非缺陷分布。
> 唯当条目之人工规范声明要求该 Gate 而代码未实现，方成立 FAIL。

### 四层 Gate 架构

`Identity`（`bet02`／`rn`）→ `Population`（`age022`／`category`）→
`Business State`（`bet38`）→ `Data Quality`（`bet05`／`bet08`／`bet11`／`bet04`）

### 三态定义

| 态 | 条件 |
|---|---|
| `PASS` | 代码存在，且与声明一致 |
| `WAIVER_DECLARED` | 代码缺失，但有**人工预先登记之明确豁免理由**（须点名该 Gate） |
| `FAIL` | 代码缺失，且无有效豁免 |
| `DECLARATION_MISMATCH` | 声明与代码不符（优先于其余；二者皆不得默认为准） |

**WAIVED 永久禁止由机器推定**——已由检查器代码强制。

---

## 五 · 问题登记

### P0 · P01 恒等族（🔴 最高优先）

`P01A`／`P01B`／`P01C`／`P01D`／`P01E` 五件全缺 `rn=1`。

- **污染性质有限而确凿**：`P01E` 之恒等式为**行内**运算，多版本不令「恒等」变「不恒等」；
  受污染者为 `exact_match_rate`（分母含重复）与 `abs_err` 分位（加权失真）。
- **方向性结论**（H16 成立、H41 不成立）可暂留 **INFERRED**；
  **「100% 精确匹配」之比率读数须去重重算方可称 OBSERVED**。
- **可证伪之预测**：若旧版本行为未结算中间态，去重后匹配率**只升不降**。
- **`P01C` 例外——不得机械补 `rn=1`**：其检验对象即「语义是否随版本切换」，
  补闸将摧毁被检对象。须**双跑**：Run A（原始版本集合）／Run B（`rn=1`），以差分分离
  「历史版本残留」与「语义随时间演化」两种解释。
- `age022`／`category`／`bet38` 于 P01 族拟判 WAIVED，**更优解为分层输出**
  （按 `bet38`／`category` 分组）——各层一致即得正面证据，不一致即捕获真分叉。
- **工程质量校正**：P01 五件**全部**采 `CAST(NULLIF(TRIM(x),'') AS DECIMAL(20,4))`，无裸转；
  且为原币行内比较、不涉 `bet11` 汇率，故 `DECIMAL(20,4)` 判 **PASS**。
  **P01 族应自外来血统归化债务名单移出。**
- **§Z-18 封条处置**：行 8312「结论不得再由本条重新开启」约束的是**本条 SQL 不得改**，
  非结论免于复核。§Z-18 五件原样冻结存档；去重版另立新条（在册序号承 §75／§76）。

### P1 · §Z-13 登记过期

§Z-13「交付件缺闸 14 条」立于 2026-08-13；其后 v4–v7 并入 Z 族、P01/P02 族、P11/P12/P13 族，
新增条目未受同一闸门审查。

**处置**：§Z-13 保留为**历史快照**并标 `SUPERSEDED`，不删除、不改写；
另立当前版登记（Gate Matrix v1）。
**并须知**：§Z-13 内嵌行号已漂移（如「行4810 §DX-04」现行 4810 已是 §E02c 之锚）。

> **引用优先级铁律**：§编号／SQL 对象名 ＞ 导出文件名 ＞ 当前版本行号 ＞ 历史行号。
> 历史行号只作历史版本定位信息，不得作当前证据。

### P2 · 外来血统归化债务（External-lineage normalization debt）

v5–v7 自评估档并入之语句共享同一批外来写法：

| 缺陷 | 命中 |
|---|---|
| `bet11` 仅用 `DECIMAL(20,4)` | §P11a（阿泽）、§P12a、§P12c |
| 同段并用 20,4 与 20,8（`PRECISION_DRIFT`） | §P11c（阿泽） |
| `CAST(bet05 AS BIGINT)` 裸转（`UNSAFE_CAST_GATE`） | §P11a、§P11c、§P12a、§P12b、§P12c、§P13 |

对照：包内原生节点（§R01／§B01／§T02／§E02c／§P11b/d）全篇 `DECIMAL(20,8)` ＋
`CAST(NULLIF(TRIM(x),'') AS …)`，34 条仅用 20,8。

**共享缺陷 ≠ 共享血统**：`P11 → EXTERNAL/EXIT`；`P12/P13 → IN-PACK/NORMALIZE`。
阿泽退包**不清除**该缺陷——它留在 P12a/b/c、P13 四件之中。

**精度判定细则**（防伪警报）：单独 `DECIMAL(20,4)` 且不作跨精度比例 → 不自动 FAIL；
同一指标上下游 20,4→20,8 → `PRECISION_DRIFT`；分子分母精度不同后作精细比例 → 须复核。

> **新铁律**：外来语句须过四段验收方可在册——
> `EXTERNAL → 语义审计 → 工程归化 → 血统接纳`。v5–v7 三次并入皆未过此道。

### P3 · 语义与登记斧正（归 v11）

| 项 | 内容 |
|---|---|
| 阿泽血统迁移 | 16 处五级处置：随件迁出 9（§P11a 8564–8636、§P11c 8646–8790）／目录改登 2（288、290）／族头重写 1（8561）／**悬挂引用 2（9258、9323）**／历史照登 2（9363、9369） |
| 悬挂引用之实质 | 9258「三模型并行」迁出后包内只余二模型；9323 `entry_round_avg` 之「供与阿泽轴交叉」将成孤儿列——**保留该列**，用途声明改登为跨界 |
| 雷欧正名 | 19 处三级：可改 12／契约名冻结 6（289、291、8639、8644、8794、8799）／历史照登 1（9369） |
| `scope_def` 锁列 | 行 8854 → `'2_R01准入域（六闸 ∧ n>=100 ∧ 本金基数）'` |
| `bet41` 登记失效 | 行 217-218「五处皆取自表二、无一处取自表一」已证伪——§P11c/d（8661、8672、8698、8740、8809、8831）皆自表一取用。表一 `bet41`＝**下注退水金额**，表二 `bet41`＝**有效投注**；`rebate41_sum` 命名正确 |
| §P11d 头注 | 「未施 bet08，实测其筛除量为零」降为 INFERRED，附探针待办 |

**迁移完整性铁律**：迁出物 ＋ 悬挂引用 ＋ 共用块头，三者须同时处置。
只搬语句而留下指向空处的引用，比不搬更坏。

---

## 六 · 正名裁定

「雷欧」一名负二义（口径轴＝本金基数／人群域轴＝§R01 六闸 ∧ `n>=100`），
阿泽迁出后成孤儿名。双轴拆分：

| 轴 | 正式名 | 短码 | 血统式 |
|---|---|---|---|
| 人群域 | **R01 准入域** | `R01_ADM` | §R01 六闸 ∧ `HAVING n>=100` |
| 结算基数 | **本金基数** | `STK_BASIS` | 基数取 stake（非 `bet41`） |
| 对照域 | 全量洁净 | `R01_ALL` | §R01 六闸，无门槛 |
| （已迁出） | 洗码基数 | `VBT_BASIS` | 轴保留、标签停用 |

**正名及于术语与锁列，不及于契约文件名**——契约名经载入器硬约即为不可变身份。

**双名制口径分层保留**，仅血统归属改变。

---

## 七 · 方法论资产（不入 SQL）

| 项 | 裁定 |
|---|---|
| §P14 噪声地板 | 🟢 R 侧读 `B01_bt_panel.csv`；0 SQL／0 CSV／0 交付件 |
| §P15 盈亏平衡锚 | 🟢 同上 |
| §P16 处置成本锚 | 🔴 **BLOCKED**（Treatment Ledger 为 CPOS 唯一剩余 S1 缺口，Probe-02 OPEN）；只占章节号、无在册序号、不入任何计数 |
| `1.96 × sd/√N` | ⛔ 排除为个体 MCID 定义器——该式为**群体均值之抽样不确定度**，非个体可分辨位移；且违「博彩 ROI 不得套用 1/√n」铁律 |
| 附件历史读数（91,212 等） | ⛔ 全数退出证据链——轴定义与本包不符，连数量级参照亦不可 |
| `bet08` 非空 | 正名 **Data Quality · Event-Time Presence Gate**；非阿泽独有（19/63 既有），议题由「引入」改「统一」 |
| 双条件准入模式 | 🟢 模式保留（暴露 ∧ 行为）；数值 `30`／`0.60`／`40` 全弃 |
| `win_rate` | 🟡 v12 候选；且须先锁语义——`bet14>bet13` 系**订单胜率**，非物理局胜率 |

**§B01 五闸全数 PASS（OBSERVED）**，`P14/P15` 复用路线门禁全闭。
（`bet38 IS NULL` 之绝对覆盖率仍 UNKNOWN，留 §Z-07 哨兵普查。）

---

## 八 · SC 册（自我纠错记录）

| # | 内容 |
|---|---|
| 2 | 正则 `bet38\s*\)?\s*=\s*'N'` 无法匹配 `UPPER(TRIM(r.bet38)) = 'N'` → 误报 §B01 为「别式书写」 |
| 3 | 分类器关键词误判：§Z-08／09／11 属诊断却判为 ANALYSIS/EVENT |
| 4 | 修正③矫枉过正：`§Z-\d` 把 §Z-18（P01）／§Z-19（P02）误归 DIAG，致 P01 同族五件被判三种性质 |
| 5 | 正文写「12 件」而明细列 14 件（真 FAIL 清单） |
| 6 | 检查器段边界取「受检条目锚」而非「全部锚」，致段落吞并后续语句，制造伪 PASS（V_ipmatch 一条误得 6 个假「有」） |
| 7 | 文本模式读取致 MD5 失真（CRLF→LF） |
| 8 | `rn` 判据仅验 `ROW_NUMBER`，未验 `rn = 1`——开窗而不取首版等同未去重 |

> **Meta-Gate：Validator Must Be Validated.** 校验器须有自己的测试集；
> 本会话七次纠错中，五次源于校验器本身而非被校对象。

---

## 九 · 铁律登记（本会话新立／重申）

1. **Meta-Rule 08**：发现缺陷后不得直接修复；先判其属 ① SQL 逻辑错误 ② 数据血统错误
   ③ 工程规范未归化 ④ 诊断任务之合法差异 ⑤ 验证器自身错误。
2. **规范性结论不得由代码分类器自动生成**：
   `FACT = machine-observed` → `SEMANTIC/WAIVER = human-declared` → `CONSISTENCY = machine-validated`。
3. **数字不是身份，集合才是身份**。
4. **交付件发现范围 = `数据库/` 顶层非递归**；项目根递归 `*.csv` 为 REDTEAM 禁止模式。
5. **三命名空间不可混放**：`数据库/`＝交付契约／`函数/`＝执行函数／`审计/`＝审计工装。
6. **迁移完整性**＝迁出物＋悬挂引用＋共用块头。
7. **外来语句四段验收**：EXTERNAL → 语义审计 → 工程归化 → 血统接纳。
8. **引用优先级**：§编号／对象名 ＞ 导出文件名 ＞ 当前行号 ＞ 历史行号。
9. **泛化豁免理由无效**：豁免须点名具体 Gate，否则 `WAIVER` 沦为后门。

---

## 十 · OPEN 命题（不得写为已闭合）

| 命题 | 状态 |
|---|---|
| Probe-02 总命题 | ⏳ **OPEN**——任何下游文件写为「已验证／处置已定义」即 REDTEAM FAIL |
| R03b STOP CAUSE | ⚪ UNKNOWN（Export/Artifact 层机制未识别） |
| `bet16` ↔ `bet41` ↔ `validbet` 恒等 | 🔴 去重版复核前不得称 OBSERVED |
| `bet38 IS NULL` 绝对覆盖 | ⚪ UNKNOWN |
| `bet08` 筛除量是否为零 | 🟡 INFERRED（块注释所载），待探针 |
| `.GL` 53→59 | 🟡 CONDITIONAL，待去重核验 |
| `make_export_params.R` | 🟡 `v1.0.0-restored`／**EXECUTION_UNVERIFIED**（复原成功 ≠ 原件恢复；语法自检 PASS ≠ 执行 PASS） |
| 四个 R 工具 | 🟡 EXECUTION_UNVERIFIED（迄今未运行） |

---

## 十一 · 版本路线

| 版本 | 内容 | 状态 |
|---|---|---|
| **v10** | 原始生产／交付 SQL | 🔒 永久冻结基线 |
| **v10-REDTEAM** | 本档：审计结论、Gate Matrix、问题登记 | 🟢 当前工作基线 |
| **v11** | 阿泽迁出＋雷欧正名＋悬挂引用＋登记斧正；**SQL 计算逻辑零改动** | 🟡 待授权 |
| **v12** | P0 修复＋外来血统工程归化＋确认后之 Gate 强化 | ⏸ 后续 |

### 更新 SQL 总包之十步门禁（须全数满足）

```
① filled_v1 完成
② declaration-vs-code validator PASS
③ P01 恒等族复核完成
④ 所有真 FAIL 已逐条人工定谳
⑤ 修改项确定归属 v11 或 v12
⑥ 新 SQL 完成回归测试
⑦ 交付件重新生成
⑧ Namespace Gate PASS
⑨ 六元组重新登记
⑩ 新版本冻结
```

**第⑦步之前，旧 v10 一律不碰。**

> 一旦覆盖 v10，日后将无法回答：「这个问题究竟是原 v10 的问题，还是修正造成的问题？」
> 可追溯性是本审计体系最不应失去者。

---

## 十二 · 本档之修订纪律

- 本档可增补、可修订，但**历史条目照登不改**；修订以追加变更记录行实现。
- 本档一切数字须现算，不得硬编码；引用集合须带指纹。
- 本档**不置于 `数据库/`**，不参与交付件计数。
