# `P0-L05` 采集回执 · 2026-08-15

```
项目编号   P0-L05（Phase 0 首刀）
性质       只读采集 ＋ 独立复算
采集时刻   2026-08-15 13:23:00 ～ 13:23:01（本机 UTC+08:00）
补测时刻   2026-08-15 13:27:08（本机 UTC+08:00）
复算方     REDTEAM，于独立环境对上传副本重算，未接触原机
本回执     零结论。事件内容判读属 L05 阶段，依冻结序尚在 E05/E01/E02/E04 之后。
```

---

## 一、环境基准（全案对时依据）

| 项 | 值 |
|---|---|
| 时区 | `China Standard Time` · `UTC+08:00` · **无夏令时** |
| 采集时刻（本机） | `2026-08-15T13:27:08.2676780+08:00` |
| 采集时刻（UTC） | `2026-08-15T05:27:08.2706784Z` |
| 主机名 | `SHJ-H0647-RYOCH` |
| 系统 | Microsoft Windows 10 专业版 · `10.0.19045` · Build 19045 |
| 末次开机 | `2026-08-15 12:52:39`（采集前约 30 分钟） |

> ⚪ **仍未定**：`2026-08-12 17:30:20`（Superset 执行时点）属本机时区、服务器时区抑或 UTC。
> **在此确定之前，该时点不得与事件日志时间直接并列比对。** 待 `P0-02 / L01`。

---

## 二、证物清单与六元组

| 档 | 字节 | SHA-256 | 复算 |
|---|---:|---|---|
| `System.evtx` | 10,555,392 | `7DD2B5B23DA6104642F3AF4C0A6822643EFBD550561D46D4EBE4B82974EB8001` | 🟢 逐位相符 |
| `Application.evtx` | 12,652,544 | `7D675CE180BF58D746F70635CC64D787FCB32691A3D5516DA0A1543A2ABDD027` | 🟢 逐位相符 |

**结构复算**：两档皆 `ElfFile\0` · ver 3.2 · 档头 128 B · 区块 4096 B

```
System.evtx       10,555,392 = 4096 + 161 × 65536   声明分块 160  NextRecordId 20356
Application.evtx  12,652,544 = 4096 + 193 × 65536   声明分块 180  NextRecordId 18217
```

**记录号连续性**（依分块档头 `NumLogRecFirst/Last` 设界）

| 档 | 区间 | 笔数 | 缺号 | 重号 |
|---|---|---:|---|---|
| `System` | 1 ～ 20355 | 20,355 | 0 | 0 |
| `Application` | 1 ～ 18216 | 18,216 | 0 | 0 |

`System` 之 20,355 与实时查询 `RecordCount = 20355`、最末 `RecordId = 20355` **三者一致**。
`Application` 导出止于 18216，实时查询（13:24 后）见 18218 —— **两笔系采集时刻之后新增，属 method artifact 分界之内，非缺失。**

**实时档与导出档体积差已闭合**

```
实时 System.evtx  11,603,968 = 4096 + 177 × 65536   （CreationTime 2026-06-28 14:38:11）
导出 System.evtx  10,555,392 = 4096 + 161 × 65536
差 1,048,576 = 16 × 65536  ← 实时档预留之未用分块
```

---

## 三、🔒 证物身份锁

```
本目录内 .evtx 系 wevtutil epl 之导出产物，非实时日志之字节副本。
实时 .evtx 受 EventLog 服务锁定，无法直接复制。

SHA-256 证明「本档案自采集时刻起未变」
        不证明「与原始日志字节一致」
两次导出之字节必不相同（档头含导出时刻）。

导出档   = METHOD ARTIFACT
原始日志 = SCENE ARTIFACT（未取得字节副本，待 P0-06 VSS）
```

---

## 四、保留窗与覆盖率

**未滚动之证明（三项互证）**

```
OldestRecordNumber = 1
RecordCount = 最末 RecordId
档体积 10,555,392 / 上限 20,971,520 = 50.33%
    ↓
System 频道自 2026-06-28 建立以来，一条记录未被滚动覆盖。
```

**效力边界**：仅证「当前这一份日志实例内」无覆盖。`2026-06-28` 之前之记录不可见（日志档 CreationTime 即 06-28，两频道首笔皆 `RecordId 1 @ 14:38:11`）。**目标窗在 06-28 之后，故与本案无涉。**

**目标窗每日记录密度（本机时区）**

| 日期 | System | Application | |
|---|---:|---:|---|
| 2026-08-07 | 351 | 355 | |
| **2026-08-08** | **350** | **386** | ← 目标窗 |
| **2026-08-09** | **649** | **650** | ← |
| **2026-08-10** | **245** | **344** | ← |
| **2026-08-11** | **954** | **732** | ← |
| **2026-08-12** | **971** | **691** | ← 事发日 |
| 2026-08-13 | 298 | 298 | |
| 2026-08-14 | 831 | 646 | |
| 2026-08-15 | 224 | 167 | 采集日（截至导出时刻） |

**判：🟢 `WINDOW_COVERS_TARGET` · `COLLECTED` · `VERIFIED`**

**目标窗外之观测**：`2026-06-28 → 2026-07-12` 十三日两频道皆零记录。成因 ⚪ UNKNOWN。**在目标窗之外，本案不涉，仅记档备查。**

---

## 五、转储格（依预注册表落格）

```
DebugInfoType              = 7   （Active memory dump，转储已启用）
DebugFilePath              = %SystemRoot%\MEMORY.DMP
MiniDumpDirectory          = %SystemRoot%\Minidump
OverwriteExistingDebugFile = True
AutoReboot                 = True

Test-Path C:\Windows\MEMORY.DMP     → False
C:\Windows\Minidump                 → 目录本身不存在（非空目录）
```

**判：🟠 目标窗内无 BugCheck 遗骸落盘 —— `MECH-01` 之「宕机」义相对削弱。**

**效力上限（越之即废）**

```
不得推出「未曾宕机」。转储可能因下列情形写不成：
    页面档不足 · 磁盘空间不足 · 崩溃过早 · 转储写入本身失败 · 目录被清理
且本结论只覆盖 2026-06-28 日志建立之后。
```

---

## 六、缺口与更正

| # | 项 | 状态 |
|---|---|---|
| 1 | ⑦ 之六档（`Kernel-Boot`／`Kernel-EventTracing`／`Ntfs`／`StorageSpaces-Driver`／`Diagnostics-Performance`／`Storport`）**未随附件上传** | 🟡 在机上，**未经独立验证**；zip 系 13:23 快照，⑦ 于 13:27 执行 |
| 2 | `WER-Diagnostics/Operational` `FAILED(15007)` | ⚫ 应更正为 **`CHANNEL_NOT_PRESENT`**（该频道本机不存在），**非 `COLLECTION_FAILED`** |
| 3 | WER 报告目录 `ReportArchive` / `ReportQueue` | ⚪ **未查** —— 独立于频道，须补 |
| 4 | `P0-L05` 整体 | 🟡 **PARTIAL** |

---

## 七、纪律新增（候选，待裁）

**记录号序 ≠ 写入时间序。**
`System` 之 `RecordId 1` 时刻为 `14:38:11`，而全档最早 `WriteTime` 为 `14:38:02.69`，属较后之 RecordId。开机瞬间事件之写入次序与发生次序不一致。

> **日后按时间检索事件，不得以 `RecordId` 排序代替时间排序。**
> 与 `GAP-S2-PAGE-010`「键不等宽，字典序 ≠ 数值序」同型。

---

## 八、自纠（候选，待裁）

| 编号 | 性质 | 实害 |
|---|---|---|
| `SC-026` | 作业单 v1 以 `try/catch` 判 `wevtutil` 成败。原生指令之非零退出码不抛 PowerShell 例外，失败会被印成 `EXPORTED` | 修正后正是它抓住 `FAILED(15007)`；本轮无实害 |
| `SC-027` | 我方 evtx 解析器越过分块档头声明之记录区间，读入 chunk 43 之陈旧残余，误报 13 笔重复（`rid 4248–4260`） | 依 `NumLogRecFirst/Last` 设界后归零；**档案本身无恙** |

**「验证器先验其自身」，本轮第七次。**

---

## 九、状态

```
P0-01  L05   🟡 PARTIAL
              System       🟢 COLLECTED · VERIFIED
              Application  🟢 COLLECTED · VERIFIED
              六频道        🟡 已导出未验证
              WER 目录      ⚪ 未查
              转储          🟢 OBSERVED
P0-02 ～ P0-07              ⚪ 未开始

L05 事件内容判读             ⏸ 依冻结序，待 E05/E01/E02/E04 之后

STOP CAUSE   🔴 UNKNOWN        MECH-01   🟠 LEAD ONLY
MECH-A       🟡 CLOSED-CONDITIONAL       REBATE_UP 🟡 门槛待定
双轨回测      🔴 FROZEN          P&L GATE  🔴 BLOCKED
主方案        🔒 一字不动         口诀 v2   🔒 一字不动
六枝机制树     🔒 FROZEN          E03       ⏳ 最后，n=3
```
