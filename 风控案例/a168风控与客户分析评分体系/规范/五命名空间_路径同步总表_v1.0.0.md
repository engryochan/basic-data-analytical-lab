# 五命名空间 · 路径同步总表 v1.0.0

**日期**：2026-08-23　**置放**：`规范/`　**ARTIFACT CLASS**：`A3 SPEC`　**canonical**：`false`
**缘由**：用户裁定「SQL 总包务必置放在 `函数/`」，随之须全项目路径同步。

---

## 〇、撤回 W-8（我方）

`paths_a168_sql_main_指针斧正工单` 曾判：

> **双错**：路径 `函数/` → 应为**项目根**；版本 `v10` → 应为 `v11.0.1`

**「路径错」不成立，撤回。**

依五命名空间，`函数/` 系 **Execution**——SQL 总包正是可执行内容，**本就该在那儿**。
我当时书「SQL 总包居项目根」而**未引 SC-15 任何条文为据，属自行推定**。

> **实际只有一错：版本号滞留 `v10`。路径原本就对。**

随之作废者：`PROJ_ROOT` 常量之补法（`函数/` 系相对项目根之固定子目录，无须另立锚）。

---

## 一、五命名空间之职守（**唯一权威表**）

| 空间 | 职守 | 收什么 | **禁收什么** |
|:--|:--|:--|:--|
| `数据库/` | **Delivery** | 交付件 CSV | **禁收任何代码**（`.sql`／`.R`／`.py`） |
| `函数/` | **Execution** | **SQL 总包**、`.R` 源码、`R03b_source.sql` | 交付件数据 |
| `规范/` | **Specification** | registry YAML／CSV、规格文书、契约 | 可执行内容 |
| `配置/` | **Configuration** | `paths_a168.R` 等 | 业务逻辑 |
| `审计/` | **Audit** | 审计工具、诊断输出、验收记录 | canonical 交付件 |

**`.gitignore` 现况**：第 22 行忽略 `数据库/` 全目录（已由 `git check-ignore -v` 实测坐实）。
故 `数据库/` 下之物**一律不入库**；`审计/` 未被忽略，诊断留痕可入库。

---

## 二、逐档归位表

### canonical（唯一权威档）

| 档 | **应置** | 现况 |
|:--|:--|:--|
| `a168_取数与核验_SQL总包_v11.0.3.sql` | **`函数/`** | 须移入 |
| `…_v11.0.2_superseded.sql` 等三档 | **`函数/`** | 同上，归档不删 |
| `registry_risk_typology_v1.5.0.yaml` | `规范/` | ✅ 已正 |
| `registry_risk_typology_v1.5.0.csv` | `规范/` | ✅ 已正 |
| `registry_loader.R` | `函数/` | ✅ 已正 |
| `paths_a168.R` | `配置/` | ✅ 已正 |

### 审计工具（`A2 DIAGNOSTIC_TOOL` · `canonical = false`）

| 档 | **应置** |
|:--|:--|
| `G11_块注释拓扑闸_v2.0.2.py` | **`审计/工具/`** |
| `P-H0` ~ `P-H0-8` 八件 `.sql` | `审计/工具/` |
| `B-4_mu0分层现算_审计工具_v1.0.1.sql` | `审计/工具/` |
| `B1B2_registry_SSOT探针_v1.0.0.R` | `审计/工具/` |
| `registry_loader契约探针_v1.0.2~4.R` | `审计/工具/` |

### 诊断输出（非交付件）

| 档族 | **应置** |
|:--|:--|
| `_PH0_*.csv` | `审计/_ph0/` ✅ 已正 |
| `_B4_*.csv` | **`审计/_b4/`** |

### 规范与审计文书

| 档 | **应置** |
|:--|:--|
| `版本编号纪律_v1.0.0.md`／`版本宪法冲突_裁定案`／`C2变更契约`／`B-6_REFUTED隔离规格`／本册 | `规范/` |
| `P-H0结案文书`／`SQL总包_v11.0.2与v11.0.3_验收记录`／`批-01`／`批次切法斧正案` | `审计/` |
| `SC-15_经济KPI规格`／`H轴_可得性预检…`／`经济指标字典`／`经济结果层_粒度对齐规格` | `规范/` |

---

## 三、须同步之指针（**一处不改即断链**）

### ① `配置/paths_a168.R`

```r
## ── 斧正（v11.0.3 · 2026-08-23）────────────────────────────────────
## ★ 路径原本就对：函数/ 系 Execution 命名空间，SQL 总包居此。
##   W-8 已撤回「应居项目根」之前判。
## ★ 唯一之错系版本号滞留 v10。
sql_main = file.path("函数", "a168_取数与核验_SQL总包_v11.0.3.sql"),
```

**验收**：`registry_load()` 之警告

```
【registry】未在 函数/a168_取数与核验_SQL总包_v10.sql 找到 SQL 总包
```

须消失，且 `sql_section` 栏可回溯原文。

### ② SQL 总包卷首之血统登记表

现书 `a168_取数与核验_SQL总包_v10.sql   本包(料源)`，须改 `函数/…v11.0.3.sql`。
**属注释区 → 可并入下一 PATCH（`v11.0.4`），本轮不施工。**

### ③ 交付件导出路径

总包内 102 条导出声明皆书 `数据库/xxx.csv` —— **本就正确，不改**。
数据出 `数据库/`，代码居 `函数/`，二者各归其位。

---

## 四、跑法（PowerShell · 已实测）

```powershell
cd C:\Users\PCCPPPCCC\Documents\GitHub\basic-data-analytical-lab

# 无参 —— 自动依五命名空间定位 canonical 总包
python 审计\工具\G11_块注释拓扑闸_v2.0.2.py

# 显式给档
python 审计\工具\G11_块注释拓扑闸_v2.0.2.py 函数\a168_取数与核验_SQL总包_v11.0.3.sql
```

**⚠ 前次报错之两处成因，皆非脚本缺陷：**

| 现象 | 成因 | 正解 |
|:--|:--|:--|
| `can't open file …G11_…v2.0.0.py` | 档未落盘至 `审计/工具/` | 先下载落盘 |
| `“<”运算符是为将来使用而保留的` | **把 R 语法 `ok <- run(...)` 贴进了 PowerShell** | R 语法只能在 R 内跑 |
| `ls -la 数据库/.git` 报错 | PowerShell 无 `-la` | 用 `Get-ChildItem` 或 `dir` |
| REPL 内 `SystemExit: 2` | 整段贴入 REPL，`sys.argv` 无档名 | v2.0.2 已改为自动定位 |

**三式语言各归其位，禁混：**

```
PowerShell  →  python 审计\工具\G11_….py [档名]
Python REPL →  run()  或  run("函数/a168_….sql")
R           →  reticulate::source_python("审计/工具/G11_….py"); ok <- run()
```

---

## 五、G11 v2.0.2 之路径纪律（写入工具卷首）

搜寻序（**`数据库/` 明确排除**）：

```
① 函数/a168_取数与核验_SQL总包_v*.sql     ← Execution 正位
② a168_取数与核验_SQL总包_v*.sql          ← 项目根，过渡期容错
③ */a168_取数与核验_SQL总包_v*.sql        ← 任一子目录，兜底
★ 排除 _superseded；★ 排除 数据库/（只放数据，不放代码）
```

**七项自测全过**（模拟项目树实跑）：

| 测 | 结果 |
|:--|:--|
| ① 项目根无参自动定位 | ✅ 命中 `函数/…v11.0.3.sql`，退出码 0 |
| ② 显式给档 | ✅ PASS |
| ③ 管道截断（`head`） | ✅ BrokenPipe 守卫生效，不再吐 Traceback |
| ④ 排除 `_superseded` | ✅ 自动定位不取旧版 |
| ⑤ `数据库/` 内置 `v99.9.9.sql` | ✅ **不取**，仍命中 `函数/` |
| ⑥ 子目录内跑（定位失败） | ✅ 退出码 **2**，并印搜寻序与工作目录 |
| ⑦ 显式给旧版 | ✅ 退出码 **1**（FAIL） |

**六元组**：199 行 ／ 9,814 字节 ／ MD5 `a22718c9a41bb5038751448fd517469d` ／ LF ／ UTF-8。

---

## 六、本册不改任何在册档

`v11.0.3` MD5 `03e774de6f71d6213e493e765afe5a93` 未触。
移档系**文件系统操作**，不改内容、不改六元组、**不构成版本变更**。
惟须于下一 PATCH（`v11.0.4`）同步卷首血统登记表之路径与版本。
