-- ════════════════════════════════════════════════════════════════════════════════════════════════════
-- ★ a168 SQL 总包 v12.0.0-HF3 · 模块索引（129 件 · 129 张 CSV）★
-- ════════════════════════════════════════════════════════════════════════════════════════════════════
-- 本索引为注释，不参与执行。同步副本：规范/v12_0_0_HF1_模块索引.csv（可排序筛选）
--
-- 【HF3 · AG-03 正式生产口径（本版定版）】
--   AG-03 已判决 = 候选 B：bet23 是【公司自留比例】，代理实得 =(100−bet23)
--   判决依据（三条独立证据同指 B，实测于 1.26 亿行 / 16.1 万会员）：
--     ① 候选 A 下平台 139 天博彩净利 = −108,988，于 132 亿流水 —— 无经营可能性，结构性排除
--     ② 候选 A 在会员层并非恒零（rate p1 −1.398% / p99 +1.486%）—— 总量归零系巧合
--        巧合来源：agent_cost_b(811 万) ≈ rebate_cost(822 万)，相差仅 1.33%
--     ③ 候选 B 下账本逐层闭合：hold 1.4935% → ngr 1.4314% → net_margin 1.3701%
--   ★ Spearman(ngr, net_margin) = 1.0000 —— 会员风险排序不因 A/B 之争改变
--
-- 【生产字段 · ACCOUNTING 层（唯一权威）】
--   stake       投注本金（原币 ÷ bet11 折算）
--   valid_bet   有效投注（validbet）· 实测 /stake = 90.076%
--   profit      = -e.net                            平台牌桌毛赢 · 实测 hold 1.4935%
--   rebate_cost = e.rebate（bet16）                 退水支出 · 实测 /stake 0.0621%
--   company_retain_pct = bet23                      公司自留比例（%）· 实测均值 96.114
--   agent_pct   = 100 − bet23                       代理实得比例（%）· 实测均值 3.886
--   agent_cost  = profit ×agent_pct/100             代理成本 · 实测 /stake 0.0613%
--   ngr         = profit − rebate_cost            ★ 净博彩收入 · 实测 /stake 1.4314%
--   net_margin  = ngr − agent_cost                ★ 平台真净利 · 实测 /stake 1.3701%
--   ngr_rate / net_margin_rate = 上二者 ÷ stake
--   hold_rate   = -e.net/e.stake                    平台毛利率 ＝ −roi
--   roi         =  e.net/e.stake                    玩家净回报率（正＝玩家赚）
--   ★ 风险判据只认 net_margin，不认 roi 单项、不认 net_pnl
--
-- 【审计留档字段 · 不进商业消费层，仅供追溯，禁用于决策】
--   agent_formula_version         = 'AG03-B-v1'
--   agent_cost_a_legacy           候选A：profit ×bet23/100（已否决）
--   agent_cost_valid_base_legacy  v12.0.0 原式：valid_bet ×Σ(bet23..27)/100（PROVEN WRONG）
--   net_margin_a_legacy / net_margin_a_rate_legacy
--
-- 【HF2 已修 · 代理层级（继承）】1.26 亿行实测：bet23≥bet24≥…≥bet27 单调递减 99.9821%，仅 106 种组合
--   ⇒ 五级为嵌套下行链，各级实得＝相邻差分，末级＝bet27；望远镜消项 Σ各级 = bet23
--   ⇒ v12.0.0 原式 x_agpct = Σ(五级) 判 PROVEN WRONG，高估 4.8 倍
--     后果：agent_cost = 流水×403%，net_margin = −401.93%，129 件商业结论全部清零
--
-- 【HF1 已修 · 别名（继承）】公式未动：
--   43 处 ambiguous 撞名 → e 侧加 _xagg（无后缀＝q 侧本件自算；带 _xagg＝e 侧六层口径）
--   5 处 ORDER BY roi → roi_xagg ； 59 件 ramp_recent 双出 → _vs_prior / _vs_all
--
-- 【audit_rn 主排序键】net_margin ASC（HF2 曾暂锚 ngr，HF3 回接正式 net_margin）
--   economic_value / action_priority 亦已统一回接 net_margin
--
-- 【ngr / net_margin 可信度 —— 运行前必读】129 件皆含 ngr 与两候选 net_margin：
--   OK                    24 件  连接 1:1，本行 net_margin 即本行事实       → 可直接做商业判定
--   WRONG_GRAIN·上卷广播   37 件  e 侧按上层粒度连接，值在多行重复          → 仅作背景，勿算比率
--   INVALID·笛卡尔广播     67 件  CROSS JOIN x_agg（会员级），值不相干      → 勿用；行数=|q|×会员数
--   NULL·跨实体未命中       1 件  #079 creator↔bet05 连不上，六层全 NULL   → 只看前 7 列
--
-- 【典型学 M 码 —— 包内自带的风险机理分类】
--   M01 同桌团伙           1 件
--   M02 对冲对打           4 件
--   M03 尾段投注           9 件
--   M04 IP             8 件
--   M05 退水套利          18 件
--   M06 代理线            3 件
--   M07 荷官关联           6 件
--   M09 处置限额          20 件
--   M10 责任博彩           4 件
--   M11 异常告警           3 件
--   M12 资金轨迹           2 件
--   M13 复投循环           1 件
--   M14 投注形态          10 件
--   M15 活跃周期          12 件
--   R01 风控专员           1 件
--   Z00 基建/元数据        27 件
--   ⚠ M08 在 v12.0.0 中无对应模块（M01~M15 缺 M08）
--
-- 【业务域 A–F】
--   A 风险识别          44 件
--   B 会员画像          12 件
--   C 平台经营          20 件
--   D 处置与因果         26 件
--   F 元数据字典         27 件
--
-- 【risk_usage 用法标记】
--   PRIMARY          可直接判风险会员（会员粒度 + net_margin 可信）
--   BRIDGE           实体桥接（如 IP↔会员），本身不判风险
--   SCREENING        筛查线索（IP/网段/配对层），须落到会员才可判
--   SUPPORT          支撑维度（荷官/代理/桌台/产品）
--   REFERENCE        参照/背景
--   NOT_FOR_DECISION 不可用于决策（net_margin 不可信或本为元数据）
-- ════════════════════════════════════════════════════════════════════════════════════════════════════
-- 序号 CSV 名称                            M码 域 实体      粒度                    列  net_margin  用法
-- ----------------------------------------------------------------------------------------------------
-- 001 A_anchor.csv                       M06  C IP       g.ip                    68 OK          SCREENING
-- 002 B01_bt_panel.csv                   M15  B 会员       member_id × bet_date    77 WRONG_GRAIN NOT_FOR_DECISION
-- 003 B01_bt_panel_delta.csv             M15  B 会员       member_id × bet_date    77 WRONG_GRAIN NOT_FOR_DECISION
-- 004 B4_2b_funnel_and_mu0.csv           M14  C 全窗/元数据   n_raw × n_g1 × n_g2     89 INVALID     NOT_FOR_DECISION
-- 005 B4_5b_tip_nature.csv               M14  C IP       TRIM(bet09)             79 OK          SCREENING
-- 006 B_online_base.csv                  Z00  F 桌台       table_id × sec_elapsed  70 WRONG_GRAIN NOT_FOR_DECISION
-- 007 C01_ip_chain.csv                   M04  A IP       bet_ip                  71 OK          SCREENING
-- 008 C06_hedge_pairs.csv                M02  A IP       bet_ip × m_a × m_b      70 WRONG_GRAIN SCREENING
-- 009 C06d_hedge_pairs_dated.csv         M02  A 会员       a.bet_ip × a.member_id  72 WRONG_GRAIN NOT_FOR_DECISION
-- 010 C08_subnet_all.csv                 M04  A 网段       1                       71 INVALID     SCREENING
-- 011 C08_subnet_sparse.csv              M04  A 网段       1                       71 INVALID     SCREENING
-- 012 D03S_daily_roi_diff.csv            M15  B 日期       bet_date                70 OK          REFERENCE
-- 013 DICT_01a_bet41_tri.csv             M05  A 全窗/元数据   scope × n_rows × sum_s  77 INVALID     NOT_FOR_DECISION
-- 014 DICT_01b_roundkey_card.csv         Z00  F 全窗/元数据   scope × n_rows × nd_be  77 INVALID     NOT_FOR_DECISION
-- 015 DICT_01c_dimension.csv             Z00  F 全窗/元数据   scope × n_rows × avg_v  75 INVALID     NOT_FOR_DECISION
-- 016 DICT_01d_bet_latency.csv           Z00  F 全窗/元数据   TRIM(bet39)             77 OK          REFERENCE
-- 017 DX04_bet09_profile.csv             M14  C 产品       bet_side                72 OK          SUPPORT
-- 018 DX05_product_panorama.csv          M14  C 产品       product_code            77 OK          SUPPORT
-- 019 E02_segment_econ.csv               M05  A 全窗/元数据   m.rebate_rate_bkt × m.  72 INVALID     NOT_FOR_DECISION
-- 020 EV01_disposal_events.csv           M09  D 日期       event_id × event_time   76 WRONG_GRAIN NOT_FOR_DECISION
-- 021 EV02_member_disposal.csv           M09  D 会员       event_id × event_date    0 WRONG_GRAIN NOT_FOR_DECISION
-- 022 I_ip_agg.csv                       M04  A IP       bet_ip                  77 OK          SCREENING
-- 023 I_ip_player.csv                    M04  A 会员       b.bet_ip × b.member_id  77 WRONG_GRAIN NOT_FOR_DECISION
-- 024 K01_risk_feature_matrix.csv        M11  A 会员       member_id × is_seed ×   78 WRONG_GRAIN NOT_FOR_DECISION
-- 025 K01b_seed_goldip.csv               M04  A 会员       member_id × seed_src    65 WRONG_GRAIN NOT_FOR_DECISION
-- 026 K01b_seed_manual.csv               M11  A 会员       member_id × seed_src    65 WRONG_GRAIN NOT_FOR_DECISION
-- 027 K01c_seed_dated.csv                M04  A IP       seed_id × seed_ip × cr  69 WRONG_GRAIN SCREENING
-- 028 L_label_dist.csv                   M11  A 全窗/元数据   risk × orders           69 INVALID     NOT_FOR_DECISION
-- 029 P01A_crosstab.csv                  M05  A 全窗/元数据   n_rows × bet16_null ×   76 INVALID     NOT_FOR_DECISION
-- 030 P01B_diff_ratio.csv                M05  A 全窗/元数据   n × diff_min × diff_p5  74 INVALID     NOT_FOR_DECISION
-- 031 P01C_split_by_dim.csv              M05  A 产品       DATE_TRUNC('month', dt  77 INVALID     NOT_FOR_DECISION
-- 032 P01D_vs_validbet.csv               M05  A 全窗/元数据   n × comparable_n × pla  72 INVALID     NOT_FOR_DECISION
-- 033 P01E_identity_test.csv             M05  A 风控专员     hypothesis × n × exact  71 INVALID     NOT_FOR_DECISION
-- 034 P02A_lmc_samples.csv               M09  D 全窗/元数据   类别 × 取样端 × 異動時間         72 INVALID     NOT_FOR_DECISION
-- 035 P02B_changestatus_patterns.csv     M09  D 全窗/元数据   SUBSTRING(TRIM(lmc05),  69 INVALID     NOT_FOR_DECISION
-- 036 P02C_key_check.csv                 M09  D 全窗/元数据   l.lmc03                 71 INVALID     NOT_FOR_DECISION
-- 037 P02D_events_in_window.csv          M09  D 全窗/元数据   lmc04 × lmc03           67 INVALID     NOT_FOR_DECISION
-- 038 P0C02_标签口径.csv                     Z00  F 全窗/元数据   产品口径 × 正例观测数_会员日 × 正例会  69 INVALID     NOT_FOR_DECISION
-- 039 P0C03_标记次数分布.csv                   Z00  F 全窗/元数据   标记次数                    66 INVALID     NOT_FOR_DECISION
-- 040 P0C04_同日泄漏检验.csv                   M05  A 全窗/元数据   d0.mem × d0.mark_day    70 INVALID     NOT_FOR_DECISION
-- 041 P0C05_标记时点定位.csv                   Z00  F 全窗/元数据   mem × mark_day × 首投日    71 INVALID     NOT_FOR_DECISION
-- 042 P0C06_LabelB骨架.csv                 M02  A 全窗/元数据   a.mem × a.anchor_day    71 INVALID     NOT_FOR_DECISION
-- 043 P0C09_安慰剂检验.csv                    M05  A 全窗/元数据   a.mem × a.d × a.注单数 ×   70 INVALID     NOT_FOR_DECISION
-- 044 P11_阿泽尾段投注总帐.csv                   M03  A 全窗/元数据   scope_def × n_orders ×  84 INVALID     NOT_FOR_DECISION
-- 045 P11_阿泽尾段投注模型会员名单.csv               M03  A 会员       player_id × total_orde  72 WRONG_GRAIN NOT_FOR_DECISION
-- 046 P11_雷欧尾段投注总帐.csv                   M03  A 全窗/元数据   scope_def × n_orders ×  84 INVALID     NOT_FOR_DECISION
-- 047 P12a_platform_daily.csv            Z00  F 日期       dt                      72 OK          REFERENCE
-- 048 P12b_member_events.csv             M09  D 会员       uid × first_dt × last_  71 WRONG_GRAIN NOT_FOR_DECISION
-- 049 P12c_member_daily_pnl.csv          M15  B 会员       g.uid × g.dt            69 WRONG_GRAIN NOT_FOR_DECISION
-- 050 P13_personal_entry.csv             M03  A 会员       uid                     70 OK          PRIMARY
-- 051 PH0_8r1_rg_case.csv                M10  D 日期       dt_day × category × ta  71 WRONG_GRAIN NOT_FOR_DECISION
-- 052 PH0_8r2_rg_crosstable.csv          M10  D 桌台       src × n_rows × n_rg_tr  68 INVALID     NOT_FOR_DECISION
-- 053 PI01_treatment_delta_member.csv    M09  D 会员       member_id × treat_time  84 WRONG_GRAIN NOT_FOR_DECISION
-- 054 PI02_treatment_delta_platform.csv  M09  D 全窗/元数据   grp × phase             76 INVALID     NOT_FOR_DECISION
-- 055 PI03_reinvest_loop.csv             M13  A 全窗/元数据   outcome                 76 INVALID     NOT_FOR_DECISION
-- 056 PI04_member_5d_profile.csv         M15  B 会员       mid                     83 OK          PRIMARY
-- 057 P_player_month.csv                 M15  B 会员       member_id × DATE_TRUNC  72 WRONG_GRAIN NOT_FOR_DECISION
-- 058 QSB_01a_category_census.csv        M14  C 全窗/元数据   COALESCE(NULLIF(TRIM(c  85 INVALID     NOT_FOR_DECISION
-- 059 QSB_01c_cat1_contamination.csv     M14  C 产品       TRIM(bet09)             73 OK          SUPPORT
-- 060 QSB_02a_lowcard_fields.csv         M14  C 全窗/元数据   scope × n_rows × nd_be  81 INVALID     NOT_FOR_DECISION
-- 061 QSB_02b2_lowcard_five_gates.csv    M14  C 全窗/元数据   COALESCE(NULLIF(TRIM(b  82 INVALID     NOT_FOR_DECISION
-- 062 QSB_02b_state_cross.csv            M14  C 全窗/元数据   COALESCE(NULLIF(TRIM(b  72 INVALID     NOT_FOR_DECISION
-- 063 QSB_02c_push_by_family.csv         M14  C 会员       CASE WHEN TRIM(bet09)   73 INVALID     NOT_FOR_DECISION
-- 064 R01_late_shoe.csv                  M03  A 会员       member_id               78 OK          PRIMARY
-- 065 R01_late_shoe_w2.csv               M03  A 会员       member_id               78 OK          PRIMARY
-- 066 R01anm_shoe_anomaly.csv            M03  A 桌台       dt × table_id × shoe_i  74 WRONG_GRAIN NOT_FOR_DECISION
-- 067 R01chk_shoe_len_by_table.csv       M03  A 桌台       table_id × table_flag   91 WRONG_GRAIN NOT_FOR_DECISION
-- 068 R01inv_shoe_len_census.csv         M03  A 桌台       n_tables × n_shoes × m  82 INVALID     NOT_FOR_DECISION
-- 069 R02_same_table.csv                 M01  A 会员       uid1 × uid2 × n_same_o  75 WRONG_GRAIN NOT_FOR_DECISION
-- 070 R03_player_dealer.csv              M07  C 会员       uid × dealer_id × is_s  96 WRONG_GRAIN NOT_FOR_DECISION
-- 071 R03b_player_dealer_daily.csv       M07  C 会员       p.bet_date × p.member_  87 WRONG_GRAIN NOT_FOR_DECISION
-- 072 R03chk_settlement_form.csv         M07  C 投注面      UPPER(TRIM(bet_side))   69 OK          REFERENCE
-- 073 R03inv_pair_census.csv             M07  C 会员配对     1 × 2                   69 INVALID     SCREENING
-- 074 R_rebate_dist.csv                  M05  A 全窗/元数据   mem003                  66 INVALID     NOT_FOR_DECISION
-- 075 S01_player_score.csv               M15  B 会员       b.member_id             74 OK          PRIMARY
-- 076 S02_dealer_score.csv               M07  C 荷官       b.dealer_id             72 OK          SUPPORT
-- 077 S02b_dealer_pace.csv               M07  C 荷官       d.dealer_id             66 OK          SUPPORT
-- 078 S03_agent_score.csv                M06  C 代理线      b.lv3                   72 INVALID     NOT_FOR_DECISION
-- 079 S04_analyst_score.csv              R01  C 风控专员     COALESCE(NULLIF(TRIM(c  70 NULL        NOT_FOR_DECISION
-- 080 S05_member_month_panel.csv         M15  B 会员       b.member_id × DATE_TRU  73 WRONG_GRAIN NOT_FOR_DECISION
-- 081 S_player_tail.csv                  M15  B 会员       member_id               81 OK          PRIMARY
-- 082 S_second_dist.csv                  M15  B 桌台       table_id                71 OK          SUPPORT
-- 083 T02_daily_roi.csv                  M15  B 会员       o.member_id × o.bet_da  68 WRONG_GRAIN NOT_FOR_DECISION
-- 084 T03_arbitrage.csv                  M02  A 会员       member_id               73 OK          PRIMARY
-- 085 TL00_table_inventory.csv           Z00  F 桌台       c.TABLE_NAME            68 INVALID     NOT_FOR_DECISION
-- 086 TL02_config_change.csv             M09  D 全窗/元数据   变更列                     67 INVALID     NOT_FOR_DECISION
-- 087 TL03_pseudo_treatment.csv          M09  D 会员       member_id × change_dat  69 WRONG_GRAIN NOT_FOR_DECISION
-- 088 TL03b_pseudo_treatment.csv         M09  D 会员       member_id × cate × pre  71 WRONG_GRAIN NOT_FOR_DECISION
-- 089 TL04_log_schema.csv                Z00  F 桌台       TABLE_NAME × ORDINAL_P  68 INVALID     NOT_FOR_DECISION
-- 090 TL06_log_mem_change.csv            Z00  F 日期       dt                      65 OK          REFERENCE
-- 091 TL07_mem_change_shape.csv          M09  D 全窗/元数据   1                       73 INVALID     NOT_FOR_DECISION
-- 092 TL08_treatment_events.csv          M09  D 会员       member_id × action_dat  72 WRONG_GRAIN NOT_FOR_DECISION
-- 093 TL09_member_schema.csv             Z00  F 会员       ORDINAL_POSITION × COL  67 INVALID     NOT_FOR_DECISION
-- 094 TL10_treatment_ledger.csv          M09  D 会员       member_id × action_dat  73 WRONG_GRAIN NOT_FOR_DECISION
-- 095 TL11_treatment_episode.csv         M09  D 会员       member_id × field_name  87 WRONG_GRAIN NOT_FOR_DECISION
-- 096 TL11c_population_reconcile.csv     M09  D 全窗/元数据   lvl × level_name × n_a  70 INVALID     NOT_FOR_DECISION
-- 097 TL14_limit_treatment.csv           M09  D 会员       member_id × action_dat  78 WRONG_GRAIN NOT_FOR_DECISION
-- 098 T_table_span.csv                   M15  B 桌台       gi011                   69 OK          SUPPORT
-- 099 VB_00_validbet_contract.csv        M05  A 全窗/元数据   scope × n_rows × n_vb_  76 INVALID     NOT_FOR_DECISION
-- 100 VB_00b_zero_five_rates.csv         M05  A 会员       scope × n_base_rows ×   81 INVALID     NOT_FOR_DECISION
-- 101 VB_41_identity_global.csv          M05  A 风控专员     test × n_rows × n_both  80 INVALID     NOT_FOR_DECISION
-- 102 VB_41b_identity_strata.csv         M05  A 产品       TRIM(bet09) × TRIM(com  73 WRONG_GRAIN NOT_FOR_DECISION
-- 103 VB_41b_identity_strata_v18b.csv    M05  A 风控专员     g × comm                81 WRONG_GRAIN NOT_FOR_DECISION
-- 104 VB_41c1_964_detail.csv             M05  A 会员       grp × bet01 × member_i  85 WRONG_GRAIN NOT_FOR_DECISION
-- 105 VB_41c2_concentration.csv          M05  A 全窗/元数据   t.grp                   83 INVALID     NOT_FOR_DECISION
-- 106 VB_41c2_concentration_v2.csv       M05  A 会员       grp × n_rows × n_membe  83 INVALID     NOT_FOR_DECISION
-- 107 V_ipmatch.csv                      M04  A 会员       ip × n_member_log × co  68 WRONG_GRAIN NOT_FOR_DECISION
-- 108 V_recon.csv                        Z00  F 全窗/元数据   n_matched_round × mae_  75 INVALID     NOT_FOR_DECISION
-- 109 V_table_counts.csv                 Z00  F 桌台       表名 × 行数                 65 INVALID     NOT_FOR_DECISION
-- 110 X_combo.csv                        M06  C 全窗/元数据   1 × 2                   69 INVALID     NOT_FOR_DECISION
-- 111 Z01_table_inventory.csv            Z00  F 桌台       表名 × 表类型 × 估计行数         68 INVALID     NOT_FOR_DECISION
-- 112 Z02_excluded_tables.csv            Z00  F 桌台       表名 × 估计行数 × 表注释         67 INVALID     NOT_FOR_DECISION
-- 113 Z03_column_dictionary.csv          Z00  F 全窗/元数据   表名 × 列序 × 列名            71 INVALID     NOT_FOR_DECISION
-- 114 Z04_name_collision.csv             Z00  F 全窗/元数据   COLUMN_NAME             69 INVALID     NOT_FOR_DECISION
-- 115 Z05_missing_typehint.csv           Z00  F 全窗/元数据   表名 × 列序 × 列名            69 INVALID     NOT_FOR_DECISION
-- 116 Z06_generated_probe_sql.csv        Z00  F 全窗/元数据   表名 × 列序 × 列名            68 INVALID     NOT_FOR_DECISION
-- 117 Z07_probe_bet02.csv                Z00  F 全窗/元数据   3                       68 INVALID     NOT_FOR_DECISION
-- 118 Z08_roundkey_integrity.csv         Z00  F 全窗/元数据   注单行数 × 局键数 × 单局最大注单行数   72 INVALID     NOT_FOR_DECISION
-- 119 Z09_time_axis_recon.csv            Z00  F 日期       行数 × dt等于账务日 × dt等于下注日  80 INVALID     NOT_FOR_DECISION
-- 120 Z11_sentinel_impact.csv            M09  D 全窗/元数据   未处置_注单行数 × 已处置_注单行数 ×   76 INVALID     NOT_FOR_DECISION
-- 121 Z14_zero_month_diagnosis.csv       M12  D 会员       member_id × bet_month   86 WRONG_GRAIN NOT_FOR_DECISION
-- 122 _P12b-pre.csv                      M09  D 会员       member_id × action × c  67 WRONG_GRAIN NOT_FOR_DECISION
-- 123 _PH0_1b_table_inventory.csv        Z00  F 会员       table_name × h_src × h  73 INVALID     NOT_FOR_DECISION
-- 124 _PH0_2b_column_inventory.csv       Z00  F 桌台       table_name × col_pos ×  70 INVALID     NOT_FOR_DECISION
-- 125 _PH0_3_lmc_category.csv            M10  D 全窗/元数据   COALESCE(NULLIF(TRIM(l  75 INVALID     NOT_FOR_DECISION
-- 126 _PH0_4L1_member_columns.csv        Z00  F 会员       table_name × col_pos ×  68 INVALID     NOT_FOR_DECISION
-- 127 _PH0_4b_rg_keyword.csv             M10  D 全窗/元数据   COALESCE(NULLIF(TRIM(l  74 INVALID     NOT_FOR_DECISION
-- 128 _PH0_4w2_balance_points.csv        M12  D 日期       dt_slice × n_user_acti  75 INVALID     NOT_FOR_DECISION
-- 129 _PH0_6b_member_daily.csv           Z00  F 会员       dt                      69 OK          PRIMARY
-- ════════════════════════════════════════════════════════════════════════════════════════════════════
-- 【异常 IP → 风险会员 · 三段证据链（本包已具备，勿另建大一统 JOIN）】
--   ① 筛查  #022 I_ip_agg → #007 C01_ip_chain → #010/#011 C08_subnet → #107 V_ipmatch
--   ② 桥接  #023 I_ip_player  ★唯一 IP↔会员桥（只用无后缀 q 侧列：stake_ip/net_pnl_ip/ip_order_share）
--   ③ 落槌  #008/#009 C06_hedge_pairs（同IP同局对赌）→ #024 K01_risk_feature_matrix → 会员 net_margin
--   种子对照 #025 K01b_seed_goldip / #027 K01c_seed_dated（alert_ip_setting 全表仅 17 行 / 6 标注人，勿作主判据）
-- ════════════════════════════════════════════════════════════════════════════════════════════════════
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ★ 六层商业指标块 · 十六类口径特化 · 129 件同步套挂 ★
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 立于 2026-08-24 · 军令：ROI/P50/AVG/RATE/RAMP/PROFIT/STAKE/VALID_BET **一个不准丢**，
--   同时**不得止步于此** —— 须从 StarRocks 现有维度灵活组合，产生新商业信号。
--
-- 【六层 · 每件皆全】
--   一·经济  stake / valid_bet / profit / roi / roi_<分区> / roi_ex_rebate
--             ＋ ROI 三守卫 roi_numerator · roi_denominator · roi_denominator_status
--             ＋ 成本 rebate_cost（bet16）· agent_cost（bet23~27 占成）
--             ★ net_margin ＝ profit − rebate_cost − agent_cost　**平台真净利**
--   二·分布  AVG 七 grain：per_bet / per_round(valid) / profit_per_round / per_day /
--             per_shoe / stake_per_active_day / rounds_per_active_day
--             P 六档：p25 p50 p75 p90 p95 p99 ＋ p50_valid_bet ＋ p50_profit
--             长尾比：p95/p50 · p99/p50 · avg/p50 ← 识别分布是否突生长尾
--   三·关系  common_table / dealer / ip / product count ＋ share_<分区>
--   四·rate  hold_rate · rebate_rate_actual · rebate_pct_config（bet15）
--             turnover_multiple · free_comm_rate · roi_delta_<分区>
--   五·动态  first/last_seen · active_days/shoes/sessions · span · persistence
--             ramp_<分区>（该类特化）· ramp_recent · stake_ramp
--   六·决策  vip_tier · economic_value · risk_value · action_priority
--
-- 【★ 每类之分区不同 —— 此即口径特化，非通用模板】
--   M01 同桌(≥2人同局) · M02 对冲(同局庄闲并存) · M03 尾段(shoe_pos≥0.80)
--   M05 高洗码(valid>stake) · M13 追损(net<0) · M14 免佣(commission=1) · 余类 近30日
--
-- 【禁硬编码】零金额阈值；切点全由 PERCENT_RANK / NTILE / PERCENTILE_APPROX 定。
-- 【新维度已入】x_dealer / x_ip / x_shoe / x_product / x_tblshoe / bet15 / bet23~27 /
--   commission —— 皆为档内早已实测而 129 件从未用过之维度。
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ══════════════════════════════════════════════════════════════════════════════════════════════
--   1. A_anchor.csv   [总包行 2999~3009 · 原 5 列 ＋六层约 60 列]
--     典型学：M06 代理线　分区：recent（s.x_rec = 1）　连接：q.`ip`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
-- ══════════════════════════════════════════════════════════════════════════════
-- §Z-00 · 会话参数（承 v11.1.0 §Z-00 · 每个 Superset 会话开跑前逐条单跑一次）
-- ══════════════════════════════════════════════════════════════════════════════
-- ▸ 导出：不需要 —— 会话参数设置，无结果集
-- ▸ 执行纪律（v11.1.0 三次实测失败之教训，原样承继）：
--   【一】每一行 SET 皆是独立语句，务请一行一跑，看到 OK 再跑下一行。
--        切勿整段一次提交 —— Superset 语句切分遇注释内分号可能切出残片。
--   【二】跑前先按 Esc 或点空白处，确保没有任何高亮选区。
--        「有选中就只跑选中那一段」，一次误触拖选即跑出半句。
--        实测教训：报 Unexpected input 'enable_spill' 者，病不在 SQL，在选区。
--   【三】若报「Unknown system variable」→ 本版不支持，径行跳过，不影响正确性。
--        若报「syntax error / Unexpected input」→ 是选区或切分之误，重按【一】【二】。
--   【四】新会话须重跑本段（会话级参数不跨会话）。
--
-- ★ HF3 新增说明：enable_spill 是 2026-08-25 两次 OOM 的直接对策
--   #010 C08_subnet_all · #078 S03_agent_score 皆报
--     Memory of process exceed limit ... Used: 107,095,273,592 / Limit: 107,092,346,019
--   但请注意：溢写只让它【跑得完】，不让它【跑得对】——
--   这两件的根因是 FROM q CROSS JOIN x_agg（x_agg 为会员级，约 16 万行），
--   笛卡尔积行数 = |q| × 16 万。溢写后仍是天量无意义行。见本包 §CROSS-JOIN 警示。

SET SESSION query_timeout = 259200;
SET enable_spill = true;
SET spill_mode = 'auto';
SET cbo_cte_reuse = true;
SET cbo_cte_reuse_rate = 1.0;
SET enable_pipeline_engine = true;
SET pipeline_dop = 0;
SET enable_global_runtime_filter = true;
-- ══════════════════════════════════════════════════════════════════════════════

SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`ip`, w.`n_orders`, w.`n_member`, w.`first_dt`, w.`last_dt`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_ip                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_ip IS NOT NULL AND TRIM(CAST(s.x_ip AS STRING)) <> ''
    GROUP BY s.x_ip
  ),
    q AS (
      WITH gold AS (SELECT DISTINCT TRIM(ip) AS ip                                                        -- 公共表表达式：产出「ip」
                    FROM ods_mariadb_2b.ods_a168_alert_ip_setting                                         -- 取数来源：取自风控 IP 名单配置表（人工标记史料源）
                    WHERE NULLIF(TRIM(ip),'') IS NOT NULL)                                                -- 过滤条件：限定该值非空
      SELECT g.ip,                                                                                        -- 取列：起始取列子句，本行先列 g.ip
             COUNT(b.bet01) AS n_orders,                                                                  -- 计数表达式：产出「n_orders」
             COUNT(DISTINCT b.bet05) AS n_member,                                                         -- 计数表达式：统计去重个数，产出「n_member」
             MIN(b.dt) AS first_dt, MAX(b.dt) AS last_dt                                                  -- 取最小值表达式：取最大值，取用 dt（营业日），产出「last_dt」
      FROM gold g                                                                                         -- 取数来源：取自本条自建的中间结果集 gold
      LEFT JOIN ods_mariadb_2b.ods_a168_bet02 b                                                           -- 左连接：取自注单明细表（金额与行为口径的第一料源）
        ON TRIM(b.ip)=g.ip AND b.dt>='2026-03-21' AND b.dt<'2026-08-07' AND b.bet02='101'                 -- 连接键：限定 TRIM(b.ip)等于 g.ip、b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      GROUP BY g.ip
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`ip` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--   2. B01_bt_panel.csv   [总包行 5314~5392 · 原 14 列 ＋六层约 60 列]
--     典型学：M15 活跃周期　分区：recent（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`bet_date`, w.`stake`, w.`game_pnl`, w.`net_pnl`, w.`rebate`, w.`validbet`, w.`n_rounds`, w.`n_tables`, w.`n_ip`, w.`late_def`, w.`late_share_d`, w.`hedge_rate_d`, w.`stake_cv_d`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH test_agents AS (                                                                               -- 公共表表达式：开启中间结果集 test_agents，其后各行为其定义体（§B01）
        SELECT age001 AS agent_id FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                   -- 取列：取用代理主档（五级代理线归属来源），产出「agent_id」
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§B01）
      ranked AS (                                                                                         -- 公共表表达式：开启中间结果集 ranked，其后各行为其定义体（§B01）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet09, b.bet11,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet09, b.bet11，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet13, b.bet14, b.bet16, b.bet17, b.bet18,                                               -- 续行：接续上一取列子句，续列 b.bet13, b.bet14, b.bet16, b.bet17, b.bet18，涉 bet13（下注金额）、bet14（派彩金额）、bet16（退水金额）
               b.bet19, b.bet20, b.bet21, b.bet22, b.bet38,                                               -- 续行：接续上一取列子句，续列 b.bet19, b.bet20, b.bet21, b.bet22, b.bet38，涉 bet19（二级代理线）、bet20（三级代理线）、bet21（四级代理线）
               b.bet39, b.category, b.ip, b.validbet,                                                     -- 续行：接续上一取列子句，续列 b.bet39, b.category, b.ip, b.validbet，涉 bet39（桌号）、validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b            -- ★ 表名开关
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'，涉 dt（营业日）
          AND b.bet02 = '101'                                                                             -- 并列条件：限定 b.bet02等于 '101'，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§B01）
      ord AS (                                                                                            -- 公共表表达式：开启中间结果集 ord，其后各行为其定义体（§B01）
        SELECT r.bet05 AS member_id, r.dt AS bet_date, r.bet39 AS table_id, r.ip AS bet_ip,               -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、bet39（桌号）、dt（营业日）
               r.bet03 AS shoe_id,                                                                        -- 靴号（B-01 斧正新增，供 shoe_len 求 max_round＝最大局号，非局数）
               r.bet09 AS bet_side,                                                                       -- 取值表达式：取用 bet09（玩法），产出「bet_side」
               CONCAT_WS('|', r.bet03, r.bet04, r.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               CAST(NULLIF(TRIM(r.bet04),'') AS INT)           AS round_no,                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「round_no」
               CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) AS fx,                                     -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「fx」
               CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4)) AS stake_raw,                              -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake_raw」
               CAST(NULLIF(TRIM(r.bet14),'') AS DECIMAL(20,4)) AS payout_raw,                             -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「payout_raw」
               CAST(NULLIF(TRIM(r.bet16),'') AS DECIMAL(20,4)) AS rebate_raw,                             -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate_raw」
               CAST(NULLIF(TRIM(r.bet17),'') AS DECIMAL(20,4)) AS net_raw,                                -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_raw」
               CAST(NULLIF(TRIM(r.validbet),'') AS DECIMAL(20,4)) AS vb_raw                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「vb_raw」
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet18（一级代理线）
        LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet19（二级代理线）
        LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet20（三级代理线）
        LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet21（四级代理线）
        LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND COALESCE(t1.agent_id,t2.agent_id,t3.agent_id,t4.agent_id,t5.agent_id) IS NULL               -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§B01）
      shoe_len AS (   /* ★ 命名警示：本 CTE 名为 shoe_len（靴长）系历史沿用，其所算 max_round = MAX(round_no) 为『该靴该桌观测到的最大局号』，非 COUNT(round_no) 之局数；二者仅在局号自1起连续无缺口时相等。选 MAX 为刻意设计：对中间缺口免疫，COUNT 会因清洗剔单而使分母缩水。★ B-01 斧正（2026-08-12）：定标基准逐靴现算，绝对局号不是固定靴位 */
        SELECT shoe_id, table_id, MAX(round_no) AS max_round
        FROM ord GROUP BY shoe_id, table_id
      ),
      ordx AS (       /* 把该靴之 max_round（最大局号）带回逐注单行，供相对靴位判定 */
        SELECT o.*, s.max_round
        FROM ord o
        JOIN shoe_len s ON s.shoe_id = o.shoe_id AND s.table_id = o.table_id
      ),
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk，其后各行为其定义体（§B01）
        SELECT member_id, bet_date, round_key, MAX(table_id) AS table_id,                                 -- 取列：起始取列子句，本行先取「table_id」，涉 round_key（局键）、member_id（会员号）、bet_date（营业日）
               MAX(bet_ip) AS bet_ip, MAX(round_no) AS round_no,                                          -- 取最大值表达式：取用 bet_ip（下注 IP），产出「round_no」
               MAX(round_no) * 1.0 / NULLIF(MAX(max_round),0) AS shoe_pos,                       -- 该局的相对靴位（B-01 斧正新增）
               SUM(stake_raw/fx)                     AS stake,                                            -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake」
               SUM(COALESCE(vb_raw,stake_raw)/fx)    AS validbet,                                         -- 汇总表达式：取用 validbet（有效投注（洗码量）），产出「validbet」
               SUM((payout_raw-stake_raw)/fx)        AS game_pnl,                                         -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「game_pnl」
               SUM(rebate_raw/fx)                    AS rebate,                                           -- 汇总表达式：取用 rebate（退水），产出「rebate」
               SUM(net_raw/fx)                       AS net_pnl,                                          -- 汇总表达式：取用 net_pnl（会员净输赢），产出「net_pnl」
               /* ★ DX-01 实测校准：等值匹配主线庄闲，不用 LIKE（会误纳龙宝/天牌边注） */
               CASE WHEN MAX(CASE WHEN TRIM(bet_side)='Banker' THEN 1 ELSE 0 END)=1                       -- 条件分支：取最大值
                     AND MAX(CASE WHEN TRIM(bet_side)='Player' THEN 1 ELSE 0 END)=1                       -- 并列条件：限定 MAX(CASE WHEN TRIM(bet_side)等于 'Player' THEN 1 ELSE 0 END)=1
                    THEN 1 ELSE 0 END                AS is_self_hedge                                     -- 分支取值：产出「is_self_hedge」
        FROM ordx GROUP BY member_id, bet_date, round_key                                                  -- 取数来源：取自本条自建的中间结果集 ord
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§B01）
      SELECT member_id, bet_date,                                                                         -- 取列：起始取列子句，本行先列 member_id, bet_date，涉 member_id（会员号）、bet_date（营业日）
             SUM(stake)                                     AS stake,                                     -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake」
             SUM(game_pnl)                                  AS game_pnl,                                  -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「game_pnl」
             SUM(net_pnl)                                   AS net_pnl,                                   -- 汇总表达式：取用 net_pnl（会员净输赢），产出「net_pnl」
             SUM(rebate)                                    AS rebate,                                    -- 汇总表达式：取用 rebate（退水），产出「rebate」
             SUM(validbet)                                  AS validbet,                                  -- 汇总表达式：取用 validbet（有效投注（洗码量）），产出「validbet」
             COUNT(*)                                       AS n_rounds,                                  -- 计数表达式：产出「n_rounds」
             COUNT(DISTINCT table_id)                       AS n_tables,                                  -- 计数表达式：统计去重个数，产出「n_tables」
             COUNT(DISTINCT bet_ip)                         AS n_ip,                                      -- 计数表达式：统计去重个数，产出「n_ip」
             'shoe_pos>=0.80'                               AS late_def,                                  -- 口径锁：definition_version = v2（旧版 CSV 缺此列即报错）
             SUM(CASE WHEN shoe_pos >= 0.80 THEN 1 ELSE 0 END) * 1.0 / COUNT(*) AS late_share_d,  -- 靴末段占比（B-01 斧正：相对靴位 ≥0.80；原为 round_no>=50）
             SUM(is_self_hedge) * 1.0 / COUNT(*)            AS hedge_rate_d,                              -- 汇总表达式：计数，产出「hedge_rate_d」
             STDDEV_SAMP(stake) / NULLIF(AVG(stake),0)      AS stake_cv_d                                 -- 取值表达式：求均值，取用 stake（下注额（经汇率归一化）），产出「stake_cv_d」
      FROM rk                                                                                             -- 取数来源：取自本条自建的中间结果集 rk
      GROUP BY member_id, bet_date                                                                        -- 分组：按 member_id, bet_date 汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake_xagg, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds AS n_rounds_xagg,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--   3. B01_bt_panel_delta.csv   [总包行 5406~5484 · 原 14 列 ＋六层约 60 列]
--     典型学：M15 活跃周期　分区：recent（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`bet_date`, w.`stake`, w.`game_pnl`, w.`net_pnl`, w.`rebate`, w.`validbet`, w.`n_rounds`, w.`n_tables`, w.`n_ip`, w.`late_def`, w.`late_share_d`, w.`hedge_rate_d`, w.`stake_cv_d`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH test_agents AS (                                                                               -- 公共表表达式：开启中间结果集 test_agents，其后各行为其定义体
        SELECT age001 AS agent_id FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                   -- 取列：取用代理主档（五级代理线归属来源），产出「agent_id」
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      ranked AS (                                                                                         -- 公共表表达式：开启中间结果集 ranked，其后各行为其定义体
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet09, b.bet11,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet09, b.bet11，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet13, b.bet14, b.bet16, b.bet17, b.bet18,                                               -- 续行：接续上一取列子句，续列 b.bet13, b.bet14, b.bet16, b.bet17, b.bet18，涉 bet13（下注金额）、bet14（派彩金额）、bet16（退水金额）
               b.bet19, b.bet20, b.bet21, b.bet22, b.bet38,                                               -- 续行：接续上一取列子句，续列 b.bet19, b.bet20, b.bet21, b.bet22, b.bet38，涉 bet19（二级代理线）、bet20（三级代理线）、bet21（四级代理线）
               b.bet39, b.category, b.ip, b.validbet,                                                     -- 续行：接续上一取列子句，续列 b.bet39, b.category, b.ip, b.validbet，涉 bet39（桌号）、validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-08-04' AND b.dt < '2026-08-07'   -- ★★ 每日唯一要改的一行 ★★
          AND b.bet02 = '101'                                                                             -- 并列条件：限定 b.bet02等于 '101'，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      ord AS (                                                                                            -- 公共表表达式：开启中间结果集 ord，其后各行为其定义体
        SELECT r.bet05 AS member_id, r.dt AS bet_date, r.bet39 AS table_id, r.ip AS bet_ip,               -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、bet39（桌号）、dt（营业日）
               r.bet03 AS shoe_id,                                                                        -- 靴号（B-01 斧正新增，供 shoe_len 求 max_round＝最大局号，非局数）
               r.bet09 AS bet_side,                                                                       -- 取值表达式：取用 bet09（玩法），产出「bet_side」
               CONCAT_WS('|', r.bet03, r.bet04, r.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               CAST(NULLIF(TRIM(r.bet04),'') AS INT)           AS round_no,                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「round_no」
               CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) AS fx,                                     -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「fx」
               CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4)) AS stake_raw,                              -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake_raw」
               CAST(NULLIF(TRIM(r.bet14),'') AS DECIMAL(20,4)) AS payout_raw,                             -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「payout_raw」
               CAST(NULLIF(TRIM(r.bet16),'') AS DECIMAL(20,4)) AS rebate_raw,                             -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate_raw」
               CAST(NULLIF(TRIM(r.bet17),'') AS DECIMAL(20,4)) AS net_raw,                                -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_raw」
               CAST(NULLIF(TRIM(r.validbet),'') AS DECIMAL(20,4)) AS vb_raw                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「vb_raw」
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet18（一级代理线）
        LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet19（二级代理线）
        LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet20（三级代理线）
        LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet21（四级代理线）
        LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND COALESCE(t1.agent_id,t2.agent_id,t3.agent_id,t4.agent_id,t5.agent_id) IS NULL               -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      shoe_len AS (   /* ★ 命名警示：本 CTE 名为 shoe_len（靴长）系历史沿用，其所算 max_round = MAX(round_no) 为『该靴该桌观测到的最大局号』，非 COUNT(round_no) 之局数；二者仅在局号自1起连续无缺口时相等。选 MAX 为刻意设计：对中间缺口免疫，COUNT 会因清洗剔单而使分母缩水。★ B-01 斧正（2026-08-12）：定标基准逐靴现算，绝对局号不是固定靴位 */
        SELECT shoe_id, table_id, MAX(round_no) AS max_round
        FROM ord GROUP BY shoe_id, table_id
      ),
      ordx AS (       /* 把该靴之 max_round（最大局号）带回逐注单行，供相对靴位判定 */
        SELECT o.*, s.max_round
        FROM ord o
        JOIN shoe_len s ON s.shoe_id = o.shoe_id AND s.table_id = o.table_id
      ),
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk，其后各行为其定义体
        SELECT member_id, bet_date, round_key, MAX(table_id) AS table_id,                                 -- 取列：起始取列子句，本行先取「table_id」，涉 round_key（局键）、member_id（会员号）、bet_date（营业日）
               MAX(bet_ip) AS bet_ip, MAX(round_no) AS round_no,                                          -- 取最大值表达式：取用 bet_ip（下注 IP），产出「round_no」
               MAX(round_no) * 1.0 / NULLIF(MAX(max_round),0) AS shoe_pos,                       -- 该局的相对靴位（B-01 斧正新增）
               SUM(stake_raw/fx)                     AS stake,                                            -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake」
               SUM(COALESCE(vb_raw,stake_raw)/fx)    AS validbet,                                         -- 汇总表达式：取用 validbet（有效投注（洗码量）），产出「validbet」
               SUM((payout_raw-stake_raw)/fx)        AS game_pnl,                                         -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「game_pnl」
               SUM(rebate_raw/fx)                    AS rebate,                                           -- 汇总表达式：取用 rebate（退水），产出「rebate」
               SUM(net_raw/fx)                       AS net_pnl,                                          -- 汇总表达式：取用 net_pnl（会员净输赢），产出「net_pnl」
               /* DX-01 实测校准：等值匹配主线庄闲，不用 LIKE（会误纳龙宝/天牌边注） */
               CASE WHEN MAX(CASE WHEN TRIM(bet_side)='Banker' THEN 1 ELSE 0 END)=1                       -- 条件分支：取最大值
                     AND MAX(CASE WHEN TRIM(bet_side)='Player' THEN 1 ELSE 0 END)=1                       -- 并列条件：限定 MAX(CASE WHEN TRIM(bet_side)等于 'Player' THEN 1 ELSE 0 END)=1
                    THEN 1 ELSE 0 END                AS is_self_hedge                                     -- 分支取值：产出「is_self_hedge」
        FROM ordx GROUP BY member_id, bet_date, round_key                                                  -- 取数来源：取自本条自建的中间结果集 ord
      )                                                                                                   -- 续行：收束上方的子查询或函数括号
      SELECT member_id, bet_date,                                                                         -- 取列：起始取列子句，本行先列 member_id, bet_date，涉 member_id（会员号）、bet_date（营业日）
             SUM(stake)                                     AS stake,                                     -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake」
             SUM(game_pnl)                                  AS game_pnl,                                  -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「game_pnl」
             SUM(net_pnl)                                   AS net_pnl,                                   -- 汇总表达式：取用 net_pnl（会员净输赢），产出「net_pnl」
             SUM(rebate)                                    AS rebate,                                    -- 汇总表达式：取用 rebate（退水），产出「rebate」
             SUM(validbet)                                  AS validbet,                                  -- 汇总表达式：取用 validbet（有效投注（洗码量）），产出「validbet」
             COUNT(*)                                       AS n_rounds,                                  -- 计数表达式：产出「n_rounds」
             COUNT(DISTINCT table_id)                       AS n_tables,                                  -- 计数表达式：统计去重个数，产出「n_tables」
             COUNT(DISTINCT bet_ip)                         AS n_ip,                                      -- 计数表达式：统计去重个数，产出「n_ip」
             'shoe_pos>=0.80'                               AS late_def,                                  -- 口径锁：definition_version = v2（旧版 CSV 缺此列即报错）
             SUM(CASE WHEN shoe_pos >= 0.80 THEN 1 ELSE 0 END) * 1.0 / COUNT(*) AS late_share_d,  -- 靴末段占比（B-01 斧正：相对靴位 ≥0.80；原为 round_no>=50）
             SUM(is_self_hedge) * 1.0 / COUNT(*)            AS hedge_rate_d,                              -- 汇总表达式：计数，产出「hedge_rate_d」
             STDDEV_SAMP(stake) / NULLIF(AVG(stake),0)      AS stake_cv_d                                 -- 取值表达式：求均值，取用 stake（下注额（经汇率归一化）），产出「stake_cv_d」
      FROM rk                                                                                             -- 取数来源：取自本条自建的中间结果集 rk
      GROUP BY member_id, bet_date                                                                        -- 分组：按 member_id, bet_date 汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake_xagg, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds AS n_rounds_xagg,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--   4. B4_2b_funnel_and_mu0.csv   [总包行 11938~12058 · 原 26 列 ＋六层约 60 列]
--     典型学：M14 投注形态　分区：freecomm（s.x_freecomm = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`n_raw`, w.`n_g1`, w.`n_g2`, w.`n_g3`, w.`n_g4`, w.`n_g5`, w.`n_g6`, w.`n_g7`, w.`n_g8`, w.`n_lv1`, w.`n_lv2`, w.`n_lv3`, w.`n_lv4`, w.`n_lv5`, w.`n_any_test`, w.`mu0_global_dirty_ew_pct`, w.`mu0_global_clean_ew_pct`, w.`contamination_pp`, w.`n_groups`, w.`mu0_min_pct`, w.`mu0_max_pct`, w.`abs_min`, w.`abs_max`, w.`n_near_zero`, w.`spread_ratio`, w.`verdict_spread`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ta AS (                                                                                        -- 公共表表达式：开启中间结果集 ta——公司测试线代理
        SELECT DISTINCT age001                                                 AS aid                     -- 取列：代理编号
        FROM ods_mariadb_2b.ods_a168_agent                                                                -- 取数来源：取自代理主档
        WHERE age022 = '1'                                                                                -- 过滤条件：铁律④b——公司测试线
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk——同注单号取最新版本
        SELECT b.bet01, b.bet05, b.bet09, b.bet11, b.bet13, b.bet14, b.dt, b.commission,                  -- 取列：注单号、会员、玩法、汇率、本金、派彩、分区日、佣金标记
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22, b.category, b.bet38,                          -- 续行：五级代理、注单类别、重对标记
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：取每注单之最新版本
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按注单号分区
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC)      AS rn                      -- 排序：三级排序取最新
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单主表
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定分析窗
          AND CAST(b.bet02 AS STRING) = '101'                                                             -- 并列条件：限定百家乐
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      tagged AS (                                                                                         -- 公共表表达式：开启中间结果集 tagged——逐行打 gate 标，不过滤
        SELECT r.bet01, r.bet05, TRIM(r.bet09)                                 AS g,                      -- 取列：注单号、会员、注型组
               TRIM(r.commission)                                              AS comm,                   -- 取列：佣金标记
               r.dt                                                            AS dt,                     -- 取列：分区日
               CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8))
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)), 0) AS stake,                  -- 取值表达式：本金口径 stake
               CAST(NULLIF(TRIM(r.bet14), '') AS DECIMAL(20,8))
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)), 0) AS payout,                 -- 取值表达式：派彩，同除汇率归一
               CASE WHEN r.rn = 1                            THEN 1 ELSE 0 END AS g1_latest,              -- 条件取值：gate①——最新版本
               CASE WHEN r.category = '1'                    THEN 1 ELSE 0 END AS g2_category,            -- 条件取值：gate②——一般注单
               CASE WHEN UPPER(TRIM(r.bet38)) = 'N'          THEN 1 ELSE 0 END AS g3_norebet,             -- 条件取值：gate③——非重对
               CASE WHEN t1.aid IS NULL THEN 0 ELSE 1 END                      AS lv1_hit,                -- 条件取值：★ 一级代理命中测试线
               CASE WHEN t2.aid IS NULL THEN 0 ELSE 1 END                      AS lv2_hit,                -- 条件取值：★ 二级代理命中
               CASE WHEN t3.aid IS NULL THEN 0 ELSE 1 END                      AS lv3_hit,                -- 条件取值：★ 三级代理命中
               CASE WHEN t4.aid IS NULL THEN 0 ELSE 1 END                      AS lv4_hit,                -- 条件取值：★ 四级代理命中
               CASE WHEN t5.aid IS NULL THEN 0 ELSE 1 END                      AS lv5_hit,                -- 条件取值：★ 五级代理命中
               CASE WHEN COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL
                    THEN 1 ELSE 0 END                                          AS g4_notest,              -- 条件取值：gate④——非测试线
               CASE WHEN CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)) > 0
                    THEN 1 ELSE 0 END                                          AS g5_rate,                -- 条件取值：gate⑤——汇率为正
               CASE WHEN CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8)) > 0
                    THEN 1 ELSE 0 END                                          AS g6_stake,               -- 条件取值：gate⑥——本金为正
               CASE WHEN TRIM(r.commission) IN ('0', '1')    THEN 1 ELSE 0 END AS g7_comm,                -- 条件取值：gate⑦——佣金标记非哨兵
               CASE WHEN NOT (r.dt = '2026-07-20' AND TRIM(r.bet09) IN
                    ('Bear8', 'Lucky7TwoCard', 'SmallTiger', 'SuperTie4',
                     'SuperTie5', 'SuperTie8', 'SuperTie9'))
                    THEN 1 ELSE 0 END                                          AS g8_launch               -- 条件取值：gate⑧——非上线测试单
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：一级代理是否测试线
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：二级代理是否测试线
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：三级代理是否测试线
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：四级代理是否测试线
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：五级代理是否测试线
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      funnel AS (                                                                                         -- 公共表表达式：开启中间结果集 funnel——逐 gate 漏斗
        SELECT COUNT(*)                                                        AS n_raw,                  -- 计数表达式：原始行数
               SUM(g1_latest)                                                  AS n_g1,                   -- 汇总表达式：过 gate① 者
               SUM(g1_latest * g2_category)                                    AS n_g2,                   -- 汇总表达式：累计过 gate①②
               SUM(g1_latest * g2_category * g3_norebet)                       AS n_g3,                   -- 汇总表达式：累计过 ①②③
               SUM(g1_latest * g2_category * g3_norebet * g4_notest)           AS n_g4,                   -- 汇总表达式：累计过 ①~④
               SUM(g1_latest * g2_category * g3_norebet * g4_notest * g5_rate) AS n_g5,                   -- 汇总表达式：累计过 ①~⑤
               SUM(g1_latest * g2_category * g3_norebet * g4_notest * g5_rate * g6_stake)          AS n_g6, -- 汇总表达式：累计过 ①~⑥（＝ dirty 母体）
               SUM(g1_latest * g2_category * g3_norebet * g4_notest * g5_rate * g6_stake * g7_comm) AS n_g7, -- 汇总表达式：累计过 ①~⑦
               SUM(g1_latest * g2_category * g3_norebet * g4_notest * g5_rate * g6_stake * g7_comm * g8_launch) AS n_g8, -- 汇总表达式：累计过 ①~⑧（＝ clean 母体）
               SUM(lv1_hit) AS n_lv1, SUM(lv2_hit) AS n_lv2, SUM(lv3_hit) AS n_lv3,                       -- 汇总表达式：★ 五级代理逐级命中
               SUM(lv4_hit) AS n_lv4, SUM(lv5_hit) AS n_lv5,                                              -- 汇总表达式：四级与五级命中
               SUM(CASE WHEN g4_notest = 0 THEN 1 ELSE 0 END)                  AS n_any_test              -- 条件计数：任一级命中之合计
        FROM tagged                                                                                       -- 取数来源：取自本条自建的中间结果集 tagged
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      global_mu0 AS (                                                                                     -- 公共表表达式：★ P0-1 —— 原名 glob 系保留字，今正名
        SELECT SUM(CASE WHEN g1_latest * g2_category * g3_norebet * g4_notest * g5_rate * g6_stake = 1
                        THEN stake END)                                        AS s_dirty,                -- 条件汇总：dirty 母体之本金（未剔哨兵与上线测试）
             SUM(CASE WHEN g1_latest * g2_category * g3_norebet * g4_notest * g5_rate * g6_stake = 1
                        THEN stake - payout END)                               AS g_dirty,                -- 条件汇总：dirty 母体之毛利
             SUM(CASE WHEN g1_latest * g2_category * g3_norebet * g4_notest * g5_rate * g6_stake * g7_comm * g8_launch = 1
                        THEN stake END)                                        AS s_clean,                -- 条件汇总：clean 母体之本金
             SUM(CASE WHEN g1_latest * g2_category * g3_norebet * g4_notest * g5_rate * g6_stake * g7_comm * g8_launch = 1
                        THEN stake - payout END)                               AS g_clean                 -- 条件汇总：clean 母体之毛利
        FROM tagged                                                                                       -- 取数来源：取自本条自建的中间结果集 tagged
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      byg AS (                                                                                            -- 公共表表达式：开启中间结果集 byg——逐组 μ0（★ 增报稳定性列）
        SELECT g,                                                                                         -- 取列：注型组
               COUNT(*)                                                        AS n_bets,                 -- 计数表达式：组内注单数
               COUNT(DISTINCT bet05)                                           AS n_members,              -- 计数表达式：★ 组内会员数
               COUNT(DISTINCT dt)                                              AS n_days,                 -- 计数表达式：★ 组内出现天数
               SUM(stake)                                                      AS stake_sum,              -- 汇总表达式：组内本金暴露
               SUM(payout)                                                     AS payout_sum,             -- 汇总表达式：★ 组内派彩暴露
               -1 * SUM(stake - payout) * 100.0 / NULLIF(SUM(stake), 0)        AS mu0_pct                 -- 比率表达式：该组 μ0（暴露加权，GGR 口径）
        FROM tagged                                                                                       -- 取数来源：取自本条自建的中间结果集 tagged
        WHERE g1_latest * g2_category * g3_norebet * g4_notest * g5_rate * g6_stake * g7_comm * g8_launch = 1 -- 过滤条件：只取 clean 母体
        GROUP BY g                                                                                        -- 分组：按注型组汇总
        HAVING COUNT(*) >= 1000                                                                           -- 分组后过滤：小组无判别力，不入分层诊断
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      agg AS (                                                                                            -- 公共表表达式：开启中间结果集 agg——分层极值与 near-zero 计数
        SELECT COUNT(*)                                                        AS n_groups,               -- 计数表达式：可诊断之组数
               MIN(mu0_pct)                                                    AS mu0_min_pct,            -- 聚合取值：分层下界
               MAX(mu0_pct)                                                    AS mu0_max_pct,            -- 聚合取值：分层上界
               MIN(ABS(mu0_pct))                                               AS abs_min,                -- 聚合取值：绝对值下界
               MAX(ABS(mu0_pct))                                               AS abs_max,                -- 聚合取值：绝对值上界
               SUM(CASE WHEN ABS(mu0_pct) < 0.05 THEN 1 ELSE 0 END)            AS n_near_zero             -- 条件计数：★ P0-4——μ0 逼近零之组数，极差比之爆炸源
        FROM byg                                                                                          -- 取数来源：取自本条自建的中间结果集 byg
      )                                                                                                   -- 续行：收束上方的子查询或函数括号
      SELECT                                                                                              -- 取列：起始取列子句
        f.n_raw                                                                AS n_raw,                  -- 取列：原始行数
        f.n_g1, f.n_g2, f.n_g3, f.n_g4, f.n_g5, f.n_g6, f.n_g7, f.n_g8,                                   -- 取列：★ 逐 gate 累计存活行数——污染量化之底料
        f.n_lv1, f.n_lv2, f.n_lv3, f.n_lv4, f.n_lv5, f.n_any_test,                                        -- 取列：★ 五级代理逐级命中与合计
        ROUND(-1 * gm.g_dirty * 100.0 / NULLIF(gm.s_dirty, 0), 6)              AS mu0_global_dirty_ew_pct, -- 比率表达式：★ dirty 全局 μ0（暴露加权）
        ROUND(-1 * gm.g_clean * 100.0 / NULLIF(gm.s_clean, 0), 6)              AS mu0_global_clean_ew_pct, -- 比率表达式：★ clean 全局 μ0（暴露加权）
        ROUND(ABS(-1 * gm.g_dirty * 100.0 / NULLIF(gm.s_dirty, 0)
                - (-1 * gm.g_clean * 100.0 / NULLIF(gm.s_clean, 0))), 8)       AS contamination_pp,       -- 比率表达式：★★ 二者之差即 SC-2 污染影响（百分点）
        a.n_groups                                                             AS n_groups,               -- 取列：可诊断之组数
        ROUND(a.mu0_min_pct, 6)                                                AS mu0_min_pct,            -- 取列：分层下界
        ROUND(a.mu0_max_pct, 6)                                                AS mu0_max_pct,            -- 取列：分层上界
        ROUND(a.abs_min, 6)                                                    AS abs_min,                -- 取列：★ 绝对值下界——须与 spread 并读
        ROUND(a.abs_max, 6)                                                    AS abs_max,                -- 取列：绝对值上界
        a.n_near_zero                                                          AS n_near_zero,            -- 取列：★ near-zero 组数
        ROUND(a.abs_max / NULLIF(a.abs_min, 0), 4)                             AS spread_ratio,           -- 比率表达式：极差比（★ 诊断排序器，非裁定判据）
        CASE                                                                                              -- 条件分支：起始分支——★ P0-3 判词降级
          WHEN a.n_groups < 2                THEN 'NON_DIAGNOSTIC（可诊断之组不足二）'                       -- 分支判定：无从比较
          WHEN a.n_near_zero > 0             THEN 'NON_DIAGNOSTIC（有组 μ0 逼近零，极差比失真）'              -- 分支判定：★ near-zero 保护
          WHEN a.abs_min < 0.05              THEN 'NON_DIAGNOSTIC（下界过小，极差比失真）'                    -- 分支判定：ε 门槛
          ELSE '✅ spread_ratio 可读（★ 系诊断排序器，不单独裁 S1；分层必要性须并读 abs_min／abs_max／组样本量）' -- 分支判定：可读
        END                                                                    AS verdict_spread          -- 条件分支：收束上方判词，产出「verdict_spread」
      FROM agg a                                                                                          -- 取数来源：取自本条自建的中间结果集 agg
      CROSS JOIN global_mu0 gm                                                                            -- 交叉连接：★ 别名 gm，接单行全局汇总
      CROSS JOIN funnel f                                                                                -- 交叉连接：接单行漏斗汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_freecomm,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_freecomm,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_freecomm,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_freecomm,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_freecomm,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_freecomm,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_freecomm,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_freecomm,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--   5. B4_5b_tip_nature.csv   [总包行 12072~12103 · 原 16 列 ＋六层约 60 列]
--     典型学：M14 投注形态　分区：freecomm（s.x_freecomm = 1）　连接：q.`bet09_value`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`bet09_value`, w.`n_rows`, w.`n_members`, w.`n_days`, w.`n_b13_blank`, w.`n_b13_zero`, w.`n_b13_pos`, w.`n_b14_blank`, w.`n_b14_zero`, w.`n_b14_pos`, w.`n_both_pos`, w.`n_b13pos_b14zero`, w.`b13_sum`, w.`b14_sum`, w.`n_comm`, w.`verdict_nature`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_product                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_product IS NOT NULL AND TRIM(CAST(s.x_product AS STRING)) <> ''
    GROUP BY s.x_product
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        TRIM(bet09)                                                            AS bet09_value,            -- 取列：注型原值
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：总行数
        COUNT(DISTINCT TRIM(bet05))                                            AS n_members,              -- 计数表达式：涉及会员数
        COUNT(DISTINCT dt)                                                     AS n_days,                 -- 计数表达式：出现天数
        SUM(CASE WHEN NULLIF(TRIM(bet13), '') IS NULL THEN 1 ELSE 0 END)       AS n_b13_blank,            -- 条件计数：本金为空
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_b13_zero, -- 条件计数：本金为零
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_b13_pos,  -- 条件计数：本金为正
        SUM(CASE WHEN NULLIF(TRIM(bet14), '') IS NULL THEN 1 ELSE 0 END)       AS n_b14_blank,            -- 条件计数：★ 派彩为空
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_b14_zero, -- 条件计数：★ 派彩为零
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_b14_pos,  -- 条件计数：★ 派彩为正
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0
                   AND CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_both_pos, -- 条件计数：★ 二者皆正——具赔付关系者
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0
                   AND COALESCE(CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)), 0) = 0 THEN 1 ELSE 0 END) AS n_b13pos_b14zero, -- 条件计数：★★ 有本金无派彩——单向扣款
        ROUND(SUM(CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))), 2)          AS b13_sum,                -- 汇总表达式：本金总额
        ROUND(SUM(CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8))), 2)          AS b14_sum,                -- 汇总表达式：派彩总额
        COUNT(DISTINCT TRIM(commission))                                       AS n_comm,                 -- 计数表达式：佣金标记取值数
        CASE                                                                                              -- 条件分支：起始分支——性质裁定（四态）
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) = 0
           AND SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) = COUNT(*)
            THEN '⚠ 单向扣款（本金恒正・派彩恒零）—— UNKNOWN / SPECIAL_TRANSACTION，禁径判为小费'          -- 分支判定：单向扣款
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) > 0
            THEN '◐ 具赔付关系——形似投注，惟仍须上游确认其玩法定义'                                        -- 分支判定：具赔付
          ELSE '⚠ 结构混杂——须再拆分后重验'                                                               -- 分支判定：混杂
        END                                                                    AS verdict_nature          -- 条件分支：收束上方判词，产出「verdict_nature」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
        AND TRIM(bet09) IN ('Tip_1_', 'Banker', 'Tie')                                                    -- 并列条件：受检注型 ＋ 二组对照（Banker 常态・Tie 高赔）
      GROUP BY TRIM(bet09)                                                                                -- 分组：按注型原值汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_freecomm,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_freecomm,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_freecomm,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_freecomm,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_freecomm,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_freecomm,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_freecomm,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_freecomm,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`bet09_value` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--   6. B_online_base.csv   [总包行 2929~2991 · 原 7 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：q.`table_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`table_id`, w.`sec_elapsed`, w.`n_orders`, w.`n_player`, w.`n_rounds`, w.`valid_bet`, w.`game_pnl`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_table                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_table IS NOT NULL AND TRIM(CAST(s.x_table AS STRING)) <> ''
    GROUP BY s.x_table
  ),
    q AS (
      WITH ta AS (            -- 公司测试线代理（214 条，跨五级）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 B_online_base.csv）
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk，其后各行为其定义体（交付件 B_online_base.csv）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet09，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet11, b.bet13, b.bet14, b.bet16, b.bet17,                                               -- 续行：接续上一取列子句，续列 b.bet11, b.bet13, b.bet14, b.bet16, b.bet17，涉 bet11（汇率）、bet13（下注金额）、bet14（派彩金额）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet38, b.bet39, b.category, b.eid, b.ip,                                                 -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category, b.eid, b.ip，涉 bet38（重對）、bet39（桌号）、eid（荷官工号）
               b.validbet,                                                                                -- 续行：接续上一取列子句，续列 b.validbet，涉 validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 B_online_base.csv）
      vd AS (                                                                                             -- 公共表表达式：开启中间结果集 vd，其后各行为其定义体（交付件 B_online_base.csv）
        SELECT r.* FROM rk r                                                                              -- 取列：整体承接上游结果集的全部字段，不再逐列列举，取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 B_online_base.csv）
      bs AS (                                                                                             -- 公共表表达式：开启中间结果集 bs，其后各行为其定义体（交付件 B_online_base.csv）
        SELECT v.bet05 AS member_id, v.eid AS dealer_id, v.ip AS bet_ip,                                  -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、eid（荷官工号）、bet_ip（下注 IP）
               v.bet20 AS lv3, v.bet09 AS bet_side, v.dt AS bet_date,                                     -- 取值表达式：取用 bet09（玩法）、bet20（三级代理线）、dt（营业日），产出「bet_date」
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.bet08),'') AS DATETIME) AS t_bet,                                       -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「t_bet」
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake,                              -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               CAST(NULLIF(TRIM(v.validbet),'') AS DECIMAL(20,4))                                         -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS valid_bet,                          -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「valid_bet」
               (CAST(NULLIF(TRIM(v.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS game_pnl,                           -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               CAST(NULLIF(TRIM(v.bet16),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS rebate,                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
               CAST(NULLIF(TRIM(v.bet17),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS net_pnl                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_pnl」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 B_online_base.csv）
      gi AS (SELECT CONCAT_WS('|', gi002, gi003, gi011) AS round_key,                                     -- 取值表达式：取用 gi011（桌号）、round_key（局键），产出「round_key」
                    gi004 AS t_open, gi006 AS t_reveal                                                    -- 取值表达式：取用 gi004（局开始时刻）、gi006（开牌时刻），产出「t_reveal」
             FROM ods_mariadb_2b.ods_a168_game_info                                                       -- 取数来源：取自局信息表（靴局、开牌与桌号的时序基准）
             WHERE gi001='101' AND gi013='1' AND is_lock='N'                                              -- 过滤条件：限定 gi001等于 '101'、gi013等于 '1'、is_lock等于 'N'，涉 gi001（局主键）
               AND gi004>='2026-03-21' AND gi004<'2026-08-07'),                                           -- 并列条件：限定 gi004不少于 '2026-03-21'，涉 gi004（局开始时刻）
      pr AS (SELECT b.table_id, b.round_key, b.member_id, b.valid_bet, b.game_pnl,                        -- 取值表达式：取用 valid_bet（有效投注（洗码量））、round_key（局键）、game_pnl（游戏净输赢）
                    FLOOR((UNIX_TIMESTAMP(b.t_bet)-UNIX_TIMESTAMP(g.t_open))) AS sec_elapsed              -- 取值表达式：产出「sec_elapsed」
             FROM bs b JOIN gi g ON g.round_key=b.round_key)                                              -- 取数来源：取自本条自建的中间结果集 bs，连接键为 round_key（局键）
      SELECT table_id, sec_elapsed,                                                                       -- 取列：起始取列子句，本行先列 table_id, sec_elapsed
             COUNT(*) AS n_orders, COUNT(DISTINCT member_id) AS n_player,                                 -- 计数表达式：统计去重个数，产出「n_player」
             COUNT(DISTINCT round_key) AS n_rounds,                                                       -- 计数表达式：统计去重个数，产出「n_rounds」
             SUM(valid_bet) AS valid_bet, SUM(game_pnl) AS game_pnl                                       -- 汇总表达式：取用 valid_bet（有效投注（洗码量））、game_pnl（游戏净输赢），产出「game_pnl」
      FROM pr WHERE sec_elapsed BETWEEN 0 AND 120                                                         -- 取数来源：取自本条自建的中间结果集 pr
      GROUP BY table_id, sec_elapsed
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet_xagg,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds AS n_rounds_xagg,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`table_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--   7. C01_ip_chain.csv   [总包行 1663~1723 · 原 8 列 ＋六层约 60 列]
--     典型学：M04 IP 聚集　分区：recent（s.x_rec = 1）　连接：q.`bet_ip`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`bet_ip`, w.`n_member`, w.`n_lv3_chain`, w.`member_per_chain`, w.`n_orders`, w.`stake`, w.`game_pnl`, w.`net_pnl`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_ip                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_ip IS NOT NULL AND TRIM(CAST(s.x_ip AS STRING)) <> ''
    GROUP BY s.x_ip
  ),
    q AS (
      WITH ta AS (            -- 公司测试线代理（214 条，跨五级）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 C01_ip_chain.csv）
      rk AS (                  -- 需求 §3.2：同注单号取最新版本（三级排序去重）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet09，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet11, b.bet13, b.bet14, b.bet16, b.bet17,                                               -- 续行：接续上一取列子句，续列 b.bet11, b.bet13, b.bet14, b.bet16, b.bet17，涉 bet11（汇率）、bet13（下注金额）、bet14（派彩金额）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet38, b.bet39, b.category, b.eid, b.ip,                                                 -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category, b.eid, b.ip，涉 bet38（重對）、bet39（桌号）、eid（荷官工号）
               b.validbet,                                                                                -- 续行：接续上一取列子句，续列 b.validbet，涉 validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 C01_ip_chain.csv）
      vd AS (                  -- 有效注单：非测试线、非重对、一般注单
        SELECT r.*                                                                                        -- 取列：整体承接上游结果集的全部字段，不再逐列列举
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 C01_ip_chain.csv）
      bs AS (                  -- 金额正名：本金/洗码量/游戏输赢/退水/净输赢（一律÷汇率）
        SELECT v.bet05 AS member_id, v.eid AS dealer_id, v.ip AS bet_ip,                                  -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、eid（荷官工号）、bet_ip（下注 IP）
               v.bet20 AS lv3, v.bet09 AS bet_side, v.dt AS bet_date,                                     -- 取值表达式：取用 bet09（玩法）、bet20（三级代理线）、dt（营业日），产出「bet_date」
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.bet08),'') AS DATETIME) AS t_bet,                                       -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「t_bet」
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake,                              -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               CAST(NULLIF(TRIM(v.validbet),'') AS DECIMAL(20,4))                                         -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS valid_bet,                          -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「valid_bet」
               (CAST(NULLIF(TRIM(v.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS game_pnl,                           -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               CAST(NULLIF(TRIM(v.bet16),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS rebate,                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
               CAST(NULLIF(TRIM(v.bet17),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS net_pnl                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_pnl」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（交付件 C01_ip_chain.csv）
      SELECT bet_ip,                                                                                      -- 取列：起始取列子句，本行先列 bet_ip，涉 bet_ip（下注 IP）
             COUNT(DISTINCT member_id) AS n_member,                                                       -- 计数表达式：统计去重个数，产出「n_member」
             COUNT(DISTINCT lv3)       AS n_lv3_chain,                                                    -- 计数表达式：统计去重个数，产出「n_lv3_chain」
             COUNT(DISTINCT member_id)*1.0/NULLIF(COUNT(DISTINCT lv3),0) AS member_per_chain,             -- 计数表达式：统计去重个数，产出「member_per_chain」
             COUNT(*) AS n_orders, SUM(stake) AS stake,                                                   -- 计数表达式：汇总，取用 stake（下注额（经汇率归一化）），产出「stake」
             SUM(game_pnl) AS game_pnl, SUM(net_pnl) AS net_pnl                                           -- 汇总表达式：取用 net_pnl（会员净输赢）、game_pnl（游戏净输赢），产出「net_pnl」
      FROM bs                                                                                             -- 取数来源：取自本条自建的中间结果集 bs
      WHERE NULLIF(TRIM(bet_ip),'') IS NOT NULL                                                           -- 过滤条件：限定该值非空，涉 bet_ip（下注 IP）
      GROUP BY bet_ip                                                                                     -- 分组：按 bet_ip 汇总
      HAVING COUNT(DISTINCT member_id) >= 20                                                              -- 分组后过滤：限定 member_id 的去重个数不少于 20，涉 member_id（会员号）
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake_xagg, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`bet_ip` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--   8. C06_hedge_pairs.csv   [总包行 5814~5886 · 原 7 列 ＋六层约 60 列]
--     典型学：M02 对冲对打　分区：hedge（h.n_side >= 2）　连接：q.`bet_ip`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`bet_ip`, w.`m_a`, w.`m_b`, w.`n_same_round`, w.`n_opposite_round`, w.`opposite_rate`, w.`hedge_coverage`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_hg AS (SELECT x_round, COUNT(DISTINCT x_product) AS n_side FROM x_bs GROUP BY x_round),
  x_agg AS (
    SELECT s.x_ip                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN h.n_side >= 2 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN h.n_side >= 2 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN h.n_side >= 2 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (h.n_side >= 2) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (h.n_side >= 2) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (h.n_side >= 2) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    JOIN x_hg h ON h.x_round = s.x_round
    WHERE s.x_ip IS NOT NULL AND TRIM(CAST(s.x_ip AS STRING)) <> ''
    GROUP BY s.x_ip
  ),
    q AS (
      WITH ta AS (                                                                                        -- 公共表表达式：开启中间结果集 ta，其后各行为其定义体（§C06fix）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§C06fix）
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk，其后各行为其定义体（§C06fix）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet09，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet11, b.bet13, b.bet18, b.bet19, b.bet20,                                               -- 续行：接续上一取列子句，续列 b.bet11, b.bet13, b.bet18, b.bet19, b.bet20，涉 bet11（汇率）、bet13（下注金额）、bet18（一级代理线）
               b.bet21, b.bet22, b.bet38, b.bet39, b.category,                                            -- 续行：接续上一取列子句，续列 b.bet21, b.bet22, b.bet38, b.bet39, b.category，涉 bet21（四级代理线）、bet22（五级代理线）、bet38（重對）
               b.eid, b.ip,                                                                               -- 续行：接续上一取列子句，续列 b.eid, b.ip，涉 eid（荷官工号）
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§C06fix）
      vd AS (                                                                                             -- 公共表表达式：开启中间结果集 vd，其后各行为其定义体（§C06fix）
        SELECT r.*                                                                                        -- 取列：整体承接上游结果集的全部字段，不再逐列列举
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§C06fix）
      bs AS (                                                                                             -- 公共表表达式：开启中间结果集 bs，其后各行为其定义体（§C06fix）
        SELECT v.bet05 AS member_id, v.eid AS dealer_id, v.ip AS bet_ip,                                  -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、eid（荷官工号）、bet_ip（下注 IP）
               v.bet20 AS lv3, v.bet09 AS bet_side, v.dt AS bet_date,                                     -- 取值表达式：取用 bet09（玩法）、bet20（三级代理线）、dt（营业日），产出「bet_date」
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake                               -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§C06fix）
      side AS (                                                                                           -- 公共表表达式：开启中间结果集 side，其后各行为其定义体（§C06fix）
        -- 每人每把在该IP的净方向（庄=+ 闲=-，按本金）
        -- ★ DX-01 校准：等值匹配 Banker / Player。
        --   不可用 LIKE '%BANK%'：会误纳 BankerDragonBonus(20.7万笔)、
        --   BankerNatural(6.2万笔) 等边注，那与主线庄闲不构成对冲关系。
        SELECT bet_ip, round_key, member_id,                                                              -- 取列：起始取列子句，本行先列 bet_ip, round_key, member_id，涉 round_key（局键）、bet_ip（下注 IP）、member_id（会员号）
               SUM(CASE WHEN TRIM(bet_side) = 'Banker' THEN stake                                         -- 汇总表达式：取用 stake（下注额（经汇率归一化））
                        WHEN TRIM(bet_side) = 'Player' THEN -stake                                        -- 分支判定：取用 stake（下注额（经汇率归一化））
                        ELSE 0 END) AS dir_stake                                                          -- 兜底取值：产出「dir_stake」
        FROM bs WHERE NULLIF(TRIM(bet_ip),'') IS NOT NULL                                                 -- 取数来源：取自本条自建的中间结果集 bs
        GROUP BY bet_ip, round_key, member_id                                                             -- 分组：按 bet_ip, round_key, member_id 汇总
        HAVING ABS(SUM(CASE WHEN TRIM(bet_side) = 'Banker' THEN stake                                     -- 分组后过滤：限定 ABS(SUM(CASE WHEN TRIM(bet_side)等于 'Banker' THEN stake，涉 stake（下注额（经汇率归一化））
                            WHEN TRIM(bet_side) = 'Player' THEN -stake                                    -- 分支判定：取用 stake（下注额（经汇率归一化））
                            ELSE 0 END)) > 0                                                              -- 兜底取值：承接上一子句，构成完整语句，属 §C06fix 之取数
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§C06fix）
      pairs AS (                                                                                          -- 公共表表达式：开启中间结果集 pairs，其后各行为其定义体（§C06fix）
        SELECT a.bet_ip, a.member_id AS m_a, b.member_id AS m_b,                                          -- 取列：起始取列子句，本行先取「m_b」，涉 bet_ip（下注 IP）、member_id（会员号）
               COUNT(*) AS n_same_round,                                                                  -- 计数表达式：产出「n_same_round」
               SUM(CASE WHEN a.dir_stake*b.dir_stake < 0 THEN 1 ELSE 0 END) AS n_opposite_round,          -- 汇总表达式：产出「n_opposite_round」
               SUM(CASE WHEN a.dir_stake*b.dir_stake < 0                                                  -- 汇总表达式：承接上一子句，构成完整语句，属 §C06fix 之取数
                         AND ABS(ABS(a.dir_stake)-ABS(b.dir_stake))                                       -- 并列条件：承接上一子句，构成完整语句，属 §C06fix 之取数
                             <= 0.1*GREATEST(ABS(a.dir_stake),ABS(b.dir_stake))                           -- 取值表达式：承接上一子句，构成完整语句，属 §C06fix 之取数
                        THEN 1 ELSE 0 END) AS n_hedged                                                    -- 分支取值：产出「n_hedged」
        FROM side a JOIN side b                                                                           -- 取数来源：取自本条自建的中间结果集 side
          ON a.bet_ip=b.bet_ip AND a.round_key=b.round_key                                                -- 连接键：限定 a.bet_ip等于 b.bet_ip、a.round_key等于 b.round_key，涉 round_key（局键）、bet_ip（下注 IP）
         AND a.member_id < b.member_id                                                                    -- 并列条件：限定 a.member_id小于 b.member_id，涉 member_id（会员号）
        GROUP BY a.bet_ip, a.member_id, b.member_id                                                       -- 分组：按 a.bet_ip, a.member_id, b.member_id 汇总
        HAVING COUNT(*) >= 30                                                                             -- 分组后过滤：限定行数不少于 30
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§C06fix）
      SELECT bet_ip, m_a, m_b, n_same_round, n_opposite_round,                                            -- 取列：起始取列子句，本行先列 bet_ip, m_a, m_b, n_same_round, n_opposite_round，涉 bet_ip（下注 IP）
             n_opposite_round*1.0/n_same_round AS opposite_rate,                                          -- 取值表达式：取用 opposite_rate（反向比例），产出「opposite_rate」
             n_hedged*1.0/NULLIF(n_opposite_round,0) AS hedge_coverage                                    -- 取值表达式：产出「hedge_coverage」
      FROM pairs                                                                                          -- 取数来源：取自本条自建的中间结果集 pairs
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hedge,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hedge,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hedge,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hedge,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hedge,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hedge,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hedge,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hedge,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`bet_ip` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--   9. C06d_hedge_pairs_dated.csv   [总包行 8067~8133 · 原 9 列 ＋六层约 60 列]
--     典型学：M02 对冲对打　分区：hedge（h.n_side >= 2）　连接：q.`bet_ip`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`bet_ip`, w.`m_a`, w.`m_b`, w.`n_same_round`, w.`n_opposite_round`, w.`opposite_rate`, w.`first_opposite_dt`, w.`last_opposite_dt`, w.`n_opposite_days`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_hg AS (SELECT x_round, COUNT(DISTINCT x_product) AS n_side FROM x_bs GROUP BY x_round),
  x_agg AS (
    SELECT s.x_ip                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN h.n_side >= 2 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN h.n_side >= 2 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN h.n_side >= 2 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (h.n_side >= 2) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (h.n_side >= 2) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (h.n_side >= 2) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    JOIN x_hg h ON h.x_round = s.x_round
    WHERE s.x_ip IS NOT NULL AND TRIM(CAST(s.x_ip AS STRING)) <> ''
    GROUP BY s.x_ip
  ),
    q AS (
      WITH ta AS (
        SELECT DISTINCT age001 AS aid
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'
      ),
      rk AS (
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,
               b.bet11, b.bet13, b.bet18, b.bet19, b.bet20,
               b.bet21, b.bet22, b.bet38, b.bet39, b.category,
               b.eid, b.ip,
               ROW_NUMBER() OVER (
                 PARTITION BY b.bet01
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn
        FROM ods_mariadb_2b.ods_a168_bet02 b
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'
      ),
      vd AS (
        SELECT r.*
        FROM rk r
        LEFT JOIN ta t1 ON t1.aid = r.bet18
        LEFT JOIN ta t2 ON t2.aid = r.bet19
        LEFT JOIN ta t3 ON t3.aid = r.bet20
        LEFT JOIN ta t4 ON t4.aid = r.bet21
        LEFT JOIN ta t5 ON t5.aid = r.bet22
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL
      ),
      bs AS (
        SELECT v.bet05 AS member_id, v.ip AS bet_ip, v.bet09 AS bet_side, v.dt AS bet_date,
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake
        FROM vd v
      ),
      side AS (
        SELECT bet_ip, round_key, member_id, MIN(bet_date) AS round_date,
               SUM(CASE WHEN TRIM(bet_side) = 'Banker' THEN stake
                        WHEN TRIM(bet_side) = 'Player' THEN -stake
                        ELSE 0 END) AS dir_stake
        FROM bs WHERE NULLIF(TRIM(bet_ip),'') IS NOT NULL
        GROUP BY bet_ip, round_key, member_id
        HAVING ABS(SUM(CASE WHEN TRIM(bet_side) = 'Banker' THEN stake
                            WHEN TRIM(bet_side) = 'Player' THEN -stake
                            ELSE 0 END)) > 0
      )
      SELECT  a.bet_ip,
              a.member_id                                   AS m_a,
              b.member_id                                   AS m_b,
              COUNT(*)                                      AS n_same_round,
              SUM(CASE WHEN a.dir_stake*b.dir_stake < 0 THEN 1 ELSE 0 END) AS n_opposite_round,
              SUM(CASE WHEN a.dir_stake*b.dir_stake < 0 THEN 1 ELSE 0 END)
                * 1.0 / COUNT(*)                            AS opposite_rate,
              MIN(CASE WHEN a.dir_stake*b.dir_stake < 0 THEN a.round_date END) AS first_opposite_dt,
              MAX(CASE WHEN a.dir_stake*b.dir_stake < 0 THEN a.round_date END) AS last_opposite_dt,
              COUNT(DISTINCT CASE WHEN a.dir_stake*b.dir_stake < 0 THEN a.round_date END)
                                                            AS n_opposite_days
      FROM        side a
      JOIN        side b
             ON   a.bet_ip = b.bet_ip
            AND   a.round_key = b.round_key
            AND   a.member_id < b.member_id
      GROUP BY a.bet_ip, a.member_id, b.member_id
      HAVING  COUNT(*) >= 30
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hedge,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hedge,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hedge,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hedge,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hedge,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hedge,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hedge,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hedge,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`bet_ip` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  10. C08_subnet_all.csv   [总包行 1759~1820 · 原 8 列 ＋六层约 60 列]
--     典型学：M04 IP 聚集　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`subnet_24`, w.`n_ip`, w.`n_member`, w.`n_lv3_chain`, w.`n_orders`, w.`stake`, w.`game_pnl`, w.`valid_bet`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ta AS (            -- 公司测试线代理（214 条，跨五级）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 C08_subnet_all.csv）
      rk AS (                  -- 需求 §3.2：同注单号取最新版本（三级排序去重）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet09，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet11, b.bet13, b.bet14, b.bet16, b.bet17,                                               -- 续行：接续上一取列子句，续列 b.bet11, b.bet13, b.bet14, b.bet16, b.bet17，涉 bet11（汇率）、bet13（下注金额）、bet14（派彩金额）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet38, b.bet39, b.category, b.eid, b.ip,                                                 -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category, b.eid, b.ip，涉 bet38（重對）、bet39（桌号）、eid（荷官工号）
               b.validbet,                                                                                -- 续行：接续上一取列子句，续列 b.validbet，涉 validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 C08_subnet_all.csv）
      vd AS (                  -- 有效注单：非测试线、非重对、一般注单
        SELECT r.*                                                                                        -- 取列：整体承接上游结果集的全部字段，不再逐列列举
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 C08_subnet_all.csv）
      bs AS (                  -- 金额正名：本金/洗码量/游戏输赢/退水/净输赢（一律÷汇率）
        SELECT v.bet05 AS member_id, v.eid AS dealer_id, v.ip AS bet_ip,                                  -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、eid（荷官工号）、bet_ip（下注 IP）
               v.bet20 AS lv3, v.bet09 AS bet_side, v.dt AS bet_date,                                     -- 取值表达式：取用 bet09（玩法）、bet20（三级代理线）、dt（营业日），产出「bet_date」
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.bet08),'') AS DATETIME) AS t_bet,                                       -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「t_bet」
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake,                              -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               CAST(NULLIF(TRIM(v.validbet),'') AS DECIMAL(20,4))                                         -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS valid_bet,                          -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「valid_bet」
               (CAST(NULLIF(TRIM(v.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS game_pnl,                           -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               CAST(NULLIF(TRIM(v.bet16),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS rebate,                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
               CAST(NULLIF(TRIM(v.bet17),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS net_pnl                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_pnl」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（交付件 C08_subnet_all.csv）
      SELECT                                                                                              -- 续行：接续上一取列子句，续列 SELECT
        CONCAT(SPLIT_PART(bet_ip,'.',1),'.',SPLIT_PART(bet_ip,'.',2),'.',                                 -- 取值表达式：取用 bet_ip（下注 IP）
               SPLIT_PART(bet_ip,'.',3),'.0/24') AS subnet_24,                                            -- 取值表达式：取用 bet_ip（下注 IP），产出「subnet_24」
        COUNT(DISTINCT bet_ip)    AS n_ip,                                                                -- 计数表达式：统计去重个数，产出「n_ip」
        COUNT(DISTINCT member_id) AS n_member,                                                            -- 计数表达式：统计去重个数，产出「n_member」
        COUNT(DISTINCT lv3)       AS n_lv3_chain,                                                         -- 计数表达式：统计去重个数，产出「n_lv3_chain」
        COUNT(*) AS n_orders, SUM(stake) AS stake,                                                        -- 计数表达式：汇总，取用 stake（下注额（经汇率归一化）），产出「stake」
        SUM(game_pnl) AS game_pnl, SUM(valid_bet) AS valid_bet                                            -- 汇总表达式：取用 valid_bet（有效投注（洗码量））、game_pnl（游戏净输赢），产出「valid_bet」
      FROM bs WHERE NULLIF(TRIM(bet_ip),'') IS NOT NULL                                                   -- 取数来源：取自本条自建的中间结果集 bs
      GROUP BY 1                                                                                          -- 分组：按第 1 个取列汇总
      HAVING COUNT(DISTINCT member_id) >= 5                                                               -- 分组后过滤：限定 member_id 的去重个数不少于 5，涉 member_id（会员号）
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake_xagg, ROUND(e.valid,4) AS valid_bet_xagg,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  11. C08_subnet_sparse.csv   [总包行 1829~1890 · 原 8 列 ＋六层约 60 列]
--     典型学：M04 IP 聚集　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`subnet_24`, w.`n_ip`, w.`n_member`, w.`n_lv3_chain`, w.`member_per_ip`, w.`n_orders`, w.`valid_bet`, w.`game_pnl`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ta AS (            -- 公司测试线代理（214 条，跨五级）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 C08_subnet_sparse.csv）
      rk AS (                  -- 需求 §3.2：同注单号取最新版本（三级排序去重）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet09，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet11, b.bet13, b.bet14, b.bet16, b.bet17,                                               -- 续行：接续上一取列子句，续列 b.bet11, b.bet13, b.bet14, b.bet16, b.bet17，涉 bet11（汇率）、bet13（下注金额）、bet14（派彩金额）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet38, b.bet39, b.category, b.eid, b.ip,                                                 -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category, b.eid, b.ip，涉 bet38（重對）、bet39（桌号）、eid（荷官工号）
               b.validbet,                                                                                -- 续行：接续上一取列子句，续列 b.validbet，涉 validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 C08_subnet_sparse.csv）
      vd AS (                  -- 有效注单：非测试线、非重对、一般注单
        SELECT r.*                                                                                        -- 取列：整体承接上游结果集的全部字段，不再逐列列举
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 C08_subnet_sparse.csv）
      bs AS (                  -- 金额正名：本金/洗码量/游戏输赢/退水/净输赢（一律÷汇率）
        SELECT v.bet05 AS member_id, v.eid AS dealer_id, v.ip AS bet_ip,                                  -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、eid（荷官工号）、bet_ip（下注 IP）
               v.bet20 AS lv3, v.bet09 AS bet_side, v.dt AS bet_date,                                     -- 取值表达式：取用 bet09（玩法）、bet20（三级代理线）、dt（营业日），产出「bet_date」
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.bet08),'') AS DATETIME) AS t_bet,                                       -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「t_bet」
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake,                              -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               CAST(NULLIF(TRIM(v.validbet),'') AS DECIMAL(20,4))                                         -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS valid_bet,                          -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「valid_bet」
               (CAST(NULLIF(TRIM(v.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS game_pnl,                           -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               CAST(NULLIF(TRIM(v.bet16),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS rebate,                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
               CAST(NULLIF(TRIM(v.bet17),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS net_pnl                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_pnl」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（交付件 C08_subnet_sparse.csv）
      SELECT                                                                                              -- 续行：接续上一取列子句，续列 SELECT
        CONCAT(SPLIT_PART(bet_ip,'.',1),'.',SPLIT_PART(bet_ip,'.',2),'.',                                 -- 取值表达式：取用 bet_ip（下注 IP）
               SPLIT_PART(bet_ip,'.',3),'.0/24') AS subnet_24,                                            -- 取值表达式：取用 bet_ip（下注 IP），产出「subnet_24」
        COUNT(DISTINCT bet_ip)    AS n_ip,                                                                -- 计数表达式：统计去重个数，产出「n_ip」
        COUNT(DISTINCT member_id) AS n_member,                                                            -- 计数表达式：统计去重个数，产出「n_member」
        COUNT(DISTINCT lv3)       AS n_lv3_chain,                                                         -- 计数表达式：统计去重个数，产出「n_lv3_chain」
        COUNT(DISTINCT member_id)*1.0/NULLIF(COUNT(DISTINCT bet_ip),0) AS member_per_ip,                  -- 计数表达式：统计去重个数，产出「member_per_ip」
        COUNT(*) AS n_orders, SUM(valid_bet) AS valid_bet, SUM(game_pnl) AS game_pnl                      -- 计数表达式：汇总，取用 valid_bet（有效投注（洗码量））、game_pnl（游戏净输赢），产出「game_pnl」
      FROM bs WHERE NULLIF(TRIM(bet_ip),'') IS NOT NULL                                                   -- 取数来源：取自本条自建的中间结果集 bs
      GROUP BY 1                                                                                          -- 分组：按第 1 个取列汇总
      HAVING COUNT(DISTINCT member_id) >= 5 AND COUNT(DISTINCT bet_ip) <= 30                              -- 分组后过滤：限定 member_id 的去重个数不少于 5、bet_ip 的去重个数不多于 30，涉 bet_ip（下注 IP）、member_id（会员号）
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet_xagg,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  12. D03S_daily_roi_diff.csv   [总包行 1991~2062 · 原 7 列 ＋六层约 60 列]
--     典型学：M15 活跃周期　分区：recent（s.x_rec = 1）　连接：q.`bet_date`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`bet_date`, w.`n_rounds`, w.`vb_tail`, w.`vb_norm`, w.`roi_tail`, w.`roi_norm`, w.`roi_diff`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_date                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_date IS NOT NULL AND TRIM(CAST(s.x_date AS STRING)) <> ''
    GROUP BY s.x_date
  ),
    q AS (
      WITH ta AS (            -- 公司测试线代理（214 条，跨五级）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 D03S_daily_roi_diff.csv）
      rk AS (                  -- 需求 §3.2：同注单号取最新版本（三级排序去重）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet09，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet11, b.bet13, b.bet14, b.bet16, b.bet17,                                               -- 续行：接续上一取列子句，续列 b.bet11, b.bet13, b.bet14, b.bet16, b.bet17，涉 bet11（汇率）、bet13（下注金额）、bet14（派彩金额）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet38, b.bet39, b.category, b.eid, b.ip,                                                 -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category, b.eid, b.ip，涉 bet38（重對）、bet39（桌号）、eid（荷官工号）
               b.validbet,                                                                                -- 续行：接续上一取列子句，续列 b.validbet，涉 validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 D03S_daily_roi_diff.csv）
      vd AS (                  -- 有效注单：非测试线、非重对、一般注单
        SELECT r.*                                                                                        -- 取列：整体承接上游结果集的全部字段，不再逐列列举
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 D03S_daily_roi_diff.csv）
      bs AS (                  -- 金额正名：本金/洗码量/游戏输赢/退水/净输赢（一律÷汇率）
        SELECT v.bet05 AS member_id, v.eid AS dealer_id, v.ip AS bet_ip,                                  -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、eid（荷官工号）、bet_ip（下注 IP）
               v.bet20 AS lv3, v.bet09 AS bet_side, v.dt AS bet_date,                                     -- 取值表达式：取用 bet09（玩法）、bet20（三级代理线）、dt（营业日），产出「bet_date」
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.bet08),'') AS DATETIME) AS t_bet,                                       -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「t_bet」
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake,                              -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               CAST(NULLIF(TRIM(v.validbet),'') AS DECIMAL(20,4))                                         -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS valid_bet,                          -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「valid_bet」
               (CAST(NULLIF(TRIM(v.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS game_pnl,                           -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               CAST(NULLIF(TRIM(v.bet16),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS rebate,                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
               CAST(NULLIF(TRIM(v.bet17),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS net_pnl                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_pnl」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 D03S_daily_roi_diff.csv）
      gi AS (SELECT CONCAT_WS('|', gi002, gi003, gi011) AS round_key,                                     -- 取值表达式：取用 gi011（桌号）、round_key（局键），产出「round_key」
                    gi004 AS t_open, gi006 AS t_reveal                                                    -- 取值表达式：取用 gi004（局开始时刻）、gi006（开牌时刻），产出「t_reveal」
             FROM ods_mariadb_2b.ods_a168_game_info                                                       -- 取数来源：取自局信息表（靴局、开牌与桌号的时序基准）
             WHERE gi001='101' AND gi013='1' AND is_lock='N'                                              -- 过滤条件：限定 gi001等于 '101'、gi013等于 '1'、is_lock等于 'N'，涉 gi001（局主键）
               AND gi004>='2026-03-21' AND gi004<'2026-08-07'),                                           -- 并列条件：限定 gi004不少于 '2026-03-21'，涉 gi004（局开始时刻）
      pr AS (SELECT b.bet_date, b.round_key, b.valid_bet, b.game_pnl,                                     -- 取值表达式：取用 valid_bet（有效投注（洗码量））、round_key（局键）、game_pnl（游戏净输赢）
                    (UNIX_TIMESTAMP(b.t_bet)-UNIX_TIMESTAMP(g.t_open))*1.0                                -- 取值表达式：比率之分子，乘 1.0 以避整数除法截断
                    /NULLIF(UNIX_TIMESTAMP(g.t_reveal)-UNIX_TIMESTAMP(g.t_open),0) AS progress            -- 除法或乘法计算：产出「progress」
             FROM bs b JOIN gi g ON g.round_key=b.round_key)                                              -- 取数来源：取自本条自建的中间结果集 bs，连接键为 round_key（局键）
      SELECT bet_date, COUNT(DISTINCT round_key) AS n_rounds,                                             -- 取列：起始取列子句，本行先取「n_rounds」，涉 round_key（局键）、bet_date（营业日）
        SUM(CASE WHEN progress>=0.90 THEN valid_bet ELSE 0 END) AS vb_tail,                               -- 汇总表达式：取用 valid_bet（有效投注（洗码量）），产出「vb_tail」
        SUM(CASE WHEN progress< 0.90 THEN valid_bet ELSE 0 END) AS vb_norm,                               -- 汇总表达式：取用 valid_bet（有效投注（洗码量）），产出「vb_norm」
        SUM(CASE WHEN progress>=0.90 THEN game_pnl ELSE 0 END)                                            -- 汇总表达式：取用 game_pnl（游戏净输赢）
          /NULLIF(SUM(CASE WHEN progress>=0.90 THEN valid_bet ELSE 0 END),0) AS roi_tail,                 -- 除法或乘法计算：汇总，取用 valid_bet（有效投注（洗码量）），产出「roi_tail」
        SUM(CASE WHEN progress< 0.90 THEN game_pnl ELSE 0 END)                                            -- 汇总表达式：取用 game_pnl（游戏净输赢）
          /NULLIF(SUM(CASE WHEN progress< 0.90 THEN valid_bet ELSE 0 END),0) AS roi_norm,                 -- 除法或乘法计算：汇总，取用 valid_bet（有效投注（洗码量）），产出「roi_norm」
        SUM(CASE WHEN progress>=0.90 THEN game_pnl ELSE 0 END)                                            -- 汇总表达式：取用 game_pnl（游戏净输赢）
          /NULLIF(SUM(CASE WHEN progress>=0.90 THEN valid_bet ELSE 0 END),0)                              -- 除法或乘法计算：汇总，取用 valid_bet（有效投注（洗码量））
        - SUM(CASE WHEN progress< 0.90 THEN game_pnl ELSE 0 END)                                          -- 加减计算：晚段 ROI 减早段 ROI ＝ 分段差，与 GGR 无涉
          /NULLIF(SUM(CASE WHEN progress< 0.90 THEN valid_bet ELSE 0 END),0) AS roi_diff                  -- 除法或乘法计算：汇总，取用 valid_bet（有效投注（洗码量）），产出「roi_diff」
      FROM pr WHERE progress BETWEEN 0 AND 1.5                                                            -- 取数来源：取自本条自建的中间结果集 pr
      GROUP BY bet_date
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds AS n_rounds_xagg,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`bet_date` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  13. DICT_01a_bet41_tri.csv   [总包行 11764~11798 · 原 14 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`scope`, w.`n_rows`, w.`sum_stake`, w.`sum_validbet`, w.`sum_bet41`, w.`sum_bet16`, w.`r41_vb`, w.`r16_vb`, w.`r_vb_stake`, w.`avg_bet15_pct`, w.`n_41_eq_vb`, w.`n_41_eq_16`, w.`pct_41_eq_vb`, w.`verdict_bet41`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        'bet41 × bet16 × validbet 三方比对（category=1 · 过基础闸）'              AS scope,                  -- 字面取值：受检范围
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：母体行数
        ROUND(SUM(CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))), 2)          AS sum_stake,              -- 汇总表达式：下注金额总额
        ROUND(SUM(CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))), 2)       AS sum_validbet,           -- 汇总表达式：有效投注总额
        ROUND(SUM(CAST(NULLIF(TRIM(bet41), '') AS DECIMAL(20,8))), 2)          AS sum_bet41,              -- 汇总表达式：bet41 总额
        ROUND(SUM(CAST(NULLIF(TRIM(bet16), '') AS DECIMAL(20,8))), 2)          AS sum_bet16,              -- 汇总表达式：bet16（退水金额）总额
        ROUND(SUM(CAST(NULLIF(TRIM(bet41), '') AS DECIMAL(20,8)))
              / NULLIF(SUM(CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))), 0), 8) AS r41_vb,          -- 比率表达式：★ bet41 ÷ validbet
        ROUND(SUM(CAST(NULLIF(TRIM(bet16), '') AS DECIMAL(20,8)))
              / NULLIF(SUM(CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))), 0), 8) AS r16_vb,          -- 比率表达式：★ bet16 ÷ validbet
        ROUND(SUM(CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8)))
              / NULLIF(SUM(CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))), 0), 8) AS r_vb_stake,         -- 比率表达式：有效投注 ÷ 下注（在册 ≈0.9004）
        ROUND(AVG(CAST(NULLIF(TRIM(bet15), '') AS DECIMAL(20,8))), 6)          AS avg_bet15_pct,          -- 聚合表达式：会员退水%数之均值——与 r16_vb 对照
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet41), '') AS DECIMAL(20,8))
                   = CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8)) THEN 1 ELSE 0 END) AS n_41_eq_vb,  -- 条件计数：bet41 与 validbet 逐行相等者
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet41), '') AS DECIMAL(20,8))
                   = CAST(NULLIF(TRIM(bet16), '') AS DECIMAL(20,8)) THEN 1 ELSE 0 END) AS n_41_eq_16,     -- 条件计数：★ bet41 与 bet16 逐行相等者
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet41), '') AS DECIMAL(20,8))
                         = CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8)) THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_41_eq_vb,           -- 比率表达式：相等率
        CASE                                                                                              -- 条件分支：起始分支——三方裁定
          WHEN ROUND(SUM(CAST(NULLIF(TRIM(bet41), '') AS DECIMAL(20,8)))
                     / NULLIF(SUM(CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))), 0), 4) BETWEEN 0.99 AND 1.01
            THEN '★ bet41 ÷ validbet ≈ 1 —— bet41 系有效投注之同义列，字典「下注退水金額」措辞歧义'          -- 分支判定：同义列
          WHEN ROUND(SUM(CAST(NULLIF(TRIM(bet41), '') AS DECIMAL(20,8)))
                     / NULLIF(SUM(CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))), 0), 4) < 0.1
            THEN '★★ bet41 ÷ validbet 远小于 1 —— bet41 真为退水金额，在册「≡ validbet」须重查'              -- 分支判定：真退水
          ELSE '◐ 比值居中 —— 二者关系另有structure，须逐行分层复验'                                         -- 分支判定：居中
        END                                                                    AS verdict_bet41           -- 条件分支：收束上方判词，产出「verdict_bet41」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
        AND TRIM(category) = '1'                                                                          -- 并列条件：限定一般注单（排除小费）
        AND UPPER(TRIM(bet38)) = 'N'                                                                     -- 并列条件：限定非重对
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  14. DICT_01b_roundkey_card.csv   [总包行 11812~11835 · 原 14 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`scope`, w.`n_rows`, w.`nd_bet03`, w.`nd_bet04`, w.`nd_bet39`, w.`nd_bet40`, w.`min_bet04`, w.`max_bet04`, w.`min_bet39`, w.`max_bet39`, w.`nd_round_key`, w.`nd_shoe_table`, w.`avg_bets_per_round`, w.`verdict_key`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        'bet03／bet04／bet39 基数实测'                                          AS scope,                  -- 字面取值：受检范围
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：母体行数
        COUNT(DISTINCT TRIM(bet03))                                            AS nd_bet03,               -- 计数表达式：靴号之相异数
        COUNT(DISTINCT TRIM(bet04))                                            AS nd_bet04,               -- 计数表达式：★ 局内第几把之相异数
        COUNT(DISTINCT TRIM(bet39))                                            AS nd_bet39,               -- 计数表达式：★ 桌号之相异数
        COUNT(DISTINCT TRIM(bet40))                                            AS nd_bet40,               -- 计数表达式：房间编号之相异数
        MIN(CAST(NULLIF(TRIM(bet04), '') AS BIGINT))                           AS min_bet04,              -- 聚合取值：局内序号之最小
        MAX(CAST(NULLIF(TRIM(bet04), '') AS BIGINT))                           AS max_bet04,              -- 聚合取值：★ 局内序号之最大——应为数十
        MIN(CAST(NULLIF(TRIM(bet39), '') AS BIGINT))                           AS min_bet39,              -- 聚合取值：桌号之最小
        MAX(CAST(NULLIF(TRIM(bet39), '') AS BIGINT))                           AS max_bet39,              -- 聚合取值：桌号之最大
        COUNT(DISTINCT CONCAT_WS('|', TRIM(bet03), TRIM(bet04), TRIM(bet39)))  AS nd_round_key,           -- 计数表达式：局键之相异数（canonical 之定义）
        COUNT(DISTINCT CONCAT_WS('|', TRIM(bet03), TRIM(bet39)))               AS nd_shoe_table,          -- 计数表达式：靴×桌之相异数
        ROUND(COUNT(*) * 1.0
              / NULLIF(COUNT(DISTINCT CONCAT_WS('|', TRIM(bet03), TRIM(bet04), TRIM(bet39))), 0), 4) AS avg_bets_per_round, -- 比率表达式：每局平均注数
        CASE                                                                                              -- 条件分支：起始分支——真身裁定
          WHEN COUNT(DISTINCT TRIM(bet39)) <= 100 AND MAX(CAST(NULLIF(TRIM(bet04), '') AS BIGINT)) <= 200
            THEN '★ bet39 基数小（桌号）· bet04 上限数十（局内序号）—— 与字典及 canonical 一致'                -- 分支判定：与字典一致
          ELSE '❌ 基数与字典所载不符 —— 须逐列复验'                                                        -- 分支判定：不符
        END                                                                    AS verdict_key             -- 条件分支：收束上方判词，产出「verdict_key」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
        AND TRIM(category) = '1'                                                                         -- 并列条件：限定一般注单
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  15. DICT_01c_dimension.csv   [总包行 11850~11878 · 原 12 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`scope`, w.`n_rows`, w.`avg_validbet`, w.`max_bet23`, w.`avg_bet23`, w.`max_bet27`, w.`max_bet28`, w.`avg_bet28`, w.`max_bet32`, w.`max_bet15`, w.`avg_sum_comm`, w.`verdict_dimension`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        'bet23~27 占成 · bet28~32 退水 · 量纲实测'                              AS scope,                  -- 字面取值：受检范围
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：母体行数
        ROUND(AVG(CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))), 4)       AS avg_validbet,           -- 聚合表达式：★ 有效投注之均值——量纲对照基准
        ROUND(MAX(CAST(NULLIF(TRIM(bet23), '') AS DECIMAL(20,8))), 6)          AS max_bet23,              -- 聚合取值：LV1 占成之最大
        ROUND(AVG(CAST(NULLIF(TRIM(bet23), '') AS DECIMAL(20,8))), 6)          AS avg_bet23,              -- 聚合表达式：LV1 占成之均值
        ROUND(MAX(CAST(NULLIF(TRIM(bet27), '') AS DECIMAL(20,8))), 6)          AS max_bet27,              -- 聚合取值：LV5 占成之最大
        ROUND(MAX(CAST(NULLIF(TRIM(bet28), '') AS DECIMAL(20,8))), 6)          AS max_bet28,              -- 聚合取值：LV1 退水之最大
        ROUND(AVG(CAST(NULLIF(TRIM(bet28), '') AS DECIMAL(20,8))), 6)          AS avg_bet28,              -- 聚合表达式：LV1 退水之均值
        ROUND(MAX(CAST(NULLIF(TRIM(bet32), '') AS DECIMAL(20,8))), 6)          AS max_bet32,              -- 聚合取值：LV5 退水之最大
        ROUND(MAX(CAST(NULLIF(TRIM(bet15), '') AS DECIMAL(20,8))), 6)          AS max_bet15,              -- 聚合取值：会员退水%数之最大——已知系比率
        ROUND(AVG(CAST(NULLIF(TRIM(bet23), '') AS DECIMAL(20,8))
                + CAST(NULLIF(TRIM(bet24), '') AS DECIMAL(20,8))
                + CAST(NULLIF(TRIM(bet25), '') AS DECIMAL(20,8))
                + CAST(NULLIF(TRIM(bet26), '') AS DECIMAL(20,8))
                + CAST(NULLIF(TRIM(bet27), '') AS DECIMAL(20,8))), 6)          AS avg_sum_comm,           -- 聚合表达式：★ 五级占成之和——若系比率，其和应 ≤ 100
        CASE                                                                                              -- 条件分支：起始分支——量纲裁定
          WHEN MAX(CAST(NULLIF(TRIM(bet23), '') AS DECIMAL(20,8))) <= 100
           AND MAX(CAST(NULLIF(TRIM(bet28), '') AS DECIMAL(20,8))) <= 100
            THEN '★ bet23~32 上限 ≤ 100 —— 判为**比率（百分点）**；κ 之算式须用 × validbet ÷ 100'          -- 分支判定：比率
          WHEN MAX(CAST(NULLIF(TRIM(bet23), '') AS DECIMAL(20,8)))
             > AVG(CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))) / 100
            THEN '★ 与 validbet 同量级 —— 判为**金额**；κ 之算式须直接相加'                                  -- 分支判定：金额
          ELSE '◐ 量纲未定 —— 须逐列分层复验'                                                              -- 分支判定：未定
        END                                                                    AS verdict_dimension       -- 条件分支：收束上方判词，产出「verdict_dimension」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
        AND TRIM(category) = '1'                                                                         -- 并列条件：限定一般注单
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  16. DICT_01d_bet_latency.csv   [总包行 11893~11926 · 原 14 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：q.`table_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`table_id`, w.`n_rows`, w.`n_members`, w.`n_b06_null`, w.`n_b08_null`, w.`n_negative`, w.`lat_min`, w.`lat_avg`, w.`lat_max`, w.`lat_p01`, w.`lat_p50`, w.`lat_p99`, w.`lat_sd`, w.`verdict_latency`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_table                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_table IS NOT NULL AND TRIM(CAST(s.x_table AS STRING)) <> ''
    GROUP BY s.x_table
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        TRIM(bet39)                                                            AS table_id,               -- 取列：★ 桌号（bet39，非 bet04）
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：该桌注数
        COUNT(DISTINCT TRIM(bet05))                                            AS n_members,              -- 计数表达式：该桌会员数
        SUM(CASE WHEN NULLIF(TRIM(bet06), '') IS NULL THEN 1 ELSE 0 END)       AS n_b06_null,             -- 条件计数：★ 开局时间为空者——可得性之判
        SUM(CASE WHEN NULLIF(TRIM(bet08), '') IS NULL THEN 1 ELSE 0 END)       AS n_b08_null,             -- 条件计数：下注时间为空者
        SUM(CASE WHEN TIMESTAMPDIFF(SECOND, CAST(bet06 AS DATETIME), CAST(bet08 AS DATETIME)) < 0
                 THEN 1 ELSE 0 END)                                            AS n_negative,             -- 条件计数：★ 延迟为负——时序倒挂
        ROUND(MIN(TIMESTAMPDIFF(SECOND, CAST(bet06 AS DATETIME), CAST(bet08 AS DATETIME))), 4) AS lat_min, -- 聚合取值：延迟之最小
        ROUND(AVG(TIMESTAMPDIFF(SECOND, CAST(bet06 AS DATETIME), CAST(bet08 AS DATETIME))), 4) AS lat_avg, -- 聚合表达式：延迟之均值
        ROUND(MAX(TIMESTAMPDIFF(SECOND, CAST(bet06 AS DATETIME), CAST(bet08 AS DATETIME))), 4) AS lat_max, -- 聚合取值：★ 延迟之最大——投注窗之上界
        ROUND(PERCENTILE_APPROX(
                TIMESTAMPDIFF(SECOND, CAST(bet06 AS DATETIME), CAST(bet08 AS DATETIME)), 0.01), 4) AS lat_p01, -- 分位表达式：延迟之 1% 分位
        ROUND(PERCENTILE_APPROX(
                TIMESTAMPDIFF(SECOND, CAST(bet06 AS DATETIME), CAST(bet08 AS DATETIME)), 0.50), 4) AS lat_p50, -- 分位表达式：延迟之中位
        ROUND(PERCENTILE_APPROX(
                TIMESTAMPDIFF(SECOND, CAST(bet06 AS DATETIME), CAST(bet08 AS DATETIME)), 0.99), 4) AS lat_p99, -- 分位表达式：延迟之 99% 分位
        ROUND(STDDEV_POP(
                TIMESTAMPDIFF(SECOND, CAST(bet06 AS DATETIME), CAST(bet08 AS DATETIME))), 4) AS lat_sd,   -- 聚合表达式：延迟之标准差
        CASE                                                                                              -- 条件分支：起始分支——可得性裁定
          WHEN SUM(CASE WHEN NULLIF(TRIM(bet06), '') IS NULL THEN 1 ELSE 0 END) > 0
            THEN '❌ 该桌有开局时间为空者 —— 延迟不可全算'                                                   -- 分支判定：有空值
          WHEN SUM(CASE WHEN TIMESTAMPDIFF(SECOND, CAST(bet06 AS DATETIME), CAST(bet08 AS DATETIME)) < 0
                   THEN 1 ELSE 0 END) > 0
            THEN '★ 该桌有时序倒挂（bet08 < bet06）—— 须查 ETL，禁直接用'                                    -- 分支判定：倒挂
          ELSE '✅ 该桌延迟可算（★ 只出分布，不判机器人）'                                                    -- 分支判定：可算
        END                                                                    AS verdict_latency         -- 条件分支：收束上方判词，产出「verdict_latency」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
        AND TRIM(category) = '1'                                                                          -- 并列条件：限定一般注单
        AND UPPER(TRIM(bet38)) = 'N'                                                                      -- 并列条件：限定非重对
      GROUP BY TRIM(bet39)                                                                                -- 分组：按桌号汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`table_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  17. DX04_bet09_profile.csv   [总包行 5971~6005 · 原 9 列 ＋六层约 60 列]
--     典型学：M14 投注形态　分区：freecomm（s.x_freecomm = 1）　连接：q.`bet_side`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`bet_side`, w.`n_orders`, w.`n_member`, w.`stake_total`, w.`player_pnl`, w.`house_hold_pct`, w.`win_rate_base`, w.`avg_stake`, w.`stake_cv`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_product                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_product IS NOT NULL AND TRIM(CAST(s.x_product AS STRING)) <> ''
    GROUP BY s.x_product
  ),
    q AS (
      WITH ranked AS (                                                                                    -- 公共表表达式：开启中间结果集 ranked，其后各行为其定义体（§DX-04）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet05, b.bet09, b.bet11, b.bet13, b.bet14,                                               -- 续行：接续上一取列子句，续列 b.bet05, b.bet09, b.bet11, b.bet13, b.bet14，涉 bet05（会员号）、bet09（玩法）、bet11（汇率）
               b.bet38, b.category,                                                                       -- 续行：接续上一取列子句，续列 b.bet38, b.category，涉 bet38（重對）
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§DX-04）
      o AS (                                                                                              -- 公共表表达式：开启中间结果集 o，其后各行为其定义体（§DX-04）
        SELECT r.bet09 AS bet_side, r.bet05 AS member_id,                                                 -- 取列：起始取列子句，本行先取「member_id」，涉 bet05（会员号）、bet09（玩法）、member_id（会员号）
               CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)),0) AS stake,                    -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               (CAST(NULLIF(TRIM(r.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)),0) AS game_pnl                  -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§DX-04）
      SELECT bet_side,                                                                                    -- 取列：起始取列子句，本行先列 bet_side
             COUNT(*)                                   AS n_orders,                                      -- 计数表达式：产出「n_orders」
             COUNT(DISTINCT member_id)                  AS n_member,                                      -- 计数表达式：统计去重个数，产出「n_member」
             SUM(stake)                                 AS stake_total,                                   -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake_total」
             SUM(game_pnl)                              AS player_pnl,                                    -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「player_pnl」
             -SUM(game_pnl) / NULLIF(SUM(stake),0)      AS house_hold_pct,                                -- 加减计算：汇总后取负号——会员净输赢取负即平台毛利（GGR），产出「house_hold_pct」
             SUM(CASE WHEN game_pnl > 0 THEN 1 ELSE 0 END) * 1.0                                          -- 取值表达式：比率之分子，乘 1.0 以避整数除法截断，涉 game_pnl（游戏净输赢）
               / NULLIF(SUM(CASE WHEN game_pnl <> 0 THEN 1 ELSE 0 END),0) AS win_rate_base,               -- 除法或乘法计算：汇总，取用 game_pnl（游戏净输赢），产出「win_rate_base」
             AVG(stake)                                 AS avg_stake,                                     -- 求均值表达式：取用 stake（下注额（经汇率归一化）），产出「avg_stake」
             STDDEV_SAMP(stake) / NULLIF(AVG(stake),0)  AS stake_cv                                       -- 取值表达式：求均值，取用 stake（下注额（经汇率归一化））、stake_cv（注额变异系数），产出「stake_cv」
      FROM o                                                                                              -- 取数来源：取自本条自建的中间结果集 o
      GROUP BY bet_side                                                                                   -- 分组：按 bet_side 汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_freecomm,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_freecomm,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_freecomm,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_freecomm,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_freecomm,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_freecomm,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_freecomm,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_freecomm,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`bet_side` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  18. DX05_product_panorama.csv   [总包行 6020~6073 · 原 14 列 ＋六层约 60 列]
--     典型学：M14 投注形态　分区：freecomm（s.x_freecomm = 1）　连接：q.`product_code`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`product_code`, w.`scope`, w.`n_orders`, w.`n_member`, w.`n_dealer`, w.`stake_total`, w.`validbet_total`, w.`player_pnl`, w.`ggr`, w.`house_hold_pct`, w.`rebate_total`, w.`rebate_rate`, w.`order_share`, w.`stake_share`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_product                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_product IS NOT NULL AND TRIM(CAST(s.x_product AS STRING)) <> ''
    GROUP BY s.x_product
  ),
    q AS (
      WITH test_agents AS (                                                                               -- 公共表表达式：开启中间结果集 test_agents，其后各行为其定义体（§DX-05）
        SELECT age001 AS agent_id FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                   -- 取列：取用代理主档（五级代理线归属来源），产出「agent_id」
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§DX-05）
      ranked AS (                                                                                         -- 公共表表达式：开启中间结果集 ranked，其后各行为其定义体（§DX-05）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet05, b.bet11, b.bet13, b.bet14, b.bet16,                                               -- 续行：接续上一取列子句，续列 b.bet05, b.bet11, b.bet13, b.bet14, b.bet16，涉 bet05（会员号）、bet11（汇率）、bet13（下注金额）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet38, b.category, b.eid, b.validbet,                                                    -- 续行：接续上一取列子句，续列 b.bet38, b.category, b.eid, b.validbet，涉 bet38（重對）、eid（荷官工号）、validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'，涉 dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§DX-05）
      o AS (                                                                                              -- 公共表表达式：开启中间结果集 o，其后各行为其定义体（§DX-05）
        SELECT r.bet02 AS product_code, r.bet05 AS member_id, r.eid AS dealer_id,                         -- 取列：起始取列子句，本行先取「dealer_id」，涉 bet02（游戏类别）、bet05（会员号）、eid（荷官工号）
               CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)),0) AS stake,                    -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               COALESCE(CAST(NULLIF(TRIM(r.validbet),'') AS DECIMAL(20,4)),                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                        CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4)))                                  -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)),0) AS validbet,                 -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「validbet」
               (CAST(NULLIF(TRIM(r.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)),0) AS game_pnl,                 -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               CAST(NULLIF(TRIM(r.bet16),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)),0) AS rebate                    -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet18（一级代理线）
        LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet19（二级代理线）
        LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet20（三级代理线）
        LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet21（四级代理线）
        LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND COALESCE(t1.agent_id,t2.agent_id,t3.agent_id,t4.agent_id,t5.agent_id) IS NULL               -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§DX-05）
      SELECT product_code,                                                                                -- 取列：起始取列子句，本行先列 product_code
             CASE WHEN product_code = '101' THEN '百家乐（本报告口径）' ELSE '其他产品' END AS scope,                   -- 条件分支：产出「scope」
             COUNT(*)                                  AS n_orders,                                       -- 计数表达式：产出「n_orders」
             COUNT(DISTINCT member_id)                 AS n_member,                                       -- 计数表达式：统计去重个数，产出「n_member」
             COUNT(DISTINCT dealer_id)                 AS n_dealer,                                       -- 计数表达式：统计去重个数，产出「n_dealer」
             SUM(stake)                                AS stake_total,                                    -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake_total」
             SUM(validbet)                             AS validbet_total,                                 -- 汇总表达式：取用 validbet（有效投注（洗码量）），产出「validbet_total」
             SUM(game_pnl)                             AS player_pnl,                                     -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「player_pnl」
             -SUM(game_pnl)                            AS ggr,                                            -- 加减计算：汇总后取负号——会员净输赢取负即平台毛利（GGR），产出「ggr」
             -SUM(game_pnl) / NULLIF(SUM(stake),0)     AS house_hold_pct,                                 -- 加减计算：汇总后取负号——会员净输赢取负即平台毛利（GGR），产出「house_hold_pct」
             SUM(rebate)                               AS rebate_total,                                   -- 汇总表达式：取用 rebate（退水），产出「rebate_total」
             SUM(rebate) / NULLIF(SUM(validbet),0)     AS rebate_rate,                                    -- 汇总表达式：取用 validbet（有效投注（洗码量））、rebate（退水），产出「rebate_rate」
             COUNT(*) * 1.0 / SUM(COUNT(*)) OVER ()    AS order_share,                                    -- 计数表达式：全局窗口，用作占比之分母，产出「order_share」
             SUM(stake) / SUM(SUM(stake)) OVER ()      AS stake_share                                     -- 汇总表达式：全局窗口，用作占比之分母，产出「stake_share」
      FROM o                                                                                              -- 取数来源：取自本条自建的中间结果集 o
      GROUP BY product_code                                                                               -- 分组：按 product_code 汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_freecomm,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_freecomm,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_freecomm,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_freecomm,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_freecomm,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_freecomm,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_freecomm,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_freecomm,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`product_code` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  19. E02_segment_econ.csv   [总包行 5707~5797 · 原 9 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`segment`, w.`n_member`, w.`validbet`, w.`ggr`, w.`rebate`, w.`commission`, w.`comm_scale`, w.`reb_scale`, w.`tenure_months`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH probe AS (            -- 量纲判据：三日窗即足（量纲是 schema 属性，非时序量）
        SELECT                                                                                            -- 续行：接续上一取列子句，续列 SELECT
          GREATEST(                                                                                       -- 取值表达式：承接上一子句，构成完整语句，属 §E02c 之取数
            COALESCE(MAX(CAST(NULLIF(TRIM(bet23),'') AS DECIMAL(20,4))),0),                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
            COALESCE(MAX(CAST(NULLIF(TRIM(bet24),'') AS DECIMAL(20,4))),0),                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
            COALESCE(MAX(CAST(NULLIF(TRIM(bet25),'') AS DECIMAL(20,4))),0),                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
            COALESCE(MAX(CAST(NULLIF(TRIM(bet26),'') AS DECIMAL(20,4))),0),                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
            COALESCE(MAX(CAST(NULLIF(TRIM(bet27),'') AS DECIMAL(20,4))),0)) AS max_comm,                  -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「max_comm」
          GREATEST(                                                                                       -- 取值表达式：承接上一子句，构成完整语句，属 §E02c 之取数
            COALESCE(MAX(CAST(NULLIF(TRIM(bet28),'') AS DECIMAL(20,4))),0),                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
            COALESCE(MAX(CAST(NULLIF(TRIM(bet29),'') AS DECIMAL(20,4))),0),                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
            COALESCE(MAX(CAST(NULLIF(TRIM(bet30),'') AS DECIMAL(20,4))),0),                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
            COALESCE(MAX(CAST(NULLIF(TRIM(bet31),'') AS DECIMAL(20,4))),0),                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
            COALESCE(MAX(CAST(NULLIF(TRIM(bet32),'') AS DECIMAL(20,4))),0)) AS max_reb,                   -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「max_reb」
          AVG(CAST(NULLIF(TRIM(bet41),'') AS DECIMAL(20,4)))                AS avg_vb                     -- 求均值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「avg_vb」
        FROM ods_mariadb_2b.ods_a168_dailyreport_member                                                   -- 取数来源：取自会员日结报表（日粒度业绩与风控标记）
        WHERE dt >= '2026-08-04' AND dt < '2026-08-07'                                                    -- 过滤条件：限定 dt不少于 '2026-08-04'、dt小于 '2026-08-07'，涉 dt（营业日）
          AND bet02 = '101' AND category = '1'                                                            -- 并列条件：限定 bet02等于 '101'、category等于 '1'，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§E02c）
      sc AS (                    -- 判据 → 口径与乘数，全窗单行，供下游广播
        SELECT                                                                                            -- 续行：接续上一取列子句，续列 SELECT
          CASE WHEN max_comm <= 1 THEN 'rate_decimal'                                                     -- 条件分支：承接上一子句，构成完整语句，属 §E02c 之取数
               WHEN max_comm <= 100 AND max_comm < avg_vb / 50.0 THEN 'rate_percent'                      -- 分支判定：承接上一子句，构成完整语句，属 §E02c 之取数
               ELSE 'amount' END                                   AS comm_scale,                         -- 兜底取值：产出「comm_scale」
          CASE WHEN max_reb  <= 1 THEN 'rate_decimal'                                                     -- 条件分支：承接上一子句，构成完整语句，属 §E02c 之取数
               WHEN max_reb  <= 100 AND max_reb  < avg_vb / 50.0 THEN 'rate_percent'                      -- 分支判定：承接上一子句，构成完整语句，属 §E02c 之取数
               ELSE 'amount' END                                   AS reb_scale,                          -- 兜底取值：产出「reb_scale」
          CASE WHEN max_comm <= 1 THEN 1.0                                                                -- 条件分支：承接上一子句，构成完整语句，属 §E02c 之取数
               WHEN max_comm <= 100 AND max_comm < avg_vb / 50.0 THEN 0.01                                -- 分支判定：承接上一子句，构成完整语句，属 §E02c 之取数
               ELSE 0.0 END                                        AS comm_k,  -- 乘洗码量的系数
          CASE WHEN max_reb  <= 1 THEN 1.0                                                                -- 条件分支：承接上一子句，构成完整语句，属 §E02c 之取数
               WHEN max_reb  <= 100 AND max_reb  < avg_vb / 50.0 THEN 0.01                                -- 分支判定：承接上一子句，构成完整语句，属 §E02c 之取数
               ELSE 0.0 END                                        AS reb_k                               -- 兜底取值：产出「reb_k」
        FROM probe                                                                                        -- 取数来源：取自本条自建的中间结果集 probe
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§E02c）
      mem AS (                   -- ★ 一次扫描直出会员级；量纲 CASE 已内联为 k 系数乘法
        SELECT b.bet05 AS member_id,                                                                      -- 取列：起始取列子句，本行先取「member_id」，涉 bet05（会员号）、member_id（会员号）
               SUM(CAST(NULLIF(TRIM(b.bet41),'') AS DECIMAL(20,4))                                        -- 汇总表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                   / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)))          AS validbet,               -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「validbet」
               SUM((CAST(NULLIF(TRIM(b.bet14),'') AS DECIMAL(20,4))                                       -- 汇总表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                  - CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4)))                                      -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                   / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)))          AS game_pnl,               -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               SUM(( COALESCE(CAST(NULLIF(TRIM(b.bet28),'') AS DECIMAL(20,4)),0)                          -- 汇总表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                   + COALESCE(CAST(NULLIF(TRIM(b.bet29),'') AS DECIMAL(20,4)),0)                          -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                   + COALESCE(CAST(NULLIF(TRIM(b.bet30),'') AS DECIMAL(20,4)),0)                          -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                   + COALESCE(CAST(NULLIF(TRIM(b.bet31),'') AS DECIMAL(20,4)),0)                          -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                   + COALESCE(CAST(NULLIF(TRIM(b.bet32),'') AS DECIMAL(20,4)),0))                         -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                   * (CASE WHEN s.reb_k > 0                                                               -- 除法或乘法计算：承接上一子句，构成完整语句，属 §E02c 之取数
                           THEN s.reb_k * CAST(NULLIF(TRIM(b.bet41),'') AS DECIMAL(20,4))                 -- 分支取值：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                           ELSE 1.0 END)                                                                  -- 兜底取值：承接上一子句，构成完整语句，属 §E02c 之取数
                   / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)))          AS rebate,                 -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
               SUM(( COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,4)),0)                          -- 汇总表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                   + COALESCE(CAST(NULLIF(TRIM(b.bet24),'') AS DECIMAL(20,4)),0)                          -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                   + COALESCE(CAST(NULLIF(TRIM(b.bet25),'') AS DECIMAL(20,4)),0)                          -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                   + COALESCE(CAST(NULLIF(TRIM(b.bet26),'') AS DECIMAL(20,4)),0)                          -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                   + COALESCE(CAST(NULLIF(TRIM(b.bet27),'') AS DECIMAL(20,4)),0))                         -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                   * (CASE WHEN s.comm_k > 0                                                              -- 除法或乘法计算：承接上一子句，构成完整语句，属 §E02c 之取数
                           THEN s.comm_k * CAST(NULLIF(TRIM(b.bet41),'') AS DECIMAL(20,4))                -- 分支取值：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                           ELSE 1.0 END)                                                                  -- 兜底取值：承接上一子句，构成完整语句，属 §E02c 之取数
                   / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)))          AS commission,             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「commission」
               COUNT(DISTINCT SUBSTR(b.dt, 1, 7))                              AS n_months,               -- 计数表达式：统计去重个数，产出「n_months」
               COUNT(DISTINCT b.dt)                                            AS n_days                  -- 计数表达式：统计去重个数，产出「n_days」
        FROM ods_mariadb_2b.ods_a168_dailyreport_member b                                                 -- 取数来源：取自会员日结报表（日粒度业绩与风控标记）
        CROSS JOIN sc s                                                                                   -- 交叉连接：取自本条自建的中间结果集 sc
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'，涉 dt（营业日）
          AND b.bet02 = '101' AND b.category = '1'                                                        -- 并列条件：限定 b.bet02等于 '101'、b.category等于 '1'，涉 bet02（游戏类别）
          AND CAST(NULLIF(TRIM(b.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(b.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
        GROUP BY b.bet05                                                                                  -- 分组：按 b.bet05 汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§E02c）
      mem2 AS (                                                                                           -- 公共表表达式：开启中间结果集 mem2，其后各行为其定义体（§E02c）
        SELECT m.*,                                                                                       -- 取列：整体承接上游结果集的全部字段，不再逐列列举
               /* 分群：实测退水率三位小数分档 × 洗码量四分位，全部由数据算出 */
               ROUND(m.rebate / NULLIF(m.validbet,0), 3) AS rebate_rate_bkt,                              -- 取值表达式：取用 validbet（有效投注（洗码量））、rebate（退水），产出「rebate_rate_bkt」
               NTILE(4) OVER (ORDER BY m.validbet)       AS vb_q                                          -- 等分分箱表达式：取用 validbet（有效投注（洗码量）），产出「vb_q」
        FROM mem m WHERE m.validbet > 0                                                                   -- 取数来源：取自本条自建的中间结果集 mem
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§E02c）
      SELECT                                                                                              -- 续行：接续上一取列子句，续列 SELECT
        CONCAT('实测退水', CAST(m.rebate_rate_bkt AS STRING),                                                 -- 取值表达式：承接上一子句，构成完整语句，属 §E02c 之取数
               ' · 流水Q', CAST(m.vb_q AS STRING)) AS segment,                                              -- 字面取值：取用 segment（客户分层），产出「segment」
        COUNT(*)             AS n_member,                                                                 -- 计数表达式：取用 n_member（会员数），产出「n_member」
        SUM(m.validbet)      AS validbet,                                                                 -- 汇总表达式：取用 validbet（有效投注（洗码量）），产出「validbet」
        -SUM(m.game_pnl)     AS ggr,           -- 庄家赢 = 玩家输
        SUM(m.rebate)        AS rebate,                                                                   -- 汇总表达式：取用 rebate（退水），产出「rebate」
        SUM(m.commission)    AS commission,    -- ★ 不再是 0；量纲由 comm_scale 标明
        MAX(s.comm_scale)    AS comm_scale,    -- ★ 全窗单值，由 sc 广播取得
        MAX(s.reb_scale)     AS reb_scale,                                                                -- 取最大值表达式：产出「reb_scale」
        AVG(m.n_months)      AS tenure_months                                                             -- 求均值表达式：产出「tenure_months」
      FROM mem2 m CROSS JOIN sc s                                                                         -- 取数来源：取自本条自建的中间结果集 mem2
      GROUP BY m.rebate_rate_bkt, m.vb_q                                                                  -- 分组：按 m.rebate_rate_bkt, m.vb_q 汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  20. EV01_disposal_events.csv   [总包行 7112~7153 · 原 13 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：q.`event_date`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`event_id`, w.`event_time`, w.`event_date`, w.`fn`, w.`operator`, w.`operator_ip`, w.`event_type`, w.`member_login`, w.`table_ref`, w.`round_ref`, w.`whitelist_ip`, w.`is_cancel`, w.`content_len`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_date                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_date IS NOT NULL AND TRIM(CAST(s.x_date AS STRING)) <> ''
    GROUP BY s.x_date
  ),
    q AS (
      SELECT                                                                                              -- 续行：接续上一取列子句，续列 SELECT
        CAST(id AS STRING)                                                  AS event_id,                  -- 取值表达式：产出「event_id」
        optime                                                              AS event_time,                -- 取值表达式：产出「event_time」
        dt                                                                  AS event_date,                -- 取值表达式：取用 dt（营业日），产出「event_date」
        function                                                            AS fn,                        -- 取值表达式：产出「fn」
        CAST(operator AS STRING)                                            AS operator,                  -- 取值表达式：产出「operator」
        ip                                                                  AS operator_ip,               -- 取值表达式：产出「operator_ip」
        CASE                                                                                              -- 条件分支：开启分支判定，其后逐行列出条件与取值（§EX-16b）
          WHEN function = 'realtimebet'         AND content LIKE 'Kick Out%' THEN 'kick_out'              -- 分支判定：承接上一子句，构成完整语句，属 §EX-16b 之取数
          WHEN function = 'newrisk_manager'                                  THEN 'table_switch'          -- 分支判定：承接上一子句，构成完整语句，属 §EX-16b 之取数
          WHEN function = 'gameset'             AND content LIKE '%cancel%'  THEN 'round_cancel'          -- 分支判定：承接上一子句，构成完整语句，属 §EX-16b 之取数
          WHEN function = 'gameset'                                          THEN 'round_edit'            -- 分支判定：承接上一子句，构成完整语句，属 §EX-16b 之取数
          WHEN function = 'MemberGameWhiteList' AND content LIKE 'ADD%'      THEN 'whitelist_add'         -- 分支判定：承接上一子句，构成完整语句，属 §EX-16b 之取数
          WHEN function = 'MemberGameWhiteList'                              THEN 'whitelist_del'         -- 分支判定：承接上一子句，构成完整语句，属 §EX-16b 之取数
          ELSE 'other' END                                                  AS event_type,                -- 兜底取值：产出「event_type」
        /* 会员账号：仅 kick_out 有，取 ' = ' 之后 */
        CASE WHEN content LIKE 'Kick Out%' AND content LIKE '% = %'                                       -- 条件分支：承接上一子句，构成完整语句，属 §EX-16b 之取数
             THEN TRIM(SUBSTRING(content, INSTR(content, ' = ') + 3))                                     -- 分支取值：承接上一子句，构成完整语句，属 §EX-16b 之取数
             ELSE NULL END                                                  AS member_login,              -- 兜底取值：产出「member_login」
        /* 桌台：kick_out 取 ':' 与 ' kick' 之间；table_switch 取 'tableid:' 之后 */
        CASE                                                                                              -- 条件分支：开启分支判定，其后逐行列出条件与取值（§EX-16b）
          WHEN content LIKE 'Kick Out%' AND content LIKE '% kick %'                                       -- 分支判定：承接上一子句，构成完整语句，属 §EX-16b 之取数
            THEN TRIM(SUBSTRING(content, INSTR(content, ':') + 1,                                         -- 分支取值：承接上一子句，构成完整语句，属 §EX-16b 之取数
                                INSTR(content, ' kick ') - INSTR(content, ':') - 1))                      -- 取值表达式：承接上一子句，构成完整语句，属 §EX-16b 之取数
          WHEN content LIKE '%tableid:%'                                                                  -- 分支判定：承接上一子句，构成完整语句，属 §EX-16b 之取数
            THEN TRIM(SUBSTRING(content, INSTR(content, 'tableid:') + 8, 8))                              -- 分支取值：承接上一子句，构成完整语句，属 §EX-16b 之取数
          ELSE NULL END                                                     AS table_ref,                 -- 兜底取值：产出「table_ref」
        /* 局标识：round_cancel / round_edit 取 'UPDATE ' 之后至下一空格 */
        CASE WHEN function = 'gameset' AND content LIKE 'UPDATE %'                                        -- 条件分支：承接上一子句，构成完整语句，属 §EX-16b 之取数
             THEN TRIM(SUBSTRING(content, 8,                                                              -- 分支取值：承接上一子句，构成完整语句，属 §EX-16b 之取数
                       INSTR(SUBSTRING(content, 8), ' ') - 1))                                            -- 取值表达式：承接上一子句，构成完整语句，属 §EX-16b 之取数
             ELSE NULL END                                                  AS round_ref,                 -- 兜底取值：产出「round_ref」
        /* 白名单 IP */
        CASE WHEN content LIKE '%white_list:%'                                                            -- 条件分支：承接上一子句，构成完整语句，属 §EX-16b 之取数
             THEN TRIM(SUBSTRING(content, INSTR(content, 'white_list:') + 11))                            -- 分支取值：承接上一子句，构成完整语句，属 §EX-16b 之取数
             ELSE NULL END                                                  AS whitelist_ip,              -- 兜底取值：产出「whitelist_ip」
        CASE WHEN content LIKE '%cancel%' THEN 1 ELSE 0 END                 AS is_cancel,                 -- 条件分支：产出「is_cancel」
        LENGTH(COALESCE(content, ''))                                       AS content_len                -- 取值表达式：产出「content_len」
      FROM ods_mariadb_2b.ods_a168_change_log                                                             -- 取数来源：取自通用变更日志（修订滞后与快照有效性核验）
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定 dt不少于 '2026-03-21'、dt小于 '2026-08-07'，涉 dt（营业日）
        AND function IN ('realtimebet','newrisk_manager','gameset','MemberGameWhiteList')                 -- 并列条件：限定落入所列取值
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`event_date` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  21. EV02_member_disposal.csv   [总包行 7289~7322 · 原 12 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：q.`event_date`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`event_id`, w.`event_date`, w.`event_time`, w.`type_code`, w.`action`, w.`operator`, w.`event_type`, w.`val_before`, w.`val_after`, w.`summary`, w.`is_batch`, w.`stmt_len`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_date                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_date IS NOT NULL AND TRIM(CAST(s.x_date AS STRING)) <> ''
    GROUP BY s.x_date
  ),
    q AS (
      SELECT                                                                                              -- 续行：接续上一取列子句，续列 SELECT
        CAST(lmc01 AS STRING)                                               AS event_id,                  -- 取值表达式：产出「event_id」
        dt                                                                  AS event_date,                -- 取值表达式：取用 dt（营业日），产出「event_date」
        lmc09                                                               AS event_time,                -- 取值表达式：产出「event_time」
        lmc03                                                               AS type_code,                 -- 取值表达式：产出「type_code」
        lmc04                                                               AS action,                    -- 取值表达式：产出「action」
        CAST(lmc02 AS STRING)                                               AS operator,                  -- 取值表达式：产出「operator」
        /* 处置类别：依 lmc11 摘要中出现的字段名判别 */
        CASE                                                                                              -- 条件分支：开启分支判定，其后逐行列出条件与取值（§EX-17b）
          WHEN lmc11 LIKE '%mem015%' THEN 'limit_change'      -- 限额变更（限红降档）
          WHEN lmc11 LIKE '%mem016%' THEN 'member_disable'    -- 会员停用
          WHEN lmc11 LIKE '%mem017%' THEN 'member_status'     -- 会员状态
          WHEN lmc11 LIKE '%mem020%' THEN 'member_switch'     -- 会员开关
          WHEN lmc11 LIKE '%age015%' OR lmc11 LIKE '%age016%' THEN 'agent_disable'                        -- 分支判定：承接上一子句，构成完整语句，属 §EX-17b 之取数
          WHEN lmc11 LIKE '%ag012%'  THEN 'commission_change' -- 占成率变更
          WHEN lmc11 LIKE '%opengame%' THEN 'game_access'                                                 -- 分支判定：承接上一子句，构成完整语句，属 §EX-17b 之取数
          ELSE 'other' END                                                  AS event_type,                -- 兜底取值：产出「event_type」
        /* 前后值：以 '=>' 分隔，取其两侧各 24 字 */
        CASE WHEN lmc11 LIKE '%=>%'                                                                       -- 条件分支：承接上一子句，构成完整语句，属 §EX-17b 之取数
             THEN TRIM(SUBSTRING(lmc11,                                                                   -- 分支取值：承接上一子句，构成完整语句，属 §EX-17b 之取数
                    GREATEST(INSTR(lmc11, ':') + 1, 1),                                                   -- 取值表达式：承接上一子句，构成完整语句，属 §EX-17b 之取数
                    INSTR(lmc11, '=>') - GREATEST(INSTR(lmc11, ':') + 1, 1)))                             -- 取值表达式：承接上一子句，构成完整语句，属 §EX-17b 之取数
             ELSE NULL END                                                  AS val_before,                -- 兜底取值：产出「val_before」
        CASE WHEN lmc11 LIKE '%=>%'                                                                       -- 条件分支：承接上一子句，构成完整语句，属 §EX-17b 之取数
             THEN TRIM(SUBSTRING(lmc11, INSTR(lmc11, '=>') + 2, 24))                                      -- 分支取值：承接上一子句，构成完整语句，属 §EX-17b 之取数
             ELSE NULL END                                                  AS val_after,                 -- 兜底取值：产出「val_after」
        /* 摘要截断（供人工核样，不含 SQL 原文） */
        SUBSTR(COALESCE(lmc11, ''), 1, 120)                                 AS summary,                   -- 取值表达式：产出「summary」
        /* 批量标记：一次改动多个会员者，lmc05 中会出现 IN (…) */
        CASE WHEN lmc05 LIKE '%IN (%' OR lmc05 LIKE '%in (%' THEN 1 ELSE 0 END AS is_batch,               -- 条件分支：产出「is_batch」
        LENGTH(COALESCE(lmc05, ''))                                         AS stmt_len                   -- 取值表达式：产出「stmt_len」
      FROM ods_mariadb_2b.ods_a168_log_mem_change                                                         -- 取数来源：取自会员资料变动日志（批量注册嫌疑旁证）
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定 dt不少于 '2026-03-21'、dt小于 '2026-08-07'，涉 dt（营业日）
      ORDER BY lmc09                                                                                     -- 排序：按 lmc09 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`event_date` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  22. I_ip_agg.csv   [总包行 2695~2768 · 原 14 列 ＋六层约 60 列]
--     典型学：M04 IP 聚集　分区：recent（s.x_rec = 1）　连接：q.`bet_ip`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`window_id`, w.`bet_ip`, w.`n_member_raw`, w.`n_member_eff`, w.`n_prof_A`, w.`n_prof_B`, w.`n_prof_C`, w.`n_orders`, w.`n_rounds`, w.`stake`, w.`net_pnl`, w.`game_pnl`, w.`share_p50`, w.`share_p90`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_ip                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_ip IS NOT NULL AND TRIM(CAST(s.x_ip AS STRING)) <> ''
    GROUP BY s.x_ip
  ),
    q AS (
      WITH ta AS (            -- 公司测试线代理（214 条，跨五级）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 I_ip_agg.csv）
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk，其后各行为其定义体（交付件 I_ip_agg.csv）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet09，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet11, b.bet13, b.bet14, b.bet16, b.bet17,                                               -- 续行：接续上一取列子句，续列 b.bet11, b.bet13, b.bet14, b.bet16, b.bet17，涉 bet11（汇率）、bet13（下注金额）、bet14（派彩金额）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet38, b.bet39, b.category, b.eid, b.ip,                                                 -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category, b.eid, b.ip，涉 bet38（重對）、bet39（桌号）、eid（荷官工号）
               b.validbet,                                                                                -- 续行：接续上一取列子句，续列 b.validbet，涉 validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 I_ip_agg.csv）
      vd AS (                                                                                             -- 公共表表达式：开启中间结果集 vd，其后各行为其定义体（交付件 I_ip_agg.csv）
        SELECT r.* FROM rk r                                                                              -- 取列：整体承接上游结果集的全部字段，不再逐列列举，取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 I_ip_agg.csv）
      bs AS (                                                                                             -- 公共表表达式：开启中间结果集 bs，其后各行为其定义体（交付件 I_ip_agg.csv）
        SELECT v.bet05 AS member_id, v.eid AS dealer_id, v.ip AS bet_ip,                                  -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、eid（荷官工号）、bet_ip（下注 IP）
               v.bet20 AS lv3, v.bet09 AS bet_side, v.dt AS bet_date,                                     -- 取值表达式：取用 bet09（玩法）、bet20（三级代理线）、dt（营业日），产出「bet_date」
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.bet08),'') AS DATETIME) AS t_bet,                                       -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「t_bet」
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake,                              -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               CAST(NULLIF(TRIM(v.validbet),'') AS DECIMAL(20,4))                                         -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS valid_bet,                          -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「valid_bet」
               (CAST(NULLIF(TRIM(v.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS game_pnl,                           -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               CAST(NULLIF(TRIM(v.bet16),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS rebate,                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
               CAST(NULLIF(TRIM(v.bet17),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS net_pnl                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_pnl」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 I_ip_agg.csv）
      pl AS (SELECT member_id, COUNT(*) AS n_orders_all,                                                  -- 取值表达式：计数，取用 member_id（会员号），产出「n_orders_all」
                    SUM(net_pnl) AS net_pnl_all, SUM(game_pnl) AS game_pnl_all                            -- 汇总表达式：取用 net_pnl（会员净输赢）、game_pnl（游戏净输赢），产出「game_pnl_all」
             FROM bs GROUP BY member_id),                                                                 -- 取数来源：取自本条自建的中间结果集 bs
      ipm AS (SELECT b.bet_ip, b.member_id, COUNT(*) AS n_orders_ip,                                      -- 取值表达式：计数，取用 bet_ip（下注 IP）、member_id（会员号），产出「n_orders_ip」
                     COUNT(DISTINCT b.round_key) AS n_rounds_ip,                                          -- 计数表达式：统计去重个数，产出「n_rounds_ip」
                     SUM(b.stake) AS stake_ip, SUM(b.net_pnl) AS net_pnl_ip,                              -- 汇总表达式：取用 stake（下注额（经汇率归一化））、net_pnl（会员净输赢），产出「net_pnl_ip」
                     SUM(b.game_pnl) AS game_pnl_ip,                                                      -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「game_pnl_ip」
                     MAX(p.net_pnl_all) AS net_pnl_all, MAX(p.game_pnl_all) AS game_pnl_all,              -- 取最大值表达式：产出「game_pnl_all」
                     COUNT(*)*1.0/NULLIF(MAX(p.n_orders_all),0) AS ip_order_share                         -- 计数表达式：取最大值，产出「ip_order_share」
              FROM bs b JOIN pl p ON p.member_id=b.member_id                                              -- 取数来源：取自本条自建的中间结果集 bs，连接键为 member_id（会员号）
              WHERE NULLIF(TRIM(b.bet_ip),'') IS NOT NULL                                                 -- 过滤条件：限定该值非空，涉 bet_ip（下注 IP）
              GROUP BY b.bet_ip, b.member_id)                                                             -- 分组：按 b.bet_ip, b.member_id) 汇总
      SELECT 'W1' AS window_id, bet_ip,                                                                   -- 取列：起始取列子句，本行先取「window_id」，涉 bet_ip（下注 IP）
        COUNT(*) AS n_member_raw,                                                                         -- 计数表达式：产出「n_member_raw」
        SUM(CASE WHEN n_orders_ip>=30 THEN 1 ELSE 0 END) AS n_member_eff,                                 -- 汇总表达式：产出「n_member_eff」
        SUM(CASE WHEN n_orders_ip>=30 AND net_pnl_all>0 THEN 1 ELSE 0 END) AS n_prof_A,                   -- 汇总表达式：产出「n_prof_A」
        SUM(CASE WHEN n_orders_ip>=30 AND net_pnl_ip >0 THEN 1 ELSE 0 END) AS n_prof_B,                   -- 汇总表达式：产出「n_prof_B」
        SUM(CASE WHEN n_orders_ip>=30 AND game_pnl_ip>0 THEN 1 ELSE 0 END) AS n_prof_C,                   -- 汇总表达式：产出「n_prof_C」
        SUM(n_orders_ip) AS n_orders, SUM(n_rounds_ip) AS n_rounds,                                       -- 汇总表达式：产出「n_rounds」
        SUM(stake_ip) AS stake, SUM(net_pnl_ip) AS net_pnl, SUM(game_pnl_ip) AS game_pnl,                 -- 汇总表达式：取用 stake（下注额（经汇率归一化））、net_pnl（会员净输赢）、game_pnl（游戏净输赢），产出「game_pnl」
        PERCENTILE_APPROX(ip_order_share,0.5) AS share_p50,                                               -- 取近似分位数表达式：产出「share_p50」
        PERCENTILE_APPROX(ip_order_share,0.9) AS share_p90                                                -- 取近似分位数表达式：产出「share_p90」
      FROM ipm GROUP BY bet_ip                                                                            -- 取数来源：取自本条自建的中间结果集 ipm
      HAVING SUM(CASE WHEN n_orders_ip>=30 THEN 1 ELSE 0 END) >= 5                                        -- 分组后过滤：限定 SUM(CASE WHEN n_orders_ip不少于 30 THEN 1 ELSE 0 END) >= 5
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake_xagg, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds AS n_rounds_xagg,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`bet_ip` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  23. I_ip_player.csv   [总包行 1589~1654 · 原 14 列 ＋六层约 60 列]
--     典型学：M04 IP 聚集　分区：recent（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`bet_ip`, w.`member_id`, w.`lv3`, w.`n_orders_ip`, w.`n_rounds_ip`, w.`stake_ip`, w.`game_pnl_ip`, w.`rebate_ip`, w.`net_pnl_ip`, w.`n_orders_all`, w.`n_rounds_all`, w.`net_pnl_all`, w.`game_pnl_all`, w.`ip_order_share`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ta AS (            -- 公司测试线代理（214 条，跨五级）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 I_ip_player.csv）
      rk AS (                  -- 需求 §3.2：同注单号取最新版本（三级排序去重）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet09，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet11, b.bet13, b.bet14, b.bet16, b.bet17,                                               -- 续行：接续上一取列子句，续列 b.bet11, b.bet13, b.bet14, b.bet16, b.bet17，涉 bet11（汇率）、bet13（下注金额）、bet14（派彩金额）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet38, b.bet39, b.category, b.eid, b.ip,                                                 -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category, b.eid, b.ip，涉 bet38（重對）、bet39（桌号）、eid（荷官工号）
               b.validbet,                                                                                -- 续行：接续上一取列子句，续列 b.validbet，涉 validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 I_ip_player.csv）
      vd AS (                  -- 有效注单：非测试线、非重对、一般注单
        SELECT r.*                                                                                        -- 取列：整体承接上游结果集的全部字段，不再逐列列举
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 I_ip_player.csv）
      bs AS (                  -- 金额正名：本金/洗码量/游戏输赢/退水/净输赢（一律÷汇率）
        SELECT v.bet05 AS member_id, v.eid AS dealer_id, v.ip AS bet_ip,                                  -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、eid（荷官工号）、bet_ip（下注 IP）
               v.bet20 AS lv3, v.bet09 AS bet_side, v.dt AS bet_date,                                     -- 取值表达式：取用 bet09（玩法）、bet20（三级代理线）、dt（营业日），产出「bet_date」
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.bet08),'') AS DATETIME) AS t_bet,                                       -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「t_bet」
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake,                              -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               CAST(NULLIF(TRIM(v.validbet),'') AS DECIMAL(20,4))                                         -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS valid_bet,                          -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「valid_bet」
               (CAST(NULLIF(TRIM(v.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS game_pnl,                           -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               CAST(NULLIF(TRIM(v.bet16),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS rebate,                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
               CAST(NULLIF(TRIM(v.bet17),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS net_pnl                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_pnl」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 I_ip_player.csv）
      pl AS (SELECT member_id, COUNT(*) AS n_orders_all,                                                  -- 取值表达式：计数，取用 member_id（会员号），产出「n_orders_all」
                    COUNT(DISTINCT round_key) AS n_rounds_all,                                            -- 计数表达式：统计去重个数，产出「n_rounds_all」
                    SUM(net_pnl) AS net_pnl_all, SUM(game_pnl) AS game_pnl_all                            -- 汇总表达式：取用 net_pnl（会员净输赢）、game_pnl（游戏净输赢），产出「game_pnl_all」
             FROM bs GROUP BY member_id)                                                                  -- 取数来源：取自本条自建的中间结果集 bs
      SELECT b.bet_ip, b.member_id, MAX(b.lv3) AS lv3,                                                    -- 取列：起始取列子句，本行先取「lv3」，涉 bet_ip（下注 IP）、member_id（会员号）、lv3（三级代理线）
        COUNT(*) AS n_orders_ip, COUNT(DISTINCT b.round_key) AS n_rounds_ip,                              -- 计数表达式：统计去重个数，产出「n_rounds_ip」
        SUM(b.stake) AS stake_ip, SUM(b.game_pnl) AS game_pnl_ip,                                         -- 汇总表达式：取用 stake（下注额（经汇率归一化））、game_pnl（游戏净输赢），产出「game_pnl_ip」
        SUM(b.rebate) AS rebate_ip, SUM(b.net_pnl) AS net_pnl_ip,                                         -- 汇总表达式：取用 net_pnl（会员净输赢）、rebate（退水），产出「net_pnl_ip」
        MAX(pl.n_orders_all) AS n_orders_all, MAX(pl.n_rounds_all) AS n_rounds_all,                       -- 取最大值表达式：产出「n_rounds_all」
        MAX(pl.net_pnl_all) AS net_pnl_all, MAX(pl.game_pnl_all) AS game_pnl_all,                         -- 取最大值表达式：产出「game_pnl_all」
        COUNT(*)*1.0/NULLIF(MAX(pl.n_orders_all),0) AS ip_order_share                                     -- 计数表达式：取最大值，产出「ip_order_share」
      FROM bs b JOIN pl ON pl.member_id=b.member_id                                                       -- 取数来源：取自本条自建的中间结果集 bs，连接键为 member_id（会员号）
      WHERE NULLIF(TRIM(b.bet_ip),'') IS NOT NULL                                                         -- 过滤条件：限定该值非空，涉 bet_ip（下注 IP）
      GROUP BY b.bet_ip, b.member_id                                                                      -- 分组：按 b.bet_ip, b.member_id 汇总
      HAVING COUNT(*) >= 30                                                                               -- 分组后过滤：限定行数不少于 30
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  24. K01_risk_feature_matrix.csv   [总包行 5499~5591 · 原 15 列 ＋六层约 60 列]
--     典型学：M11 异常告警　分区：alarm（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`is_seed`, w.`n_rounds`, w.`n_days`, w.`n_tables`, w.`n_ip`, w.`n_dealer`, w.`side_entropy`, w.`stake_cv`, w.`stake_p90_p50`, w.`hedge_rate`, w.`rebate_dep`, w.`night_share`, w.`roi_game`, w.`validbet`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH test_agents AS (                                                                               -- 公共表表达式：开启中间结果集 test_agents，其后各行为其定义体（§K01）
        SELECT age001 AS agent_id FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                   -- 取列：取用代理主档（五级代理线归属来源），产出「agent_id」
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§K01）
      ranked AS (                                                                                         -- 公共表表达式：开启中间结果集 ranked，其后各行为其定义体（§K01）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet09，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet11, b.bet13, b.bet14, b.bet16, b.bet17,                                               -- 续行：接续上一取列子句，续列 b.bet11, b.bet13, b.bet14, b.bet16, b.bet17，涉 bet11（汇率）、bet13（下注金额）、bet14（派彩金额）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet38, b.bet39, b.category, b.eid, b.ip,                                                 -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category, b.eid, b.ip，涉 bet38（重對）、bet39（桌号）、eid（荷官工号）
               b.validbet,                                                                                -- 续行：接续上一取列子句，续列 b.validbet，涉 validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b            -- ★ 表名开关
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'，涉 dt（营业日）
          AND b.bet02 = '101'                                                                             -- 并列条件：限定 b.bet02等于 '101'，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§K01）
      ord AS (                                                                                            -- 公共表表达式：开启中间结果集 ord，其后各行为其定义体（§K01）
        SELECT r.bet05 AS member_id, r.dt AS bet_date, r.bet39 AS table_id,                               -- 取列：起始取列子句，本行先取「table_id」，涉 bet05（会员号）、bet39（桌号）、dt（营业日）
               r.ip AS bet_ip, r.eid AS dealer_id, r.bet09 AS bet_side,                                   -- 取值表达式：取用 bet09（玩法）、eid（荷官工号）、bet_ip（下注 IP），产出「bet_side」
               CONCAT_WS('|', r.bet03, r.bet04, r.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               HOUR(CAST(NULLIF(TRIM(r.bet08),'') AS DATETIME)) AS bet_hour,                              -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「bet_hour」
               CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) AS fx,                                     -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「fx」
               CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4)) AS stake_raw,                              -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake_raw」
               CAST(NULLIF(TRIM(r.bet14),'') AS DECIMAL(20,4)) AS payout_raw,                             -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「payout_raw」
               CAST(NULLIF(TRIM(r.bet16),'') AS DECIMAL(20,4)) AS rebate_raw,                             -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate_raw」
               CAST(NULLIF(TRIM(r.bet17),'') AS DECIMAL(20,4)) AS net_raw,                                -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_raw」
               CAST(NULLIF(TRIM(r.validbet),'') AS DECIMAL(20,4)) AS vb_raw                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「vb_raw」
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet18（一级代理线）
        LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet19（二级代理线）
        LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet20（三级代理线）
        LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet21（四级代理线）
        LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND COALESCE(t1.agent_id,t2.agent_id,t3.agent_id,t4.agent_id,t5.agent_id) IS NULL               -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§K01）
      side_mix AS (      -- 玩法熵：只押单一玩法（熵≈0）是打水/技术型的共同结构特征
        SELECT member_id, bet_side, COUNT(*) AS n_side                                                    -- 取列：计数，取用 member_id（会员号），产出「n_side」
        FROM ord GROUP BY member_id, bet_side                                                             -- 取数来源：取自本条自建的中间结果集 ord
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§K01）
      side_ent AS (                                                                                       -- 公共表表达式：开启中间结果集 side_ent，其后各行为其定义体（§K01）
        SELECT s.member_id,                                                                               -- 取列：起始取列子句，本行先列 s.member_id，涉 member_id（会员号）
               -SUM((s.n_side*1.0/t.n_all) * LOG2(s.n_side*1.0/t.n_all)) AS side_entropy                  -- 加减计算：香农熵定义式之前置负号，与 GGR 无涉，产出「side_entropy」
        FROM side_mix s                                                                                   -- 取数来源：取自本条自建的中间结果集 side_mix
        JOIN (SELECT member_id, SUM(n_side) AS n_all FROM side_mix GROUP BY member_id) t                  -- 连接：取自本条自建的中间结果集 side_mix
          ON t.member_id = s.member_id                                                                    -- 连接键：限定 t.member_id等于 s.member_id，涉 member_id（会员号）
        GROUP BY s.member_id                                                                              -- 分组：按 s.member_id 汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§K01）
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk，其后各行为其定义体（§K01）
        SELECT member_id, bet_date, round_key,                                                            -- 取列：起始取列子句，本行先列 member_id, bet_date, round_key，涉 round_key（局键）、member_id（会员号）、bet_date（营业日）
               MAX(table_id) AS table_id, MAX(bet_ip) AS bet_ip,                                          -- 取最大值表达式：取用 bet_ip（下注 IP），产出「bet_ip」
               MAX(dealer_id) AS dealer_id, MAX(bet_hour) AS bet_hour,                                    -- 取最大值表达式：取用 dealer_id（荷官工号），产出「bet_hour」
               SUM(stake_raw/fx) AS stake,                                                                -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake」
               SUM(COALESCE(vb_raw,stake_raw)/fx) AS validbet,                                            -- 汇总表达式：取用 validbet（有效投注（洗码量）），产出「validbet」
               SUM((payout_raw-stake_raw)/fx) AS game_pnl,                                                -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「game_pnl」
               SUM(rebate_raw/fx) AS rebate,                                                              -- 汇总表达式：取用 rebate（退水），产出「rebate」
               SUM(net_raw/fx)    AS net_pnl,                                                             -- 汇总表达式：取用 net_pnl（会员净输赢），产出「net_pnl」
               /* ★ DX-01 实测校准：等值匹配主线庄闲，不用 LIKE（会误纳龙宝/天牌边注） */
               CASE WHEN MAX(CASE WHEN TRIM(bet_side)='Banker' THEN 1 ELSE 0 END)=1                       -- 条件分支：取最大值
                     AND MAX(CASE WHEN TRIM(bet_side)='Player' THEN 1 ELSE 0 END)=1                       -- 并列条件：限定 MAX(CASE WHEN TRIM(bet_side)等于 'Player' THEN 1 ELSE 0 END)=1
                    THEN 1 ELSE 0 END AS is_self_hedge                                                    -- 分支取值：产出「is_self_hedge」
        FROM ord GROUP BY member_id, bet_date, round_key                                                  -- 取数来源：取自本条自建的中间结果集 ord
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§K01）
      feat AS (                                                                                           -- 公共表表达式：开启中间结果集 feat，其后各行为其定义体（§K01）
        SELECT member_id,                                                                                 -- 取列：起始取列子句，本行先列 member_id，涉 member_id（会员号）
               COUNT(*)                        AS n_rounds,                                               -- 计数表达式：产出「n_rounds」
               COUNT(DISTINCT bet_date)        AS n_days,                                                 -- 计数表达式：统计去重个数，产出「n_days」
               COUNT(DISTINCT table_id)        AS n_tables,                                               -- 计数表达式：统计去重个数，产出「n_tables」
               COUNT(DISTINCT bet_ip)          AS n_ip,                                                   -- 计数表达式：统计去重个数，产出「n_ip」
               COUNT(DISTINCT dealer_id)       AS n_dealer,                                               -- 计数表达式：统计去重个数，产出「n_dealer」
               STDDEV_SAMP(stake)/NULLIF(AVG(stake),0)              AS stake_cv,                          -- 取值表达式：求均值，取用 stake（下注额（经汇率归一化））、stake_cv（注额变异系数），产出「stake_cv」
               PERCENTILE_APPROX(stake,0.9)                                                               -- 取近似分位数表达式：取用 stake（下注额（经汇率归一化））
                 / NULLIF(PERCENTILE_APPROX(stake,0.5),0)           AS stake_p90_p50,                     -- 除法或乘法计算：取近似分位数，取用 stake（下注额（经汇率归一化）），产出「stake_p90_p50」
               SUM(is_self_hedge)*1.0/COUNT(*)                      AS hedge_rate,                        -- 汇总表达式：计数，取用 hedge_rate（对打率），产出「hedge_rate」
               SUM(rebate)/NULLIF(ABS(SUM(net_pnl)),0)              AS rebate_dep,                        -- 汇总表达式：取用 net_pnl（会员净输赢）、rebate_dep（退水依赖度）、rebate（退水），产出「rebate_dep」
               SUM(CASE WHEN bet_hour BETWEEN 0 AND 6 THEN 1 ELSE 0 END)*1.0/COUNT(*) AS night_share,     -- 汇总表达式：计数，取用 night_share（夜场占比），产出「night_share」
               SUM(game_pnl)/NULLIF(SUM(stake),0)                   AS roi_game,                          -- 汇总表达式：取用 stake（下注额（经汇率归一化））、game_pnl（游戏净输赢）、roi_game（游戏回报率），产出「roi_game」
               SUM(validbet)                                        AS validbet                           -- 汇总表达式：取用 validbet（有效投注（洗码量）），产出「validbet」
        FROM rk GROUP BY member_id                                                                        -- 取数来源：取自本条自建的中间结果集 rk
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§K01）
      SELECT f.member_id,                                                                                 -- 取列：起始取列子句，本行先列 f.member_id，涉 member_id（会员号）
             CAST(0 AS INT) AS is_seed,          -- ★ 占位列：种子一律在 Python 侧并入，见下方说明
             f.n_rounds, f.n_days, f.n_tables, f.n_ip, f.n_dealer,                                        -- 续行：接续上一取列子句，续列 f.n_rounds, f.n_days, f.n_tables, f.n_ip, f.n_dealer，涉 n_ip（IP 数）
             COALESCE(se.side_entropy, 0) AS side_entropy,                                                -- 取值表达式：取用 side_entropy（投注产品结构熵），产出「side_entropy」
             f.stake_cv, f.stake_p90_p50, f.hedge_rate, f.rebate_dep,                                     -- 续行：接续上一取列子句，续列 f.stake_cv, f.stake_p90_p50, f.hedge_rate, f.rebate_dep，涉 hedge_rate（对打率）、stake_cv（注额变异系数）、rebate_dep（退水依赖度）
             f.night_share, f.roi_game, f.validbet                                                        -- 续行：接续上一取列子句，续列 f.night_share, f.roi_game, f.validbet，涉 validbet（有效投注（洗码量））、night_share（夜场占比）、roi_game（游戏回报率）
      FROM feat f                                                                                         -- 取数来源：取自本条自建的中间结果集 feat
      LEFT JOIN side_ent se ON se.member_id = f.member_id                                                 -- 左连接：取自本条自建的中间结果集 side_ent，连接键为 member_id（会员号）
      WHERE f.n_rounds >= 50                                                                              -- 过滤条件：限定 f.n_rounds不少于 50
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds AS n_rounds_xagg,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_alarm,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_alarm,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_alarm,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_alarm,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_alarm,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_alarm,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_alarm,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_alarm,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  25. K01b_seed_goldip.csv   [总包行 5637~5643 · 原 2 列 ＋六层约 60 列]
--     典型学：M04 IP 聚集　分区：recent（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`seed_src`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT DISTINCT b.bet05 AS member_id, 'gold_ip' AS seed_src                                         -- 取列：取用 bet05（会员号）、member_id（会员号），产出「seed_src」
      FROM ods_mariadb_2b.ods_a168_bet02 b                                                                -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
      JOIN ods_mariadb_2b.ods_a168_alert_ip_setting a ON TRIM(a.ip) = TRIM(b.ip)                          -- 连接：取自风控 IP 名单配置表（人工标记史料源），连接键为 TRIM(a.ip) = TRIM(b.ip)
      WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                  -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'，涉 dt（营业日）
        AND b.bet02 = '101' AND b.category = '1'                                                          -- 并列条件：限定 b.bet02等于 '101'、b.category等于 '1'，涉 bet02（游戏类别）
        AND UPPER(TRIM(b.bet38)) = 'N'                                                                    -- 并列条件：限定 UPPER(TRIM(b.bet38))等于 'N'，涉 bet38（重對）
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  26. K01b_seed_manual.csv   [总包行 5618~5625 · 原 2 列 ＋六层约 60 列]
--     典型学：M11 异常告警　分区：alarm（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`seed_src`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT DISTINCT bet05 AS member_id, 'manual_risk' AS seed_src                                       -- 取列：取用 bet05（会员号）、member_id（会员号），产出「seed_src」
      FROM ods_mariadb_2b.ods_a168_dailyreport_member                                                     -- 取数来源：取自会员日结报表（日粒度业绩与风控标记）
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定 dt不少于 '2026-03-21'、dt小于 '2026-08-07'，涉 dt（营业日）
        AND bet02 = '101'                                                                                 -- 并列条件：限定 bet02等于 '101'，涉 bet02（游戏类别）
        AND (CAST(NULLIF(TRIM(risk),'')   AS INT) = 1                                                     -- 并列条件：限定 (CAST(NULLIF(TRIM(risk),'') AS INT)等于 1
          OR CAST(NULLIF(TRIM(orders),'') AS INT) = 1)                                                    -- 择一条件：限定 CAST(NULLIF(TRIM(orders),'') AS INT)等于 1)
        AND CAST(NULLIF(TRIM(bet05),'') AS BIGINT) > 0                                                    -- 并列条件：限定 CAST(NULLIF(TRIM(bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_alarm,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_alarm,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_alarm,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_alarm,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_alarm,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_alarm,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_alarm,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_alarm,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  27. K01c_seed_dated.csv   [总包行 8330~8337 · 原 6 列 ＋六层约 60 列]
--     典型学：M04 IP 聚集　分区：recent（s.x_rec = 1）　连接：q.`seed_ip`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`seed_id`, w.`seed_ip`, w.`creator`, w.`addtime`, w.`add_date`, w.`remarks`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_ip                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_ip IS NOT NULL AND TRIM(CAST(s.x_ip AS STRING)) <> ''
    GROUP BY s.x_ip
  ),
    q AS (
      SELECT  CAST(id AS STRING)                                AS seed_id,
              TRIM(ip)                                          AS seed_ip,
              TRIM(creator)                                     AS creator,
              CAST(addtime AS STRING)                           AS addtime,
              SUBSTR(CAST(addtime AS STRING), 1, 10)            AS add_date,
              TRIM(remarks)                                     AS remarks
      FROM    ods_mariadb_2b.ods_a168_alert_ip_setting
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`seed_ip` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  28. L_label_dist.csv   [总包行 1472~1476 · 原 6 列 ＋六层约 60 列]
--     典型学：M11 异常告警　分区：alarm（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`risk`, w.`orders`, w.`n_rows`, w.`n_member`, w.`first_dt`, w.`last_dt`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT risk, orders, COUNT(*) AS n_rows,                                                            -- 取列：起始取列子句，本行先取「n_rows」
             COUNT(DISTINCT bet05) AS n_member,                                                           -- 计数表达式：统计唯一会员数（铁律分母，全窗 721,190 人），产出「n_member」
             MIN(dt) AS first_dt, MAX(dt) AS last_dt                                                      -- 取最小值表达式：取最大值，取用 dt（营业日），产出「last_dt」
      FROM ods_mariadb_2b.ods_a168_dailyreport_member                                                     -- 取数来源：取自会员日结报表（日粒度业绩与风控标记）
      GROUP BY risk, orders
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_alarm,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_alarm,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_alarm,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_alarm,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_alarm,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_alarm,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_alarm,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_alarm,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  29. P01A_crosstab.csv   [总包行 9199~9219 · 原 13 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`n_rows`, w.`bet16_null`, w.`bet16_zero`, w.`bet16_nonzero`, w.`bet41_null`, w.`bet41_zero`, w.`bet41_nonzero`, w.`equal_n`, w.`unequal_n`, w.`both_zero_n`, w.`b16zero_b41pos_n`, w.`both_pos_n`, w.`b16pos_b41zero_n`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT COUNT(*)                                                          AS n_rows,
             SUM(CASE WHEN b16 IS NULL THEN 1 ELSE 0 END)                      AS bet16_null,
             SUM(CASE WHEN b16 =  0    THEN 1 ELSE 0 END)                      AS bet16_zero,
             SUM(CASE WHEN b16 <> 0    THEN 1 ELSE 0 END)                      AS bet16_nonzero,
             SUM(CASE WHEN b41 IS NULL THEN 1 ELSE 0 END)                      AS bet41_null,
             SUM(CASE WHEN b41 =  0    THEN 1 ELSE 0 END)                      AS bet41_zero,
             SUM(CASE WHEN b41 <> 0    THEN 1 ELSE 0 END)                      AS bet41_nonzero,
             SUM(CASE WHEN b16 IS NOT NULL AND b41 IS NOT NULL AND b16 =  b41
                      THEN 1 ELSE 0 END)                                       AS equal_n,
             SUM(CASE WHEN b16 IS NOT NULL AND b41 IS NOT NULL AND b16 <> b41
                      THEN 1 ELSE 0 END)                                       AS unequal_n,
             SUM(CASE WHEN b16 = 0 AND b41 = 0 THEN 1 ELSE 0 END)              AS both_zero_n,
             SUM(CASE WHEN b16 = 0 AND b41 <> 0 THEN 1 ELSE 0 END)             AS b16zero_b41pos_n,
             SUM(CASE WHEN b16 <> 0 AND b41 <> 0 THEN 1 ELSE 0 END)            AS both_pos_n,
             SUM(CASE WHEN b16 <> 0 AND b41 = 0 THEN 1 ELSE 0 END)             AS b16pos_b41zero_n
      FROM (
        SELECT CAST(NULLIF(TRIM(bet16),'') AS DECIMAL(20,4)) AS b16,
               CAST(NULLIF(TRIM(bet41),'') AS DECIMAL(20,4)) AS b41
        FROM ods_mariadb_2b.ods_a168_bet02
        WHERE dt >= '2026-03-21' AND dt < '2026-08-07' AND bet02 = '101'
      ) t
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  30. P01B_diff_ratio.csv   [总包行 9223~9240 · 原 11 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`n`, w.`diff_min`, w.`diff_p50`, w.`diff_p90`, w.`diff_p99`, w.`diff_max`, w.`ratio_min`, w.`ratio_p01`, w.`ratio_p50`, w.`ratio_p99`, w.`ratio_max`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT COUNT(*)                                                        AS n,
             MIN(b16 - b41)                                                  AS diff_min,
             PERCENTILE_APPROX(b16 - b41, 0.50)                              AS diff_p50,
             PERCENTILE_APPROX(b16 - b41, 0.90)                              AS diff_p90,
             PERCENTILE_APPROX(b16 - b41, 0.99)                              AS diff_p99,
             MAX(b16 - b41)                                                  AS diff_max,
             MIN(b41 / NULLIF(b16, 0))                                       AS ratio_min,
             PERCENTILE_APPROX(b41 / NULLIF(b16, 0), 0.01)                   AS ratio_p01,
             PERCENTILE_APPROX(b41 / NULLIF(b16, 0), 0.50)                   AS ratio_p50,
             PERCENTILE_APPROX(b41 / NULLIF(b16, 0), 0.99)                   AS ratio_p99,
             MAX(b41 / NULLIF(b16, 0))                                       AS ratio_max
      FROM (
        SELECT CAST(NULLIF(TRIM(bet16),'') AS DECIMAL(20,4)) AS b16,
               CAST(NULLIF(TRIM(bet41),'') AS DECIMAL(20,4)) AS b41
        FROM ods_mariadb_2b.ods_a168_bet02
        WHERE dt >= '2026-03-21' AND dt < '2026-08-07' AND bet02 = '101'
      ) t
      WHERE b16 IS NOT NULL AND b41 IS NOT NULL AND b16 <> 0
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  31. P01C_split_by_dim.csv   [总包行 9246~9269 · 原 14 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`ym`, w.`bet09`, w.`commission`, w.`bet10`, w.`n`, w.`equal_n`, w.`unequal_n`, w.`b16_nonzero_n`, w.`b41_nonzero_n`, w.`avg_b16`, w.`avg_b41`, w.`avg_diff`, w.`p50_b16`, w.`p50_b41`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT DATE_TRUNC('month', dt)                                          AS ym,
             TRIM(bet09)                                                      AS bet09,
             TRIM(commission)                                                 AS commission,
             TRIM(bet10)                                                      AS bet10,
             COUNT(*)                                                         AS n,
             SUM(CASE WHEN b16 =  b41 THEN 1 ELSE 0 END)                      AS equal_n,
             SUM(CASE WHEN b16 <> b41 THEN 1 ELSE 0 END)                      AS unequal_n,
             SUM(CASE WHEN b16 <> 0 THEN 1 ELSE 0 END)                        AS b16_nonzero_n,
             SUM(CASE WHEN b41 <> 0 THEN 1 ELSE 0 END)                        AS b41_nonzero_n,
             AVG(b16)                                                         AS avg_b16,
             AVG(b41)                                                         AS avg_b41,
             AVG(b16 - b41)                                                   AS avg_diff,
             PERCENTILE_APPROX(b16, 0.50)                                     AS p50_b16,
             PERCENTILE_APPROX(b41, 0.50)                                     AS p50_b41
      FROM (
        SELECT dt, bet09, commission, bet10,
               CAST(NULLIF(TRIM(bet16),'') AS DECIMAL(20,4)) AS b16,
               CAST(NULLIF(TRIM(bet41),'') AS DECIMAL(20,4)) AS b41
        FROM ods_mariadb_2b.ods_a168_bet02
        WHERE dt >= '2026-03-21' AND dt < '2026-08-07' AND bet02 = '101'
      ) t
      WHERE b16 IS NOT NULL AND b41 IS NOT NULL
      GROUP BY DATE_TRUNC('month', dt), TRIM(bet09), TRIM(commission), TRIM(bet10)
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  32. P01D_vs_validbet.csv   [总包行 9274~9290 · 原 9 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`n`, w.`comparable_n`, w.`plausible_n`, w.`r41_p01`, w.`r41_p50`, w.`r41_p90`, w.`r41_p99`, w.`r16_p50`, w.`r16_p99`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT COUNT(*)                                                         AS n,
             SUM(CASE WHEN vb IS NOT NULL AND b41 IS NOT NULL THEN 1 ELSE 0 END)      AS comparable_n,
             SUM(CASE WHEN vb <> 0 AND b41 / vb BETWEEN 0 AND 1 THEN 1 ELSE 0 END)    AS plausible_n,
             PERCENTILE_APPROX(b41 / NULLIF(vb, 0), 0.01)                     AS r41_p01,
             PERCENTILE_APPROX(b41 / NULLIF(vb, 0), 0.50)                     AS r41_p50,
             PERCENTILE_APPROX(b41 / NULLIF(vb, 0), 0.90)                     AS r41_p90,
             PERCENTILE_APPROX(b41 / NULLIF(vb, 0), 0.99)                     AS r41_p99,
             PERCENTILE_APPROX(b16 / NULLIF(vb, 0), 0.50)                     AS r16_p50,
             PERCENTILE_APPROX(b16 / NULLIF(vb, 0), 0.99)                     AS r16_p99
      FROM (
        SELECT CAST(NULLIF(TRIM(validbet),'') AS DECIMAL(20,4)) AS vb,
               CAST(NULLIF(TRIM(bet16),'')    AS DECIMAL(20,4)) AS b16,
               CAST(NULLIF(TRIM(bet41),'')    AS DECIMAL(20,4)) AS b41
        FROM ods_mariadb_2b.ods_a168_bet02
        WHERE dt >= '2026-03-21' AND dt < '2026-08-07' AND bet02 = '101'
      ) t
      WHERE vb IS NOT NULL AND b41 IS NOT NULL AND vb <> 0
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  33. P01E_identity_test.csv   [总包行 9295~9330 · 原 8 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`hypothesis`, w.`n`, w.`exact_match_n`, w.`exact_match_rate`, w.`abs_err_p50`, w.`abs_err_p95`, w.`abs_err_p99`, w.`abs_err_max`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT 'H16: bet17 = bet14 - bet13 + bet16'                             AS hypothesis,
             COUNT(*)                                                         AS n,
             SUM(CASE WHEN e16 = 0 THEN 1 ELSE 0 END)                         AS exact_match_n,
             SUM(CASE WHEN e16 = 0 THEN 1 ELSE 0 END) * 1.0 / COUNT(*)        AS exact_match_rate,
             PERCENTILE_APPROX(ABS(e16), 0.50)                                AS abs_err_p50,
             PERCENTILE_APPROX(ABS(e16), 0.95)                                AS abs_err_p95,
             PERCENTILE_APPROX(ABS(e16), 0.99)                                AS abs_err_p99,
             MAX(ABS(e16))                                                    AS abs_err_max
      FROM (
        SELECT (CAST(NULLIF(TRIM(bet17),'') AS DECIMAL(20,4)))
             - (CAST(NULLIF(TRIM(bet14),'') AS DECIMAL(20,4))
              - CAST(NULLIF(TRIM(bet13),'') AS DECIMAL(20,4))
              + CAST(NULLIF(TRIM(bet16),'') AS DECIMAL(20,4)))                AS e16
        FROM ods_mariadb_2b.ods_a168_bet02
        WHERE dt >= '2026-03-21' AND dt < '2026-08-07' AND bet02 = '101'
      ) t
      WHERE e16 IS NOT NULL
      UNION ALL
      SELECT 'H41: bet17 = bet14 - bet13 + bet41'                             AS hypothesis,
             COUNT(*)                                                         AS n,
             SUM(CASE WHEN e41 = 0 THEN 1 ELSE 0 END)                         AS exact_match_n,
             SUM(CASE WHEN e41 = 0 THEN 1 ELSE 0 END) * 1.0 / COUNT(*)        AS exact_match_rate,
             PERCENTILE_APPROX(ABS(e41), 0.50)                                AS abs_err_p50,
             PERCENTILE_APPROX(ABS(e41), 0.95)                                AS abs_err_p95,
             PERCENTILE_APPROX(ABS(e41), 0.99)                                AS abs_err_p99,
             MAX(ABS(e41))                                                    AS abs_err_max
      FROM (
        SELECT (CAST(NULLIF(TRIM(bet17),'') AS DECIMAL(20,4)))
             - (CAST(NULLIF(TRIM(bet14),'') AS DECIMAL(20,4))
              - CAST(NULLIF(TRIM(bet13),'') AS DECIMAL(20,4))
              + CAST(NULLIF(TRIM(bet41),'') AS DECIMAL(20,4)))                AS e41
        FROM ods_mariadb_2b.ods_a168_bet02
        WHERE dt >= '2026-03-21' AND dt < '2026-08-07' AND bet02 = '101'
      ) t
      WHERE e41 IS NOT NULL
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  34. P02A_lmc_samples.csv   [总包行 9351~9363 · 原 9 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`lmc04`, w.`lmc08`, w.`lmc02`, w.`lmc03`, w.`lmc06`, w.`lmc07`, w.`lmc10`, w.`lmc05`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH r AS (
        SELECT lmc04, lmc05, lmc02, lmc03, lmc06, lmc07, lmc08, lmc10,
               ROW_NUMBER() OVER (PARTITION BY lmc04 ORDER BY lmc08 ASC,  lmc02 ASC) AS rn_old,
               ROW_NUMBER() OVER (PARTITION BY lmc04 ORDER BY lmc08 DESC, lmc02 DESC) AS rn_new
        FROM ods_mariadb_2b.ods_a168_log_mem_change
      )
      SELECT lmc04 AS 类别, '最早' AS 取样端, lmc08 AS 異動時間, lmc02 AS 实体ID, lmc03 AS 层级,
             lmc06 AS 操作者, lmc07 AS 操作者LV, lmc10 AS 操作IP, lmc05 AS 内容原文
      FROM r WHERE rn_old <= 200
      UNION ALL
      SELECT lmc04, '最新', lmc08, lmc02, lmc03, lmc06, lmc07, lmc10, lmc05
      FROM r WHERE rn_new <= 200
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  35. P02B_changestatus_patterns.csv   [总包行 9369~9378 · 原 6 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`内容前40字`, w.`n`, w.`涉实体数`, w.`最早`, w.`最晚`, w.`其中层级7数`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT SUBSTRING(TRIM(lmc05), 1, 40)                                    AS 内容前40字,
             COUNT(*)                                                         AS n,
             COUNT(DISTINCT lmc02)                                            AS 涉实体数,
             MIN(lmc08)                                                       AS 最早,
             MAX(lmc08)                                                       AS 最晚,
             SUM(CASE WHEN lmc03 = '7' THEN 1 ELSE 0 END)                     AS 其中层级7数
      FROM ods_mariadb_2b.ods_a168_log_mem_change
      WHERE lmc04 = 'changestatus'
      GROUP BY SUBSTRING(TRIM(lmc05), 1, 40)
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  36. P02C_key_check.csv   [总包行 9385~9401 · 原 8 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`层级`, w.`日志行数`, w.`去重实体数`, w.`实体ID为0之脏行`, w.`命中注单会员数`, w.`命中去重实体数`, w.`最早`, w.`最晚`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT l.lmc03                                                          AS 层级,
             COUNT(*)                                                         AS 日志行数,
             COUNT(DISTINCT l.lmc02)                                          AS 去重实体数,
             SUM(CASE WHEN TRIM(l.lmc02) = '0' THEN 1 ELSE 0 END)             AS 实体ID为0之脏行,
             SUM(CASE WHEN m.member_id IS NOT NULL THEN 1 ELSE 0 END)         AS 命中注单会员数,
             COUNT(DISTINCT CASE WHEN m.member_id IS NOT NULL
                                 THEN l.lmc02 END)                            AS 命中去重实体数,
             MIN(l.lmc08)                                                     AS 最早,
             MAX(l.lmc08)                                                     AS 最晚
      FROM ods_mariadb_2b.ods_a168_log_mem_change l
      LEFT JOIN (
        SELECT DISTINCT CAST(NULLIF(TRIM(bet05),'') AS BIGINT) AS member_id
        FROM ods_mariadb_2b.ods_a168_bet02
        WHERE dt >= '2026-03-21' AND dt < '2026-08-07' AND bet02 = '101'
      ) m ON m.member_id = CAST(NULLIF(TRIM(l.lmc02),'') AS BIGINT)
      GROUP BY l.lmc03
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  37. P02D_events_in_window.csv   [总包行 9406~9414 · 原 4 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`类别`, w.`层级`, w.`窗口内行数`, w.`涉实体数`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT lmc04                                                            AS 类别,
             lmc03                                                            AS 层级,
             COUNT(*)                                                         AS 窗口内行数,
             COUNT(DISTINCT lmc02)                                            AS 涉实体数
      FROM ods_mariadb_2b.ods_a168_log_mem_change
      WHERE CAST(NULLIF(TRIM(lmc08),'') AS DATETIME) >= '2026-03-21 00:00:00'
        AND CAST(NULLIF(TRIM(lmc08),'') AS DATETIME) <  '2026-08-07 00:00:00'
      GROUP BY lmc04, lmc03
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  38. P0C02_标签口径.csv   [总包行 7728~7761 · 原 6 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT  '百家乐(101)'                                 AS 产品口径,
              COUNT(*)                                      AS 正例观测数_会员日,
              COUNT(DISTINCT CAST(bet05 AS STRING))         AS 正例会员数_去重,
              COUNT(DISTINCT dt)                            AS 覆盖日数,
              MIN(dt)                                       AS 首个正例日,
              MAX(dt)                                       AS 末个正例日
      FROM    ods_mariadb_2b.ods_a168_dailyreport_member
      WHERE   dt >= '2026-03-21'
        AND   dt <  '2026-08-07'
        AND   CAST(risk AS STRING) = '1'
        AND   CAST(bet02 AS STRING) = '101'
      UNION ALL
      SELECT  '非百家乐',
              COUNT(*),
              COUNT(DISTINCT CAST(bet05 AS STRING)),
              COUNT(DISTINCT dt),
              MIN(dt),
              MAX(dt)
      FROM    ods_mariadb_2b.ods_a168_dailyreport_member
      WHERE   dt >= '2026-03-21'
        AND   dt <  '2026-08-07'
        AND   CAST(risk AS STRING) = '1'
        AND   CAST(bet02 AS STRING) <> '101'
      UNION ALL
      SELECT  '全产品合计',
              COUNT(*),
              COUNT(DISTINCT CAST(bet05 AS STRING)),
              COUNT(DISTINCT dt),
              MIN(dt),
              MAX(dt)
      FROM    ods_mariadb_2b.ods_a168_dailyreport_member
      WHERE   dt >= '2026-03-21'
        AND   dt <  '2026-08-07'
        AND   CAST(risk AS STRING) = '1'
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  39. P0C03_标记次数分布.csv   [总包行 7767~7781 · 原 3 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`标记次数`, w.`会员数`, w.`累计会员数`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT  标记次数,
              COUNT(*)                                      AS 会员数,
              SUM(COUNT(*)) OVER (ORDER BY 标记次数)         AS 累计会员数
      FROM (
          SELECT  CAST(bet05 AS STRING)                     AS mem,
                  COUNT(DISTINCT dt)                        AS 标记次数
          FROM    ods_mariadb_2b.ods_a168_dailyreport_member
          WHERE   dt >= '2026-03-21'
            AND   dt <  '2026-08-07'
            AND   CAST(risk AS STRING) = '1'
            AND   CAST(bet02 AS STRING) = '101'
          GROUP BY 1
      ) t
      GROUP BY 标记次数
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  40. P0C04_同日泄漏检验.csv   [总包行 7793~7828 · 原 7 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`mem`, w.`mark_day`, w.`标记日_注单数`, w.`标记日_洗码量`, w.`前30日均_注单数`, w.`前30日均_洗码量`, w.`前30日活跃日数`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH d0 AS (
          SELECT  CAST(bet05 AS STRING)                     AS mem,
                  MIN(dt)                                   AS mark_day
          FROM    ods_mariadb_2b.ods_a168_dailyreport_member
          WHERE   dt >= '2026-03-21'
            AND   dt <  '2026-08-07'
            AND   CAST(risk AS STRING) = '1'
            AND   CAST(bet02 AS STRING) = '101'
          GROUP BY 1
      ),
      act AS (
          SELECT  CAST(b.bet05 AS STRING)                   AS mem,
                  b.dt                                      AS d,
                  COUNT(*)                                  AS 注单数,
                  SUM(CAST(b.validbet AS DOUBLE))           AS 洗码量
          FROM    ods_mariadb_2b.ods_a168_bet02 b
          JOIN    d0 ON d0.mem = CAST(b.bet05 AS STRING)
          WHERE   b.dt >= '2026-03-21'
            AND   b.dt <  '2026-08-07'
            AND   CAST(b.bet02 AS STRING) = '101'
          GROUP BY 1, 2
      )
      SELECT  d0.mem,
              d0.mark_day,
              MAX(CASE WHEN act.d =  d0.mark_day THEN act.注单数 ELSE 0 END) AS 标记日_注单数,
              MAX(CASE WHEN act.d =  d0.mark_day THEN act.洗码量 ELSE 0 END) AS 标记日_洗码量,
              AVG(CASE WHEN act.d <  d0.mark_day THEN act.注单数 END)        AS 前30日均_注单数,
              AVG(CASE WHEN act.d <  d0.mark_day THEN act.洗码量 END)        AS 前30日均_洗码量,
              COUNT(DISTINCT CASE WHEN act.d < d0.mark_day THEN act.d END)   AS 前30日活跃日数
      FROM        d0
      LEFT JOIN   act
             ON   act.mem = d0.mem
            AND   act.d  >= DATE_SUB(d0.mark_day, INTERVAL 30 DAY)
            AND   act.d  <= d0.mark_day
      GROUP BY d0.mem, d0.mark_day
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  41. P0C05_标记时点定位.csv   [总包行 7833~7864 · 原 8 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`mem`, w.`mark_day`, w.`首投日`, w.`末投日`, w.`活跃日数`, w.`标记距首投_日`, w.`末投距标记_日`, w.`时点性质`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT  d0.mem,
              d0.mark_day,
              life.首投日,
              life.末投日,
              life.活跃日数,
              DATEDIFF(d0.mark_day, life.首投日)             AS 标记距首投_日,
              DATEDIFF(life.末投日, d0.mark_day)             AS 末投距标记_日,
              CASE WHEN DATEDIFF(life.末投日, d0.mark_day) <= 0  THEN '标记后再无投注'
                   WHEN DATEDIFF(life.末投日, d0.mark_day) <= 14 THEN '标记后14日内停投'
                   ELSE '标记后仍持续投注' END               AS 时点性质
      FROM (
          SELECT  CAST(bet05 AS STRING)                     AS mem,
                  MIN(dt)                                   AS mark_day
          FROM    ods_mariadb_2b.ods_a168_dailyreport_member
          WHERE   dt >= '2026-03-21'
            AND   dt <  '2026-08-07'
            AND   CAST(risk AS STRING) = '1'
            AND   CAST(bet02 AS STRING) = '101'
          GROUP BY 1
      ) d0
      LEFT JOIN (
          SELECT  CAST(bet05 AS STRING)                     AS mem,
                  MIN(dt)                                   AS 首投日,
                  MAX(dt)                                   AS 末投日,
                  COUNT(DISTINCT dt)                        AS 活跃日数
          FROM    ods_mariadb_2b.ods_a168_bet02
          WHERE   dt >= '2026-03-21'
            AND   dt <  '2026-08-07'
            AND   CAST(bet02 AS STRING) = '101'
          GROUP BY 1
      ) life ON life.mem = d0.mem
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  42. P0C06_LabelB骨架.csv   [总包行 7871~7904 · 原 8 列 ＋六层约 60 列]
--     典型学：M02 对冲对打　分区：hedge（h.n_side >= 2）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`mem`, w.`anchor_day`, w.`特征窗起`, w.`特征窗止`, w.`结果窗起`, w.`结果窗止`, w.`label_b_未来14日被标记`, w.`label_a_当日被标记`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_hg AS (SELECT x_round, COUNT(DISTINCT x_product) AS n_side FROM x_bs GROUP BY x_round),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN h.n_side >= 2 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN h.n_side >= 2 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN h.n_side >= 2 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (h.n_side >= 2) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (h.n_side >= 2) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (h.n_side >= 2) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    JOIN x_hg h ON h.x_round = s.x_round
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH mk AS (
          SELECT  DISTINCT CAST(bet05 AS STRING)            AS mem,
                  dt                                        AS mark_day
          FROM    ods_mariadb_2b.ods_a168_dailyreport_member
          WHERE   dt >= '2026-03-21'
            AND   dt <  '2026-08-07'
            AND   CAST(risk AS STRING) = '1'
            AND   CAST(bet02 AS STRING) = '101'
      ),
      anchor AS (
          SELECT  CAST(b.bet05 AS STRING)                   AS mem,
                  b.dt                                      AS anchor_day
          FROM    ods_mariadb_2b.ods_a168_bet02 b
          JOIN    (SELECT DISTINCT mem FROM mk) pos ON pos.mem = CAST(b.bet05 AS STRING)
          WHERE   b.dt >= '2026-04-20'
            AND   b.dt <  '2026-07-24'
            AND   CAST(b.bet02 AS STRING) = '101'
          GROUP BY 1, 2
      )
      SELECT  a.mem,
              a.anchor_day,
              DATE_SUB(a.anchor_day, INTERVAL 30 DAY)       AS 特征窗起,
              a.anchor_day                                  AS 特征窗止,
              DATE_ADD(a.anchor_day, INTERVAL 1  DAY)       AS 结果窗起,
              DATE_ADD(a.anchor_day, INTERVAL 14 DAY)       AS 结果窗止,
              MAX(CASE WHEN mk.mark_day >  a.anchor_day
                        AND mk.mark_day <= DATE_ADD(a.anchor_day, INTERVAL 14 DAY)
                       THEN 1 ELSE 0 END)                   AS label_b_未来14日被标记,
              MAX(CASE WHEN mk.mark_day =  a.anchor_day
                       THEN 1 ELSE 0 END)                   AS label_a_当日被标记
      FROM        anchor a
      LEFT JOIN   mk ON mk.mem = a.mem
      GROUP BY a.mem, a.anchor_day
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hedge,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hedge,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hedge,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hedge,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hedge,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hedge,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hedge,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hedge,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  43. P0C09_安慰剂检验.csv   [总包行 7995~8032 · 原 7 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`mem`, w.`参照日`, w.`参照日_注单数`, w.`参照日_洗码量`, w.`前30日均_注单数`, w.`前30日均_洗码量`, w.`前30日活跃日数`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH mk AS (
          SELECT  DISTINCT CAST(bet05 AS STRING)            AS mem,
                  dt                                        AS mark_day
          FROM    ods_mariadb_2b.ods_a168_dailyreport_member
          WHERE   dt >= '2026-03-21'
            AND   dt <  '2026-08-07'
            AND   CAST(risk AS STRING) = '1'
            AND   CAST(bet02 AS STRING) = '101'
      ),
      act AS (
          SELECT  CAST(b.bet05 AS STRING)                   AS mem,
                  b.dt                                      AS d,
                  COUNT(*)                                  AS 注单数,
                  SUM(CAST(b.validbet AS DOUBLE))           AS 洗码量
          FROM    ods_mariadb_2b.ods_a168_bet02 b
          JOIN    (SELECT DISTINCT mem FROM mk) pos ON pos.mem = CAST(b.bet05 AS STRING)
          WHERE   b.dt >= '2026-03-21'
            AND   b.dt <  '2026-08-07'
            AND   CAST(b.bet02 AS STRING) = '101'
          GROUP BY 1, 2
      )
      SELECT  a.mem,
              a.d                                           AS 参照日,
              a.注单数                                       AS 参照日_注单数,
              a.洗码量                                       AS 参照日_洗码量,
              AVG(p.注单数)                                  AS 前30日均_注单数,
              AVG(p.洗码量)                                  AS 前30日均_洗码量,
              COUNT(DISTINCT p.d)                           AS 前30日活跃日数
      FROM        act a
      LEFT JOIN   act p
             ON   p.mem = a.mem
            AND   p.d  >= DATE_SUB(a.d, INTERVAL 30 DAY)
            AND   p.d  <  a.d
      LEFT JOIN   mk ON mk.mem = a.mem AND mk.mark_day = a.d
      WHERE   mk.mem IS NULL
      GROUP BY a.mem, a.d, a.注单数, a.洗码量
      HAVING  COUNT(DISTINCT p.d) > 0
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  44. P11_阿泽尾段投注总帐.csv   [总包行 9519~9655 · 原 21 列 ＋六层约 60 列]
--     典型学：M03 尾段投注　分区：late（s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`scope_def`, w.`n_orders`, w.`n_orders_pct`, w.`n_members`, w.`n_members_pct`, w.`n_lv1`, w.`n_lv2`, w.`n_lv3`, w.`n_lv4`, w.`n_lv5`, w.`stake_sum`, w.`stake_pct`, w.`validbet_sum`, w.`validbet_pct`, w.`ggr_sum`, w.`ggr_pct`, w.`rebate16_sum`, w.`rebate16_pct`, w.`rebate41_sum`, w.`ngr_by16`, w.`ngr_by16_pct`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_sp AS (SELECT x_table, x_shoe, MAX(x_rno) AS max_rno FROM x_bs GROUP BY x_table, x_shoe),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    JOIN x_sp p ON p.x_table = s.x_table AND p.x_shoe = s.x_shoe
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH test_agents AS (
          SELECT age001 AS agent_id
          FROM ods_mariadb_2b.ods_a168_agent
          WHERE age022 = '1'
      ),
      ranked AS (
          SELECT b.bet01, b.bet03, b.bet04, b.bet05, b.bet08, b.bet11, b.bet13, b.bet14,
                 b.bet16, b.bet41, b.validbet,
                 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22, b.bet38, b.category,
                 ROW_NUMBER() OVER (
                   PARTITION BY b.bet01
                   ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn
          FROM ods_mariadb_2b.ods_a168_bet02 b
          WHERE b.bet02 = '101'
            AND b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      ),
      gated AS (
          SELECT r.bet03, r.bet04, r.bet05, r.bet11, r.bet13, r.bet14,
                 r.bet16, r.bet41, r.bet08, r.validbet,
                 r.bet18 AS a1, r.bet19 AS a2, r.bet20 AS a3, r.bet21 AS a4, r.bet22 AS a5
          FROM ranked r
          LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18
          LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19
          LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20
          LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21
          LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22
          WHERE r.rn = 1
            AND r.category = '1'
            AND UPPER(TRIM(r.bet38)) = 'N'
            AND COALESCE(t1.agent_id, t2.agent_id, t3.agent_id,
                         t4.agent_id, t5.agent_id) IS NULL
      ),
      aze_rows AS (
          SELECT CAST(bet05 AS BIGINT) AS pid,
                 CAST(bet04 AS INT)    AS rno,
                 a1, a2, a3, a4, a5,
                 CAST(bet13 AS DECIMAL(20,4))
                   / NULLIF(CAST(bet11 AS DECIMAL(20,4)), 0)                     AS stake,
                 CAST(validbet AS DECIMAL(20,4))
                   / NULLIF(CAST(bet11 AS DECIMAL(20,4)), 0)                     AS vb,
                 (CAST(bet14 AS DECIMAL(20,4)) - CAST(bet13 AS DECIMAL(20,4)))
                   / NULLIF(CAST(bet11 AS DECIMAL(20,4)), 0)                     AS pnl,
                 COALESCE(CAST(bet16 AS DECIMAL(20,4)), 0)
                   / NULLIF(CAST(bet11 AS DECIMAL(20,4)), 0)                     AS reb16,
                 COALESCE(CAST(bet41 AS DECIMAL(20,4)), 0)
                   / NULLIF(CAST(bet11 AS DECIMAL(20,4)), 0)                     AS reb41
          FROM gated
          WHERE bet03 IS NOT NULL
            AND bet04 IS NOT NULL
            AND bet05 IS NOT NULL
      ),
      aze_members AS (
          SELECT pid
          FROM aze_rows
          GROUP BY pid
          HAVING COUNT(*) >= 30
             AND SUM(CASE WHEN rno >= 40 THEN 1 ELSE 0 END) * 1.0 / COUNT(*) >= 0.60
      ),
      aze AS (
          SELECT '2_阿泽域（丙版五闸＋会员门槛）' AS scope_def,
                 COUNT(*)                                   AS n_orders,
                 COUNT(DISTINCT r.pid)                      AS n_members,
                 COUNT(DISTINCT r.a1)                       AS n_lv1,
                 COUNT(DISTINCT r.a2)                       AS n_lv2,
                 COUNT(DISTINCT r.a3)                       AS n_lv3,
                 COUNT(DISTINCT r.a4)                       AS n_lv4,
                 COUNT(DISTINCT r.a5)                       AS n_lv5,
                 SUM(r.stake)                               AS stake_sum,
                 SUM(r.vb)                                  AS validbet_sum,
                 -SUM(r.pnl)                                AS ggr_sum,
                 SUM(r.reb16)                               AS rebate16_sum,
                 SUM(r.reb41)                               AS rebate41_sum
          FROM aze_rows r
          JOIN aze_members m ON m.pid = r.pid
      ),
      clean_rows AS (
          SELECT CAST(bet05 AS BIGINT) AS pid,
                 a1, a2, a3, a4, a5,
                 CAST(bet13 AS DECIMAL(20,4))
                   / CAST(bet11 AS DECIMAL(20,8))                                AS stake,
                 CAST(validbet AS DECIMAL(20,4))
                   / CAST(bet11 AS DECIMAL(20,8))                                AS vb,
                 (CAST(bet14 AS DECIMAL(20,4)) - CAST(bet13 AS DECIMAL(20,4)))
                   / CAST(bet11 AS DECIMAL(20,8))                                AS pnl,
                 COALESCE(CAST(bet16 AS DECIMAL(20,4)), 0)
                   / CAST(bet11 AS DECIMAL(20,8))                                AS reb16,
                 COALESCE(CAST(bet41 AS DECIMAL(20,4)), 0)
                   / CAST(bet11 AS DECIMAL(20,8))                                AS reb41
          FROM gated
          WHERE CAST(NULLIF(TRIM(bet05), '') AS BIGINT) > 0
            AND CAST(NULLIF(TRIM(bet11), '') AS DECIMAL(20,8)) > 0
            AND CAST(NULLIF(TRIM(bet04), '') AS INT) > 0
            AND NULLIF(TRIM(bet08), '') IS NOT NULL
      ),
      clean AS (
          SELECT '1_全量洁净（七闸）' AS scope_def,
                 COUNT(*)                                   AS n_orders,
                 COUNT(DISTINCT pid)                        AS n_members,
                 COUNT(DISTINCT a1)                         AS n_lv1,
                 COUNT(DISTINCT a2)                         AS n_lv2,
                 COUNT(DISTINCT a3)                         AS n_lv3,
                 COUNT(DISTINCT a4)                         AS n_lv4,
                 COUNT(DISTINCT a5)                         AS n_lv5,
                 SUM(stake)                                 AS stake_sum,
                 SUM(vb)                                    AS validbet_sum,
                 -SUM(pnl)                                  AS ggr_sum,
                 SUM(reb16)                                 AS rebate16_sum,
                 SUM(reb41)                                 AS rebate41_sum
          FROM clean_rows
      ),
      both AS (
          SELECT * FROM clean
          UNION ALL
          SELECT * FROM aze
      )
      SELECT
          b.scope_def,
          b.n_orders,
          ROUND(b.n_orders   * 100.0 / c.n_orders,   4)                  AS n_orders_pct,
          b.n_members,
          ROUND(b.n_members  * 100.0 / c.n_members,  4)                  AS n_members_pct,
          b.n_lv1, b.n_lv2, b.n_lv3, b.n_lv4, b.n_lv5,
          ROUND(b.stake_sum, 2)                                          AS stake_sum,
          ROUND(b.stake_sum    * 100.0 / c.stake_sum, 4)                 AS stake_pct,
          ROUND(b.validbet_sum, 2)                                       AS validbet_sum,
          ROUND(b.validbet_sum * 100.0 / c.validbet_sum, 4)              AS validbet_pct,
          ROUND(b.ggr_sum, 2)                                            AS ggr_sum,
          ROUND(b.ggr_sum      * 100.0 / c.ggr_sum, 4)                   AS ggr_pct,
          ROUND(b.rebate16_sum, 2)                                       AS rebate16_sum,
          ROUND(b.rebate16_sum * 100.0 / NULLIF(c.rebate16_sum, 0), 4)   AS rebate16_pct,
          ROUND(b.rebate41_sum, 2)                                       AS rebate41_sum,
          ROUND(b.ggr_sum - b.rebate16_sum, 2)                           AS ngr_by16,
          ROUND((b.ggr_sum - b.rebate16_sum) * 100.0
                / NULLIF(c.ggr_sum - c.rebate16_sum, 0), 4)              AS ngr_by16_pct
      FROM both b
      CROSS JOIN clean c
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_late,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_late,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_late,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_late,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_late,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_late,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_late,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_late,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  45. P11_阿泽尾段投注模型会员名单.csv   [总包行 9443~9500 · 原 9 列 ＋六层约 60 列]
--     典型学：M03 尾段投注　分区：late（s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80）　连接：q.`player_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi_xagg` DESC, w.`player_id`, w.`total_orders`, w.`late_orders`, w.`late_ratio`, w.`win_orders`, w.`win_rate`, w.`valid_bet`, w.`profit`, w.`roi`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_sp AS (SELECT x_table, x_shoe, MAX(x_rno) AS max_rno FROM x_bs GROUP BY x_table, x_shoe),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    JOIN x_sp p ON p.x_table = s.x_table AND p.x_shoe = s.x_shoe
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH test_agents AS (
          SELECT age001 AS agent_id
          FROM ods_mariadb_2b.ods_a168_agent
          WHERE age022 = '1'
      ),
      ranked AS (
          SELECT b.bet01, b.bet03, b.bet04, b.bet05, b.bet11, b.bet13, b.bet14,
                 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,
                 b.bet38, b.category, b.validbet,
                 ROW_NUMBER() OVER (
                   PARTITION BY b.bet01
                   ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn
          FROM ods_mariadb_2b.ods_a168_bet02 b
          WHERE b.bet02 = '101'
            AND b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      ),
      gated AS (
          SELECT r.bet03, r.bet04, r.bet05, r.bet11, r.bet13, r.bet14, r.validbet
          FROM ranked r
          LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18
          LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19
          LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20
          LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21
          LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22
          WHERE r.rn = 1
            AND r.category = '1'
            AND UPPER(TRIM(r.bet38)) = 'N'
            AND COALESCE(t1.agent_id, t2.agent_id, t3.agent_id,
                         t4.agent_id, t5.agent_id) IS NULL
      ),
      player_stat AS (
          SELECT
              CAST(bet05 AS BIGINT) AS player_id,
              COUNT(*) AS total_orders,
              SUM(CASE WHEN CAST(bet04 AS INT) >= 40 THEN 1 ELSE 0 END) AS late_orders,
              SUM(CASE WHEN CAST(bet14 AS DECIMAL(20,4))
                          > CAST(bet13 AS DECIMAL(20,4)) THEN 1 ELSE 0 END) AS win_orders,
              SUM((CAST(bet14 AS DECIMAL(20,4)) - CAST(bet13 AS DECIMAL(20,4)))
                  / NULLIF(CAST(bet11 AS DECIMAL(20,4)), 0)) AS profit,
              SUM(CAST(validbet AS DECIMAL(20,4))
                  / NULLIF(CAST(bet11 AS DECIMAL(20,4)), 0)) AS valid_bet
          FROM gated
          WHERE bet03 IS NOT NULL
            AND bet04 IS NOT NULL
            AND bet05 IS NOT NULL
          GROUP BY CAST(bet05 AS BIGINT)
      )
      SELECT player_id, total_orders, late_orders,
             ROUND(late_orders * 1.0 / total_orders, 4) AS late_ratio,
             win_orders,
             ROUND(win_orders * 1.0 / total_orders, 4) AS win_rate,
             ROUND(valid_bet, 2) AS valid_bet,
             ROUND(profit, 2)    AS profit,
             ROUND(profit / NULLIF(valid_bet, 0), 4) AS roi
      FROM player_stat
      WHERE total_orders >= 30
        AND late_orders * 1.0 / total_orders >= 0.60
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet_xagg,
         ROUND(-e.net,4) AS profit_xagg, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi_xagg,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_late,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_late,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_late,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_late,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_late,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_late,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_late,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_late,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`player_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  46. P11_雷欧尾段投注总帐.csv   [总包行 9667~9769 · 原 21 列 ＋六层约 60 列]
--     典型学：M03 尾段投注　分区：late（s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`scope_def`, w.`n_orders`, w.`n_orders_pct`, w.`n_members`, w.`n_members_pct`, w.`n_lv1`, w.`n_lv2`, w.`n_lv3`, w.`n_lv4`, w.`n_lv5`, w.`stake_sum`, w.`stake_pct`, w.`validbet_sum`, w.`validbet_pct`, w.`ggr_sum`, w.`ggr_pct`, w.`rebate16_sum`, w.`rebate16_pct`, w.`rebate41_sum`, w.`ngr_by16`, w.`ngr_by16_pct`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_sp AS (SELECT x_table, x_shoe, MAX(x_rno) AS max_rno FROM x_bs GROUP BY x_table, x_shoe),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    JOIN x_sp p ON p.x_table = s.x_table AND p.x_shoe = s.x_shoe
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH test_agents AS (
        SELECT age001 AS agent_id
        FROM ods_mariadb_2b.ods_a168_agent
        WHERE age022 = '1'
      ),
      ranked AS (
        SELECT b.bet01, b.bet03, b.bet04, b.bet05, b.bet11, b.bet13, b.bet14,
               b.bet16, b.bet41, b.validbet,
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22, b.bet38, b.bet39, b.category,
               ROW_NUMBER() OVER (
                 PARTITION BY b.bet01
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn
        FROM ods_mariadb_2b.ods_a168_bet02 b
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
          AND b.bet02 = '101'
      ),
      base AS (
        SELECT CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)        AS member_id,
               r.bet18 AS a1, r.bet19 AS a2, r.bet20 AS a3,
               r.bet21 AS a4, r.bet22 AS a5,
               CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4))
                 / CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))            AS stake,
               CAST(NULLIF(TRIM(r.validbet),'') AS DECIMAL(20,4))
                 / CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))            AS vb,
               (CAST(NULLIF(TRIM(r.bet14),'') AS DECIMAL(20,4))
                - CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4)))
                 / CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))            AS game_pnl,
               COALESCE(CAST(NULLIF(TRIM(r.bet16),'') AS DECIMAL(20,4)), 0)
                 / CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))            AS reb16,
               COALESCE(CAST(NULLIF(TRIM(r.bet41),'') AS DECIMAL(20,4)), 0)
                 / CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))            AS reb41
        FROM ranked r
        LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18
        LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19
        LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20
        LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21
        LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22
        WHERE r.rn = 1
          AND r.category = '1'
          AND UPPER(TRIM(r.bet38)) = 'N'
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0
          AND CAST(NULLIF(TRIM(r.bet04),'') AS INT) > 0
          AND COALESCE(t1.agent_id,t2.agent_id,t3.agent_id,t4.agent_id,t5.agent_id) IS NULL
      ),
      ryo_members AS (
        SELECT member_id
        FROM base
        GROUP BY member_id
        HAVING COUNT(*) >= 100
      ),
      ryo AS (
        SELECT '2_雷欧域（§R01 原准入 ∧ n>=100）' AS scope_def,
               COUNT(*)                     AS n_orders,
               COUNT(DISTINCT b.member_id)  AS n_members,
               COUNT(DISTINCT b.a1) AS n_lv1, COUNT(DISTINCT b.a2) AS n_lv2,
               COUNT(DISTINCT b.a3) AS n_lv3, COUNT(DISTINCT b.a4) AS n_lv4,
               COUNT(DISTINCT b.a5) AS n_lv5,
               SUM(b.stake) AS stake_sum, SUM(b.vb) AS validbet_sum,
               -SUM(b.game_pnl) AS ggr_sum,
               SUM(b.reb16) AS rebate16_sum, SUM(b.reb41) AS rebate41_sum
        FROM base b
        JOIN ryo_members m ON m.member_id = b.member_id
      ),
      clean AS (
        SELECT '1_全量洁净（§R01 六闸）' AS scope_def,
               COUNT(*)                   AS n_orders,
               COUNT(DISTINCT member_id)  AS n_members,
               COUNT(DISTINCT a1) AS n_lv1, COUNT(DISTINCT a2) AS n_lv2,
               COUNT(DISTINCT a3) AS n_lv3, COUNT(DISTINCT a4) AS n_lv4,
               COUNT(DISTINCT a5) AS n_lv5,
               SUM(stake) AS stake_sum, SUM(vb) AS validbet_sum,
               -SUM(game_pnl) AS ggr_sum,
               SUM(reb16) AS rebate16_sum, SUM(reb41) AS rebate41_sum
        FROM base
      ),
      both AS (
        SELECT * FROM clean
        UNION ALL
        SELECT * FROM ryo
      )
      SELECT
        b.scope_def,
        b.n_orders,
        ROUND(b.n_orders     * 100.0 / c.n_orders,     4)                AS n_orders_pct,
        b.n_members,
        ROUND(b.n_members    * 100.0 / c.n_members,    4)                AS n_members_pct,
        b.n_lv1, b.n_lv2, b.n_lv3, b.n_lv4, b.n_lv5,
        ROUND(b.stake_sum, 2)                                            AS stake_sum,
        ROUND(b.stake_sum    * 100.0 / c.stake_sum,    4)                AS stake_pct,
        ROUND(b.validbet_sum, 2)                                         AS validbet_sum,
        ROUND(b.validbet_sum * 100.0 / c.validbet_sum, 4)                AS validbet_pct,
        ROUND(b.ggr_sum, 2)                                              AS ggr_sum,
        ROUND(b.ggr_sum      * 100.0 / c.ggr_sum,      4)                AS ggr_pct,
        ROUND(b.rebate16_sum, 2)                                         AS rebate16_sum,
        ROUND(b.rebate16_sum * 100.0 / NULLIF(c.rebate16_sum, 0), 4)     AS rebate16_pct,
        ROUND(b.rebate41_sum, 2)                                         AS rebate41_sum,
        ROUND(b.ggr_sum - b.rebate16_sum, 2)                             AS ngr_by16,
        ROUND((b.ggr_sum - b.rebate16_sum) * 100.0
              / NULLIF(c.ggr_sum - c.rebate16_sum, 0), 4)                AS ngr_by16_pct
      FROM both b
      CROSS JOIN clean c
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_late,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_late,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_late,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_late,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_late,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_late,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_late,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_late,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  47. P12a_platform_daily.csv   [总包行 9808~9873 · 原 9 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：q.`dt`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`dt`, w.`n_orders`, w.`n_members`, w.`stake_sum`, w.`validbet_sum`, w.`ggr_sum`, w.`late_stake_share`, w.`stake_ramp_day`, w.`hold_pct`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_date                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_date IS NOT NULL AND TRIM(CAST(s.x_date AS STRING)) <> ''
    GROUP BY s.x_date
  ),
    q AS (
      WITH test_agents AS (                                                                               -- 公共表表达式：开启中间结果集 test_agents——公司测试线代理（§P12a）
          SELECT age001 AS agent_id                                                                       -- 取值表达式：取用 age001（代理号），产出「agent_id」
          FROM ods_mariadb_2b.ods_a168_agent                                                              -- 取数来源：取自代理主档（五级代理线归属来源）
          WHERE age022 = '1'                                                                              -- 过滤条件：限定 age022 等于 '1'——测试线旗标（既证 214 条）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§P12a）
      ranked AS (                                                                                         -- 公共表表达式：开启中间结果集 ranked——同注单号取最新版本（§P12a）
          SELECT b.bet01, b.bet03, b.bet04, b.bet05, b.bet11, b.bet13, b.bet14,                           -- 取列：起始取列子句，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
                 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                             -- 续行：接续取列，涉 bet18~22（五级代理线，缺一即孤儿率 78.17%）
                 b.bet38, b.category, b.validbet, b.dt,                                                   -- 续行：接续取列，涉 bet38（重對）、validbet（洗码量）、dt（营业日）
                 ROW_NUMBER() OVER (                                                                      -- 行号窗口表达式：以行号窗口去重，免出重复行
                   PARTITION BY b.bet01                                                                   -- 窗口分区：按 b.bet01（注单号）分组开窗
                   ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                         -- 排序：三级排序取最新版本；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
          FROM ods_mariadb_2b.ods_a168_bet02 b                                                            -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
          WHERE b.bet02 = '101'                                                                           -- 过滤条件：限定百家乐
            AND b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                              -- 并列条件：限定全局窗，右端开区间（2026-08-07 不含当日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§P12a）
      gated AS (                                                                                          -- 公共表表达式：开启中间结果集 gated——七闸清洗后洁净底集（§P12a）
          SELECT r.dt, r.bet03, r.bet04, r.bet05, r.bet11, r.bet13, r.bet14, r.validbet                   -- 取列：透传日度聚合所需八列
          FROM ranked r                                                                                   -- 取数来源：取自本条自建的中间结果集 ranked
          LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18                                               -- 左联结：一级代理线对测试名单
          LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19                                               -- 左联结：二级代理线对测试名单
          LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20                                               -- 左联结：三级代理线对测试名单
          LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21                                               -- 左联结：四级代理线对测试名单
          LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22                                               -- 左联结：五级代理线对测试名单（五级全联，单级孤儿率 78.17% 之教训）
          WHERE r.rn = 1                                                                                  -- 过滤条件：只留每注单最新版本
            AND r.category = '1'                                                                          -- 并列条件：一般单准入
            AND UPPER(TRIM(r.bet38)) = 'N'                                                                -- 并列条件：排重对单
            AND COALESCE(t1.agent_id, t2.agent_id, t3.agent_id,                                           -- 并列条件：五级任一命中测试线即剔——
                         t4.agent_id, t5.agent_id) IS NULL                                                -- 续行：COALESCE 五级归并判空（§R03 家族缺此闸之勘误在案）
            AND r.bet03 IS NOT NULL AND r.bet04 IS NOT NULL AND r.bet05 IS NOT NULL                       -- 并列条件：三键非空（靴号/局号/会员号）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§P12a）
      shoe_len AS (          /* ★★ 命名与粒度双注（2026-08-23 立；G-5 已据实测纠错）★★ 一、本 CTE 名为 shoe_len（靴长）系历史沿用，所算 max_round = MAX(round_no) 为『最大局号』非『局数』，下游切勿以 COUNT 重实现。二、本处 GROUP BY 只含 bet03，与 §R01／§B01／§B01-D／§P11b 之 (shoe_id, table_id) 写法不同；然 §R01-0b 全域实测：pct_cross_table = 0、max_tables_per_shoe = 1、两粒度靴数同为 123,357（delta = 0）、avg max_round 同为 49.7806，§R01-0d 逐行证据零行——故二者在本核验窗口内逐条一致，本轮无跨桌合并证据。此系本窗口本口径之 OBSERVED 事实，不外推为 shoe_id 永久全局唯一之保证，换窗口须重验。三、切勿改用 GROUP BY dt, bet03：该写法会切断跨零点续接之同一物理靴（§R01-0c 实测 3,092 条、pct_contiguous = 100%）。 */
          SELECT bet03, MAX(CAST(bet04 AS INT)) AS max_round                                              -- 计数表达式：每靴最大局号为观测靴长（真靴长权威表 P0 未解前之既定代理）
          FROM gated                                                                                      -- 取数来源：取自本条自建的中间结果集 gated
          GROUP BY bet03                                                                                  -- 分组：按靴号汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§P12a）
      day_flag AS (                                                                                       -- 公共表表达式：开启中间结果集 day_flag——逐单折原币并打晚段旗（§P12a）
          SELECT g.dt,                                                                                    -- 取列：透传营业日
                 CAST(g.bet05 AS BIGINT) AS uid,                                                          -- 取值表达式：会员号铸型 BIGINT（varchar 全库之铁律）
                 (CAST(g.bet14 AS DECIMAL(20,4)) - CAST(g.bet13 AS DECIMAL(20,4)))                        -- 取值表达式：派彩减本金为会员净盈亏——
                   / NULLIF(CAST(g.bet11 AS DECIMAL(20,4)), 0) AS pnl,                                    -- 续行：除以 bet11（汇率）归原币，NULLIF 防除零，产出「pnl」
                 CAST(g.bet13 AS DECIMAL(20,4))                                                           -- 取值表达式：bet13（下注金额）——
                   / NULLIF(CAST(g.bet11 AS DECIMAL(20,4)), 0) AS stake,                                  -- 续行：归原币，产出「stake」
                 CAST(g.validbet AS DECIMAL(20,4))                                                        -- 取值表达式：validbet（洗码量）——
                   / NULLIF(CAST(g.bet11 AS DECIMAL(20,4)), 0) AS vb,                                     -- 续行：归原币，产出「vb」
                 CASE WHEN CAST(g.bet04 AS INT) * 1.0                                                     -- 条件表达式：局号除观测靴长——
                           / NULLIF(s.max_round, 0) >= 0.80                                               -- 续行：相对靴位不小于 0.80 即晚段（与 §R01 口径锁全同）
                      THEN 1 ELSE 0 END AS is_late                                                        -- 续行：产出「is_late」晚段旗
          FROM gated g                                                                                    -- 取数来源：取自本条自建的中间结果集 gated
          JOIN shoe_len s ON s.bet03 = g.bet03                                                            -- 内联结：逐单挂靴长
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§P12a）
      SELECT dt,                                                                                          -- 取列：营业日为主键（139 行预期）
             COUNT(*)                                        AS n_orders,                                 -- 计数表达式：当日注单数，产出「n_orders」
             COUNT(DISTINCT uid)                             AS n_members,                                -- 计数表达式：当日活跃会员数，产出「n_members」
             ROUND(SUM(stake), 4)                            AS stake_sum,                                -- 聚合表达式：当日本金合计，产出「stake_sum」
             ROUND(SUM(vb), 4)                               AS validbet_sum,                             -- 聚合表达式：当日洗码合计，产出「validbet_sum」
             ROUND(-SUM(pnl), 4)                             AS ggr_sum,                                  -- 聚合表达式：会员净盈亏取负即平台 GGR，产出「ggr_sum」
             ROUND(SUM(CASE WHEN is_late = 1 THEN stake ELSE 0 END)                                       -- 聚合表达式：晚段本金——
                   / NULLIF(SUM(stake), 0), 6)               AS late_stake_share,                         -- 续行：除全日本金，产出「late_stake_share」（结构维）
             ROUND(AVG(CASE WHEN is_late = 1 THEN stake END)                                              -- 聚合表达式：晚段均注——
                   / NULLIF(AVG(CASE WHEN is_late = 0 THEN stake END), 0), 6)                             -- 续行：除早段均注，NULLIF 防除零——
                                                             AS stake_ramp_day,                          -- 续行：产出「stake_ramp_day」（行业监控「靴末模式突变」之日度量化）
             ROUND(-SUM(pnl) / NULLIF(SUM(stake), 0) * 100, 6) AS hold_pct                                -- 聚合表达式：GGR 除本金乘百，产出「hold_pct」（本金口径）
      FROM day_flag                                                                                       -- 取数来源：取自本条自建的中间结果集 day_flag
      GROUP BY dt                                                                                         -- 分组：按 dt 汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`dt` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  48. P12b_member_events.csv   [总包行 9885~9941 · 原 8 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：q.`uid`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`uid`, w.`first_dt`, w.`last_dt`, w.`active_days`, w.`span_days`, w.`n_orders`, w.`mark_dt`, w.`censor_flag`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH test_agents AS (                                                                               -- 公共表表达式：开启中间结果集 test_agents——公司测试线代理（§P12b）
          SELECT age001 AS agent_id                                                                       -- 取值表达式：取用 age001（代理号），产出「agent_id」
          FROM ods_mariadb_2b.ods_a168_agent                                                              -- 取数来源：取自代理主档
          WHERE age022 = '1'                                                                              -- 过滤条件：测试线旗标
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§P12b）
      ranked AS (                                                                                         -- 公共表表达式：开启中间结果集 ranked——同注单号取最新版本（§P12b）
          SELECT b.bet01, b.bet05, b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                           -- 取列：起始取列子句，涉 bet05（会员号）、bet18~22（五级代理线）
                 b.bet38, b.category, b.dt,                                                               -- 续行：接续取列，涉 bet38（重對）、dt（营业日）
                 ROW_NUMBER() OVER (                                                                      -- 行号窗口表达式：以行号窗口去重
                   PARTITION BY b.bet01                                                                   -- 窗口分区：按注单号分组开窗
                   ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                         -- 排序：三级排序取最新版本
          FROM ods_mariadb_2b.ods_a168_bet02 b                                                            -- 取数来源：取自注单明细表
          WHERE b.bet02 = '101'                                                                           -- 过滤条件：限定百家乐
            AND b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                              -- 并列条件：限定全局窗
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§P12b）
      gated AS (                                                                                          -- 公共表表达式：开启中间结果集 gated——七闸清洗（§P12b）
          SELECT r.dt, CAST(r.bet05 AS BIGINT) AS uid                                                     -- 取值表达式：会员号铸型 BIGINT，产出「uid」
          FROM ranked r                                                                                   -- 取数来源：取自本条自建的中间结果集 ranked
          LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18                                               -- 左联结：一级代理线对测试名单
          LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19                                               -- 左联结：二级代理线对测试名单
          LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20                                               -- 左联结：三级代理线对测试名单
          LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21                                               -- 左联结：四级代理线对测试名单
          LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22                                               -- 左联结：五级代理线对测试名单
          WHERE r.rn = 1                                                                                  -- 过滤条件：只留最新版本
            AND r.category = '1'                                                                          -- 并列条件：一般单准入
            AND UPPER(TRIM(r.bet38)) = 'N'                                                                -- 并列条件：排重对单
            AND COALESCE(t1.agent_id, t2.agent_id, t3.agent_id,                                           -- 并列条件：五级任一命中测试线即剔——
                         t4.agent_id, t5.agent_id) IS NULL                                                -- 续行：COALESCE 五级归并判空
            AND r.bet05 IS NOT NULL                                                                       -- 并列条件：会员键非空
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§P12b）
      member_span AS (                                                                                    -- 公共表表达式：开启中间结果集 member_span——会员活动跨度（§P12b）
          SELECT uid,                                                                                     -- 取列：会员号为主键
                 MIN(dt)              AS first_dt,                                                        -- 聚合表达式：首注日，产出「first_dt」
                 MAX(dt)              AS last_dt,                                                         -- 聚合表达式：末注日，产出「last_dt」
                 COUNT(DISTINCT dt)   AS active_days,                                                     -- 计数表达式：活跃天数，产出「active_days」
                 COUNT(*)             AS n_orders                                                         -- 计数表达式：注单数，产出「n_orders」
          FROM gated                                                                                      -- 取数来源：取自本条自建的中间结果集 gated
          GROUP BY uid                                                                                    -- 分组：按会员汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§P12b）
      first_mark AS (                                                                                     -- 公共表表达式：开启中间结果集 first_mark——首次限制事件（§P12b）
          SELECT  CAST(lmc02 AS BIGINT)                        AS uid,                                    -- 取值表达式：lmc02（被改会员号）铸型 BIGINT，产出「uid」——实证 schema，非臆测列
                  MIN(SUBSTR(CAST(lmc08 AS STRING), 1, 10))    AS mark_dt                                 -- 聚合表达式：lmc08（操作时间）取日期段之最小值，产出「mark_dt」——首次事件即标记日
          FROM    ods_mariadb_2b.ods_a168_log_mem_change                                                  -- 取数来源：取自会员变更日志表（与 §TL-14 同源同法）
          WHERE   dt >= '2026-03-21' AND dt < '2026-08-07'                                                -- 过滤条件：限定全局窗
            AND   CAST(lmc04 AS STRING) IN ('edit', 'changestatus')                                       -- 并列条件：只留配置修改与状态变更两类（§TL-14 同款）
            AND   ( CAST(lmc05 AS STRING) LIKE '%mem016%'                                                 -- 并列条件：变更串含 mem016（enable 停启）——
                 OR CAST(lmc05 AS STRING) LIKE '%mem017%' )                                               -- 并列条件：或含 mem017（canbet 禁投）；段式以 §P12b-pre 目检为准
          GROUP BY CAST(lmc02 AS BIGINT)                                                                  -- 分组：按会员取首次
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§P12b）
      SELECT m.uid, m.first_dt, m.last_dt, m.active_days,                                                 -- 取列：会员四要素透传
             DATEDIFF(m.last_dt, m.first_dt) + 1             AS span_days,                                -- 取值表达式：首末跨天数含两端，产出「span_days」
             m.n_orders,                                                                                  -- 取列：注单数透传
             f.mark_dt,                                                                                   -- 取列：标记日（无处置记录者为空，不入处理组）
             CASE WHEN m.last_dt >= '2026-08-01' THEN 1 ELSE 0 END AS censor_flag                         -- 条件表达式：窗口末仍活跃即右删失，产出「censor_flag」
      FROM member_span m                                                                                  -- 取数来源：取自本条自建的中间结果集 member_span
      LEFT JOIN first_mark f ON f.uid = m.uid                                                             -- 左联结：挂首次标记（多数会员为空属常态）
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days AS active_days_xagg, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days_xagg,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`uid` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  49. P12c_member_daily_pnl.csv   [总包行 9954~10005 · 原 6 列 ＋六层约 60 列]
--     典型学：M15 活跃周期　分区：recent（s.x_rec = 1）　连接：q.`uid`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`uid`, w.`dt`, w.`day_orders`, w.`day_stake`, w.`day_vb`, w.`day_pnl`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH test_agents AS (                                                                               -- 公共表表达式：开启中间结果集 test_agents——公司测试线代理（§P12c）
          SELECT age001 AS agent_id                                                                       -- 取值表达式：取用 age001（代理号），产出「agent_id」
          FROM ods_mariadb_2b.ods_a168_agent                                                              -- 取数来源：取自代理主档
          WHERE age022 = '1'                                                                              -- 过滤条件：测试线旗标
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§P12c）
      ranked AS (                                                                                         -- 公共表表达式：开启中间结果集 ranked——同注单号取最新版本（§P12c）
          SELECT b.bet01, b.bet05, b.bet11, b.bet13, b.bet14,                                             -- 取列：起始取列子句，涉 bet05（会员号）、bet11（汇率）、bet13/14（本金/派彩）
                 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                             -- 续行：接续取列，涉五级代理线
                 b.bet38, b.category, b.validbet, b.dt,                                                   -- 续行：接续取列，涉测试标识、洗码量、营业日
                 ROW_NUMBER() OVER (                                                                      -- 行号窗口表达式：以行号窗口去重
                   PARTITION BY b.bet01                                                                   -- 窗口分区：按注单号分组开窗
                   ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                         -- 排序：三级排序取最新版本
          FROM ods_mariadb_2b.ods_a168_bet02 b                                                            -- 取数来源：取自注单明细表
          WHERE b.bet02 = '101'                                                                           -- 过滤条件：限定百家乐
            AND b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                              -- 并列条件：限定全局窗
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§P12c）
      gated AS (                                                                                          -- 公共表表达式：开启中间结果集 gated——七闸清洗并折原币（§P12c）
          SELECT r.dt, CAST(r.bet05 AS BIGINT) AS uid,                                                    -- 取值表达式：会员号铸型 BIGINT，产出「uid」
                 (CAST(r.bet14 AS DECIMAL(20,4)) - CAST(r.bet13 AS DECIMAL(20,4)))                        -- 取值表达式：派彩减本金——
                   / NULLIF(CAST(r.bet11 AS DECIMAL(20,4)), 0) AS pnl,                                    -- 续行：归原币，产出「pnl」
                 CAST(r.bet13 AS DECIMAL(20,4))                                                           -- 取值表达式：bet13（下注金额）——
                   / NULLIF(CAST(r.bet11 AS DECIMAL(20,4)), 0) AS stake,                                  -- 续行：归原币，产出「stake」
                 CAST(r.validbet AS DECIMAL(20,4))                                                        -- 取值表达式：validbet（洗码量）——
                   / NULLIF(CAST(r.bet11 AS DECIMAL(20,4)), 0) AS vb                                      -- 续行：归原币，产出「vb」
          FROM ranked r                                                                                   -- 取数来源：取自本条自建的中间结果集 ranked
          LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18                                               -- 左联结：一级代理线对测试名单
          LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19                                               -- 左联结：二级代理线对测试名单
          LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20                                               -- 左联结：三级代理线对测试名单
          LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21                                               -- 左联结：四级代理线对测试名单
          LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22                                               -- 左联结：五级代理线对测试名单
          WHERE r.rn = 1                                                                                  -- 过滤条件：只留最新版本
            AND r.category = '1'                                                                          -- 并列条件：一般单准入
            AND UPPER(TRIM(r.bet38)) = 'N'                                                                -- 并列条件：排重对单
            AND COALESCE(t1.agent_id, t2.agent_id, t3.agent_id,                                           -- 并列条件：五级任一命中测试线即剔——
                         t4.agent_id, t5.agent_id) IS NULL                                                -- 续行：COALESCE 五级归并判空（§R03b 缺此闸，故两件不可互代）
            AND r.bet05 IS NOT NULL                                                                       -- 并列条件：会员键非空
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§P12c）
      hf AS (                                                                                             -- 公共表表达式：开启中间结果集 hf——雷欧域会员宇宙（§P12c）
          SELECT uid                                                                                      -- 取列：会员号
          FROM gated                                                                                      -- 取数来源：取自本条自建的中间结果集 gated
          GROUP BY uid                                                                                    -- 分组：按会员汇总
          HAVING COUNT(*) >= 100                                                                          -- 分组过滤：与 §R01 名单同门槛（n>=100），防口径漂移
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§P12c）
      SELECT g.uid, g.dt,                                                                                 -- 取列：会员×营业日双主键
             COUNT(*)               AS day_orders,                                                        -- 计数表达式：当日注单数，产出「day_orders」
             ROUND(SUM(g.stake), 4) AS day_stake,                                                         -- 聚合表达式：当日本金，产出「day_stake」
             ROUND(SUM(g.vb), 4)    AS day_vb,                                                            -- 聚合表达式：当日洗码，产出「day_vb」
             ROUND(SUM(g.pnl), 4)   AS day_pnl                                                            -- 聚合表达式：当日净盈亏（会员视角），产出「day_pnl」
      FROM gated g                                                                                        -- 取数来源：取自本条自建的中间结果集 gated
      JOIN hf ON hf.uid = g.uid                                                                           -- 内联结：限定雷欧域会员
      GROUP BY g.uid, g.dt                                                                                -- 分组：按会员×日汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`uid` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  50. P13_personal_entry.csv   [总包行 10145~10204 · 原 7 列 ＋六层约 60 列]
--     典型学：M03 尾段投注　分区：late（s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80）　连接：q.`uid`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`uid`, w.`n_shoes`, w.`n_orders`, w.`entry_pos_p50`, w.`exit_pos_p50`, w.`bet_density_p50`, w.`entry_round_avg`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_sp AS (SELECT x_table, x_shoe, MAX(x_rno) AS max_rno FROM x_bs GROUP BY x_table, x_shoe),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    JOIN x_sp p ON p.x_table = s.x_table AND p.x_shoe = s.x_shoe
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH test_agents AS (                                                                               -- 公共表表达式：公司测试线代理（§P13）
          SELECT age001 AS agent_id                                                                       -- 取值表达式：取用 age001（代理号），产出「agent_id」
          FROM ods_mariadb_2b.ods_a168_agent                                                              -- 取数来源：取自代理主档
          WHERE age022 = '1'                                                                              -- 过滤条件：测试线旗标
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§P13）
      ranked AS (                                                                                         -- 公共表表达式：同注单号取最新版本（§P13）
          SELECT b.bet01, b.bet03, b.bet04, b.bet05,                                                      -- 取列：涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
                 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                             -- 续行：五级代理线
                 b.bet38, b.category, b.dt,                                                               -- 续行：测试标识、类别、营业日
                 ROW_NUMBER() OVER (                                                                      -- 行号窗口表达式：去重
                   PARTITION BY b.bet01                                                                   -- 窗口分区：按注单号
                   ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                         -- 排序：三级排序取最新版本
          FROM ods_mariadb_2b.ods_a168_bet02 b                                                            -- 取数来源：注单明细表
          WHERE b.bet02 = '101'                                                                           -- 过滤条件：限定百家乐
            AND b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                              -- 并列条件：限定全局窗
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§P13）
      gated AS (                                                                                          -- 公共表表达式：七闸清洗（§P13）
          SELECT r.bet03, CAST(r.bet04 AS INT) AS round_no, CAST(r.bet05 AS BIGINT) AS uid                -- 取值表达式：铸型产出「round_no」「uid」
          FROM ranked r                                                                                   -- 取数来源：中间结果集 ranked
          LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18                                               -- 左联结：一级线对测试名单
          LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19                                               -- 左联结：二级线对测试名单
          LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20                                               -- 左联结：三级线对测试名单
          LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21                                               -- 左联结：四级线对测试名单
          LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22                                               -- 左联结：五级线对测试名单
          WHERE r.rn = 1                                                                                  -- 过滤条件：只留最新版本
            AND r.category = '1'                                                                          -- 并列条件：一般单准入
            AND UPPER(TRIM(r.bet38)) = 'N'                                                                -- 并列条件：排重对单
            AND COALESCE(t1.agent_id, t2.agent_id, t3.agent_id,                                           -- 并列条件：五级任一命中即剔——
                         t4.agent_id, t5.agent_id) IS NULL                                                -- 续行：COALESCE 五级归并判空
            AND r.bet03 IS NOT NULL AND r.bet04 IS NOT NULL AND r.bet05 IS NOT NULL                       -- 并列条件：三键非空
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§P13）
      shoe_len AS (          /* ★★ 命名与粒度双注（2026-08-23 立；G-5 已据实测纠错）★★ 一、本 CTE 名为 shoe_len（靴长）系历史沿用，所算 max_round = MAX(round_no) 为『最大局号』非『局数』。二、本处 GROUP BY 只含 bet03，与 §R01／§B01／§B01-D／§P11b 之 (shoe_id, table_id) 写法不同；然 §R01-0b 全域实测：pct_cross_table = 0、max_tables_per_shoe = 1、两粒度靴数同为 123,357（delta = 0）、avg max_round 同为 49.7806，§R01-0d 逐行证据零行——故二者在本核验窗口内逐条一致，本轮无跨桌合并证据。此系本窗口本口径之 OBSERVED 事实，不外推为 shoe_id 永久全局唯一之保证，换窗口须重验。三、切勿改用 GROUP BY dt, bet03：该写法会切断跨零点续接之同一物理靴（§R01-0c 实测 3,092 条、pct_contiguous = 100%）。 */
          SELECT bet03, MAX(round_no) AS max_round                                                        -- 计数表达式：每靴最大局号（真靴长权威表 P0 未解前之代理）
          FROM gated                                                                                      -- 取数来源：中间结果集 gated
          GROUP BY bet03                                                                                  -- 分组：按靴号
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§P13）
      per_shoe AS (                                                                                       -- 公共表表达式：逐靴六量（§P13）
          SELECT g.uid, g.bet03,                                                                          -- 取列：会员×靴双键
                 MIN(g.round_no)                     AS first_r,                                          -- 聚合表达式：个人入场局，产出「first_r」
                 MAX(g.round_no)                     AS last_r,                                           -- 聚合表达式：个人末注局，产出「last_r」
                 COUNT(*)                            AS n_bets,                                           -- 计数表达式：靴内注数，产出「n_bets」
                 MAX(s.max_round)                    AS max_round                                         -- 聚合表达式：该靴观测靴长透传
          FROM gated g                                                                                    -- 取数来源：中间结果集 gated
          JOIN shoe_len s ON s.bet03 = g.bet03                                                            -- 内联结：逐靴挂靴长
          GROUP BY g.uid, g.bet03                                                                         -- 分组：会员×靴
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§P13）
      SELECT uid,                                                                                         -- 取列：会员号为主键
             COUNT(*)                                            AS n_shoes,                              -- 计数表达式：观测靴数，产出「n_shoes」
             SUM(n_bets)                                         AS n_orders,                             -- 聚合表达式：注单合计，产出「n_orders」
             ROUND(PERCENTILE_APPROX(first_r * 1.0                                                        -- 取近似分位数表达式：入场相对靴位——
                   / NULLIF(max_round, 0), 0.5), 4)              AS entry_pos_p50,                        -- 续行：中位，产出「entry_pos_p50」
             ROUND(PERCENTILE_APPROX(last_r * 1.0                                                         -- 取近似分位数表达式：退出相对靴位——
                   / NULLIF(max_round, 0), 0.5), 4)              AS exit_pos_p50,                         -- 续行：中位，产出「exit_pos_p50」
             ROUND(PERCENTILE_APPROX(n_bets * 1.0                                                         -- 取近似分位数表达式：个人跨度内下注密度——
                   / NULLIF(last_r - first_r + 1, 0), 0.5), 4)   AS bet_density_p50,                      -- 续行：中位，产出「bet_density_p50」
             ROUND(AVG(first_r), 2)                              AS entry_round_avg                       -- 聚合表达式：绝对入场局均值（供与阿泽轴交叉），产出「entry_round_avg」
      FROM per_shoe                                                                                       -- 取数来源：中间结果集 per_shoe
      GROUP BY uid                                                                                        -- 分组：按会员
      HAVING SUM(n_bets) >= 100                                                                           -- 分组过滤：雷欧域同门槛（n>=100），防口径漂移
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_late,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_late,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_late,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_late,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_late,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_late,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_late,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_late,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`uid` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  51. PH0_8r1_rg_case.csv   [总包行 13308~13322 · 原 8 列 ＋六层约 60 列]
--     典型学：M10 责任博彩　分区：pre_rg（s.x_rec = 1）　连接：q.`target_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`dt_day`, w.`category`, w.`target_id`, w.`operator_id`, w.`change_time`, w.`operator_ip`, w.`content_full`, w.`sql_full`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        dt                                                                     AS dt_day,                 -- 取列：分区日
        TRIM(lmc04)                                                            AS category,               -- 取列：變更類別
        TRIM(lmc02)                                                            AS target_id,              -- 取列：被变更对象 ID
        TRIM(lmc06)                                                            AS operator_id,            -- 取列：操作者
        TRIM(lmc08)                                                            AS change_time,            -- 取列：異動時間
        TRIM(lmc10)                                                            AS operator_ip,            -- 取列：操作者 IP
        lmc05                                                                  AS content_full,           -- 取列：★ 內容全文——判语境之主据
        lmc09                                                                  AS sql_full                -- 取列：★ 原始 SQL 全文——判语境之佐证
      FROM ods_mariadb_2b.ods_a168_log_mem_change                                                         -- 取数来源：取自会员变更日志表
      WHERE CONCAT(COALESCE(lmc05, ''), COALESCE(lmc09, '')) LIKE '%冷靜%'                                 -- 过滤条件：繁体「冷靜」
         OR CONCAT(COALESCE(lmc05, ''), COALESCE(lmc09, '')) LIKE '%冷静%'                                 -- 并列条件：简体「冷静」
         OR CONCAT(COALESCE(lmc05, ''), COALESCE(lmc09, '')) LIKE '%cooling%'                             -- 并列条件：英文 cooling
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_pre_rg,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_pre_rg,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_pre_rg,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_pre_rg,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_pre_rg,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_pre_rg,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_pre_rg,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_pre_rg,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`target_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  52. PH0_8r2_rg_crosstable.csv   [总包行 13334~13390 · 原 5 列 ＋六层约 60 列]
--     典型学：M10 责任博彩　分区：pre_rg（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT 'member.mem028(備註)'                                             AS src,                    -- 字面取值：受检文本源
             COUNT(*)                                                          AS n_rows,                 -- 计数表达式：总行数
             SUM(CASE WHEN mem028 LIKE '%自我排除%' OR mem028 LIKE '%冷靜%'
                        OR mem028 LIKE '%冷静%'     OR mem028 LIKE '%責任博彩%'
                        OR mem028 LIKE '%责任博彩%' THEN 1 ELSE 0 END)         AS n_rg_true,              -- 条件计数：★★ 真 RG 组
             SUM(CASE WHEN mem028 LIKE '%限額%' OR mem028 LIKE '%限额%'
                        OR mem028 LIKE '%凍結%' OR mem028 LIKE '%冻结%'
                        OR mem028 LIKE '%停用%' OR mem028 LIKE '%封鎖%'
                        OR mem028 LIKE '%封锁%' THEN 1 ELSE 0 END)             AS n_biz_control,          -- 条件计数：商业管控组（★ 非 RG）
             SUM(CASE WHEN mem028 IS NOT NULL AND TRIM(mem028) <> '' THEN 1 ELSE 0 END) AS n_nonblank     -- 条件计数：非空文本行数——判该列是否有料
      FROM ods_mariadb_2b.ods_a168_member                                                                 -- 取数来源：取自会员主表
      UNION ALL                                                                                           -- 纵向合并：接续下一文本源
      SELECT 'alarmMessage.content',                                           COUNT(*),                  -- 字面取值与计数：告警消息内容
             SUM(CASE WHEN content LIKE '%自我排除%' OR content LIKE '%冷靜%'
                        OR content LIKE '%冷静%'     OR content LIKE '%責任博彩%'
                        OR content LIKE '%责任博彩%' THEN 1 ELSE 0 END),                                   -- 条件计数：真 RG 组
             SUM(CASE WHEN content LIKE '%限額%' OR content LIKE '%限额%'
                        OR content LIKE '%凍結%' OR content LIKE '%冻结%'
                        OR content LIKE '%停用%' OR content LIKE '%封鎖%'
                        OR content LIKE '%封锁%' THEN 1 ELSE 0 END),                                       -- 条件计数：商业管控组
             SUM(CASE WHEN content IS NOT NULL AND TRIM(content) <> '' THEN 1 ELSE 0 END)                 -- 条件计数：非空文本行数
      FROM ods_mariadb_2b.ods_a168_alarmMessage                                                           -- 取数来源：取自告警消息表
      UNION ALL                                                                                           -- 纵向合并：接续下一文本源
      SELECT 'alertMessage.message',                                           COUNT(*),                  -- 字面取值与计数：警示消息正文
             SUM(CASE WHEN message LIKE '%自我排除%' OR message LIKE '%冷靜%'
                        OR message LIKE '%冷静%'     OR message LIKE '%責任博彩%'
                        OR message LIKE '%责任博彩%' THEN 1 ELSE 0 END),                                   -- 条件计数：真 RG 组
             SUM(CASE WHEN message LIKE '%限額%' OR message LIKE '%限额%'
                        OR message LIKE '%凍結%' OR message LIKE '%冻结%'
                        OR message LIKE '%停用%' OR message LIKE '%封鎖%'
                        OR message LIKE '%封锁%' THEN 1 ELSE 0 END),                                       -- 条件计数：商业管控组
             SUM(CASE WHEN message IS NOT NULL AND TRIM(message) <> '' THEN 1 ELSE 0 END)                 -- 条件计数：非空文本行数
      FROM ods_mariadb_2b.ods_a168_alertMessage                                                           -- 取数来源：取自警示消息表
      UNION ALL                                                                                           -- 纵向合并：接续下一文本源
      SELECT 'alert_ip_setting.remarks',                                       COUNT(*),                  -- 字面取值与计数：IP 告警备注
             SUM(CASE WHEN remarks LIKE '%自我排除%' OR remarks LIKE '%冷靜%'
                        OR remarks LIKE '%冷静%'     OR remarks LIKE '%責任博彩%'
                        OR remarks LIKE '%责任博彩%' THEN 1 ELSE 0 END),                                   -- 条件计数：真 RG 组
             SUM(CASE WHEN remarks LIKE '%限額%' OR remarks LIKE '%限额%'
                        OR remarks LIKE '%凍結%' OR remarks LIKE '%冻结%'
                        OR remarks LIKE '%停用%' OR remarks LIKE '%封鎖%'
                        OR remarks LIKE '%封锁%' THEN 1 ELSE 0 END),                                       -- 条件计数：商业管控组
             SUM(CASE WHEN remarks IS NOT NULL AND TRIM(remarks) <> '' THEN 1 ELSE 0 END)                 -- 条件计数：非空文本行数
      FROM ods_mariadb_2b.ods_a168_alert_ip_setting                                                       -- 取数来源：取自 IP 告警设定表
      UNION ALL                                                                                           -- 纵向合并：接续下一文本源
      SELECT 'wallet_dtl.detail(窗内)',                                        COUNT(*),                  -- 字面取值与计数：钱包处理明细
             SUM(CASE WHEN detail LIKE '%自我排除%' OR detail LIKE '%冷靜%'
                        OR detail LIKE '%冷静%'     OR detail LIKE '%責任博彩%'
                        OR detail LIKE '%责任博彩%' THEN 1 ELSE 0 END),                                    -- 条件计数：真 RG 组
             SUM(CASE WHEN detail LIKE '%限額%' OR detail LIKE '%限额%'
                        OR detail LIKE '%凍結%' OR detail LIKE '%冻结%'
                        OR detail LIKE '%停用%' OR detail LIKE '%封鎖%'
                        OR detail LIKE '%封锁%' THEN 1 ELSE 0 END),                                        -- 条件计数：商业管控组
             SUM(CASE WHEN detail IS NOT NULL AND TRIM(detail) <> '' THEN 1 ELSE 0 END)                   -- 条件计数：非空文本行数
      FROM ods_mariadb_2b.ods_a168_wallet_dtl                                                             -- 取数来源：取自钱包明细表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限窗，免全表 1.146 亿行之扫描
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_pre_rg,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_pre_rg,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_pre_rg,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_pre_rg,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_pre_rg,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_pre_rg,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_pre_rg,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_pre_rg,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  53. PI01_treatment_delta_member.csv   [总包行 13468~13599 · 原 21 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`treat_time`, w.`treat_field`, w.`n_treat_events`, w.`n_pre`, w.`n_post`, w.`d_pre`, w.`d_post`, w.`stake_pre`, w.`stake_post`, w.`d_stake`, w.`d_validbet`, w.`d_revenue`, w.`hold_pre_pct`, w.`hold_post_pct`, w.`d_hold_pct`, w.`roi_pre`, w.`roi_post`, w.`d_roi`, w.`d_bets_per_day`, w.`verdict_exposure`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ev AS (                                                                                        -- 公共表表达式：开启中间结果集 ev——处置日志原始事件
        SELECT CAST(lmc02 AS STRING)                                           AS member_id,              -- 取值表达式：取用 lmc02（会员编号）
               CAST(lmc08 AS STRING)                                           AS action_time,            -- 取值表达式：★ 取用 lmc08（操作时点·实证列义，见 §TL-10）
               CAST(lmc05 AS STRING)                                           AS content                 -- 取值表达式：★ 取用 lmc05（变更内容·实证列义，见 §TL-10）
        FROM ods_mariadb_2b.ods_a168_log_mem_change                                                       -- 取数来源：取自会员变更日志
        WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                    -- 过滤条件：限定分析窗
          AND CAST(lmc04 AS STRING) IN ('edit', 'changestatus')                                           -- 并列条件：★ 只留二类，剔 add（§TL-10 实测百家乐零命中）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      seg AS (                                                                                            -- 公共表表达式：开启中间结果集 seg——内容逐段展开
        SELECT e.member_id, e.action_time, TRIM(s.piece)                       AS piece                   -- 取列：会员、时点、逐段去空白
        FROM ev e, unnest(split(e.content, ';')) AS s(piece)                                              -- 行展开：以分号切段（§TL-11 同法）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      norm AS (                                                                                           -- 公共表表达式：开启中间结果集 norm——冒号式与空格式并收
        SELECT member_id, action_time,                                                                    -- 取列：会员、时点
               CASE WHEN piece LIKE '%:%'
                    THEN TRIM(SPLIT_PART(SPLIT_PART(piece, ':', 1), ' ', 1))
                    ELSE TRIM(SPLIT_PART(piece, ' ', 1)) END                   AS field_name,             -- 条件取值：★ 两式并收（§TL-11 载空格式 815 条曾被静默丢弃）
               CASE WHEN piece LIKE '%:%'
                    THEN TRIM(SPLIT_PART(SPLIT_PART(piece, ':', 2), '=>', 1))
                    ELSE TRIM(SPLIT_PART(SPLIT_PART(piece, ' ', 2), '=>', 1)) END AS value_before,        -- 条件取值：旧值
               TRIM(SPLIT_PART(piece, '=>', 2))                                AS value_after             -- 取值表达式：新值一律取箭头之后
        FROM seg                                                                                          -- 取数来源：取自本条自建的中间结果集 seg
        WHERE piece LIKE '%=>%'                                                                           -- 过滤条件：须含箭头方为跳变
          AND LENGTH(TRIM(SPLIT_PART(piece, '=>', 1))) BETWEEN 2 AND 20                                   -- 并列条件：★ 护栏——列名须短（§TL-10 载 URL 冒号曾骗过解析）
          AND piece NOT LIKE '%//%'                                                                       -- 并列条件：护栏——排除 URL
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      tr AS (                                                                                             -- 公共表表达式：开启中间结果集 tr——每会员之首次收紧处置
        SELECT member_id                                                       AS mid,                    -- 取列：会员编号
               MIN(CAST(action_time AS DATETIME))                              AS t_treat,                -- 聚合取值：★ 首次处置时点——前后窗之切点
               COUNT(*)                                                        AS n_events,               -- 计数表达式：该会员之跳变数
               MIN(field_name)                                                 AS first_field              -- 聚合取值：首个受改字段（供追溯）
        FROM norm                                                                                         -- 取数来源：取自本条自建的中间结果集 norm
        WHERE field_name IN ('mem016', 'mem017', 'mem015', 'mem020')                                      -- 过滤条件：★ 四列系风控处置（§TL-09 列义已译）
          AND UPPER(TRIM(value_after)) IN ('N', '0')                                                      -- 并列条件：★ 只取收紧向（停用／禁投），非解除向
        GROUP BY member_id                                                                                -- 分组：按会员汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      ta AS (                                                                                             -- 公共表表达式：开启中间结果集 ta——公司测试线代理
        SELECT DISTINCT age001 AS aid FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'               -- 取列与过滤：铁律④b——公司测试线
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk——同注单号取最新版本
        SELECT b.bet05, b.bet09, b.bet13, b.bet14, b.bet11, b.validbet, b.bet39,                          -- 取列：会员、注型、本金、派彩、汇率、洗码、桌号
               b.bet08, b.bet06, b.dt, b.category, b.bet38, b.commission,                                 -- 续行：下注时间、开局时间、营业日、类别、重对、佣金制
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：五级代理位
               ROW_NUMBER() OVER (PARTITION BY b.bet01
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC)      AS rn                      -- 行号窗口表达式：三级排序取最新（§M0-6 已证唯一）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单主表
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定分析窗
          AND CAST(b.bet02 AS STRING) = '101'                                                             -- 并列条件：限定百家乐
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      base AS (                                                                                           -- 公共表表达式：开启中间结果集 base——过基础闸之母体
        SELECT TRIM(r.bet05)                                                   AS mid,                    -- 取列：会员编号
               TRIM(r.bet09)                                                   AS g9,                     -- 取列：注型
               TRIM(r.bet39)                                                   AS tbl,                    -- 取列：★ 桌号取 bet39（bet04 系子场次，§DICT-01b 已证）
               TRIM(r.commission)                                              AS comm,                   -- 取列：佣金制标记（★ 实测 8 值，语义 UNKNOWN）
               r.dt                                                            AS biz_date,               -- 取列：营业日
               CAST(r.bet08 AS DATETIME)                                       AS t_bet,                  -- 取值表达式：下注时点
               CAST(r.bet06 AS DATETIME)                                       AS t_open,                 -- 取值表达式：开局时点
               CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8))
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)), 0) AS stake,                  -- 取值表达式：本金归一
               CAST(NULLIF(TRIM(r.validbet), '') AS DECIMAL(20,8))
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)), 0) AS vbet,                   -- 取值表达式：有效投注归一
               (CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8))
              - CAST(NULLIF(TRIM(r.bet14), '') AS DECIMAL(20,8)))
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)), 0) AS ggr                     -- 取值表达式：平台毛利（本金减派彩）
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：一级代理是否测试线
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：二级代理是否测试线
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：三级代理是否测试线
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：四级代理是否测试线
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：五级代理是否测试线
        WHERE r.rn = 1                                                                                    -- 过滤条件：基础闸①——最新版本
          AND r.category = '1'                                                                            -- 并列条件：基础闸②——一般注单（§QSB-01 已证 cat2 系小费）
          AND UPPER(TRIM(r.bet38)) = 'N'                                                                  -- 并列条件：基础闸③——非重对
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：基础闸④——非测试线
          AND CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)) > 0                                        -- 并列条件：基础闸⑤——汇率为正
          AND CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8)) > 0                                        -- 并列条件：基础闸⑥——本金为正
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      tw AS (                                                                                             -- 公共表表达式：开启中间结果集 tw——按处置时点切前后窗
        SELECT b.mid,                                                                                     -- 取列：会员编号
               CASE WHEN b.t_bet < t.t_treat THEN 'PRE' ELSE 'POST' END        AS phase,                  -- 条件取值：★ 以首次处置时点切前后
               b.stake, b.vbet, b.ggr, b.biz_date                                                         -- 续行：本金、有效投注、毛利、营业日
        FROM base b                                                                                       -- 取数来源：取自本条自建的中间结果集 base
        JOIN tr t ON t.mid = b.mid                                                                        -- 连接：只取受处置之会员
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      agg AS (                                                                                            -- 公共表表达式：开启中间结果集 agg——前后窗各自汇总
        SELECT mid,                                                                                       -- 取列：会员编号
               SUM(CASE WHEN phase = 'PRE'  THEN stake ELSE 0 END)             AS stake_pre,              -- 条件汇总：处置前本金
               SUM(CASE WHEN phase = 'POST' THEN stake ELSE 0 END)             AS stake_post,             -- 条件汇总：处置后本金
               SUM(CASE WHEN phase = 'PRE'  THEN vbet  ELSE 0 END)             AS vbet_pre,               -- 条件汇总：处置前有效投注
               SUM(CASE WHEN phase = 'POST' THEN vbet  ELSE 0 END)             AS vbet_post,              -- 条件汇总：处置后有效投注
               SUM(CASE WHEN phase = 'PRE'  THEN ggr   ELSE 0 END)             AS ggr_pre,                -- 条件汇总：处置前平台毛利
               SUM(CASE WHEN phase = 'POST' THEN ggr   ELSE 0 END)             AS ggr_post,               -- 条件汇总：处置后平台毛利
               SUM(CASE WHEN phase = 'PRE'  THEN 1 ELSE 0 END)                 AS n_pre,                  -- 条件计数：处置前注数
               SUM(CASE WHEN phase = 'POST' THEN 1 ELSE 0 END)                 AS n_post,                 -- 条件计数：处置后注数
               COUNT(DISTINCT CASE WHEN phase = 'PRE'  THEN biz_date END)      AS d_pre,                  -- 条件去重计数：处置前活跃天数
               COUNT(DISTINCT CASE WHEN phase = 'POST' THEN biz_date END)      AS d_post                  -- 条件去重计数：处置后活跃天数
        FROM tw                                                                                           -- 取数来源：取自本条自建的中间结果集 tw
        GROUP BY mid                                                                                      -- 分组：按会员汇总
      )                                                                                                   -- 续行：收束上方的子查询或函数括号
      SELECT                                                                                              -- 取列：起始取列子句
        a.mid                                                                  AS member_id,              -- 取列：会员编号
        t.t_treat                                                              AS treat_time,             -- 取列：★ 处置时点（可回溯 §TL-11 之 episode）
        t.first_field                                                          AS treat_field,            -- 取列：首个受改字段
        t.n_events                                                             AS n_treat_events,         -- 取列：该会员之跳变数
        a.n_pre, a.n_post, a.d_pre, a.d_post,                                                             -- 续行：前后注数与活跃天数
        ROUND(a.stake_pre, 2)                                                  AS stake_pre,              -- 取值表达式：处置前本金
        ROUND(a.stake_post, 2)                                                 AS stake_post,             -- 取值表达式：处置后本金
        ROUND(a.stake_post - a.stake_pre, 2)                                   AS d_stake,                -- 加减计算：★ ΔStake
        ROUND(a.vbet_post - a.vbet_pre, 2)                                     AS d_validbet,             -- 加减计算：★ ΔValidBet
        ROUND(a.ggr_post - a.ggr_pre, 2)                                       AS d_revenue,              -- 加减计算：★ ΔRevenue（平台毛利增减）
        ROUND(a.ggr_pre  * 100.0 / NULLIF(a.vbet_pre, 0), 6)                   AS hold_pre_pct,           -- 比率表达式：处置前 hold%
        ROUND(a.ggr_post * 100.0 / NULLIF(a.vbet_post, 0), 6)                  AS hold_post_pct,          -- 比率表达式：处置后 hold%
        ROUND(a.ggr_post * 100.0 / NULLIF(a.vbet_post, 0)
            - a.ggr_pre  * 100.0 / NULLIF(a.vbet_pre, 0), 6)                   AS d_hold_pct,             -- 加减计算：★ ΔHold%
        ROUND(-1 * a.ggr_pre  / NULLIF(a.stake_pre, 0), 6)                     AS roi_pre,                -- 比率表达式：处置前会员 ROI
        ROUND(-1 * a.ggr_post / NULLIF(a.stake_post, 0), 6)                    AS roi_post,               -- 比率表达式：处置后会员 ROI
        ROUND(-1 * a.ggr_post / NULLIF(a.stake_post, 0)
            + 1 * a.ggr_pre  / NULLIF(a.stake_pre, 0), 6)                      AS d_roi,                  -- 加减计算：★ ΔROI
        ROUND(a.n_post * 1.0 / NULLIF(a.d_post, 0)
            - a.n_pre  * 1.0 / NULLIF(a.d_pre, 0), 4)                          AS d_bets_per_day,         -- 加减计算：★ Δ日均注数
        CASE                                                                                              -- 条件分支：起始分支——暴露态裁定
          WHEN a.n_post = 0
            THEN 'NO_EXPOSURE —— 处置后完全未下注（★ 禁写 ΔROI = 0）'                                       -- 分支判定：零暴露
          WHEN a.n_pre = 0
            THEN 'NO_BASELINE —— 处置前无注单，无从比对'                                                    -- 分支判定：无基线
          WHEN a.ggr_post - a.ggr_pre < 0
            THEN '★ 平台毛利下降 —— 处置后少赚（须并读 ΔStake 判其系收敛抑或流失）'                             -- 分支判定：毛利降
          ELSE '◐ 平台毛利上升或持平'                                                                       -- 分支判定：毛利升
        END                                                                    AS verdict_exposure        -- 条件分支：收束上方判词，产出「verdict_exposure」
      FROM agg a                                                                                          -- 取数来源：取自本条自建的中间结果集 agg
      JOIN tr t ON t.mid = a.mid                                                                          -- 连接：接回处置台账取其时点与字段
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post_xagg,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  54. PI02_treatment_delta_platform.csv   [总包行 13612~13713 · 原 13 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi_xagg` DESC, w.`cohort`, w.`phase`, w.`n_members`, w.`n_bets`, w.`n_dates`, w.`sum_stake`, w.`sum_validbet`, w.`sum_revenue`, w.`hold_pct`, w.`roi`, w.`revenue_per_member`, w.`share_of_stake_pct`, w.`share_of_revenue_pct`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ev AS (                                                                                        -- 公共表表达式：开启中间结果集 ev——处置日志原始事件
        SELECT CAST(lmc02 AS STRING)                                           AS member_id,              -- 取值表达式：取用 lmc02（会员编号）
               CAST(lmc08 AS STRING)                                           AS action_time,            -- 取值表达式：★ 取用 lmc08（操作时点·实证列义，见 §TL-10）
               CAST(lmc05 AS STRING)                                           AS content                 -- 取值表达式：★ 取用 lmc05（变更内容·实证列义，见 §TL-10）
        FROM ods_mariadb_2b.ods_a168_log_mem_change                                                       -- 取数来源：取自会员变更日志
        WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                    -- 过滤条件：限定分析窗
          AND CAST(lmc04 AS STRING) IN ('edit', 'changestatus')                                           -- 并列条件：★ 只留二类，剔 add（§TL-10 实测百家乐零命中）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      seg AS (                                                                                            -- 公共表表达式：开启中间结果集 seg——内容逐段展开
        SELECT e.member_id, e.action_time, TRIM(s.piece)                       AS piece                   -- 取列：会员、时点、逐段去空白
        FROM ev e, unnest(split(e.content, ';')) AS s(piece)                                              -- 行展开：以分号切段（§TL-11 同法）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      norm AS (                                                                                           -- 公共表表达式：开启中间结果集 norm——冒号式与空格式并收
        SELECT member_id, action_time,                                                                    -- 取列：会员、时点
               CASE WHEN piece LIKE '%:%'
                    THEN TRIM(SPLIT_PART(SPLIT_PART(piece, ':', 1), ' ', 1))
                    ELSE TRIM(SPLIT_PART(piece, ' ', 1)) END                   AS field_name,             -- 条件取值：★ 两式并收（§TL-11 载空格式 815 条曾被静默丢弃）
               CASE WHEN piece LIKE '%:%'
                    THEN TRIM(SPLIT_PART(SPLIT_PART(piece, ':', 2), '=>', 1))
                    ELSE TRIM(SPLIT_PART(SPLIT_PART(piece, ' ', 2), '=>', 1)) END AS value_before,        -- 条件取值：旧值
               TRIM(SPLIT_PART(piece, '=>', 2))                                AS value_after             -- 取值表达式：新值一律取箭头之后
        FROM seg                                                                                          -- 取数来源：取自本条自建的中间结果集 seg
        WHERE piece LIKE '%=>%'                                                                           -- 过滤条件：须含箭头方为跳变
          AND LENGTH(TRIM(SPLIT_PART(piece, '=>', 1))) BETWEEN 2 AND 20                                   -- 并列条件：★ 护栏——列名须短（§TL-10 载 URL 冒号曾骗过解析）
          AND piece NOT LIKE '%//%'                                                                       -- 并列条件：护栏——排除 URL
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      tr AS (                                                                                             -- 公共表表达式：开启中间结果集 tr——每会员之首次收紧处置
        SELECT member_id                                                       AS mid,                    -- 取列：会员编号
               MIN(CAST(action_time AS DATETIME))                              AS t_treat,                -- 聚合取值：★ 首次处置时点——前后窗之切点
               COUNT(*)                                                        AS n_events,               -- 计数表达式：该会员之跳变数
               MIN(field_name)                                                 AS first_field              -- 聚合取值：首个受改字段（供追溯）
        FROM norm                                                                                         -- 取数来源：取自本条自建的中间结果集 norm
        WHERE field_name IN ('mem016', 'mem017', 'mem015', 'mem020')                                      -- 过滤条件：★ 四列系风控处置（§TL-09 列义已译）
          AND UPPER(TRIM(value_after)) IN ('N', '0')                                                      -- 并列条件：★ 只取收紧向（停用／禁投），非解除向
        GROUP BY member_id                                                                                -- 分组：按会员汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      ta AS (                                                                                             -- 公共表表达式：开启中间结果集 ta——公司测试线代理
        SELECT DISTINCT age001 AS aid FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'               -- 取列与过滤：铁律④b——公司测试线
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk——同注单号取最新版本
        SELECT b.bet05, b.bet09, b.bet13, b.bet14, b.bet11, b.validbet, b.bet39,                          -- 取列：会员、注型、本金、派彩、汇率、洗码、桌号
               b.bet08, b.bet06, b.dt, b.category, b.bet38, b.commission,                                 -- 续行：下注时间、开局时间、营业日、类别、重对、佣金制
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：五级代理位
               ROW_NUMBER() OVER (PARTITION BY b.bet01
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC)      AS rn                      -- 行号窗口表达式：三级排序取最新（§M0-6 已证唯一）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单主表
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定分析窗
          AND CAST(b.bet02 AS STRING) = '101'                                                             -- 并列条件：限定百家乐
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      base AS (                                                                                           -- 公共表表达式：开启中间结果集 base——过基础闸之母体
        SELECT TRIM(r.bet05)                                                   AS mid,                    -- 取列：会员编号
               TRIM(r.bet09)                                                   AS g9,                     -- 取列：注型
               TRIM(r.bet39)                                                   AS tbl,                    -- 取列：★ 桌号取 bet39（bet04 系子场次，§DICT-01b 已证）
               TRIM(r.commission)                                              AS comm,                   -- 取列：佣金制标记（★ 实测 8 值，语义 UNKNOWN）
               r.dt                                                            AS biz_date,               -- 取列：营业日
               CAST(r.bet08 AS DATETIME)                                       AS t_bet,                  -- 取值表达式：下注时点
               CAST(r.bet06 AS DATETIME)                                       AS t_open,                 -- 取值表达式：开局时点
               CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8))
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)), 0) AS stake,                  -- 取值表达式：本金归一
               CAST(NULLIF(TRIM(r.validbet), '') AS DECIMAL(20,8))
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)), 0) AS vbet,                   -- 取值表达式：有效投注归一
               (CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8))
              - CAST(NULLIF(TRIM(r.bet14), '') AS DECIMAL(20,8)))
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)), 0) AS ggr                     -- 取值表达式：平台毛利（本金减派彩）
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：一级代理是否测试线
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：二级代理是否测试线
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：三级代理是否测试线
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：四级代理是否测试线
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：五级代理是否测试线
        WHERE r.rn = 1                                                                                    -- 过滤条件：基础闸①——最新版本
          AND r.category = '1'                                                                            -- 并列条件：基础闸②——一般注单（§QSB-01 已证 cat2 系小费）
          AND UPPER(TRIM(r.bet38)) = 'N'                                                                  -- 并列条件：基础闸③——非重对
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：基础闸④——非测试线
          AND CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)) > 0                                        -- 并列条件：基础闸⑤——汇率为正
          AND CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8)) > 0                                        -- 并列条件：基础闸⑥——本金为正
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      lab AS (                                                                                            -- 公共表表达式：开启中间结果集 lab——标注受处置与否
        SELECT b.mid, b.stake, b.vbet, b.ggr, b.biz_date,                                                 -- 取列：会员、本金、有效投注、毛利、营业日
               CASE WHEN t.mid IS NULL THEN 'UNTREATED' ELSE 'TREATED' END     AS grp,                    -- 条件取值：受处置与否
               CASE WHEN t.mid IS NULL THEN 'NA'
                    WHEN b.t_bet < t.t_treat THEN 'PRE' ELSE 'POST' END        AS phase                   -- 条件取值：前后窗
        FROM base b                                                                                       -- 取数来源：取自本条自建的中间结果集 base
        LEFT JOIN tr t ON t.mid = b.mid                                                                   -- 左连接：处置台账（未命中即未处置）
      )                                                                                                   -- 续行：收束上方的子查询或函数括号
      SELECT                                                                                              -- 取列：起始取列子句
        grp                                                                    AS cohort,                 -- 取列：分组
        phase                                                                  AS phase,                  -- 取列：前后窗
        COUNT(DISTINCT mid)                                                    AS n_members,              -- 计数表达式：会员数
        COUNT(*)                                                               AS n_bets,                 -- 计数表达式：注数
        COUNT(DISTINCT biz_date)                                               AS n_dates,                -- 计数表达式：涉及天数
        ROUND(SUM(stake), 2)                                                   AS sum_stake,              -- 汇总表达式：本金总额
        ROUND(SUM(vbet), 2)                                                    AS sum_validbet,           -- 汇总表达式：有效投注总额
        ROUND(SUM(ggr), 2)                                                     AS sum_revenue,            -- 汇总表达式：★ 平台毛利总额
        ROUND(SUM(ggr) * 100.0 / NULLIF(SUM(vbet), 0), 6)                      AS hold_pct,               -- 比率表达式：hold%
        ROUND(-1 * SUM(ggr) / NULLIF(SUM(stake), 0), 6)                        AS roi,                    -- 比率表达式：会员侧 ROI
        ROUND(SUM(ggr) / NULLIF(COUNT(DISTINCT mid), 0), 2)                    AS revenue_per_member,     -- 比率表达式：★ 人均贡献平台毛利
        ROUND(SUM(stake) * 100.0 / SUM(SUM(stake)) OVER (), 6)                 AS share_of_stake_pct,     -- 比率表达式：★ 占全平台本金之比
        ROUND(SUM(ggr) * 100.0 / NULLIF(SUM(SUM(ggr)) OVER (), 0), 6)          AS share_of_revenue_pct    -- 比率表达式：★ 占全平台毛利之比
      FROM lab                                                                                            -- 取数来源：取自本条自建的中间结果集 lab
      GROUP BY grp, phase                                                                                 -- 分组：按组别与前后窗汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets AS n_bets_xagg, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi_xagg,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  55. PI03_reinvest_loop.csv   [总包行 13728~13791 · 原 13 列 ＋六层约 60 列]
--     典型学：M13 复投循环　分区：loss（s.x_net < 0）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`prev_outcome`, w.`n_pairs`, w.`n_members`, w.`n_negative_gap`, w.`gap_p10`, w.`gap_p50`, w.`gap_p90`, w.`gap_avg`, w.`ratio_p50`, w.`ratio_p90`, w.`ratio_avg`, w.`n_double_up`, w.`pct_double_up`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_net < 0 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_net < 0 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_net < 0 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_net < 0) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_net < 0) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_net < 0) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ta AS (                                                                                        -- 公共表表达式：开启中间结果集 ta——公司测试线代理
        SELECT DISTINCT age001 AS aid FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'               -- 取列与过滤：铁律④b——公司测试线
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk——同注单号取最新版本
        SELECT b.bet05, b.bet13, b.bet14, b.bet11, b.bet08, b.bet39, b.category, b.bet38,                 -- 取列：会员、本金、派彩、汇率、下注时点、桌号、类别、重对
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：五级代理位
               ROW_NUMBER() OVER (PARTITION BY b.bet01
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC)      AS rn                      -- 行号窗口表达式：三级排序取最新
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单主表
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定分析窗
          AND CAST(b.bet02 AS STRING) = '101'                                                             -- 并列条件：限定百家乐
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      base AS (                                                                                           -- 公共表表达式：开启中间结果集 base——过基础闸之母体
        SELECT TRIM(r.bet05)                                                   AS mid,                    -- 取列：会员编号
               CAST(r.bet08 AS DATETIME)                                       AS t_bet,                  -- 取值表达式：下注时点
               CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8))
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)), 0) AS stake,                  -- 取值表达式：本金归一
               (CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8))
              - CAST(NULLIF(TRIM(r.bet14), '') AS DECIMAL(20,8)))
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)), 0) AS ggr                     -- 取值表达式：平台毛利
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：一级代理是否测试线
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：二级代理是否测试线
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：三级代理是否测试线
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：四级代理是否测试线
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：五级代理是否测试线
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：基础闸①②③
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：基础闸④——非测试线
          AND CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)) > 0                                        -- 并列条件：基础闸⑤——汇率为正
          AND CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8)) > 0                                        -- 并列条件：基础闸⑥——本金为正
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      seq AS (                                                                                            -- 公共表表达式：开启中间结果集 seq——按会员排下注序
        SELECT mid, t_bet, stake, ggr,                                                                    -- 取列：会员、时点、本金、毛利
               LEAD(t_bet) OVER (PARTITION BY mid ORDER BY t_bet)              AS t_next,                 -- 前瞻窗口表达式：同会员之下一注时点
               LEAD(stake) OVER (PARTITION BY mid ORDER BY t_bet)              AS stake_next,             -- 前瞻窗口表达式：同会员之下一注注额
               CASE WHEN ggr > 0 THEN 'LOSS'
                    WHEN ggr < 0 THEN 'WIN' ELSE 'PUSH' END                    AS outcome                 -- 条件取值：★ 本注结果三态（会员视角）
        FROM base                                                                                         -- 取数来源：取自本条自建的中间结果集 base
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      gp AS (                                                                                             -- 公共表表达式：开启中间结果集 gp——计间隔与加码比
        SELECT mid, outcome,                                                                              -- 取列：会员、本注结果
               TIMESTAMPDIFF(SECOND, t_bet, t_next)                            AS gap_sec,                -- 时间差表达式：★ 至下一注之间隔（秒）
               stake_next / NULLIF(stake, 0)                                   AS stake_ratio             -- 除法计算：★ 下一注注额倍数
        FROM seq                                                                                          -- 取数来源：取自本条自建的中间结果集 seq
        WHERE t_next IS NOT NULL                                                                          -- 过滤条件：末注无下一注，剔除
      )                                                                                                   -- 续行：收束上方的子查询或函数括号
      SELECT                                                                                              -- 取列：起始取列子句
        outcome                                                                AS prev_outcome,           -- 取列：★ 前一注之结果
        COUNT(*)                                                               AS n_pairs,                -- 计数表达式：相邻注对数
        COUNT(DISTINCT mid)                                                    AS n_members,              -- 计数表达式：涉及会员数
        SUM(CASE WHEN gap_sec < 0 THEN 1 ELSE 0 END)                           AS n_negative_gap,         -- 条件计数：★ 负间隔——保留并查
        ROUND(PERCENTILE_APPROX(gap_sec, 0.10), 4)                             AS gap_p10,                -- 分位表达式：间隔 10% 分位
        ROUND(PERCENTILE_APPROX(gap_sec, 0.50), 4)                             AS gap_p50,                -- 分位表达式：★ 间隔中位——三态并读即见差异
        ROUND(PERCENTILE_APPROX(gap_sec, 0.90), 4)                             AS gap_p90,                -- 分位表达式：间隔 90% 分位
        ROUND(AVG(gap_sec), 4)                                                 AS gap_avg,                -- 聚合表达式：间隔均值
        ROUND(PERCENTILE_APPROX(stake_ratio, 0.50), 6)                         AS ratio_p50,              -- 分位表达式：★ 加码比中位——>1 即亏后加码
        ROUND(PERCENTILE_APPROX(stake_ratio, 0.90), 6)                         AS ratio_p90,              -- 分位表达式：加码比 90% 分位
        ROUND(AVG(stake_ratio), 6)                                             AS ratio_avg,              -- 聚合表达式：加码比均值
        SUM(CASE WHEN stake_ratio >= 2 THEN 1 ELSE 0 END)                      AS n_double_up,            -- 条件计数：★ 下一注加倍及以上者
        ROUND(SUM(CASE WHEN stake_ratio >= 2 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_double_up           -- 比率表达式：加倍率
      FROM gp                                                                                             -- 取数来源：取自本条自建的中间结果集 gp
      GROUP BY outcome                                                                                    -- 分组：按前一注结果汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_loss,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_loss,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_loss,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_loss,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_loss,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_loss,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_loss,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_loss,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  56. PI04_member_5d_profile.csv   [总包行 13808~13877 · 原 20 列 ＋六层约 60 列]
--     典型学：M15 活跃周期　分区：recent（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi_xagg` DESC, w.`member_id`, w.`n_bets`, w.`n_active_days`, w.`bets_per_day`, w.`n_bet_types`, w.`sum_stake`, w.`sum_validbet`, w.`sum_revenue`, w.`hold_pct`, w.`roi`, w.`n_tables`, w.`n_comm_mode`, w.`first_date`, w.`last_date`, w.`span_days`, w.`night_share_pct`, w.`avg_latency_sec`, w.`share_stake_pct`, w.`share_revenue_pct`, w.`verdict_observability`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ta AS (                                                                                        -- 公共表表达式：开启中间结果集 ta——公司测试线代理
        SELECT DISTINCT age001 AS aid FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'               -- 取列与过滤：铁律④b——公司测试线
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk——同注单号取最新版本
        SELECT b.bet05, b.bet09, b.bet13, b.bet14, b.bet11, b.validbet, b.bet39, b.commission,            -- 取列：会员、注型、本金、派彩、汇率、洗码、桌号、佣金制
               b.bet08, b.bet06, b.dt, b.category, b.bet38,                                               -- 续行：下注时点、开局时点、营业日、类别、重对
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：五级代理位
               ROW_NUMBER() OVER (PARTITION BY b.bet01
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC)      AS rn                      -- 行号窗口表达式：三级排序取最新
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单主表
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定分析窗
          AND CAST(b.bet02 AS STRING) = '101'                                                             -- 并列条件：限定百家乐
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      base AS (                                                                                           -- 公共表表达式：开启中间结果集 base——过基础闸之母体
        SELECT TRIM(r.bet05)                                                   AS mid,                    -- 取列：会员编号
               TRIM(r.bet09)                                                   AS g9,                     -- 取列：注型
               TRIM(r.bet39)                                                   AS tbl,                    -- 取列：★ 桌号取 bet39
               TRIM(r.commission)                                              AS comm,                   -- 取列：佣金制标记
               r.dt                                                            AS biz_date,               -- 取列：营业日
               CAST(r.bet08 AS DATETIME)                                       AS t_bet,                  -- 取值表达式：下注时点
               CAST(r.bet06 AS DATETIME)                                       AS t_open,                 -- 取值表达式：开局时点
               CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8))
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)), 0) AS stake,                  -- 取值表达式：本金归一
               CAST(NULLIF(TRIM(r.validbet), '') AS DECIMAL(20,8))
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)), 0) AS vbet,                   -- 取值表达式：有效投注归一
               (CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8))
              - CAST(NULLIF(TRIM(r.bet14), '') AS DECIMAL(20,8)))
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)), 0) AS ggr                     -- 取值表达式：平台毛利
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：一级代理是否测试线
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：二级代理是否测试线
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：三级代理是否测试线
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：四级代理是否测试线
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：五级代理是否测试线
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：基础闸①②③
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：基础闸④
          AND CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)) > 0                                        -- 并列条件：基础闸⑤
          AND CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8)) > 0                                        -- 并列条件：基础闸⑥
      )                                                                                                   -- 续行：收束上方的子查询或函数括号
      SELECT                                                                                              -- 取列：起始取列子句
        mid                                                                    AS member_id,              -- 取列：会员编号
        COUNT(*)                                                               AS n_bets,                 -- 计数表达式：〔B〕注数
        COUNT(DISTINCT biz_date)                                               AS n_active_days,          -- 计数表达式：〔B〕活跃天数
        ROUND(COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT biz_date), 0), 4)         AS bets_per_day,           -- 比率表达式：〔B〕日均注数
        COUNT(DISTINCT g9)                                                     AS n_bet_types,            -- 计数表达式：〔B〕所押注型数
        ROUND(SUM(stake), 2)                                                   AS sum_stake,              -- 汇总表达式：〔E〕本金
        ROUND(SUM(vbet), 2)                                                    AS sum_validbet,           -- 汇总表达式：〔E〕有效投注
        ROUND(SUM(ggr), 2)                                                     AS sum_revenue,            -- 汇总表达式：〔E〕★ 贡献之平台毛利
        ROUND(SUM(ggr) * 100.0 / NULLIF(SUM(vbet), 0), 6)                      AS hold_pct,               -- 比率表达式：〔E〕hold%
        ROUND(-1 * SUM(ggr) / NULLIF(SUM(stake), 0), 6)                        AS roi,                    -- 比率表达式：〔E〕会员 ROI
        COUNT(DISTINCT tbl)                                                    AS n_tables,               -- 计数表达式：〔N〕★ 所涉桌数
        COUNT(DISTINCT comm)                                                   AS n_comm_mode,            -- 计数表达式：〔N〕所涉佣金制数
        MIN(biz_date)                                                          AS first_date,             -- 聚合取值：〔T〕首注日
        MAX(biz_date)                                                          AS last_date,              -- 聚合取值：〔T〕末注日
        DATEDIFF(MAX(biz_date), MIN(biz_date)) + 1                             AS span_days,              -- 时间差表达式：〔T〕★ 观测跨度（非窗长）
        ROUND(SUM(CASE WHEN HOUR(t_bet) < 6 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS night_share_pct,        -- 比率表达式：〔T〕夜间注占比（0–6 时）
        ROUND(AVG(TIMESTAMPDIFF(SECOND, t_open, t_bet)), 4)                    AS avg_latency_sec,        -- 聚合表达式：〔T〕★ 下注延迟均值
        ROUND(SUM(stake) * 100.0 / SUM(SUM(stake)) OVER (), 8)                 AS share_stake_pct,        -- 比率表达式：〔I〕★ 占全平台本金之比
        ROUND(SUM(ggr) * 100.0 / NULLIF(SUM(SUM(ggr)) OVER (), 0), 8)          AS share_revenue_pct,      -- 比率表达式：〔I〕★ 占全平台毛利之比
        CASE                                                                                              -- 条件分支：起始分支——可观测性裁定
          WHEN MIN(biz_date) = '2026-03-21'
            THEN 'LEFT_CENSORED —— 首注即窗首，真实起点在窗外（禁解为新会员）'                                 -- 分支判定：左截断
          WHEN DATEDIFF(MAX(biz_date), MIN(biz_date)) + 1 < 7
            THEN 'SHORT_SPAN —— 跨度不足七日，指标不稳'                                                     -- 分支判定：跨度过短
          ELSE 'OBSERVABLE'                                                                               -- 分支判定：可观测
        END                                                                    AS verdict_observability   -- 条件分支：收束上方判词，产出「verdict_observability」
      FROM base                                                                                           -- 取数来源：取自本条自建的中间结果集 base
      GROUP BY mid                                                                                        -- 分组：按会员汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets AS n_bets_xagg, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi_xagg,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days_xagg,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  57. P_player_month.csv   [总包行 2865~2921 · 原 9 列 ＋六层约 60 列]
--     典型学：M15 活跃周期　分区：recent（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi_xagg` DESC, w.`member_id`, w.`ym`, w.`n_rounds`, w.`n_days`, w.`valid_bet`, w.`game_pnl`, w.`rebate`, w.`net_pnl`, w.`roi`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ta AS (            -- 公司测试线代理（214 条，跨五级）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 P_player_month.csv）
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk，其后各行为其定义体（交付件 P_player_month.csv）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet09，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet11, b.bet13, b.bet14, b.bet16, b.bet17,                                               -- 续行：接续上一取列子句，续列 b.bet11, b.bet13, b.bet14, b.bet16, b.bet17，涉 bet11（汇率）、bet13（下注金额）、bet14（派彩金额）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet38, b.bet39, b.category, b.eid, b.ip,                                                 -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category, b.eid, b.ip，涉 bet38（重對）、bet39（桌号）、eid（荷官工号）
               b.validbet,                                                                                -- 续行：接续上一取列子句，续列 b.validbet，涉 validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 P_player_month.csv）
      vd AS (                                                                                             -- 公共表表达式：开启中间结果集 vd，其后各行为其定义体（交付件 P_player_month.csv）
        SELECT r.* FROM rk r                                                                              -- 取列：整体承接上游结果集的全部字段，不再逐列列举，取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 P_player_month.csv）
      bs AS (                                                                                             -- 公共表表达式：开启中间结果集 bs，其后各行为其定义体（交付件 P_player_month.csv）
        SELECT v.bet05 AS member_id, v.eid AS dealer_id, v.ip AS bet_ip,                                  -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、eid（荷官工号）、bet_ip（下注 IP）
               v.bet20 AS lv3, v.bet09 AS bet_side, v.dt AS bet_date,                                     -- 取值表达式：取用 bet09（玩法）、bet20（三级代理线）、dt（营业日），产出「bet_date」
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.bet08),'') AS DATETIME) AS t_bet,                                       -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「t_bet」
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake,                              -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               CAST(NULLIF(TRIM(v.validbet),'') AS DECIMAL(20,4))                                         -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS valid_bet,                          -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「valid_bet」
               (CAST(NULLIF(TRIM(v.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS game_pnl,                           -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               CAST(NULLIF(TRIM(v.bet16),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS rebate,                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
               CAST(NULLIF(TRIM(v.bet17),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS net_pnl                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_pnl」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（交付件 P_player_month.csv）
      SELECT member_id, DATE_TRUNC('month', bet_date) AS ym,                                              -- 取列：起始取列子句，本行先取「ym」，涉 member_id（会员号）、bet_date（营业日）
        COUNT(DISTINCT round_key) AS n_rounds, COUNT(DISTINCT bet_date) AS n_days,                        -- 计数表达式：统计去重个数，产出「n_days」
        SUM(valid_bet) AS valid_bet, SUM(game_pnl) AS game_pnl,                                           -- 汇总表达式：取用 valid_bet（有效投注（洗码量））、game_pnl（游戏净输赢），产出「game_pnl」
        SUM(rebate) AS rebate, SUM(net_pnl) AS net_pnl,                                                   -- 汇总表达式：取用 net_pnl（会员净输赢）、rebate（退水），产出「net_pnl」
        SUM(game_pnl)/NULLIF(SUM(valid_bet),0) AS roi                                                     -- 汇总表达式：取用 valid_bet（有效投注（洗码量））、game_pnl（游戏净输赢），产出「roi」
      FROM bs GROUP BY member_id, DATE_TRUNC('month', bet_date)                                           -- 取数来源：取自本条自建的中间结果集 bs
      HAVING COUNT(DISTINCT round_key) >= 30                                                              -- 分组后过滤：限定 round_key 的去重个数不少于 30，涉 round_key（局键）
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet_xagg,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds AS n_rounds_xagg,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi_xagg,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  58. QSB_01a_category_census.csv   [总包行 11153~11197 · 原 22 列 ＋六层约 60 列]
--     典型学：M14 投注形态　分区：freecomm（s.x_freecomm = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`category_value`, w.`n_rows`, w.`pct_of_total`, w.`n_members`, w.`n_dates`, w.`min_dt`, w.`max_dt`, w.`n_bet09`, w.`n_b14_null`, w.`n_b14_zero`, w.`n_b14_pos`, w.`n_pnl_zero`, w.`n_pnl_house_win`, w.`n_pnl_player_win`, w.`n_vb_zero`, w.`n_b41_zero`, w.`stake_sum`, w.`payout_sum`, w.`validbet_sum`, w.`n_rebet_flag`, w.`n_commission`, w.`shape_hint`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        COALESCE(NULLIF(TRIM(category), ''), '(空值或空串)')                    AS category_value,         -- 条件取值：★ 空与空串合并另列，不与有值混
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：该取值之行数
        ROUND(COUNT(*) * 100.0 / SUM(COUNT(*)) OVER (), 8)                     AS pct_of_total,           -- 比率表达式：占全母体之比
        COUNT(DISTINCT TRIM(bet05))                                            AS n_members,              -- 计数表达式：涉及会员数
        COUNT(DISTINCT dt)                                                     AS n_dates,                -- 计数表达式：涉及天数（对照全窗 139）
        MIN(dt)                                                                AS min_dt,                 -- 聚合取值：首现日
        MAX(dt)                                                                AS max_dt,                 -- 聚合取值：末现日
        COUNT(DISTINCT TRIM(bet09))                                            AS n_bet09,                -- 计数表达式：涉及注型数
        SUM(CASE WHEN NULLIF(TRIM(bet14), '') IS NULL THEN 1 ELSE 0 END)       AS n_b14_null,             -- 条件计数：★ 派彩为空——未结算之候选
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_b14_zero, -- 条件计数：派彩为零
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_b14_pos,  -- 条件计数：派彩为正
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_pnl_zero, -- 条件计数：★ 净输赢为零——push／取消之候选
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_pnl_house_win, -- 条件计数：庄家赢（会员输）
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) AS n_pnl_player_win, -- 条件计数：会员赢
        SUM(CASE WHEN CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_vb_zero, -- 条件计数：洗码为零
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet41), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END)   AS n_b41_zero, -- 条件计数：bet41 为零
        ROUND(SUM(CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))), 2)          AS stake_sum,              -- 汇总表达式：本金总额
        ROUND(SUM(CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8))), 2)          AS payout_sum,             -- 汇总表达式：派彩总额
        ROUND(SUM(CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))), 2)       AS validbet_sum,           -- 汇总表达式：洗码总额
        COUNT(DISTINCT TRIM(bet38))                                            AS n_rebet_flag,           -- 计数表达式：重对标记之取值数
        COUNT(DISTINCT TRIM(commission))                                       AS n_commission,           -- 计数表达式：佣金标记之取值数
        CASE                                                                                              -- 条件分支：起始分支——形态线索（★ 非定谳）
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                           - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) = COUNT(*)
            THEN '★ 净输赢恒零 —— 取消／退还之候选（须上游确认）'                                            -- 分支判定：pnl 恒零
          WHEN SUM(CASE WHEN NULLIF(TRIM(bet14), '') IS NULL THEN 1 ELSE 0 END) = COUNT(*)
            THEN '★ 派彩恒空 —— 未结算之候选（须上游确认）'                                                 -- 分支判定：派彩恒空
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) = COUNT(*)
            THEN '★ 派彩恒零 —— 单向扣款之候选（同 Tip_1_ 形态）'                                           -- 分支判定：派彩恒零
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                           - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) > 0
           AND SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                           - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) > 0
            THEN '◐ 输赢两向俱备 —— 常态结算单之候选'                                                       -- 分支判定：两向俱备
          ELSE '◐ 形态未归类 —— 须逐项人查'                                                                -- 分支判定：未归类
        END                                                                    AS shape_hint              -- 条件分支：收束上方判词，产出「shape_hint」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
      GROUP BY COALESCE(NULLIF(TRIM(category), ''), '(空值或空串)')                                        -- 分组：按 category 取值汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_freecomm,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_freecomm,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_freecomm,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_freecomm,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_freecomm,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_freecomm,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_freecomm,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_freecomm,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  59. QSB_01c_cat1_contamination.csv   [总包行 11250~11283 · 原 10 列 ＋六层约 60 列]
--     典型学：M14 投注形态　分区：freecomm（s.x_freecomm = 1）　连接：q.`bet09_value`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`bet09_value`, w.`n_rows`, w.`n_b14_null`, w.`n_b13_null`, w.`n_b13_zero`, w.`n_pnl_zero_stake_pos`, w.`n_exact_refund`, w.`pct_pnl_zero`, w.`stake_sum`, w.`verdict_contamination`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_product                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_product IS NOT NULL AND TRIM(CAST(s.x_product AS STRING)) <> ''
    GROUP BY s.x_product
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        TRIM(bet09)                                                            AS bet09_value,            -- 取列：注型——按注型拆，免整体掩盖局部
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：该注型于 cat=1 内之行数
        SUM(CASE WHEN NULLIF(TRIM(bet14), '') IS NULL THEN 1 ELSE 0 END)       AS n_b14_null,             -- 条件计数：★★ 派彩为空——未结算之疑
        SUM(CASE WHEN NULLIF(TRIM(bet13), '') IS NULL THEN 1 ELSE 0 END)       AS n_b13_null,             -- 条件计数：本金为空
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_b13_zero, -- 条件计数：本金为零
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0
                   AND CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_pnl_zero_stake_pos, -- 条件计数：★ 本金为正而净输赢为零——push／取消同形
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                   = CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8))
                  AND CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_exact_refund, -- 条件计数：★ 派彩恰等本金——原额退还之形态
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0
                         AND CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                           - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_pnl_zero,           -- 比率表达式：★ 净输赢为零之占比——即 push 率之上界
        ROUND(SUM(CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))), 2)          AS stake_sum,              -- 汇总表达式：本金总额
        CASE                                                                                              -- 条件分支：起始分支——污染裁定
          WHEN SUM(CASE WHEN NULLIF(TRIM(bet14), '') IS NULL THEN 1 ELSE 0 END) > 0
            THEN '❌❌ QSB LINEAGE BREAK —— category=1 内有派彩为空者，疑未结算单混入，54 处准入须复核'        -- 分支判定：未结算混入
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) > 0
            THEN '★ 有本金为零者列为一般单 —— 语义可疑，须查'                                                -- 分支判定：本金为零
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0
                          AND CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                            - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) > 0
            THEN '◐ 有净输赢为零者 —— 系 push 抑或取消单，二者同形，须再拆（★ 本件答不了）'                     -- 分支判定：有零输赢
          ELSE '✅ 本注型内无上述三疑'                                                                      -- 分支判定：无疑
        END                                                                    AS verdict_contamination   -- 条件分支：收束上方判词，产出「verdict_contamination」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
        AND TRIM(category) = '1'                                                                          -- 并列条件：★ 只看 category='1' 之内部
      GROUP BY TRIM(bet09)                                                                                -- 分组：按注型汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_freecomm,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_freecomm,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_freecomm,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_freecomm,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_freecomm,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_freecomm,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_freecomm,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_freecomm,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`bet09_value` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  60. QSB_02a_lowcard_fields.csv   [总包行 11295~11316 · 原 18 列 ＋六层约 60 列]
--     典型学：M14 投注形态　分区：freecomm（s.x_freecomm = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`scope`, w.`n_rows`, w.`nd_bet04`, w.`nd_bet06`, w.`nd_bet07`, w.`nd_bet10`, w.`nd_bet23`, w.`nd_bet24`, w.`nd_bet25`, w.`nd_bet26`, w.`nd_bet28`, w.`nd_bet29`, w.`nd_bet30`, w.`nd_bet31`, w.`nd_bet40`, w.`nd_bet38`, w.`nd_category`, w.`nd_commission`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        'bet02 表之低基数字段普查（限窗 · 限百家乐）'                              AS scope,                  -- 字面取值：受检范围
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：母体行数
        COUNT(DISTINCT TRIM(bet04))                                            AS nd_bet04,               -- 计数表达式：局号／桌号之候选
        COUNT(DISTINCT TRIM(bet06))                                            AS nd_bet06,               -- 计数表达式：★ 未注字段
        COUNT(DISTINCT TRIM(bet07))                                            AS nd_bet07,               -- 计数表达式：账务日（在册 dt = bet07 100%）
        COUNT(DISTINCT TRIM(bet10))                                            AS nd_bet10,               -- 计数表达式：★ 未注字段——币别之候选
        COUNT(DISTINCT TRIM(bet23))                                            AS nd_bet23,               -- 计数表达式：★ 未注字段
        COUNT(DISTINCT TRIM(bet24))                                            AS nd_bet24,               -- 计数表达式：★ 未注字段
        COUNT(DISTINCT TRIM(bet25))                                            AS nd_bet25,               -- 计数表达式：★ 未注字段
        COUNT(DISTINCT TRIM(bet26))                                            AS nd_bet26,               -- 计数表达式：★ 未注字段
        COUNT(DISTINCT TRIM(bet28))                                            AS nd_bet28,               -- 计数表达式：★ 未注字段
        COUNT(DISTINCT TRIM(bet29))                                            AS nd_bet29,               -- 计数表达式：★ 未注字段
        COUNT(DISTINCT TRIM(bet30))                                            AS nd_bet30,               -- 计数表达式：★ 未注字段
        COUNT(DISTINCT TRIM(bet31))                                            AS nd_bet31,               -- 计数表达式：★ 未注字段
        COUNT(DISTINCT TRIM(bet40))                                            AS nd_bet40,               -- 计数表达式：★ 未注字段
        COUNT(DISTINCT TRIM(bet38))                                            AS nd_bet38,               -- 计数表达式：重对标记（已知 'N'）
        COUNT(DISTINCT TRIM(category))                                         AS nd_category,            -- 计数表达式：注单类别（已证仅 1／2）
        COUNT(DISTINCT TRIM(commission))                                       AS nd_commission           -- 计数表达式：佣金标记（已证 8 值）
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                -- 并列条件：限定百家乐
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_freecomm,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_freecomm,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_freecomm,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_freecomm,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_freecomm,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_freecomm,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_freecomm,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_freecomm,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  61. QSB_02b2_lowcard_five_gates.csv   [总包行 11498~11747 · 原 19 列 ＋六层约 60 列]
--     典型学：M14 投注形态　分区：freecomm（s.x_freecomm = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`field_name`, w.`field_value`, w.`n_rows`, w.`pct_of_field`, w.`n_null`, w.`n_bet09`, w.`n_dates`, w.`min_dt`, w.`max_dt`, w.`n_pnl_zero`, w.`n_pnl_pos`, w.`n_pnl_neg`, w.`pct_pnl_zero`, w.`pct_pnl_pos`, w.`pct_pnl_neg`, w.`n_exact_refund`, w.`n_payout_zero`, w.`payout_over_stake`, w.`verdict_state`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        'bet28'                                                                  AS field_name,             -- 字面取值：受检字段名（★ 低基数未检字段）
        COALESCE(NULLIF(TRIM(bet28), ''), '(空)')                                AS field_value,            -- 条件取值：字段取值，空另列
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：〔闸②〕该取值之行数
        ROUND(COUNT(*) * 100.0 / SUM(COUNT(*)) OVER (), 6)                     AS pct_of_field,           -- 比率表达式：〔闸②〕占该字段母体之比
        SUM(CASE WHEN NULLIF(TRIM(bet28), '') IS NULL THEN 1 ELSE 0 END)         AS n_null,                 -- 条件计数：〔闸②〕空值数
        COUNT(DISTINCT TRIM(bet09))                                            AS n_bet09,                -- 计数表达式：〔闸③〕涉及注型数
        COUNT(DISTINCT dt)                                                     AS n_dates,                -- 计数表达式：〔闸⑤〕涉及天数
        MIN(dt)                                                                AS min_dt,                 -- 聚合取值：〔闸⑤〕首现日
        MAX(dt)                                                                AS max_dt,                 -- 聚合取值：〔闸⑤〕末现日
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_pnl_zero, -- 条件计数：〔闸④〕净输赢为零
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_pnl_pos,  -- 条件计数：〔闸④〕会员输
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) AS n_pnl_neg,  -- 条件计数：〔闸④〕会员赢
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_pnl_zero,           -- 比率表达式：〔闸①〕零输赢纯度
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_pnl_pos,            -- 比率表达式：〔闸①〕会员输纯度
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_pnl_neg,            -- 比率表达式：〔闸①〕会员赢纯度
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                   = CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8))
                  AND CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_exact_refund, -- 条件计数：〔闸④〕原额退还
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0
                  AND CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_payout_zero, -- 条件计数：〔闸④〕派彩为零而本金为正
        ROUND(SUM(CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)))
              / NULLIF(SUM(CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))), 0), 6) AS payout_over_stake,  -- 比率表达式：〔闸④〕派彩本金比
        CASE                                                                                              -- 条件分支：起始分支——四态判词（★ 五闸全过 ≠ 语义已证）
          WHEN COUNT(*) < 1000
            THEN 'UNRESOLVED —— 样本不足千笔，无判别力'                                                    -- 分支判定：样本不足
          WHEN COUNT(*) = SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END)
            THEN 'STATE_CANDIDATE —— pnl 恒零，纯度 100%（★ 语义仍待证）'                                   -- 分支判定：恒零纯态
          WHEN COUNT(*) = SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END)
            THEN 'STATE_CANDIDATE —— pnl 恒正，纯度 100%（★ 语义仍待证）'                                   -- 分支判定：恒正纯态
          WHEN COUNT(*) = SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END)
            THEN 'STATE_CANDIDATE —— pnl 恒负，纯度 100%（★ 语义仍待证）'                                   -- 分支判定：恒负纯态
          WHEN GREATEST(
                 SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                            - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END),
                 SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                            - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END),
                 SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                            - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END)
               ) * 100.0 / NULLIF(COUNT(*), 0) >= 95
            THEN 'WEAK_CANDIDATE —— 单态占比逾 95% 而未达 100%，须查残余'                                    -- 分支判定：近纯态
          ELSE 'NON_STATE —— 三态混杂，该值非结算态之判别'                                                  -- 分支判定：混杂
        END                                                                    AS verdict_state           -- 条件分支：收束上方判词，产出「verdict_state」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
        AND TRIM(category) = '1'                                                                          -- 并列条件：只看 QSB 候选宇宙
      GROUP BY COALESCE(NULLIF(TRIM(bet28), ''), '(空)')                                                    -- 分组：按该字段取值汇总
      
      UNION ALL                                                                                           -- 集合运算：并接下一受检字段之同构结果
      
      SELECT                                                                                              -- 取列：起始取列子句
        'bet29'                                                                  AS field_name,             -- 字面取值：受检字段名（★ 低基数未检字段）
        COALESCE(NULLIF(TRIM(bet29), ''), '(空)')                                AS field_value,            -- 条件取值：字段取值，空另列
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：〔闸②〕该取值之行数
        ROUND(COUNT(*) * 100.0 / SUM(COUNT(*)) OVER (), 6)                     AS pct_of_field,           -- 比率表达式：〔闸②〕占该字段母体之比
        SUM(CASE WHEN NULLIF(TRIM(bet29), '') IS NULL THEN 1 ELSE 0 END)         AS n_null,                 -- 条件计数：〔闸②〕空值数
        COUNT(DISTINCT TRIM(bet09))                                            AS n_bet09,                -- 计数表达式：〔闸③〕涉及注型数
        COUNT(DISTINCT dt)                                                     AS n_dates,                -- 计数表达式：〔闸⑤〕涉及天数
        MIN(dt)                                                                AS min_dt,                 -- 聚合取值：〔闸⑤〕首现日
        MAX(dt)                                                                AS max_dt,                 -- 聚合取值：〔闸⑤〕末现日
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_pnl_zero, -- 条件计数：〔闸④〕净输赢为零
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_pnl_pos,  -- 条件计数：〔闸④〕会员输
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) AS n_pnl_neg,  -- 条件计数：〔闸④〕会员赢
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_pnl_zero,           -- 比率表达式：〔闸①〕零输赢纯度
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_pnl_pos,            -- 比率表达式：〔闸①〕会员输纯度
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_pnl_neg,            -- 比率表达式：〔闸①〕会员赢纯度
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                   = CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8))
                  AND CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_exact_refund, -- 条件计数：〔闸④〕原额退还
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0
                  AND CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_payout_zero, -- 条件计数：〔闸④〕派彩为零而本金为正
        ROUND(SUM(CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)))
              / NULLIF(SUM(CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))), 0), 6) AS payout_over_stake,  -- 比率表达式：〔闸④〕派彩本金比
        CASE                                                                                              -- 条件分支：起始分支——四态判词（★ 五闸全过 ≠ 语义已证）
          WHEN COUNT(*) < 1000
            THEN 'UNRESOLVED —— 样本不足千笔，无判别力'                                                    -- 分支判定：样本不足
          WHEN COUNT(*) = SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END)
            THEN 'STATE_CANDIDATE —— pnl 恒零，纯度 100%（★ 语义仍待证）'                                   -- 分支判定：恒零纯态
          WHEN COUNT(*) = SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END)
            THEN 'STATE_CANDIDATE —— pnl 恒正，纯度 100%（★ 语义仍待证）'                                   -- 分支判定：恒正纯态
          WHEN COUNT(*) = SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END)
            THEN 'STATE_CANDIDATE —— pnl 恒负，纯度 100%（★ 语义仍待证）'                                   -- 分支判定：恒负纯态
          WHEN GREATEST(
                 SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                            - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END),
                 SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                            - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END),
                 SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                            - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END)
               ) * 100.0 / NULLIF(COUNT(*), 0) >= 95
            THEN 'WEAK_CANDIDATE —— 单态占比逾 95% 而未达 100%，须查残余'                                    -- 分支判定：近纯态
          ELSE 'NON_STATE —— 三态混杂，该值非结算态之判别'                                                  -- 分支判定：混杂
        END                                                                    AS verdict_state           -- 条件分支：收束上方判词，产出「verdict_state」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
        AND TRIM(category) = '1'                                                                          -- 并列条件：只看 QSB 候选宇宙
      GROUP BY COALESCE(NULLIF(TRIM(bet29), ''), '(空)')                                                    -- 分组：按该字段取值汇总
      
      UNION ALL                                                                                           -- 集合运算：并接下一受检字段之同构结果
      
      SELECT                                                                                              -- 取列：起始取列子句
        'bet30'                                                                  AS field_name,             -- 字面取值：受检字段名（★ 低基数未检字段）
        COALESCE(NULLIF(TRIM(bet30), ''), '(空)')                                AS field_value,            -- 条件取值：字段取值，空另列
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：〔闸②〕该取值之行数
        ROUND(COUNT(*) * 100.0 / SUM(COUNT(*)) OVER (), 6)                     AS pct_of_field,           -- 比率表达式：〔闸②〕占该字段母体之比
        SUM(CASE WHEN NULLIF(TRIM(bet30), '') IS NULL THEN 1 ELSE 0 END)         AS n_null,                 -- 条件计数：〔闸②〕空值数
        COUNT(DISTINCT TRIM(bet09))                                            AS n_bet09,                -- 计数表达式：〔闸③〕涉及注型数
        COUNT(DISTINCT dt)                                                     AS n_dates,                -- 计数表达式：〔闸⑤〕涉及天数
        MIN(dt)                                                                AS min_dt,                 -- 聚合取值：〔闸⑤〕首现日
        MAX(dt)                                                                AS max_dt,                 -- 聚合取值：〔闸⑤〕末现日
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_pnl_zero, -- 条件计数：〔闸④〕净输赢为零
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_pnl_pos,  -- 条件计数：〔闸④〕会员输
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) AS n_pnl_neg,  -- 条件计数：〔闸④〕会员赢
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_pnl_zero,           -- 比率表达式：〔闸①〕零输赢纯度
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_pnl_pos,            -- 比率表达式：〔闸①〕会员输纯度
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_pnl_neg,            -- 比率表达式：〔闸①〕会员赢纯度
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                   = CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8))
                  AND CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_exact_refund, -- 条件计数：〔闸④〕原额退还
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0
                  AND CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_payout_zero, -- 条件计数：〔闸④〕派彩为零而本金为正
        ROUND(SUM(CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)))
              / NULLIF(SUM(CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))), 0), 6) AS payout_over_stake,  -- 比率表达式：〔闸④〕派彩本金比
        CASE                                                                                              -- 条件分支：起始分支——四态判词（★ 五闸全过 ≠ 语义已证）
          WHEN COUNT(*) < 1000
            THEN 'UNRESOLVED —— 样本不足千笔，无判别力'                                                    -- 分支判定：样本不足
          WHEN COUNT(*) = SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END)
            THEN 'STATE_CANDIDATE —— pnl 恒零，纯度 100%（★ 语义仍待证）'                                   -- 分支判定：恒零纯态
          WHEN COUNT(*) = SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END)
            THEN 'STATE_CANDIDATE —— pnl 恒正，纯度 100%（★ 语义仍待证）'                                   -- 分支判定：恒正纯态
          WHEN COUNT(*) = SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END)
            THEN 'STATE_CANDIDATE —— pnl 恒负，纯度 100%（★ 语义仍待证）'                                   -- 分支判定：恒负纯态
          WHEN GREATEST(
                 SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                            - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END),
                 SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                            - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END),
                 SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                            - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END)
               ) * 100.0 / NULLIF(COUNT(*), 0) >= 95
            THEN 'WEAK_CANDIDATE —— 单态占比逾 95% 而未达 100%，须查残余'                                    -- 分支判定：近纯态
          ELSE 'NON_STATE —— 三态混杂，该值非结算态之判别'                                                  -- 分支判定：混杂
        END                                                                    AS verdict_state           -- 条件分支：收束上方判词，产出「verdict_state」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
        AND TRIM(category) = '1'                                                                          -- 并列条件：只看 QSB 候选宇宙
      GROUP BY COALESCE(NULLIF(TRIM(bet30), ''), '(空)')                                                    -- 分组：按该字段取值汇总
      
      UNION ALL                                                                                           -- 集合运算：并接下一受检字段之同构结果
      
      SELECT                                                                                              -- 取列：起始取列子句
        'bet31'                                                                  AS field_name,             -- 字面取值：受检字段名（★ 低基数未检字段）
        COALESCE(NULLIF(TRIM(bet31), ''), '(空)')                                AS field_value,            -- 条件取值：字段取值，空另列
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：〔闸②〕该取值之行数
        ROUND(COUNT(*) * 100.0 / SUM(COUNT(*)) OVER (), 6)                     AS pct_of_field,           -- 比率表达式：〔闸②〕占该字段母体之比
        SUM(CASE WHEN NULLIF(TRIM(bet31), '') IS NULL THEN 1 ELSE 0 END)         AS n_null,                 -- 条件计数：〔闸②〕空值数
        COUNT(DISTINCT TRIM(bet09))                                            AS n_bet09,                -- 计数表达式：〔闸③〕涉及注型数
        COUNT(DISTINCT dt)                                                     AS n_dates,                -- 计数表达式：〔闸⑤〕涉及天数
        MIN(dt)                                                                AS min_dt,                 -- 聚合取值：〔闸⑤〕首现日
        MAX(dt)                                                                AS max_dt,                 -- 聚合取值：〔闸⑤〕末现日
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_pnl_zero, -- 条件计数：〔闸④〕净输赢为零
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_pnl_pos,  -- 条件计数：〔闸④〕会员输
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) AS n_pnl_neg,  -- 条件计数：〔闸④〕会员赢
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_pnl_zero,           -- 比率表达式：〔闸①〕零输赢纯度
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_pnl_pos,            -- 比率表达式：〔闸①〕会员输纯度
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_pnl_neg,            -- 比率表达式：〔闸①〕会员赢纯度
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                   = CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8))
                  AND CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_exact_refund, -- 条件计数：〔闸④〕原额退还
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0
                  AND CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_payout_zero, -- 条件计数：〔闸④〕派彩为零而本金为正
        ROUND(SUM(CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)))
              / NULLIF(SUM(CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))), 0), 6) AS payout_over_stake,  -- 比率表达式：〔闸④〕派彩本金比
        CASE                                                                                              -- 条件分支：起始分支——四态判词（★ 五闸全过 ≠ 语义已证）
          WHEN COUNT(*) < 1000
            THEN 'UNRESOLVED —— 样本不足千笔，无判别力'                                                    -- 分支判定：样本不足
          WHEN COUNT(*) = SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END)
            THEN 'STATE_CANDIDATE —— pnl 恒零，纯度 100%（★ 语义仍待证）'                                   -- 分支判定：恒零纯态
          WHEN COUNT(*) = SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END)
            THEN 'STATE_CANDIDATE —— pnl 恒正，纯度 100%（★ 语义仍待证）'                                   -- 分支判定：恒正纯态
          WHEN COUNT(*) = SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END)
            THEN 'STATE_CANDIDATE —— pnl 恒负，纯度 100%（★ 语义仍待证）'                                   -- 分支判定：恒负纯态
          WHEN GREATEST(
                 SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                            - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END),
                 SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                            - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END),
                 SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                            - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END)
               ) * 100.0 / NULLIF(COUNT(*), 0) >= 95
            THEN 'WEAK_CANDIDATE —— 单态占比逾 95% 而未达 100%，须查残余'                                    -- 分支判定：近纯态
          ELSE 'NON_STATE —— 三态混杂，该值非结算态之判别'                                                  -- 分支判定：混杂
        END                                                                    AS verdict_state           -- 条件分支：收束上方判词，产出「verdict_state」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
        AND TRIM(category) = '1'                                                                          -- 并列条件：只看 QSB 候选宇宙
      GROUP BY COALESCE(NULLIF(TRIM(bet31), ''), '(空)')                                                    -- 分组：按该字段取值汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_freecomm,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_freecomm,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_freecomm,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_freecomm,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_freecomm,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_freecomm,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_freecomm,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_freecomm,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  62. QSB_02b_state_cross.csv   [总包行 11331~11424 · 原 9 列 ＋六层约 60 列]
--     典型学：M14 投注形态　分区：freecomm（s.x_freecomm = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`field_name`, w.`field_value`, w.`n_rows`, w.`n_pnl_zero`, w.`n_pnl_pos`, w.`n_pnl_neg`, w.`n_payout_zero`, w.`n_bet09`, w.`verdict_align`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        'bet23'                                                                AS field_name,             -- 字面取值：受检字段名
        COALESCE(NULLIF(TRIM(bet23), ''), '(空)')                              AS field_value,            -- 条件取值：字段取值，空另列
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：该取值之行数
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_pnl_zero, -- 条件计数：净输赢为零
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_pnl_pos,  -- 条件计数：会员输
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) AS n_pnl_neg,  -- 条件计数：会员赢
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END)   AS n_payout_zero, -- 条件计数：派彩为零
        COUNT(DISTINCT TRIM(bet09))                                            AS n_bet09,                -- 计数表达式：涉及注型数
        CASE                                                                                              -- 条件分支：起始分支——对齐度裁定
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                           - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) = COUNT(*)
            THEN '★ 该取值下 pnl 恒零 —— PUSH 之强候选'                                                     -- 分支判定：恒零
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                           - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) = COUNT(*)
            THEN '★ 该取值下 pnl 恒正 —— LOSS 之强候选'                                                     -- 分支判定：恒正
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                           - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) = COUNT(*)
            THEN '★ 该取值下 pnl 恒负 —— WIN 之强候选'                                                      -- 分支判定：恒负
          ELSE '◐ 三态混杂 —— 该取值非结算态之判别'                                                          -- 分支判定：混杂
        END                                                                    AS verdict_align           -- 条件分支：收束上方判词，产出「verdict_align」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
        AND TRIM(category) = '1'                                                                          -- 并列条件：只看 QSB 候选宇宙
      GROUP BY COALESCE(NULLIF(TRIM(bet23), ''), '(空)')                                                  -- 分组：按 bet23 取值汇总
      
      UNION ALL                                                                                           -- 集合运算：并接下一候选字段之同构结果
      
      SELECT                                                                                              -- 取列：起始取列子句
        'bet24'                                                                AS field_name,             -- 字面取值：受检字段名
        COALESCE(NULLIF(TRIM(bet24), ''), '(空)')                              AS field_value,            -- 条件取值：字段取值，空另列
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：该取值之行数
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_pnl_zero, -- 条件计数：净输赢为零
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_pnl_pos,  -- 条件计数：会员输
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) AS n_pnl_neg,  -- 条件计数：会员赢
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END)   AS n_payout_zero, -- 条件计数：派彩为零
        COUNT(DISTINCT TRIM(bet09))                                            AS n_bet09,                -- 计数表达式：涉及注型数
        CASE                                                                                              -- 条件分支：起始分支——对齐度裁定
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                           - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) = COUNT(*)
            THEN '★ 该取值下 pnl 恒零 —— PUSH 之强候选'                                                     -- 分支判定：恒零
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                           - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) = COUNT(*)
            THEN '★ 该取值下 pnl 恒正 —— LOSS 之强候选'                                                     -- 分支判定：恒正
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                           - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) = COUNT(*)
            THEN '★ 该取值下 pnl 恒负 —— WIN 之强候选'                                                      -- 分支判定：恒负
          ELSE '◐ 三态混杂 —— 该取值非结算态之判别'                                                          -- 分支判定：混杂
        END                                                                    AS verdict_align           -- 条件分支：收束上方判词，产出「verdict_align」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
        AND TRIM(category) = '1'                                                                          -- 并列条件：只看 QSB 候选宇宙
      GROUP BY COALESCE(NULLIF(TRIM(bet24), ''), '(空)')                                                  -- 分组：按 bet24 取值汇总
      
      UNION ALL                                                                                           -- 集合运算：并接下一候选字段之同构结果
      
      SELECT                                                                                              -- 取列：起始取列子句
        'bet40'                                                                AS field_name,             -- 字面取值：受检字段名
        COALESCE(NULLIF(TRIM(bet40), ''), '(空)')                              AS field_value,            -- 条件取值：字段取值，空另列
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：该取值之行数
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_pnl_zero, -- 条件计数：净输赢为零
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_pnl_pos,  -- 条件计数：会员输
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) AS n_pnl_neg,  -- 条件计数：会员赢
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END)   AS n_payout_zero, -- 条件计数：派彩为零
        COUNT(DISTINCT TRIM(bet09))                                            AS n_bet09,                -- 计数表达式：涉及注型数
        CASE                                                                                              -- 条件分支：起始分支——对齐度裁定
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                           - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) = COUNT(*)
            THEN '★ 该取值下 pnl 恒零 —— PUSH 之强候选'                                                     -- 分支判定：恒零
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                           - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) = COUNT(*)
            THEN '★ 该取值下 pnl 恒正 —— LOSS 之强候选'                                                     -- 分支判定：恒正
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                           - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) = COUNT(*)
            THEN '★ 该取值下 pnl 恒负 —— WIN 之强候选'                                                      -- 分支判定：恒负
          ELSE '◐ 三态混杂 —— 该取值非结算态之判别'                                                          -- 分支判定：混杂
        END                                                                    AS verdict_align           -- 条件分支：收束上方判词，产出「verdict_align」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
        AND TRIM(category) = '1'                                                                          -- 并列条件：只看 QSB 候选宇宙
      GROUP BY COALESCE(NULLIF(TRIM(bet40), ''), '(空)')                                                  -- 分组：按 bet40 取值汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_freecomm,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_freecomm,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_freecomm,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_freecomm,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_freecomm,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_freecomm,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_freecomm,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_freecomm,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  63. QSB_02c_push_by_family.csv   [总包行 11437~11489 · 原 10 列 ＋六层约 60 列]
--     典型学：M14 投注形态　分区：freecomm（s.x_freecomm = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`bet09_family`, w.`n_bet09_in_family`, w.`n_rows`, w.`n_pnl_zero`, w.`pct_pnl_zero`, w.`n_exact_refund`, w.`n_payout_zero_stake_pos`, w.`pct_player_win`, w.`mu0_pct`, w.`verdict_family`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_freecomm = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_freecomm = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        CASE                                                                                              -- 条件分支：起始分支——注型分族
          WHEN TRIM(bet09) IN ('Banker', 'Player')                        THEN 'F1_庄闲主注'               -- 分支判定：主注族
          WHEN TRIM(bet09) = 'Tie'                                        THEN 'F2_和局'                   -- 分支判定：和局
          WHEN TRIM(bet09) IN ('BPair', 'PPair', 'AnyPair', 'PerfectPair') THEN 'F3_对子族'                 -- 分支判定：对子族
          WHEN TRIM(bet09) IN ('BankerNatural', 'PlayerNatural')          THEN 'F4_例牌族'                  -- 分支判定：例牌族
          WHEN TRIM(bet09) IN ('BankerDragonBonus', 'PlayerDragonBonus')  THEN 'F5_龙宝族'                  -- 分支判定：龙宝族
          WHEN TRIM(bet09) IN ('Super6', 'Lucky7', 'SuperLucky7')         THEN 'F6_幸运族'                  -- 分支判定：幸运族
          WHEN TRIM(bet09) IN ('Big', 'Small')                            THEN 'F7_大小'                    -- 分支判定：大小
          ELSE 'F8_其他与新上线'                                                                            -- 分支判定：其余
        END                                                                    AS bet09_family,           -- 条件分支：收束上方分族，产出「bet09_family」
        COUNT(DISTINCT TRIM(bet09))                                            AS n_bet09_in_family,      -- 计数表达式：族内注型数
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：族内行数
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                     - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_pnl_zero, -- 条件计数：净输赢为零
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_pnl_zero,           -- 比率表达式：★ 该族之零输赢率
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                   = CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8))
                  AND CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_exact_refund, -- 条件计数：★ 原额退还
        SUM(CASE WHEN CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0
                  AND CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_payout_zero_stake_pos, -- 条件计数：★ 派彩为零而本金为正——全输
        ROUND(SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS pct_player_win,         -- 比率表达式：会员赢之占比
        ROUND(-1 * SUM(CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                      - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8))) * 100.0
              / NULLIF(SUM(CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))), 0), 6) AS mu0_pct,            -- 比率表达式：★ 该族之 μ0——与 push 率并读
        CASE                                                                                              -- 条件分支：起始分支——机制线索
          WHEN SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          - CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END)
             = SUM(CASE WHEN CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))
                          = CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8))
                         AND CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END)
            THEN '★ 零输赢者 100% 系原额退还 —— 形态一致（★ 惟机制仍须上游确认）'                              -- 分支判定：形态一致
          ELSE '❌ 零输赢者非全为原额退还 —— 该族另有他因，须逐笔查'                                            -- 分支判定：形态不一
        END                                                                    AS verdict_family          -- 条件分支：收束上方判词，产出「verdict_family」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
        AND TRIM(category) = '1'                                                                          -- 并列条件：只看 QSB 候选宇宙
      GROUP BY CASE                                                                                       -- 分组：按上方同一分族表达式汇总
          WHEN TRIM(bet09) IN ('Banker', 'Player')                        THEN 'F1_庄闲主注'
          WHEN TRIM(bet09) = 'Tie'                                        THEN 'F2_和局'
          WHEN TRIM(bet09) IN ('BPair', 'PPair', 'AnyPair', 'PerfectPair') THEN 'F3_对子族'
          WHEN TRIM(bet09) IN ('BankerNatural', 'PlayerNatural')          THEN 'F4_例牌族'
          WHEN TRIM(bet09) IN ('BankerDragonBonus', 'PlayerDragonBonus')  THEN 'F5_龙宝族'
          WHEN TRIM(bet09) IN ('Super6', 'Lucky7', 'SuperLucky7')         THEN 'F6_幸运族'
          WHEN TRIM(bet09) IN ('Big', 'Small')                            THEN 'F7_大小'
          ELSE 'F8_其他与新上线'
        END                                                                                               -- 续行：收束上方分组表达式
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_freecomm,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_freecomm,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_freecomm,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_freecomm,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_freecomm,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_freecomm,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_freecomm,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_freecomm,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  64. R01_late_shoe.csv   [总包行 3463~3539 · 原 15 列 ＋六层约 60 列]
--     典型学：M03 尾段投注　分区：late（s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80）　连接：q.`uid`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`uid`, w.`n_orders_all`, w.`n_orders_late`, w.`late_def`, w.`late_share`, w.`shoe_pos_p50`, w.`stake_late_avg`, w.`stake_early_avg`, w.`stake_ramp`, w.`roi_late`, w.`roi_early`, w.`stake_late`, w.`game_pnl_late`, w.`stake_all`, w.`game_pnl_all`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_sp AS (SELECT x_table, x_shoe, MAX(x_rno) AS max_rno FROM x_bs GROUP BY x_table, x_shoe),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    JOIN x_sp p ON p.x_table = s.x_table AND p.x_shoe = s.x_shoe
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH test_agents AS (                                                                               -- 公共表表达式：开启中间结果集 test_agents，其后各行为其定义体（§R01）
        SELECT age001 AS agent_id                                                                         -- 取列：产出「agent_id」
        FROM ods_mariadb_2b.ods_a168_agent                                                                -- 取数来源：取自代理主档（五级代理线归属来源）
        WHERE age022 = '1'                                  -- 铁律④b：公司测试线
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01）
      ranked AS (                                            -- 需求 §3.2：同单号保留最新版本
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet11, b.bet13,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet11, b.bet13，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet14, b.bet18, b.bet19, b.bet20, b.bet21,                                               -- 续行：接续上一取列子句，续列 b.bet14, b.bet18, b.bet19, b.bet20, b.bet21，涉 bet14（派彩金额）、bet18（一级代理线）、bet19（二级代理线）
               b.bet22, b.bet38, b.bet39, b.category,                                                     -- 续行：接续上一取列子句，续列 b.bet22, b.bet38, b.bet39, b.category，涉 bet22（五级代理线）、bet38（重對）、bet39（桌号）
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                 -- ★ 表名开关
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'，涉 dt（营业日）
          AND b.bet02 = '101'                                                                             -- 并列条件：限定 b.bet02等于 '101'，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01）
      base AS (                                                                                           -- 公共表表达式：开启中间结果集 base，其后各行为其定义体（§R01）
        SELECT r.bet01 AS bet_id, r.bet05 AS member_id,                                                   -- 取列：起始取列子句，本行先取「member_id」，涉 bet05（会员号）、member_id（会员号）
               r.bet03 AS shoe_id, r.bet39 AS table_id,                                                   -- 取值表达式：取用 bet03（靴号）、bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(r.bet04),'') AS INT)           AS round_no,                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「round_no」
               CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) AS fx,                                     -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「fx」
               CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4)) AS stake_raw,                              -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake_raw」
               CAST(NULLIF(TRIM(r.bet14),'') AS DECIMAL(20,4)) AS payout_raw                              -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「payout_raw」
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        /* StarRocks 不支持 EXISTS + 多列 IN，测试线剔除改五路 LEFT JOIN */
        LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet18（一级代理线）
        LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet19（二级代理线）
        LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet20（三级代理线）
        LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet21（四级代理线）
        LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet22（五级代理线）
        WHERE r.rn = 1                                                                                    -- 过滤条件：限定 r.rn等于 1
          AND r.category = '1'                                                                            -- 并列条件：限定 r.category等于 '1'
          AND UPPER(TRIM(r.bet38)) = 'N'                                                                  -- 并列条件：限定 UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND CAST(NULLIF(TRIM(r.bet04),'') AS INT) > 0                                                   -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet04),'') AS INT)大于 0，涉 bet04（局内序号）
          AND COALESCE(t1.agent_id,t2.agent_id,t3.agent_id,t4.agent_id,t5.agent_id) IS NULL               -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01）
      shoe_len AS (          /* ★ 命名警示：本 CTE 名为 shoe_len（靴长）系历史沿用，其所算 max_round = MAX(round_no) 为『该靴该桌观测到的最大局号』，非 COUNT(round_no) 之局数；二者仅在局号自1起连续无缺口时相等。选 MAX 为刻意设计：对中间缺口免疫，COUNT 会因清洗剔单而使分母缩水。★ B-01 斧正（2026-08-12）：定标基准逐靴现算，绝对局号不是固定靴位 */   -- 缺陷一修正：定标基准不定，固定 50 不是固定位置
        SELECT shoe_id, table_id, MAX(round_no) AS max_round                                              -- 取列：取最大局号（≠COUNT 局数），产出「max_round」
        FROM base GROUP BY shoe_id, table_id                                                              -- 取数来源：取自本条自建的中间结果集 base
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01）
      enriched AS (                                                                                       -- 公共表表达式：开启中间结果集 enriched，其后各行为其定义体（§R01）
        SELECT b.member_id, b.shoe_id, b.table_id, b.round_no, s.max_round,                               -- 取列：起始取列子句，本行先列 b.member_id, b.shoe_id, b.table_id, b.round_no, s.max_round，涉 member_id（会员号）
               b.round_no * 1.0 / NULLIF(s.max_round,0)     AS shoe_pos,                                  -- 取值表达式：产出「shoe_pos」
               b.stake_raw / b.fx                           AS stake,                                     -- 取值表达式：取用 stake（下注额（经汇率归一化）），产出「stake」
               (b.payout_raw - b.stake_raw) / b.fx          AS game_pnl,                                  -- 续行：取用 game_pnl（游戏净输赢），产出「game_pnl」
               CASE WHEN b.round_no * 1.0 / NULLIF(s.max_round,0) >= 0.80
                    THEN 1 ELSE 0 END                        AS is_late   -- B-01 斧正：相对靴位                                    -- 条件分支：产出「is_late」
        FROM base b                                                                                       -- 取数来源：取自本条自建的中间结果集 base
        JOIN shoe_len s ON s.shoe_id = b.shoe_id AND s.table_id = b.table_id                              -- 连接：取自本条自建的中间结果集 shoe_len，连接键为 s.shoe_id = b.shoe_id AND s.table_id = b.table_id
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§R01）
      SELECT                                                                                              -- 续行：接续上一取列子句，续列 SELECT
        member_id                                                     AS uid,                             -- 取值表达式：取用 member_id（会员号）、uid（会员号），产出「uid」
        COUNT(*)                                                      AS n_orders_all,                    -- 计数表达式：产出「n_orders_all」
        SUM(is_late)                                                  AS n_orders_late,                   -- 汇总表达式：产出「n_orders_late」
        'shoe_pos>=0.80'                                              AS late_def,        -- 口径锁：definition_version = v2
        SUM(is_late) * 1.0 / COUNT(*)                                 AS late_share,                      -- 汇总表达式：计数，取用 late_share（晚注占比），产出「late_share」
        PERCENTILE_APPROX(CASE WHEN is_late=1 THEN shoe_pos END, 0.5) AS shoe_pos_p50,                    -- 取近似分位数表达式：产出「shoe_pos_p50」
        SUM(CASE WHEN is_late=1 THEN stake END) / NULLIF(SUM(is_late),0)              AS stake_late_avg,  -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake_late_avg」
        SUM(CASE WHEN is_late=0 THEN stake END) / NULLIF(COUNT(*)-SUM(is_late),0)     AS stake_early_avg,  -- 汇总表达式：早段注额合计除以早段注单数 ＝ 早段均注，与 GGR 无涉，产出「stake_early_avg」
        (SUM(CASE WHEN is_late=1 THEN stake END) / NULLIF(SUM(is_late),0))                                -- 续行：汇总，取用 stake（下注额（经汇率归一化））
          / NULLIF(SUM(CASE WHEN is_late=0 THEN stake END)                                                -- 除法或乘法计算：汇总，取用 stake（下注额（经汇率归一化））
                   / NULLIF(COUNT(*)-SUM(is_late),0), 0)                              AS stake_ramp,      -- 除法计算：晚段均注除以早段均注 ＝ 注额斜率，与 GGR 无涉，产出「stake_ramp」
        SUM(CASE WHEN is_late=1 THEN game_pnl END)                                                        -- 汇总表达式：取用 game_pnl（游戏净输赢）
          / NULLIF(SUM(CASE WHEN is_late=1 THEN stake END),0)                         AS roi_late,        -- 除法或乘法计算：汇总，取用 stake（下注额（经汇率归一化）），产出「roi_late」
        SUM(CASE WHEN is_late=0 THEN game_pnl END)                                                        -- 汇总表达式：取用 game_pnl（游戏净输赢）
          / NULLIF(SUM(CASE WHEN is_late=0 THEN stake END),0)                         AS roi_early,       -- 除法或乘法计算：汇总，取用 stake（下注额（经汇率归一化）），产出「roi_early」
        SUM(CASE WHEN is_late=1 THEN stake END)                                       AS stake_late,      -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake_late」
        SUM(CASE WHEN is_late=1 THEN game_pnl END)                                    AS game_pnl_late,   -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「game_pnl_late」
        SUM(stake)                                                                    AS stake_all,       -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake_all」
        SUM(game_pnl)                                                                 AS game_pnl_all     -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「game_pnl_all」
      FROM enriched                                                                                       -- 取数来源：取自本条自建的中间结果集 enriched
      GROUP BY member_id                                                                                  -- 分组：按 member_id 汇总
      HAVING COUNT(*) >= 100                                    -- 需求：订单数据 > 100 笔
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_late_xagg,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_late,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_late,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_late,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_late,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_late,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_late,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_late,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp_xagg,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`uid` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  65. R01_late_shoe_w2.csv   [总包行 10048~10124 · 原 15 列 ＋六层约 60 列]
--     典型学：M03 尾段投注　分区：late（s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80）　连接：q.`uid`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`uid`, w.`n_orders_all`, w.`n_orders_late`, w.`late_def`, w.`late_share`, w.`shoe_pos_p50`, w.`stake_late_avg`, w.`stake_early_avg`, w.`stake_ramp`, w.`roi_late`, w.`roi_early`, w.`stake_late`, w.`game_pnl_late`, w.`stake_all`, w.`game_pnl_all`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_sp AS (SELECT x_table, x_shoe, MAX(x_rno) AS max_rno FROM x_bs GROUP BY x_table, x_shoe),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    JOIN x_sp p ON p.x_table = s.x_table AND p.x_shoe = s.x_shoe
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH test_agents AS (                                                                               -- 公共表表达式：开启中间结果集 test_agents，其后各行为其定义体（§R01）
        SELECT age001 AS agent_id                                                                         -- 取列：产出「agent_id」
        FROM ods_mariadb_2b.ods_a168_agent                                                                -- 取数来源：取自代理主档（五级代理线归属来源）
        WHERE age022 = '1'                                  -- 铁律④b：公司测试线
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01）
      ranked AS (                                            -- 需求 §3.2：同单号保留最新版本
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet11, b.bet13,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet11, b.bet13，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet14, b.bet18, b.bet19, b.bet20, b.bet21,                                               -- 续行：接续上一取列子句，续列 b.bet14, b.bet18, b.bet19, b.bet20, b.bet21，涉 bet14（派彩金额）、bet18（一级代理线）、bet19（二级代理线）
               b.bet22, b.bet38, b.bet39, b.category,                                                     -- 续行：接续上一取列子句，续列 b.bet22, b.bet38, b.bet39, b.category，涉 bet22（五级代理线）、bet38（重對）、bet39（桌号）
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                 -- ★ 表名开关
        WHERE b.dt >= '2026-08-07' AND b.dt < '2026-08-20'                                                -- 过滤条件：限定 b.dt不少于 '2026-08-07'、b.dt小于 '2026-08-20'，涉 dt（营业日）——W2 紧接主窗右开端点
          AND b.bet02 = '101'                                                                             -- 并列条件：限定 b.bet02等于 '101'，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01）
      base AS (                                                                                           -- 公共表表达式：开启中间结果集 base，其后各行为其定义体（§R01）
        SELECT r.bet01 AS bet_id, r.bet05 AS member_id,                                                   -- 取列：起始取列子句，本行先取「member_id」，涉 bet05（会员号）、member_id（会员号）
               r.bet03 AS shoe_id, r.bet39 AS table_id,                                                   -- 取值表达式：取用 bet03（靴号）、bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(r.bet04),'') AS INT)           AS round_no,                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「round_no」
               CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) AS fx,                                     -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「fx」
               CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4)) AS stake_raw,                              -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake_raw」
               CAST(NULLIF(TRIM(r.bet14),'') AS DECIMAL(20,4)) AS payout_raw                              -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「payout_raw」
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        /* StarRocks 不支持 EXISTS + 多列 IN，测试线剔除改五路 LEFT JOIN */
        LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet18（一级代理线）
        LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet19（二级代理线）
        LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet20（三级代理线）
        LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet21（四级代理线）
        LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet22（五级代理线）
        WHERE r.rn = 1                                                                                    -- 过滤条件：限定 r.rn等于 1
          AND r.category = '1'                                                                            -- 并列条件：限定 r.category等于 '1'
          AND UPPER(TRIM(r.bet38)) = 'N'                                                                  -- 并列条件：限定 UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND CAST(NULLIF(TRIM(r.bet04),'') AS INT) > 0                                                   -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet04),'') AS INT)大于 0，涉 bet04（局内序号）
          AND COALESCE(t1.agent_id,t2.agent_id,t3.agent_id,t4.agent_id,t5.agent_id) IS NULL               -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01）
      shoe_len AS (          /* ★ 命名警示：本 CTE 名为 shoe_len（靴长）系历史沿用，其所算 max_round = MAX(round_no) 为『该靴该桌观测到的最大局号』，非 COUNT(round_no) 之局数；二者仅在局号自1起连续无缺口时相等。选 MAX 为刻意设计：对中间缺口免疫，COUNT 会因清洗剔单而使分母缩水。★ B-01 斧正（2026-08-12）：定标基准逐靴现算，绝对局号不是固定靴位 */   -- 缺陷一修正：定标基准不定，固定 50 不是固定位置
        SELECT shoe_id, table_id, MAX(round_no) AS max_round                                              -- 取列：取最大局号（≠COUNT 局数），产出「max_round」
        FROM base GROUP BY shoe_id, table_id                                                              -- 取数来源：取自本条自建的中间结果集 base
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01）
      enriched AS (                                                                                       -- 公共表表达式：开启中间结果集 enriched，其后各行为其定义体（§R01）
        SELECT b.member_id, b.shoe_id, b.table_id, b.round_no, s.max_round,                               -- 取列：起始取列子句，本行先列 b.member_id, b.shoe_id, b.table_id, b.round_no, s.max_round，涉 member_id（会员号）
               b.round_no * 1.0 / NULLIF(s.max_round,0)     AS shoe_pos,                                  -- 取值表达式：产出「shoe_pos」
               b.stake_raw / b.fx                           AS stake,                                     -- 取值表达式：取用 stake（下注额（经汇率归一化）），产出「stake」
               (b.payout_raw - b.stake_raw) / b.fx          AS game_pnl,                                  -- 续行：取用 game_pnl（游戏净输赢），产出「game_pnl」
               CASE WHEN b.round_no * 1.0 / NULLIF(s.max_round,0) >= 0.80
                    THEN 1 ELSE 0 END                        AS is_late   -- B-01 斧正：相对靴位                                    -- 条件分支：产出「is_late」
        FROM base b                                                                                       -- 取数来源：取自本条自建的中间结果集 base
        JOIN shoe_len s ON s.shoe_id = b.shoe_id AND s.table_id = b.table_id                              -- 连接：取自本条自建的中间结果集 shoe_len，连接键为 s.shoe_id = b.shoe_id AND s.table_id = b.table_id
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§R01）
      SELECT                                                                                              -- 续行：接续上一取列子句，续列 SELECT
        member_id                                                     AS uid,                             -- 取值表达式：取用 member_id（会员号）、uid（会员号），产出「uid」
        COUNT(*)                                                      AS n_orders_all,                    -- 计数表达式：产出「n_orders_all」
        SUM(is_late)                                                  AS n_orders_late,                   -- 汇总表达式：产出「n_orders_late」
        'shoe_pos>=0.80'                                              AS late_def,        -- 口径锁：definition_version = v2
        SUM(is_late) * 1.0 / COUNT(*)                                 AS late_share,                      -- 汇总表达式：计数，取用 late_share（晚注占比），产出「late_share」
        PERCENTILE_APPROX(CASE WHEN is_late=1 THEN shoe_pos END, 0.5) AS shoe_pos_p50,                    -- 取近似分位数表达式：产出「shoe_pos_p50」
        SUM(CASE WHEN is_late=1 THEN stake END) / NULLIF(SUM(is_late),0)              AS stake_late_avg,  -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake_late_avg」
        SUM(CASE WHEN is_late=0 THEN stake END) / NULLIF(COUNT(*)-SUM(is_late),0)     AS stake_early_avg,  -- 汇总表达式：早段注额合计除以早段注单数 ＝ 早段均注，与 GGR 无涉，产出「stake_early_avg」
        (SUM(CASE WHEN is_late=1 THEN stake END) / NULLIF(SUM(is_late),0))                                -- 续行：汇总，取用 stake（下注额（经汇率归一化））
          / NULLIF(SUM(CASE WHEN is_late=0 THEN stake END)                                                -- 除法或乘法计算：汇总，取用 stake（下注额（经汇率归一化））
                   / NULLIF(COUNT(*)-SUM(is_late),0), 0)                              AS stake_ramp,      -- 除法计算：晚段均注除以早段均注 ＝ 注额斜率，与 GGR 无涉，产出「stake_ramp」
        SUM(CASE WHEN is_late=1 THEN game_pnl END)                                                        -- 汇总表达式：取用 game_pnl（游戏净输赢）
          / NULLIF(SUM(CASE WHEN is_late=1 THEN stake END),0)                         AS roi_late,        -- 除法或乘法计算：汇总，取用 stake（下注额（经汇率归一化）），产出「roi_late」
        SUM(CASE WHEN is_late=0 THEN game_pnl END)                                                        -- 汇总表达式：取用 game_pnl（游戏净输赢）
          / NULLIF(SUM(CASE WHEN is_late=0 THEN stake END),0)                         AS roi_early,       -- 除法或乘法计算：汇总，取用 stake（下注额（经汇率归一化）），产出「roi_early」
        SUM(CASE WHEN is_late=1 THEN stake END)                                       AS stake_late,      -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake_late」
        SUM(CASE WHEN is_late=1 THEN game_pnl END)                                    AS game_pnl_late,   -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「game_pnl_late」
        SUM(stake)                                                                    AS stake_all,       -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake_all」
        SUM(game_pnl)                                                                 AS game_pnl_all     -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「game_pnl_all」
      FROM enriched                                                                                       -- 取数来源：取自本条自建的中间结果集 enriched
      GROUP BY member_id                                                                                  -- 分组：按 member_id 汇总
      HAVING COUNT(*) >= 100                                    -- 需求：订单数据 > 100 笔
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_late_xagg,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_late,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_late,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_late,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_late,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_late,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_late,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_late,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp_xagg,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`uid` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  66. R01anm_shoe_anomaly.csv   [总包行 3948~4009 · 原 11 列 ＋六层约 60 列]
--     典型学：M03 尾段投注　分区：late（s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80）　连接：q.`table_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`dt`, w.`table_id`, w.`shoe_id`, w.`max_round`, w.`min_round`, w.`rounds_with_bet`, w.`gap`, w.`n_bettors`, w.`n_bets`, w.`anomaly_type`, w.`hi_cut_used`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_sp AS (SELECT x_table, x_shoe, MAX(x_rno) AS max_rno FROM x_bs GROUP BY x_table, x_shoe),
  x_agg AS (
    SELECT s.x_table                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    JOIN x_sp p ON p.x_table = s.x_table AND p.x_shoe = s.x_shoe
    WHERE s.x_table IS NOT NULL AND TRIM(CAST(s.x_table AS STRING)) <> ''
    GROUP BY s.x_table
  ),
    q AS (
      WITH test_agents AS (                                                                               -- 公共表表达式：开启中间结果集 test_agents，其后各行为其定义体（§R01-anm）
        SELECT age001 AS agent_id                                                                         -- 取列：产出「agent_id」
        FROM ods_mariadb_2b.ods_a168_agent                                                                -- 取数来源：取自代理主档（五级代理线归属来源）
        WHERE age022 = '1'                                  -- 铁律④b：公司测试线
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01-anm）
      ranked AS (                                            -- 需求 §3.2：同单号保留最新版本
        SELECT b.bet01, b.dt, b.bet03, b.bet04, b.bet05, b.bet11,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.dt, b.bet03, b.bet04, b.bet05, b.bet11，涉 dt（营业日）、bet03（靴号）、bet04（局内序号）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18~b.bet22，涉五级代理线（测试线剔除之连接键）
               b.bet38, b.bet39, b.category,                                                              -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category，涉 bet38（重對）、bet39（桌号）、category（注单类别）
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                 -- ★ 表名开关
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'，涉 dt（营业日）
          AND b.bet02 = '101'                                                                             -- 并列条件：限定 b.bet02等于 '101'，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01-anm）
      base AS (                                                                                           -- 公共表表达式：开启中间结果集 base，其后各行为其定义体（§R01-anm）
        SELECT r.dt                                          AS dt,                                       -- 取列：起始取列子句，本行先取「dt」，涉 dt（营业日）——本族较 §R01 之唯一增列
               CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)      AS member_id,                                -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「member_id」
               COALESCE(NULLIF(TRIM(r.bet03),''), '(空)')    AS shoe_id,                                  -- 取值表达式：空串归哨兵「(空)」以免与真靴号混算，涉 bet03（靴号），产出「shoe_id」
               COALESCE(NULLIF(TRIM(r.bet39),''), '(空)')    AS table_id,                                 -- 取值表达式：空串归哨兵「(空)」以免与真桌号混算，涉 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(r.bet04),'') AS INT)         AS round_no                                  -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「round_no」
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet18（一级代理线）
        LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet19（二级代理线）
        LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet20（三级代理线）
        LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet21（四级代理线）
        LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet22（五级代理线）
        WHERE r.rn = 1                                                                                    -- 过滤条件：限定 r.rn等于 1
          AND r.category = '1'                                                                            -- 并列条件：限定 r.category等于 '1'
          AND UPPER(TRIM(r.bet38)) = 'N'                                                                  -- 并列条件：限定 UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND CAST(NULLIF(TRIM(r.bet04),'') AS INT) > 0                                                   -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet04),'') AS INT)大于 0，涉 bet04（局内序号）
          AND COALESCE(t1.agent_id,t2.agent_id,t3.agent_id,t4.agent_id,t5.agent_id) IS NULL               -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01-anm）
      shoe_new AS (          /* ★ 新粒度（(dt, table_id, shoe_id)）：所算 max_round = MAX(round_no) 为最大局号，非局数；不沿用 shoe_len 之名，见卷首命名警示横幅。★ */
        SELECT dt, table_id, shoe_id,                                                                     -- 取列：起始取列子句，本行先列 dt, table_id, shoe_id，涉 dt（营业日）、bet39（桌号）、bet03（靴号）
               MAX(round_no)                                        AS max_round,                         -- 取最大局号（≠COUNT 局数），产出「max_round」
               MIN(round_no)                                        AS min_round,                         -- 聚合表达式：最小局号——不为 1 者示首局无人下注，产出「min_round」
               COUNT(DISTINCT round_no)                             AS rounds_with_bet,                   -- 计数表达式：局内序号的去重个数——有注局数，产出「rounds_with_bet」
               MAX(round_no) - COUNT(DISTINCT round_no)             AS gap,                               -- 取值表达式：最大局号减有注局数——内部缺口，非尾部截断，产出「gap」
               COUNT(DISTINCT member_id)                            AS n_bettors,                         -- 计数表达式：会员号的去重个数，产出「n_bettors」
               COUNT(*)                                             AS n_bets                             -- 计数表达式：注单数，产出「n_bets」
        FROM base                                                                                         -- 取数来源：取自本条自建的中间结果集 base
        GROUP BY dt, table_id, shoe_id                                                                    -- 分组：按 dt, table_id, shoe_id 汇总——新粒度
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01-anm）
      cut AS (               /* 阈值由数据反解，禁硬写——本包不设普适门槛（见卷首常数集中登记之纪律）。 */
        SELECT PERCENTILE_APPROX(max_round, 0.99) * 1.5 AS hi_cut                                         -- 取近似分位数表达式：全域九九分位之 1.5 倍——超长靴之切点，产出「hi_cut」
        FROM shoe_new                                                                                     -- 取数来源：取自本条自建的中间结果集 shoe_new
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§R01-anm）
      SELECT s.dt, s.table_id, s.shoe_id,                                                                 -- 取列：起始取列子句，本行先列 s.dt, s.table_id, s.shoe_id，涉 dt（营业日）、bet39（桌号）、bet03（靴号）
             s.max_round, s.min_round, s.rounds_with_bet, s.gap,                                          -- 续行：接续上一取列子句，续列最大局号、最小局号、有注局数、内部缺口
             s.n_bettors, s.n_bets,                                                                       -- 续行：接续上一取列子句，续列下注会员数、注单数
             CASE WHEN s.max_round > c.hi_cut THEN '超长：碰撞或跨日合并可疑'                              -- 条件分支：起始分支——超长者列为碰撞候选
                  WHEN s.max_round <= 5       THEN '残靴：截断／换荷官／开收台可疑'                        -- 分支判定：残靴者列为截断候选
                  ELSE '—' END                             AS anomaly_type,                               -- 条件分支：收束上方判词，产出「anomaly_type」
             ROUND(c.hi_cut, 2)                            AS hi_cut_used                                 -- 取值表达式：本次实际所用之上界切点（随数据现算），产出「hi_cut_used」
      FROM shoe_new s                                                                                     -- 取数来源：取自本条自建的中间结果集 shoe_new
      CROSS JOIN cut c                                                                                    -- 连接：取自本条自建的中间结果集 cut，单行广播（阈值现算）
      WHERE s.max_round > c.hi_cut OR s.max_round <= 5                                                    -- 过滤条件：限定超长或残靴二者之一
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets AS n_bets_xagg, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_late,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_late,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_late,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_late,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_late,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_late,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_late,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_late,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`table_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  67. R01chk_shoe_len_by_table.csv   [总包行 3725~3855 · 原 28 列 ＋六层约 60 列]
--     典型学：M03 尾段投注　分区：late（s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80）　连接：q.`table_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`table_id`, w.`table_flag`, w.`n_shoes`, w.`n_shoes_old`, w.`n_shoes_delta`, w.`avg_max_round`, w.`avg_max_round_old`, w.`avg_maxround_inflation`, w.`sd_max_round`, w.`min_max_round`, w.`ceiling_observed`, w.`ceiling_old`, w.`p50_max_round`, w.`p90_max_round`, w.`p99_max_round`, w.`collision_flag`, w.`n_stub_shoes`, w.`avg_gap`, w.`avg_n_bettors`, w.`avg_bets_per_round`, w.`round_at_pos080`, w.`corr_len_density`, w.`corr_density_lo95`, w.`corr_density_hi95`, w.`corr_density_verdict`, w.`corr_len_bettors`, w.`corr_bettors_lo95`, w.`corr_bettors_hi95`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_sp AS (SELECT x_table, x_shoe, MAX(x_rno) AS max_rno FROM x_bs GROUP BY x_table, x_shoe),
  x_agg AS (
    SELECT s.x_table                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    JOIN x_sp p ON p.x_table = s.x_table AND p.x_shoe = s.x_shoe
    WHERE s.x_table IS NOT NULL AND TRIM(CAST(s.x_table AS STRING)) <> ''
    GROUP BY s.x_table
  ),
    q AS (
      WITH test_agents AS (                                                                               -- 公共表表达式：开启中间结果集 test_agents，其后各行为其定义体（§R01-chk）
        SELECT age001 AS agent_id                                                                         -- 取列：产出「agent_id」
        FROM ods_mariadb_2b.ods_a168_agent                                                                -- 取数来源：取自代理主档（五级代理线归属来源）
        WHERE age022 = '1'                                  -- 铁律④b：公司测试线
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01-chk）
      ranked AS (                                            -- 需求 §3.2：同单号保留最新版本
        SELECT b.bet01, b.dt, b.bet03, b.bet04, b.bet05, b.bet11,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.dt, b.bet03, b.bet04, b.bet05, b.bet11，涉 dt（营业日）、bet03（靴号）、bet04（局内序号）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18~b.bet22，涉五级代理线（测试线剔除之连接键）
               b.bet38, b.bet39, b.category,                                                              -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category，涉 bet38（重對）、bet39（桌号）、category（注单类别）
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                 -- ★ 表名开关
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'，涉 dt（营业日）
          AND b.bet02 = '101'                                                                             -- 并列条件：限定 b.bet02等于 '101'，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01-chk）
      base AS (                                                                                           -- 公共表表达式：开启中间结果集 base，其后各行为其定义体（§R01-chk）
        SELECT r.dt                                          AS dt,                                       -- 取列：起始取列子句，本行先取「dt」，涉 dt（营业日）——本族较 §R01 之唯一增列
               CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)      AS member_id,                                -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「member_id」
               COALESCE(NULLIF(TRIM(r.bet03),''), '(空)')    AS shoe_id,                                  -- 取值表达式：空串归哨兵「(空)」以免与真靴号混算，涉 bet03（靴号），产出「shoe_id」
               COALESCE(NULLIF(TRIM(r.bet39),''), '(空)')    AS table_id,                                 -- 取值表达式：空串归哨兵「(空)」以免与真桌号混算，涉 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(r.bet04),'') AS INT)         AS round_no                                  -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「round_no」
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet18（一级代理线）
        LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet19（二级代理线）
        LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet20（三级代理线）
        LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet21（四级代理线）
        LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet22（五级代理线）
        WHERE r.rn = 1                                                                                    -- 过滤条件：限定 r.rn等于 1
          AND r.category = '1'                                                                            -- 并列条件：限定 r.category等于 '1'
          AND UPPER(TRIM(r.bet38)) = 'N'                                                                  -- 并列条件：限定 UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND CAST(NULLIF(TRIM(r.bet04),'') AS INT) > 0                                                   -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet04),'') AS INT)大于 0，涉 bet04（局内序号）
          AND COALESCE(t1.agent_id,t2.agent_id,t3.agent_id,t4.agent_id,t5.agent_id) IS NULL               -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01-chk）
      shoe_new AS (          /* ★ 新粒度（(dt, table_id, shoe_id)）：靴号纵跨日复用亦不致合并。所算 max_round = MAX(round_no) 为『该靴该桌该日观测到的最大局号』，非 COUNT(round_no) 之局数；此处刻意不沿用 shoe_len 之名，见卷首命名警示横幅。★ */
        SELECT dt, table_id, shoe_id,                                                                     -- 取列：起始取列子句，本行先列 dt, table_id, shoe_id，涉 dt（营业日）、bet39（桌号）、bet03（靴号）
               MAX(round_no)                                        AS max_round,                         -- 取最大局号（≠COUNT 局数），产出「max_round」
               COUNT(DISTINCT round_no)                             AS rounds_with_bet,                   -- 计数表达式：局内序号的去重个数——有注局数，产出「rounds_with_bet」
               MAX(round_no) - COUNT(DISTINCT round_no)             AS gap,                               -- 取值表达式：最大局号减有注局数——内部缺口，非尾部截断，产出「gap」
               COUNT(DISTINCT member_id)                            AS n_bettors,                         -- 计数表达式：会员号的去重个数——桌面稀疏度之一，产出「n_bettors」
               COUNT(*)                                             AS n_bets,                            -- 计数表达式：注单数，产出「n_bets」
               COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT round_no), 0) AS bets_per_round                     -- 除法计算：每局注单数——桌面稀疏度之二，分母以 NULLIF 守门防零除，产出「bets_per_round」
        FROM base                                                                                         -- 取数来源：取自本条自建的中间结果集 base
        GROUP BY dt, table_id, shoe_id                                                                    -- 分组：按 dt, table_id, shoe_id 汇总——新粒度
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01-chk）
      shoe_old AS (          /* ★ 旧粒度（(table_id, shoe_id)）：§R01 现行口径，留作对照，其与新粒度之差即粒度污染之量。★ */
        SELECT table_id, shoe_id,                                                                         -- 取列：起始取列子句，本行先列 table_id, shoe_id，涉 bet39（桌号）、bet03（靴号）
               MAX(round_no) AS max_round_old                                                             -- 取最大局号（≠COUNT 局数），产出「max_round_old」
        FROM base                                                                                         -- 取数来源：取自本条自建的中间结果集 base
        GROUP BY table_id, shoe_id                                                                        -- 分组：按 table_id, shoe_id 汇总——旧粒度
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01-chk）
      agg AS (                                                                                            -- 公共表表达式：开启中间结果集 agg，其后各行为其定义体（§R01-chk）
        SELECT table_id,                                                                                  -- 取列：起始取列子句，本行先取「table_id」，涉 bet39（桌号）
               COUNT(*)                                     AS n_shoes,                                   -- 计数表达式：本桌之靴数（新粒度），产出「n_shoes」
               AVG(max_round)                               AS avg_max_round,                             -- 聚合表达式：最大局号均值，产出「avg_max_round」
               STDDEV_SAMP(max_round)                       AS sd_max_round,                              -- 聚合表达式：最大局号样本标准差，产出「sd_max_round」
               MIN(max_round)                               AS min_max_round,                             -- 聚合表达式：最大局号下界——残靴之证据，产出「min_max_round」
               MAX(max_round)                               AS ceiling_observed,                          -- 聚合表达式：最大局号上界——物理上限（切牌收靴）之证据，产出「ceiling_observed」
               PERCENTILE_APPROX(max_round, 0.50)           AS p50_max_round,                             -- 取近似分位数表达式：中位，产出「p50_max_round」
               PERCENTILE_APPROX(max_round, 0.90)           AS p90_max_round,                             -- 取近似分位数表达式：九十分位，产出「p90_max_round」
               PERCENTILE_APPROX(max_round, 0.99)           AS p99_max_round,                             -- 取近似分位数表达式：九九分位，产出「p99_max_round」
               AVG(gap)                                     AS avg_gap,                                   -- 聚合表达式：内部缺口均值，产出「avg_gap」
               AVG(n_bettors)                               AS avg_n_bettors,                             -- 聚合表达式：每靴下注会员数均值，产出「avg_n_bettors」
               AVG(bets_per_round)                          AS avg_bets_per_round,                        -- 聚合表达式：每局注单数均值，产出「avg_bets_per_round」
               CORR(max_round, bets_per_round)              AS r_density,                                 -- 聚合表达式：靴长与桌面密度之相关——低估假说之判别力核心，产出「r_density」
               CORR(max_round, n_bettors)                   AS r_bettors,                                 -- 聚合表达式：靴长与下注人数之相关，产出「r_bettors」
               SUM(CASE WHEN max_round <= 5 THEN 1 ELSE 0 END) AS n_stub_shoes                            -- 条件分支：最大局号不超过 5 之残靴数，产出「n_stub_shoes」
        FROM shoe_new                                                                                     -- 取数来源：取自本条自建的中间结果集 shoe_new
        GROUP BY table_id                                                                                 -- 分组：按桌号汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01-chk）
      agg_old AS (                                                                                        -- 公共表表达式：开启中间结果集 agg_old，其后各行为其定义体（§R01-chk）
        SELECT table_id,                                                                                  -- 取列：起始取列子句，本行先取「table_id」，涉 bet39（桌号）
               COUNT(*)           AS n_shoes_old,                                                         -- 计数表达式：本桌之靴数（旧粒度），产出「n_shoes_old」
               AVG(max_round_old) AS avg_max_round_old,                                                   -- 聚合表达式：旧粒度最大局号均值，产出「avg_max_round_old」
               MAX(max_round_old) AS ceiling_old                                                          -- 聚合表达式：旧粒度最大局号上界，产出「ceiling_old」
        FROM shoe_old                                                                                     -- 取数来源：取自本条自建的中间结果集 shoe_old
        GROUP BY table_id                                                                                 -- 分组：按桌号汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01-chk）
      zc AS (                /* Fisher-z 变换：z = 0.5·ln((1+r)/(1−r))，se = 1/√(n−3)；反变换 tanh(x) = (e^{2x}−1)/(e^{2x}+1)。样本量护栏不设普适门槛，由统计量本身反解：n ≤ 3 者 z 无定义，|r| → 1 者对数发散，二者一律归 NULL 照登，禁以点估计代 CI 下结论。 */
        SELECT a.*,                                                                                       -- 取列：承接上一子句，取 agg 全列
               CASE WHEN a.n_shoes > 3 AND ABS(a.r_density) < 0.999999                                    -- 分支判定：样本量大于 3 且相关系数未贴边者方可变换
                    THEN 0.5 * LN((1 + a.r_density) / (1 - a.r_density)) END AS z_d,                      -- 条件分支：密度相关之 Fisher-z，产出「z_d」
               CASE WHEN a.n_shoes > 3 AND ABS(a.r_bettors) < 0.999999                                    -- 分支判定：样本量大于 3 且相关系数未贴边者方可变换
                    THEN 0.5 * LN((1 + a.r_bettors) / (1 - a.r_bettors)) END AS z_b,                      -- 条件分支：人数相关之 Fisher-z，产出「z_b」
               CASE WHEN a.n_shoes > 3                                                                    -- 分支判定：样本量大于 3 者方有标准误
                    THEN 1.959964 / SQRT(a.n_shoes - 3) END                  AS hw                        -- 条件分支：95% 区间半宽（z=1.959964 双侧），产出「hw」
        FROM agg a                                                                                        -- 取数来源：取自本条自建的中间结果集 agg
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§R01-chk）
      SELECT                                                                                              -- 取列：起始取列子句
        z.table_id,                                                                                       -- 取列：涉 bet39（桌号）
        CASE WHEN z.table_id = '(空)' THEN '哨兵（禁并入正常桌）' ELSE '正常' END      AS table_flag,      -- 条件分支：哨兵标记——空串桌号单列，禁与正常桌混算，产出「table_flag」
        z.n_shoes,                                                                                        -- 取列：本桌靴数（新粒度）
        o.n_shoes_old,                                                                                    -- 取列：本桌靴数（旧粒度）
        z.n_shoes - o.n_shoes_old                                    AS n_shoes_delta,                    -- 取值表达式：新旧粒度靴数之差——大于 0 即证靴号跨日复用，产出「n_shoes_delta」
        ROUND(z.avg_max_round, 4)                                    AS avg_max_round,                    -- 聚合表达式：最大局号均值（新粒度），产出「avg_max_round」
        ROUND(o.avg_max_round_old, 4)                                AS avg_max_round_old,                -- 聚合表达式：最大局号均值（旧粒度），产出「avg_max_round_old」
        ROUND(o.avg_max_round_old - z.avg_max_round, 4)              AS avg_maxround_inflation,           -- 取值表达式：旧粒度较新粒度夸大之量，产出「avg_maxround_inflation」
        ROUND(z.sd_max_round, 4)                                     AS sd_max_round,                     -- 聚合表达式：最大局号样本标准差，产出「sd_max_round」
        z.min_max_round,                                                                                  -- 取列：最大局号下界
        z.ceiling_observed,                                                                               -- 取列：最大局号上界（新粒度）
        o.ceiling_old,                                                                                    -- 取列：最大局号上界（旧粒度）
        ROUND(z.p50_max_round, 2)                                    AS p50_max_round,                    -- 取近似分位数表达式：中位，产出「p50_max_round」
        ROUND(z.p90_max_round, 2)                                    AS p90_max_round,                    -- 取近似分位数表达式：九十分位，产出「p90_max_round」
        ROUND(z.p99_max_round, 2)                                    AS p99_max_round,                    -- 取近似分位数表达式：九九分位，产出「p99_max_round」
        CASE WHEN z.ceiling_observed > z.p99_max_round * 1.5                                              -- 分支判定：上界逾九九分位之 1.5 倍者
             THEN '碰撞可疑' ELSE '—' END                            AS collision_flag,                   -- 条件分支：靴号碰撞或跨日合并之指纹，产出「collision_flag」
        z.n_stub_shoes,                                                                                   -- 取列：残靴数（最大局号不超过 5）
        ROUND(z.avg_gap, 4)                                          AS avg_gap,                          -- 聚合表达式：内部缺口均值，产出「avg_gap」
        ROUND(z.avg_n_bettors, 4)                                    AS avg_n_bettors,                    -- 聚合表达式：每靴下注会员数均值，产出「avg_n_bettors」
        ROUND(z.avg_bets_per_round, 4)                               AS avg_bets_per_round,               -- 聚合表达式：每局注单数均值，产出「avg_bets_per_round」
        ROUND(0.80 * z.avg_max_round, 2)                             AS round_at_pos080,                  -- 取值表达式：本桌 shoe_pos>=0.80（LATE_DEF）对应之起始局号——跨桌可比性之直接刻度，产出「round_at_pos080」
        ROUND(z.r_density, 6)                                        AS corr_len_density,                 -- 聚合表达式：靴长与桌面密度之相关点估计，产出「corr_len_density」
        ROUND((EXP(2*(z.z_d - z.hw)) - 1) / (EXP(2*(z.z_d - z.hw)) + 1), 6) AS corr_density_lo95,         -- 取值表达式：密度相关之 95% 下界（Fisher-z 反变换），产出「corr_density_lo95」
        ROUND((EXP(2*(z.z_d + z.hw)) - 1) / (EXP(2*(z.z_d + z.hw)) + 1), 6) AS corr_density_hi95,         -- 取值表达式：密度相关之 95% 上界（Fisher-z 反变换），产出「corr_density_hi95」
        CASE                                                                                              -- 条件分支：起始分支——密度相关之判词，四态互斥
          WHEN z.z_d IS NULL THEN '○ n 不足（须 > 3）'                                                    -- 分支判定：样本量不足者照登，禁读方向
          WHEN ((EXP(2*(z.z_d - z.hw)) - 1) / (EXP(2*(z.z_d - z.hw)) + 1)) <= 0                           -- 分支判定：下界不大于零
           AND ((EXP(2*(z.z_d + z.hw)) - 1) / (EXP(2*(z.z_d + z.hw)) + 1)) >= 0                           -- 分支判定：且上界不小于零——即区间跨零
            THEN 'CI 跨 0：方向不可读'                                                                    -- 分支判定：区间跨零者方向不可读
          WHEN ((EXP(2*(z.z_d - z.hw)) - 1) / (EXP(2*(z.z_d - z.hw)) + 1)) > 0                            -- 分支判定：下界大于零
            THEN 'CI > 0：低估假说得证'                                                                   -- 分支判定：密桌靴长——分母低估坐实
          ELSE 'CI < 0：与低估假说反向'                                                                   -- 分支判定：余者密桌反而靴短——指向切牌规程异质
        END                                                          AS corr_density_verdict,             -- 条件分支：收束上方判词，产出「corr_density_verdict」
        ROUND(z.r_bettors, 6)                                        AS corr_len_bettors,                 -- 聚合表达式：靴长与下注人数之相关点估计，产出「corr_len_bettors」
        ROUND((EXP(2*(z.z_b - z.hw)) - 1) / (EXP(2*(z.z_b - z.hw)) + 1), 6) AS corr_bettors_lo95,         -- 取值表达式：人数相关之 95% 下界（Fisher-z 反变换），产出「corr_bettors_lo95」
        ROUND((EXP(2*(z.z_b + z.hw)) - 1) / (EXP(2*(z.z_b + z.hw)) + 1), 6) AS corr_bettors_hi95          -- 取值表达式：人数相关之 95% 上界（Fisher-z 反变换），产出「corr_bettors_hi95」
      FROM zc z                                                                                           -- 取数来源：取自本条自建的中间结果集 zc
      LEFT JOIN agg_old o ON o.table_id = z.table_id                                                      -- 左连接：取自本条自建的中间结果集 agg_old，连接键为 table_id（桌号）
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_late,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_late,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_late,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_late,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_late,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_late,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_late,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_late,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`table_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  68. R01inv_shoe_len_census.csv   [总包行 3865~3940 · 原 19 列 ＋六层约 60 列]
--     典型学：M03 尾段投注　分区：late（s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`n_tables`, w.`n_shoes`, w.`mean_len`, w.`sd_len`, w.`p0`, w.`p01`, w.`p25`, w.`p50`, w.`p75`, w.`p90`, w.`p99`, w.`p100`, w.`n_len_ge_70`, w.`n_len_le_5`, w.`pct_with_gap`, w.`avg_gap`, w.`corr_cross_table`, w.`round_at_080_shortest`, w.`round_at_080_longest`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_sp AS (SELECT x_table, x_shoe, MAX(x_rno) AS max_rno FROM x_bs GROUP BY x_table, x_shoe),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rno*1.0/NULLIF(p.max_rno,0) >= 0.80) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    JOIN x_sp p ON p.x_table = s.x_table AND p.x_shoe = s.x_shoe
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH test_agents AS (                                                                               -- 公共表表达式：开启中间结果集 test_agents，其后各行为其定义体（§R01-inv）
        SELECT age001 AS agent_id                                                                         -- 取列：产出「agent_id」
        FROM ods_mariadb_2b.ods_a168_agent                                                                -- 取数来源：取自代理主档（五级代理线归属来源）
        WHERE age022 = '1'                                  -- 铁律④b：公司测试线
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01-inv）
      ranked AS (                                            -- 需求 §3.2：同单号保留最新版本
        SELECT b.bet01, b.dt, b.bet03, b.bet04, b.bet05, b.bet11,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.dt, b.bet03, b.bet04, b.bet05, b.bet11，涉 dt（营业日）、bet03（靴号）、bet04（局内序号）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18~b.bet22，涉五级代理线（测试线剔除之连接键）
               b.bet38, b.bet39, b.category,                                                              -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category，涉 bet38（重對）、bet39（桌号）、category（注单类别）
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                 -- ★ 表名开关
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'，涉 dt（营业日）
          AND b.bet02 = '101'                                                                             -- 并列条件：限定 b.bet02等于 '101'，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01-inv）
      base AS (                                                                                           -- 公共表表达式：开启中间结果集 base，其后各行为其定义体（§R01-inv）
        SELECT r.dt                                          AS dt,                                       -- 取列：起始取列子句，本行先取「dt」，涉 dt（营业日）——本族较 §R01 之唯一增列
               CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)      AS member_id,                                -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「member_id」
               COALESCE(NULLIF(TRIM(r.bet03),''), '(空)')    AS shoe_id,                                  -- 取值表达式：空串归哨兵「(空)」以免与真靴号混算，涉 bet03（靴号），产出「shoe_id」
               COALESCE(NULLIF(TRIM(r.bet39),''), '(空)')    AS table_id,                                 -- 取值表达式：空串归哨兵「(空)」以免与真桌号混算，涉 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(r.bet04),'') AS INT)         AS round_no                                  -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「round_no」
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet18（一级代理线）
        LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet19（二级代理线）
        LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet20（三级代理线）
        LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet21（四级代理线）
        LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet22（五级代理线）
        WHERE r.rn = 1                                                                                    -- 过滤条件：限定 r.rn等于 1
          AND r.category = '1'                                                                            -- 并列条件：限定 r.category等于 '1'
          AND UPPER(TRIM(r.bet38)) = 'N'                                                                  -- 并列条件：限定 UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND CAST(NULLIF(TRIM(r.bet04),'') AS INT) > 0                                                   -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet04),'') AS INT)大于 0，涉 bet04（局内序号）
          AND COALESCE(t1.agent_id,t2.agent_id,t3.agent_id,t4.agent_id,t5.agent_id) IS NULL               -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01-inv）
      shoe_new AS (          /* ★ 新粒度（(dt, table_id, shoe_id)）：所算 max_round = MAX(round_no) 为最大局号，非局数；不沿用 shoe_len 之名，见卷首命名警示横幅。★ */
        SELECT dt, table_id, shoe_id,                                                                     -- 取列：起始取列子句，本行先列 dt, table_id, shoe_id，涉 dt（营业日）、bet39（桌号）、bet03（靴号）
               MAX(round_no)                                        AS max_round,                         -- 取最大局号（≠COUNT 局数），产出「max_round」
               COUNT(DISTINCT round_no)                             AS rounds_with_bet,                   -- 计数表达式：局内序号的去重个数——有注局数，产出「rounds_with_bet」
               MAX(round_no) - COUNT(DISTINCT round_no)             AS gap,                               -- 取值表达式：最大局号减有注局数——内部缺口，非尾部截断，产出「gap」
               COUNT(DISTINCT member_id)                            AS n_bettors,                         -- 计数表达式：会员号的去重个数，产出「n_bettors」
               COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT round_no), 0) AS bets_per_round                     -- 除法计算：每局注单数，分母以 NULLIF 守门防零除，产出「bets_per_round」
        FROM base                                                                                         -- 取数来源：取自本条自建的中间结果集 base
        GROUP BY dt, table_id, shoe_id                                                                    -- 分组：按 dt, table_id, shoe_id 汇总——新粒度
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R01-inv）
      by_table AS (               /* 跨桌相关之单位＝桌（非靴）：与 §R01-chk 之桌内相关分属两层，禁互相援引。 */
        SELECT table_id,                                                                                  -- 取列：起始取列子句，本行先取「table_id」，涉 bet39（桌号）
               COUNT(*)            AS n_shoes,                                                            -- 计数表达式：本桌靴数，产出「n_shoes」
               AVG(max_round)      AS avg_len,                                                            -- 聚合表达式：本桌最大局号均值，产出「avg_len」
               AVG(bets_per_round) AS avg_density                                                         -- 聚合表达式：本桌每局注单数均值，产出「avg_density」
        FROM shoe_new                                                                                     -- 取数来源：取自本条自建的中间结果集 shoe_new
        GROUP BY table_id                                                                                 -- 分组：按桌号汇总
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§R01-inv）
      SELECT                                                                                              -- 取列：起始取列子句
        (SELECT COUNT(*) FROM by_table)                                   AS n_tables,                         -- 计数表达式：全域桌数——供核对 §R01-chk 结果行数是否被显示上限截断，产出「n_tables」
        COUNT(*)                                                     AS n_shoes,                          -- 计数表达式：全域靴数（新粒度），产出「n_shoes」
        ROUND(AVG(max_round), 4)                                     AS mean_len,                         -- 聚合表达式：最大局号均值，产出「mean_len」
        ROUND(STDDEV_SAMP(max_round), 4)                             AS sd_len,                           -- 聚合表达式：最大局号样本标准差，产出「sd_len」
        MIN(max_round)                                               AS p0,                               -- 聚合表达式：最小值，产出「p0」
        ROUND(PERCENTILE_APPROX(max_round, 0.01), 2)                 AS p01,                              -- 取近似分位数表达式：一分位，产出「p01」
        ROUND(PERCENTILE_APPROX(max_round, 0.25), 2)                 AS p25,                              -- 取近似分位数表达式：二五分位，产出「p25」
        ROUND(PERCENTILE_APPROX(max_round, 0.50), 2)                 AS p50,                              -- 取近似分位数表达式：中位，产出「p50」
        ROUND(PERCENTILE_APPROX(max_round, 0.75), 2)                 AS p75,                              -- 取近似分位数表达式：七五分位，产出「p75」
        ROUND(PERCENTILE_APPROX(max_round, 0.90), 2)                 AS p90,                              -- 取近似分位数表达式：九十分位，产出「p90」
        ROUND(PERCENTILE_APPROX(max_round, 0.99), 2)                 AS p99,                              -- 取近似分位数表达式：九九分位，产出「p99」
        MAX(max_round)                                               AS p100,                             -- 聚合表达式：最大值，产出「p100」
        SUM(CASE WHEN max_round >= 70 THEN 1 ELSE 0 END)             AS n_len_ge_70,                      -- 条件分支：最大局号不少于 70 之靴数——深切规程之证据，产出「n_len_ge_70」
        SUM(CASE WHEN max_round <= 5  THEN 1 ELSE 0 END)             AS n_len_le_5,                       -- 条件分支：最大局号不超过 5 之残靴数，产出「n_len_le_5」
        ROUND(SUM(CASE WHEN gap > 0 THEN 1 ELSE 0 END) * 100.0                                            -- 条件分支：有内部缺口之靴数
              / NULLIF(COUNT(*), 0), 4)                              AS pct_with_gap,                     -- 除法计算：分母以 NULLIF 守门防零除，产出「pct_with_gap」
        ROUND(AVG(gap), 4)                                           AS avg_gap,                          -- 聚合表达式：内部缺口均值，产出「avg_gap」
        ROUND((SELECT CORR(avg_density, avg_len) FROM by_table), 6)       AS corr_cross_table,                 -- 聚合表达式：跨桌相关（单位＝桌）——为负即密桌反而靴短，产出「corr_cross_table」
        ROUND(0.80 * (SELECT MIN(avg_len) FROM by_table), 2)              AS round_at_080_shortest,            -- 取值表达式：最短桌之 shoe_pos>=0.80 起始局号，产出「round_at_080_shortest」
        ROUND(0.80 * (SELECT MAX(avg_len) FROM by_table), 2)              AS round_at_080_longest              -- 取值表达式：最长桌之 shoe_pos>=0.80 起始局号——与上一列之差即跨桌规程异质之刻度，产出「round_at_080_longest」
      FROM shoe_new                                                                                      -- 取数来源：取自本条自建的中间结果集 shoe_new
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_late,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_late,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_late,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_late,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_late,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_late,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_late,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_late,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  69. R02_same_table.csv   [总包行 4293~4365 · 原 12 列 ＋六层约 60 列]
--     典型学：M01 同桌团伙　分区：same（c.n_seat >= 2）　连接：q.`uid1`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`uid1`, w.`uid2`, w.`n_same_orders`, w.`rounds_1`, w.`rounds_2`, w.`same_rate`, w.`jaccard`, w.`lift`, w.`exp_same`, w.`n_tables`, w.`first_day`, w.`last_day`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_co AS (SELECT x_round, COUNT(DISTINCT x_member) AS n_seat FROM x_bs GROUP BY x_round),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN c.n_seat >= 2 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN c.n_seat >= 2 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN c.n_seat >= 2 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (c.n_seat >= 2) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (c.n_seat >= 2) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (c.n_seat >= 2) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    JOIN x_co c ON c.x_round = s.x_round
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH mr AS (                          -- 阶段一：会员 × 物理局，去重后每人每局一行
        SELECT DISTINCT                                                                                   -- 取列：承接上一子句，构成完整语句，属 §R02 之取数
               b.bet05 AS member_id,                                                                      -- 取值表达式：取用 bet05（会员号）、member_id（会员号），产出「member_id」
               CONCAT_WS('|', b.bet03, b.bet04, b.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               b.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               b.dt    AS bet_date                                                                        -- 取值表达式：取用 dt（营业日）、bet_date（营业日），产出「bet_date」
        FROM ods_mariadb_2b.ods_a168_bet02 b            -- ★ 表名开关
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'，涉 dt（营业日）
          AND b.bet02 = '101' AND b.category = '1'                                                        -- 并列条件：限定 b.bet02等于 '101'、b.category等于 '1'，涉 bet02（游戏类别）
          AND UPPER(TRIM(b.bet38)) = 'N'                                                                  -- 并列条件：限定 UPPER(TRIM(b.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(b.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(b.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R02）
      act AS (                              -- 候选池：局数不够就不可能凑到 100 局同桌
        --   ★ 门槛由 100 提至 300（降内存斧正三）：候选池是配对数的平方项底数，
        --     门槛提三倍，配对数约降一个数量级。判据本要求同桌 ≥100 笔，
        --     总局数不足 300 者几无可能构成稳定团伙对。要复原口径改回 100 即可，
        --     但须改走 §R02-1 分批模板。
        SELECT member_id, COUNT(*) AS n_rounds                                                            -- 取列：计数，取用 member_id（会员号），产出「n_rounds」
        FROM mr GROUP BY member_id HAVING COUNT(*) >= 300                                                 -- 取数来源：取自本条自建的中间结果集 mr
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R02）
      mr2 AS (                                                                                            -- 公共表表达式：开启中间结果集 mr2，其后各行为其定义体（§R02）
        SELECT m.* FROM mr m JOIN act a ON a.member_id = m.member_id                                      -- 取列：整体承接上游结果集的全部字段，不再逐列列举，取自本条自建的中间结果集 mr
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R02）
      pair AS (                             -- 阶段二：同局自连接（已压到候选池）
        SELECT x.member_id AS uid1, y.member_id AS uid2,                                                  -- 取列：起始取列子句，本行先取「uid2」，涉 member_id（会员号）
               COUNT(*)                    AS same_rounds,                                                -- 计数表达式：产出「same_rounds」
               COUNT(DISTINCT x.table_id)  AS n_tables,                                                   -- 计数表达式：统计去重个数，产出「n_tables」
               MIN(x.bet_date)             AS first_day,                                                  -- 取最小值表达式：取用 bet_date（营业日），产出「first_day」
               MAX(x.bet_date)             AS last_day                                                    -- 取最大值表达式：取用 bet_date（营业日），产出「last_day」
        FROM mr2 x                                                                                        -- 取数来源：取自本条自建的中间结果集 mr2
        JOIN mr2 y ON x.round_key = y.round_key AND x.member_id < y.member_id                             -- 连接：取自本条自建的中间结果集 mr2，连接键为 round_key（局键）、member_id（会员号）
        GROUP BY x.member_id, y.member_id                                                                 -- 分组：按 x.member_id, y.member_id 汇总
        HAVING COUNT(*) >= 100                           -- 需求：同桌订单数 > 100 笔
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R02）
      td AS (                               -- Lift 零假设的分子件：每人每桌每日局数
        SELECT table_id, bet_date, member_id, COUNT(*) AS n_md                                            -- 取列：计数，取用 member_id（会员号）、bet_date（营业日），产出「n_md」
        FROM mr2 GROUP BY table_id, bet_date, member_id                                                   -- 取数来源：取自本条自建的中间结果集 mr2
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R02）
      tot AS (                              -- Lift 零假设的分母件：每桌每日总局数
        SELECT table_id, bet_date, COUNT(DISTINCT round_key) AS n_td                                      -- 取列：统计去重个数，产出「n_td」
        FROM mr2 GROUP BY table_id, bet_date                                                              -- 取数来源：取自本条自建的中间结果集 mr2
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R02）
      expct AS (                            -- E[same] = Σ n_a(t,d)·n_b(t,d) / N(t,d)
        --   ★ 降内存斧正一（决定性）：改以 pair 为**驱动表**，只为实际存在的对算期望。
        --     原式先算「同桌同日」的全组合再 LEFT JOIN 取用——同桌同日的候选会员
        --     可达数千，配对数按其平方增长，且绝大多数对根本不在 pair 里，算了白算。
        --     今由 pair 驱动，配对数降为「pair 对数 × 其共同桌日数」。
        --     期望值的定义式一字未动，输出逐值一致。
        SELECT p.uid1, p.uid2,                                                                            -- 取列：起始取列子句，本行先列 p.uid1, p.uid2
               SUM(a.n_md * b.n_md * 1.0 / NULLIF(t.n_td,0)) AS exp_same                                  -- 汇总表达式：产出「exp_same」
        FROM pair p                                                                                       -- 取数来源：取自本条自建的中间结果集 pair
        JOIN td a  ON a.member_id = p.uid1                                                                -- 连接：取自本条自建的中间结果集 td，连接键为 member_id（会员号）
        JOIN td b  ON b.member_id = p.uid2                                                                -- 连接：取自本条自建的中间结果集 td，连接键为 member_id（会员号）
                  AND b.table_id  = a.table_id                                                            -- 并列条件：限定 b.table_id等于 a.table_id
                  AND b.bet_date  = a.bet_date                                                            -- 并列条件：限定 b.bet_date等于 a.bet_date，涉 bet_date（营业日）
        JOIN tot t ON t.table_id  = a.table_id AND t.bet_date = a.bet_date                                -- 连接：取自本条自建的中间结果集 tot，连接键为 bet_date（营业日）
        GROUP BY p.uid1, p.uid2                                                                           -- 分组：按 p.uid1, p.uid2 汇总
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§R02）
      SELECT p.uid1, p.uid2,                                                                              -- 取列：起始取列子句，本行先列 p.uid1, p.uid2
             p.same_rounds                                          AS n_same_orders,                     -- 取值表达式：产出「n_same_orders」
             a1.n_rounds                                            AS rounds_1,                          -- 取值表达式：产出「rounds_1」
             a2.n_rounds                                            AS rounds_2,                          -- 取值表达式：产出「rounds_2」
             p.same_rounds * 1.0                                                                          -- 取值表达式：比率之分子，乘 1.0 以避整数除法截断
               / NULLIF(LEAST(a1.n_rounds, a2.n_rounds),0)          AS same_rate,   -- 需求口径
             p.same_rounds * 1.0                                                                          -- 取值表达式：比率之分子，乘 1.0 以避整数除法截断
               / NULLIF(a1.n_rounds + a2.n_rounds - p.same_rounds,0) AS jaccard,    -- 对照口径
             p.same_rounds * 1.0 / NULLIF(e.exp_same, 0)            AS lift,        -- ★ 去巧合
             e.exp_same, p.n_tables, p.first_day, p.last_day                                              -- 续行：接续上一取列子句，续列 e.exp_same, p.n_tables, p.first_day, p.last_day
      FROM pair p                                                                                         -- 取数来源：取自本条自建的中间结果集 pair
      JOIN act a1 ON a1.member_id = p.uid1                                                                -- 连接：取自本条自建的中间结果集 act，连接键为 member_id（会员号）
      JOIN act a2 ON a2.member_id = p.uid2                                                                -- 连接：取自本条自建的中间结果集 act，连接键为 member_id（会员号）
      LEFT JOIN expct e ON e.uid1 = p.uid1 AND e.uid2 = p.uid2                                            -- 左连接：取自本条自建的中间结果集 expct，连接键为 e.uid1 = p.uid1 AND e.uid2 = p.uid2
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_same,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_same,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_same,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_same,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_same,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_same,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_same,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_same,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`uid1` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  70. R03_player_dealer.csv   [总包行 4502~4633 · 原 33 列 ＋六层约 60 列]
--     典型学：M07 荷官关联　分区：recent（s.x_rec = 1）　连接：q.`uid`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`uid`, w.`dealer_id`, w.`is_sentinel_dealer`, w.`stake_amount`, w.`profit_amount`, w.`net_pnl`, w.`win_rate`, w.`n_related_orders`, w.`n_rounds_eff`, w.`p_base_mix_w`, w.`p_base_mix_unw`, w.`total_bet_amount`, w.`n_bets`, w.`z_score_w`, w.`z_score_unw`, w.`z_score`, w.`delta_p`, w.`delta_z`, w.`net_pnl_all`, w.`game_pnl_all`, w.`stake_all`, w.`win_rate_all`, w.`win_rate_other`, w.`eligibility_status`, w.`eligibility_min_n`, w.`eligibility_rule`, w.`filter_stage`, w.`filter_rule_version`, w.`comparison_id`, w.`cmp_time_window`, w.`cmp_population`, w.`cmp_label`, w.`z_score_alias_status`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ranked AS (                                                                                    -- 公共表表达式：开启中间结果集 ranked，其后各行为其定义体（§R03）
        SELECT b.bet01,                                                                                   -- 取列：起始取列子句——★ 2026-08-11 提速：只投影下游真正用到的列
               b.bet03, b.bet04, b.bet05, b.bet09, b.bet11,                                               -- 续行：涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）、bet09（玩法）、bet11（汇率）
               b.bet13, b.bet14, b.bet17, b.bet38, b.bet39,                                               -- 续行：涉 bet13（下注金额）、bet14（派彩金额）、bet17（会员净输赢）、bet38（重對）、bet39（桌号）
               b.eid,                                                                                     -- 续行：涉 eid（荷官工号）——★ 已去 updatetime／sync_time／dt／bet02／category 五列（说明见段首提速注记）
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b            -- ★ 表名开关
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'，涉 dt（营业日）
          AND b.bet02 = '101' AND b.category = '1'                                                        -- 并列条件：限定 b.bet02等于 '101'、b.category等于 '1'，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03）
      base AS (                                                                                           -- 公共表表达式：开启中间结果集 base，其后各行为其定义体（§R03）
        SELECT r.bet01 AS bet_id, r.bet05 AS member_id, r.eid AS dealer_id,                               -- 取列：起始取列子句，本行先取「dealer_id」，涉 bet05（会员号）、eid（荷官工号）、member_id（会员号）
               CASE WHEN TRIM(r.eid) IN ('-1','0') THEN 1 ELSE 0 END AS is_sentinel_dealer,               -- 取值表达式：哨兵标记——★ 2026-08-11 改：由**删除**改为**标注**（事实层不删，分析层才筛）
               r.bet09 AS bet_side,                                                                       -- 取值表达式：取用 bet09（玩法），产出「bet_side」
               r.bet03, r.bet04, r.bet39,                                                                 -- 续行：★ 提速：局键三列**原样透传**，不再 CONCAT_WS 造字符串（1.25 亿行少造 1.25 亿个串）
               CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) AS fx,                                     -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「fx」
               CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4)) AS stake_raw,                              -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake_raw」
               CAST(NULLIF(TRIM(r.bet14),'') AS DECIMAL(20,4)) AS payout_raw,                             -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「payout_raw」
               CAST(NULLIF(TRIM(r.bet17),'') AS DECIMAL(20,4)) AS net_raw                                 -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_raw」
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        WHERE r.rn = 1                                                                                    -- 过滤条件：限定 r.rn等于 1
          AND UPPER(TRIM(r.bet38)) = 'N'                                                                  -- 并列条件：限定 UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.eid),'') IS NOT NULL                                                          -- 并列条件：限定该值非空，涉 eid（荷官工号）
          AND UPPER(TRIM(r.bet09)) NOT LIKE 'TIP\_1\_%'      -- 阈值报告：排除小费单
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03）
      ord AS (                                                                                            -- 公共表表达式：开启中间结果集 ord，其后各行为其定义体（§R03）
        SELECT bet_id, member_id, dealer_id, is_sentinel_dealer, bet_side, bet03, bet04, bet39,           -- 取列：起始取列子句，局键改三列透传，涉 bet03（靴号）、bet04（局内序号）、bet39（桌号）
               stake_raw / fx                AS stake,                                                    -- 取值表达式：取用 stake（下注额（经汇率归一化）），产出「stake」
               (payout_raw - stake_raw) / fx AS game_pnl,                                                 -- 续行：取用 game_pnl（游戏净输赢），产出「game_pnl」
               net_raw / fx                  AS net_pnl                                                   -- 取值表达式：取用 net_pnl（会员净输赢），产出「net_pnl」
        FROM base                                                                                         -- 取数来源：取自本条自建的中间结果集 base
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03）
      side_base AS (      -- ★ 玩法基准胜率：各投注产品各算各的（仅 23 行，连接时广播，无倾斜）
        SELECT bet_side,                                                                                  -- 取列：起始取列子句，本行先列 bet_side
               SUM(CASE WHEN game_pnl >  0 THEN 1 ELSE 0 END) * 1.0                                       -- 取值表达式：该产品赢局数；乘 1.0 以避整数除法截断
                 / NULLIF(SUM(CASE WHEN game_pnl <> 0 THEN 1 ELSE 0 END), 0) AS p_base                    -- 除法或乘法计算：除以决胜局数——退还局不入分母，产出「p_base」
        FROM ord WHERE is_sentinel_dealer = 0                                                             -- 取数来源：取自本条自建的中间结果集 ord——基准只由真实牌桌估计
        GROUP BY bet_side                                                                                 -- 分组：按投注产品汇总——结果仅 23 行
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03）
      ordb AS (             -- 注单层挂上各自产品的基准，供下方按注额加权
        SELECT o.member_id, o.dealer_id, o.is_sentinel_dealer, o.bet03, o.bet04, o.bet39, o.bet_side,     -- 取列：起始取列子句，局键三列透传
               o.stake, o.game_pnl, o.net_pnl, s.p_base AS p_side                                         -- 续行：并取三项金额与该注自身产品的基准胜率，产出「p_side」
        FROM      ord o                                                                                   -- 取数来源：取自本条自建的中间结果集 ord
        LEFT JOIN side_base s ON s.bet_side = o.bet_side                                                  -- 左连接：取自本条自建的中间结果集 side_base（23 行，广播）——基准未定义者留空，不以 0.5 顶替
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03）
      pr AS (             -- ★ 局级去重：同局多单折成一局，Z-score 才不被 √k 倍放大
        SELECT member_id, dealer_id, is_sentinel_dealer, bet03, bet04, bet39,                             -- 取列：起始取列子句，局键改**三列原生分组**——较 CONCAT 字符串分组显著更省
               SUM(stake)    AS stake,                                                                    -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake」
               SUM(game_pnl) AS game_pnl,                                                                 -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「game_pnl」
               SUM(net_pnl)  AS net_pnl,                                                                  -- 汇总表达式：取用 net_pnl（会员净输赢），产出「net_pnl」
               COUNT(*)      AS n_orders_in_round,                                                        -- 计数表达式：产出「n_orders_in_round」
               SUM(stake * p_side)                                                                        -- 汇总表达式：注额加权基准起算——★ 2026-08-11 斧正，旧法 MAX(bet_side) 取字母序最大而非主注
                 / NULLIF(SUM(CASE WHEN p_side IS NOT NULL THEN stake ELSE 0 END), 0)                     -- 除法或乘法计算：除以有基准之注的注额合计，权重口径与分子对齐
                             AS p_base_round_w,                                                             -- 续行：产出「p_base_round_w」——该局的**注额加权**基准；全注皆无基准则留空
               AVG(p_side)   AS p_base_round_unw,                                                         -- 汇总表达式：同局各注基准的**等权**均值——★ 2026-08-11 增，供两种 estimand 对照
               MAX(bet_side) AS main_side                                                                 -- 取最大值表达式：字母序最大注项，**仅备查、不参与计算**（旧版据此取基准，已废）
        FROM ordb GROUP BY member_id, dealer_id, is_sentinel_dealer, bet03, bet04, bet39                  -- 取数来源：取自本条自建的中间结果集 ordb；★ 三列原生分组，等价于原 round_key 分组
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03）
      pd AS (                                                                                             -- 公共表表达式：开启中间结果集 pd，其后各行为其定义体（§R03）
        SELECT p.member_id, p.dealer_id, p.is_sentinel_dealer,                                            -- 取列：起始取列子句，透传哨兵标记，涉 member_id（会员号）、dealer_id（荷官工号）
               COUNT(*)                                          AS n_rounds_eff,                         -- 计数表达式：取用 n_rounds_eff（有效局数），产出「n_rounds_eff」
               SUM(p.n_orders_in_round)                          AS n_orders,                             -- 汇总表达式：产出「n_orders」
               SUM(p.stake)                                      AS stake,                                -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake」
               SUM(p.game_pnl)                                   AS game_pnl,                             -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「game_pnl」
               SUM(p.net_pnl)                                    AS net_pnl,                              -- 汇总表达式：取用 net_pnl（会员净输赢），产出「net_pnl」
               SUM(CASE WHEN p.game_pnl >  0 THEN 1 ELSE 0 END)  AS n_win,                                -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「n_win」
               SUM(CASE WHEN p.game_pnl <> 0 THEN 1 ELSE 0 END)  AS n_dec,                                -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「n_dec」
               AVG(p.p_base_round_w)                               AS p_base_mix_w,                           -- 汇总表达式：对各局的注额加权基准取均值——★ 斧正：旧法以 0.5 顶替未定义基准，今删兜底
               AVG(p.p_base_round_unw)                           AS p_base_mix_unw                        -- 汇总表达式：等权口径之对照量——★ 二者背离即示该会员的资金集中于某一产品
        FROM pr p                                                                                         -- 取数来源：取自本条自建的中间结果集 pr
        -- （已废）旧版在此按 main_side 连 side_base 取基准，2026-08-11 改为注单层注额加权                                       -- 注：连接已移至 ordb，本处不再取基准
        GROUP BY p.member_id, p.dealer_id, p.is_sentinel_dealer                                           -- 分组：按会员×荷官×哨兵标记汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03）
      pdw AS (             -- ★ 2026-08-11 提速：会员合计改由 pd 开窗求得，原 player_all 已删
        SELECT p.*,                                                                                       -- 取列：整体承接上游结果集的全部字段，不再逐列列举
               SUM(p.net_pnl)  OVER (PARTITION BY p.member_id) AS net_pnl_all,                            -- 取值表达式：按会员开窗求净输赢合计——★ 与原 player_all 逐值等价（pd 为 pr 之细分组）
               SUM(p.game_pnl) OVER (PARTITION BY p.member_id) AS game_pnl_all,                           -- 取值表达式：按会员开窗求游戏净输赢合计
               SUM(p.stake)    OVER (PARTITION BY p.member_id) AS stake_all,                              -- 取值表达式：按会员开窗求下注额合计
               SUM(p.n_win)    OVER (PARTITION BY p.member_id) AS win_all,                                -- 取值表达式：按会员开窗求赢局合计
               SUM(p.n_dec)    OVER (PARTITION BY p.member_id) AS dec_all                                 -- 取值表达式：按会员开窗求决胜局合计
        FROM pd p                                                                                         -- 取数来源：取自本条自建的中间结果集 pd——★ pr 自此只被引用一次，免 inline 重算
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§R03）
      SELECT                                                                                              -- 续行：接续上一取列子句，续列 SELECT
        pdw.member_id AS uid, pdw.dealer_id, pdw.is_sentinel_dealer,                                         -- 取列：起始取列子句，本行先取「uid」与哨兵标记，涉 member_id（会员号）、dealer_id（荷官工号）
        pdw.stake      AS stake_amount,      -- 需求「投注金额」
        pdw.game_pnl   AS profit_amount,     -- 需求「盈利金额」（剔返水口径）
        pdw.net_pnl,                                                                                       -- 续行：接续上一取列子句，续列 pd.net_pnl，涉 net_pnl（会员净输赢）
        pdw.n_win * 1.0 / NULLIF(pdw.n_dec, 0)              AS win_rate,          -- 需求「胜率」
        pdw.n_orders  AS n_related_orders,                                       -- 需求「关联订单笔数」
        pdw.n_rounds_eff, pdw.p_base_mix_w, pdw.p_base_mix_unw,                                                -- 续行：并出两种口径的基准——注额加权（判定所用）与等权（对照），涉 n_rounds_eff（有效局数）
        pdw.stake      AS total_bet_amount,                                                                -- 取值表达式：该会员×荷官的总注额——★ 暴露量，供判读加权口径是否被少数巨注支配
        pdw.n_orders   AS n_bets,                                                                          -- 取值表达式：注单数——★ 与 n_rounds_eff 并列，可见同局多注之程度
        (pdw.n_win - pdw.n_dec * pdw.p_base_mix_w)                                                             -- 取值表达式：Z 分数之分子——观测胜局减期望胜局（**注额加权基准**，主估计量）
          / NULLIF(SQRT(pdw.n_dec * pdw.p_base_mix_w * (1 - pdw.p_base_mix_w)), 0)  AS z_score_w,            -- 除法或乘法计算：除以伯努利标准差，产出「z_score_w」——**加权候选臂**，分母 n_dec 为决胜局
        (pdw.n_win - pdw.n_dec * pdw.p_base_mix_unw)                                                         -- 取值表达式：★ 2026-08-11 增·**对照臂**——同式改用等权基准（Dixon & Coles 1996 之法：
          / NULLIF(SQRT(pdw.n_dec * pdw.p_base_mix_unw * (1 - pdw.p_base_mix_unw)), 0) AS z_score_unw,       -- 续行：先立无权重基线，加权之优势方能由对照证成），产出「z_score_unw」
        (pdw.n_win - pdw.n_dec * pdw.p_base_mix_w)                                                           -- 取值表达式：兼容别名之分子——与 z_score_w 逐字同式
          / NULLIF(SQRT(pdw.n_dec * pdw.p_base_mix_w * (1 - pdw.p_base_mix_w)), 0)  AS z_score,              -- 除法或乘法计算：产出「z_score」——★ **兼容别名，恒等于 z_score_w**；既有报告与数据契约沿用此名
        pdw.p_base_mix_w - pdw.p_base_mix_unw                AS delta_p,                                    -- 加减计算：比较层——两臂基准之差，产出「delta_p」；正即加权把基准抬高
        (pdw.n_win - pdw.n_dec * pdw.p_base_mix_w)                                                           -- 取值表达式：比较层之 Z 差起算——加权臂
          / NULLIF(SQRT(pdw.n_dec * pdw.p_base_mix_w * (1 - pdw.p_base_mix_w)), 0)                           -- 续行：加权臂 Z
        - (pdw.n_win - pdw.n_dec * pdw.p_base_mix_unw)                                                       -- 续行：减基线臂
          / NULLIF(SQRT(pdw.n_dec * pdw.p_base_mix_unw * (1 - pdw.p_base_mix_unw)), 0) AS delta_z,           -- 续行：产出「delta_z」——★ 判据是 Δ，不是加权臂本身之大小
        pdw.net_pnl_all, pdw.game_pnl_all, pdw.stake_all,                                                    -- 续行：接续上一取列子句，续列 pa.net_pnl_all, pa.game_pnl_all, pa.stake_all
        pdw.win_all * 1.0 / NULLIF(pdw.dec_all, 0)          AS win_rate_all,                                -- 取值表达式：产出「win_rate_all」
        (pdw.win_all - pdw.n_win) * 1.0                                                                     -- 取值表达式：比率之分子，乘 1.0 以避整数除法截断
          / NULLIF(pdw.dec_all - pdw.n_dec, 0)              AS win_rate_other,     -- ★ 对照检验
        CASE WHEN pdw.is_sentinel_dealer = 1        THEN 'SENTINEL_DEALER'                                 -- 取值表达式：统计资格判定起算——★ 2026-08-11 立：**事实层不删，此处只标注资格**
             WHEN pdw.p_base_mix_w IS NULL            THEN 'NO_BASE_RATE'                                    -- 续行：基准未定义（该会员所押产品皆无决胜局）
             WHEN pdw.n_dec = 0                     THEN 'NO_DECISIVE_ROUND'                               -- 续行：全为退还局，无胜负可判
             WHEN pdw.n_rounds_eff < 30             THEN 'INSUFFICIENT_N'                                  -- 续行：有效局数低于阈值报告所立的 30 局下限
             ELSE 'ELIGIBLE' END                   AS eligibility_status,                                 -- 续行：产出「eligibility_status」——分析层据此筛选，规则显式可追踪
        30                                         AS eligibility_min_n,                                  -- 取值表达式：本次所用的最小有效局数——显式化，改阈只改此处并记入变更日志
        'n_rounds_eff >= 30 且基准非空且非哨兵'      AS eligibility_rule,                                           -- 取值表达式：资格规则之明文，随行落档，免日后追问「当时筛的是什么」
        'L1_ELIGIBILITY'                           AS filter_stage,                                       -- 取值表达式：过滤所处之层——★ L0 事实层不删行，此处只标注其在 L1 资格层的去留
        'v2026-08-11'                              AS filter_rule_version,                                 -- 取值表达式：资格规则版本号——改规则须改版本号并记入变更日志，免「同名不同义」
        'R03_20260811_FULL_v1'                     AS comparison_id,                                      -- 取值表达式：比较批次号——★ 两臂须同批次方可比；口径见下五列，随行落档
        '2026-03-21..2026-08-06'                   AS cmp_time_window,                                    -- 取值表达式：时间窗，产出「cmp_time_window」——两臂必同
        'baccarat_bet02_101_all_pairs_incl_sentinel' AS cmp_population,                                   -- 取值表达式：总体定义（含哨兵之全量对），产出「cmp_population」——两臂必同
        'round_win = game_pnl > 0 (decisive only)' AS cmp_label,                                          -- 取值表达式：标签定义，产出「cmp_label」——两臂必同
        'COMPATIBILITY_ONLY_NOT_PRODUCTION'        AS z_score_alias_status                                -- 取值表达式：★ `z_score` 系兼容别名之状态标记——**禁止作任何模型／排序／阈值／能力值／处置之输入**
      FROM pdw                                                                                             -- 取数来源：取自本条自建的中间结果集 pdw（已含会员合计，无须再连 player_all）
      -- （已删）旧版在此连 player_all 取会员合计；2026-08-11 改由 pdw 开窗求得，pr 遂只算一遍
      WHERE   pdw.is_sentinel_dealer = 0                                                                  -- 过滤条件：★ 2026-08-11 立·L1 合格层——非哨兵荷官
        AND   pdw.p_base_mix_w IS NOT NULL                                                                -- 并列条件：基准已定义（该会员所押产品至少一项有决胜局）
        AND   pdw.n_dec > 0                                                                               -- 并列条件：至少有一局分出胜负（全为退还局者无从判定）
        AND   pdw.n_rounds_eff >= 30                                                                      -- 并列条件：有效局数不少于 30——四者合起来即 eligibility_status = 'ELIGIBLE'
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets AS n_bets_xagg, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`uid` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  71. R03b_player_dealer_daily.csv   [总包行 6143~6242 · 原 24 列 ＋六层约 60 列]
--     典型学：M07 荷官关联　分区：recent（s.x_rec = 1）　连接：q.`uid`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`bet_date`, w.`uid`, w.`dealer_id`, w.`is_sentinel_dealer`, w.`stake_amount`, w.`profit_amount`, w.`net_pnl`, w.`win_rate`, w.`n_related_orders`, w.`n_rounds_eff`, w.`p_base_mix_w`, w.`p_base_mix_unw`, w.`z_score_w`, w.`z_score_unw`, w.`z_score`, w.`delta_p`, w.`eligibility_status`, w.`comparison_id`, w.`filter_stage`, w.`filter_rule_version`, w.`cmp_time_window`, w.`cmp_population`, w.`cmp_label`, w.`z_score_alias_status`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ranked AS (                                                                                    -- 公共表表达式：开启中间结果集 ranked，其后各行为其定义体（§R03b）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet09, b.bet11,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet09, b.bet11，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet13, b.bet14, b.bet17, b.bet38, b.bet39,                                               -- 续行：接续上一取列子句，续列 b.bet13, b.bet14, b.bet17, b.bet38, b.bet39，涉 bet13（下注金额）、bet14（派彩金额）、bet17（会员净输赢）
               b.category, b.eid,                                                                         -- 续行：接续上一取列子句，续列 b.category, b.eid，涉 eid（荷官工号）
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'，涉 dt（营业日）
          AND b.bet02 = '101' AND b.category = '1'                                                        -- 并列条件：限定 b.bet02等于 '101'、b.category等于 '1'，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03b）
      base AS (                                                                                           -- 公共表表达式：开启中间结果集 base，其后各行为其定义体（§R03b）
        SELECT r.bet05 AS member_id, r.eid AS dealer_id, r.dt AS bet_date,                                -- 取列：起始取列子句，本行先取「bet_date」，涉 bet05（会员号）、eid（荷官工号）、dt（营业日）
               CASE WHEN TRIM(r.eid) IN ('-1','0') THEN 1 ELSE 0 END AS is_sentinel_dealer,               -- 取值表达式：哨兵标记——★ 由删除改为标注（事实层不删，分析层才筛）
               r.bet09 AS bet_side,                                                                       -- 取值表达式：取用 bet09（玩法），产出「bet_side」
               CONCAT_WS('|', r.bet03, r.bet04, r.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) AS fx,                                     -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「fx」
               CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4)) AS stake_raw,                              -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake_raw」
               CAST(NULLIF(TRIM(r.bet14),'') AS DECIMAL(20,4)) AS payout_raw,                             -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「payout_raw」
               CAST(NULLIF(TRIM(r.bet17),'') AS DECIMAL(20,4)) AS net_raw                                 -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_raw」
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        WHERE r.rn = 1 AND UPPER(TRIM(r.bet38)) = 'N'                                                     -- 过滤条件：限定 r.rn等于 1、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.eid),'') IS NOT NULL                                                          -- 并列条件：限定该值非空，涉 eid（荷官工号）
          AND UPPER(TRIM(r.bet09)) NOT LIKE 'TIP\_1\_%'                                                   -- 并列条件：限定不匹配所给模式，涉 bet09（玩法）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03b）
      ord AS (                                                                                            -- 公共表表达式：开启中间结果集 ord，其后各行为其定义体（§R03b）
        SELECT member_id, dealer_id, is_sentinel_dealer, bet_date, bet_side, round_key,                   -- 取列：起始取列子句，透传哨兵标记，涉 bet_date（营业日）、round_key（局键）
               stake_raw / fx                AS stake,                                                    -- 取值表达式：取用 stake（下注额（经汇率归一化）），产出「stake」
               (payout_raw - stake_raw) / fx AS game_pnl,                                                 -- 续行：取用 game_pnl（游戏净输赢），产出「game_pnl」
               net_raw / fx                  AS net_pnl                                                   -- 取值表达式：取用 net_pnl（会员净输赢），产出「net_pnl」
        FROM base                                                                                         -- 取数来源：取自本条自建的中间结果集 base
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03b）
      side_base AS (                                                                                      -- 公共表表达式：开启中间结果集 side_base，其后各行为其定义体（§R03b）
        SELECT bet_side,                                                                                  -- 取列：起始取列子句，本行先列 bet_side
               SUM(CASE WHEN game_pnl > 0 THEN 1 ELSE 0 END) * 1.0                                        -- 取值表达式：比率之分子，乘 1.0 以避整数除法截断，涉 game_pnl（游戏净输赢）
                 / NULLIF(SUM(CASE WHEN game_pnl <> 0 THEN 1 ELSE 0 END), 0) AS p_base                    -- 除法或乘法计算：汇总，取用 game_pnl（游戏净输赢），产出「p_base」
        FROM ord WHERE is_sentinel_dealer = 0                                                             -- 取数来源：取自本条自建的中间结果集 ord——★ 基准只由**真实牌桌**估计
        GROUP BY bet_side                                                                                 -- 分组：按投注产品汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03b）
      ordb AS (             -- ★ 2026-08-11 增：注单层挂上各自产品的基准，供下方按注额加权
        SELECT o.member_id, o.dealer_id, o.is_sentinel_dealer, o.bet_date, o.round_key, o.bet_side,       -- 取列：起始取列子句，透传五键与哨兵标记
               o.stake, o.game_pnl, o.net_pnl, s.p_base AS p_side                                         -- 续行：并取三项金额与该注自身产品的基准胜率，产出「p_side」
        FROM      ord o                                                                                   -- 取数来源：取自本条自建的中间结果集 ord
        LEFT JOIN side_base s ON s.bet_side = o.bet_side                                                  -- 左连接：取自本条自建的中间结果集 side_base，基准未定义者留空——不以 0.5 顶替
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03b）
      pr AS (   -- 局级去重：同一物理局折成一局，避免 Z-score 被 sqrt(k) 放大
        SELECT member_id, dealer_id, is_sentinel_dealer, bet_date, round_key,                             -- 取列：起始取列子句，透传哨兵标记
               SUM(stake) AS stake, SUM(game_pnl) AS game_pnl, SUM(net_pnl) AS net_pnl,                   -- 汇总表达式：取用 stake（下注额（经汇率归一化））、net_pnl（会员净输赢）、game_pnl（游戏净输赢），产出「net_pnl」
               COUNT(*) AS n_orders_in_round,                                                             -- 计数表达式：产出「n_orders_in_round」
               SUM(stake * p_side)                                                                        -- 汇总表达式：注额加权基准起算——★ 斧正，旧法 MAX(bet_side) 取字母序最大而非主注
                 / NULLIF(SUM(CASE WHEN p_side IS NOT NULL THEN stake ELSE 0 END), 0)                     -- 除法或乘法计算：除以有基准之注的注额合计
                             AS p_base_round_w,                                                             -- 续行：产出「p_base_round_w」——该局的**注额加权**基准；全注皆无基准则留空
               AVG(p_side)   AS p_base_round_unw,                                                         -- 汇总表达式：同局各注基准的**等权**均值——★ 2026-08-11 增，供两种 estimand 对照
               MAX(bet_side) AS main_side                                                                 -- 取最大值表达式：字母序最大注项，**仅备查、不参与计算**
        FROM ordb GROUP BY member_id, dealer_id, is_sentinel_dealer, bet_date, round_key                  -- 取数来源：取自本条自建的中间结果集 ordb（已挂基准）
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§R03b）
      SELECT p.bet_date, p.member_id AS uid, p.dealer_id, p.is_sentinel_dealer,                           -- 取列：起始取列子句，本行先取「uid」与哨兵标记
             SUM(p.stake)                                        AS stake_amount,                         -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake_amount」
             SUM(p.game_pnl)                                     AS profit_amount,                        -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「profit_amount」
             SUM(p.net_pnl)                                      AS net_pnl,                              -- 汇总表达式：取用 net_pnl（会员净输赢），产出「net_pnl」
             SUM(CASE WHEN p.game_pnl > 0 THEN 1 ELSE 0 END) * 1.0                                        -- 取值表达式：比率之分子，乘 1.0 以避整数除法截断，涉 game_pnl（游戏净输赢）
               / NULLIF(SUM(CASE WHEN p.game_pnl <> 0 THEN 1 ELSE 0 END), 0) AS win_rate,                 -- 除法或乘法计算：汇总，取用 game_pnl（游戏净输赢），产出「win_rate」
             SUM(p.n_orders_in_round)                            AS n_related_orders,                     -- 汇总表达式：产出「n_related_orders」
             COUNT(*)                                            AS n_rounds_eff,                         -- 计数表达式：取用 n_rounds_eff（有效局数），产出「n_rounds_eff」
             AVG(p.p_base_round_w)                                 AS p_base_mix_w,                           -- 汇总表达式：对各局的**注额加权**基准取均值——判定所用者
             AVG(p.p_base_round_unw)                             AS p_base_mix_unw,                       -- 汇总表达式：**等权**口径之对照量——★ 2026-08-11 与 §R03 对齐，二者背离即示资金集中于某产品
             (SUM(CASE WHEN p.game_pnl > 0 THEN 1 ELSE 0 END)                                             -- 续行：汇总，取用 game_pnl（游戏净输赢）
              - SUM(CASE WHEN p.game_pnl <> 0 THEN 1 ELSE 0 END) * AVG(p.p_base_round_w))           -- 加减计算：观测赢局数减期望赢局数 ＝ 超额，与 GGR 无涉
               / NULLIF(SQRT(SUM(CASE WHEN p.game_pnl <> 0 THEN 1 ELSE 0 END)                             -- 除法或乘法计算：汇总，取用 game_pnl（游戏净输赢）
                             * AVG(p.p_base_round_w)                                                -- 除法或乘法计算：求均值
                             * (1 - AVG(p.p_base_round_w))), 0)        AS z_score_w,                      -- 除法或乘法计算：产出「z_score_w」——**加权候选臂**
             (SUM(CASE WHEN p.game_pnl > 0 THEN 1 ELSE 0 END)                                             -- 取值表达式：★ **对照臂**之分子——同式改用等权基准（Dixon & Coles 1996：先立无权重基线）
              - SUM(CASE WHEN p.game_pnl <> 0 THEN 1 ELSE 0 END) * AVG(p.p_base_round_unw))               -- 续行：期望胜局按等权基准算
               / NULLIF(SQRT(SUM(CASE WHEN p.game_pnl <> 0 THEN 1 ELSE 0 END)                             -- 除法或乘法计算：除以伯努利标准差
                             * AVG(p.p_base_round_unw)                                                    -- 续行：等权基准
                             * (1 - AVG(p.p_base_round_unw))), 0)    AS z_score_unw,                      -- 续行：产出「z_score_unw」——加权之优势须由本臂对照证成
             (SUM(CASE WHEN p.game_pnl > 0 THEN 1 ELSE 0 END)                                             -- 取值表达式：兼容别名之分子——与 z_score_w 逐字同式
              - SUM(CASE WHEN p.game_pnl <> 0 THEN 1 ELSE 0 END) * AVG(p.p_base_round_w))                 -- 续行：期望胜局按加权基准算
               / NULLIF(SQRT(SUM(CASE WHEN p.game_pnl <> 0 THEN 1 ELSE 0 END)                             -- 除法或乘法计算：除以伯努利标准差
                             * AVG(p.p_base_round_w)                                                      -- 续行：加权基准
                             * (1 - AVG(p.p_base_round_w))), 0)        AS z_score,                        -- 续行：产出「z_score」——★ **兼容别名，恒等于 z_score_w**；既有报告与数据契约沿用此名
             AVG(p.p_base_round_w) - AVG(p.p_base_round_unw)          AS delta_p,                         -- 加减计算：比较层——两臂基准之差，产出「delta_p」
             CASE WHEN p.is_sentinel_dealer = 1 THEN 'SENTINEL_DEALER'                                    -- 取值表达式：统计资格判定起算——事实层不删，此处只标注资格
                  WHEN AVG(p.p_base_round_w) IS NULL THEN 'NO_BASE_RATE'                                    -- 续行：基准未定义
                  WHEN SUM(CASE WHEN p.game_pnl <> 0 THEN 1 ELSE 0 END) = 0 THEN 'NO_DECISIVE_ROUND'      -- 续行：全为退还局，无胜负可判
                  ELSE 'ELIGIBLE' END                                 AS eligibility_status,               -- 续行：产出「eligibility_status」——日粒度不设局数下限，下限由分析层施加
             'R03b_20260811_FULL_v1'                              AS comparison_id,                       -- 取值表达式：比较批次号——与 §R03 同规格，两臂须同批次方可比
             'L1_ELIGIBILITY'                           AS filter_stage,                                  -- 取值表达式：过滤所处之层——★ L0 事实层不删行，此处只标注其在 L1 资格层的去留
             'v2026-08-11'                              AS filter_rule_version,                            -- 取值表达式：资格规则版本号——改规则须改版本号并记入变更日志，免「同名不同义」
             '2026-03-21..2026-08-06'                             AS cmp_time_window,                     -- 取值表达式：时间窗，产出「cmp_time_window」
             'baccarat_bet02_101_all_pairs_incl_sentinel'         AS cmp_population,                      -- 取值表达式：总体定义（含哨兵之全量对）
             'round_win = game_pnl > 0 (decisive only)'           AS cmp_label,                           -- 取值表达式：标签定义
             'COMPATIBILITY_ONLY_NOT_PRODUCTION'                  AS z_score_alias_status                 -- 取值表达式：★ 兼容别名状态——禁止作生产输入
      FROM pr p                                                                                           -- 取数来源：取自本条自建的中间结果集 pr
      -- （已废）旧版在此按 main_side 连 side_base 取基准，2026-08-11 改注单层注额加权                                          -- 注：连接已移至 ordb，本处不再取基准
      GROUP BY p.bet_date, p.member_id, p.dealer_id, p.is_sentinel_dealer                                 -- 分组：按营业日×会员×荷官×哨兵标记汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`uid` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  72. R03chk_settlement_form.csv   [总包行 4684~4723 · 原 6 列 ＋六层约 60 列]
--     典型学：M07 荷官关联　分区：recent（s.x_rec = 1）　连接：q.`bet_side`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`bet_side`, w.`n_rounds`, w.`n_push`, w.`push_rate`, w.`p_base_dnb`, w.`p_base_incl`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_product                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_product IS NOT NULL AND TRIM(CAST(s.x_product AS STRING)) <> ''
    GROUP BY s.x_product
  ),
    q AS (
      WITH ranked AS (                                                                                    -- 公共表表达式：开启中间结果集 ranked——口径与 §R03 逐字一致，便于对照（§R03-chk）
        SELECT b.bet01, b.bet03, b.bet04, b.bet39, b.bet05, b.bet09,                                      -- 取列：起始取列子句，涉 bet03（靴号）、bet04（局内序号）、bet39（桌号）、bet05（会员号）、bet09（玩法）
               b.bet11, b.bet13, b.bet14, b.bet38, b.eid,                                                 -- 续行：接续上一取列子句，涉 bet11（汇率）、bet13（下注金额）、bet14（派彩金额）、bet38（重對）
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每注最终态，免修订滞后造成重复
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：取最新一版，产出「rn」
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定 E1 全局窗，涉 dt（营业日）
          AND b.bet02 = '101' AND b.category = '1'                                                        -- 并列条件：限定百家乐产品大类，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03-chk）
      base AS (                                                                                           -- 公共表表达式：开启中间结果集 base——有效注单，与 §R03 同口径（§R03-chk）
        SELECT r.bet05 AS member_id, r.eid AS dealer_id, r.bet09 AS bet_side,                             -- 取列：起始取列子句，涉 bet05（会员号）、eid（荷官工号）、bet09（玩法）
               CONCAT_WS('|', r.bet03, r.bet04, r.bet39) AS round_key,                                    -- 取值表达式：三键拼物理局键，产出「round_key」
               (CAST(NULLIF(TRIM(r.bet14),'') AS DECIMAL(20,4))                                           -- 取值表达式：派彩额起算，涉 bet14（派彩金额）
                - CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4)))                                        -- 续行：减投注额得游戏净输赢，涉 bet13（下注金额）
                / CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) AS game_pnl                             -- 续行：折汇率归一，产出「game_pnl」——恰为 0 即退还局
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        WHERE r.rn = 1                                                                                    -- 过滤条件：限定 r.rn等于 1，只取每注最终态
          AND UPPER(TRIM(r.bet38)) = 'N'                                                                  -- 并列条件：剔除测试注单，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：剔除无效会员号，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：汇率须为正，涉 bet11（汇率）
          AND UPPER(TRIM(r.bet09)) NOT LIKE 'TIP\_1\_%'                                                 -- 并列条件：排除小费单，涉 bet09（玩法）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03-chk）
      pr AS (                                                                                             -- 公共表表达式：开启中间结果集 pr——同局拆单先折叠，与 §R03 同法（§R03-chk）
        SELECT member_id, dealer_id, bet_side, round_key,                                                 -- 取列：起始取列子句，本行先列四键
               SUM(game_pnl) AS game_pnl                                                                  -- 聚合：局内合计游戏净输赢，产出「game_pnl」
        FROM base GROUP BY member_id, dealer_id, bet_side, round_key                                      -- 分组：按会员×荷官×玩法×物理局汇总
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§R03-chk）
      SELECT  UPPER(TRIM(bet_side))                          AS bet_side,                                 -- 取列：起始取列子句，本行先取「bet_side」——逐投注产品分列
              COUNT(*)                                       AS n_rounds,                                 -- 聚合：该产品的总局数，产出「n_rounds」
              SUM(CASE WHEN game_pnl = 0 THEN 1 ELSE 0 END)  AS n_push,                                   -- 聚合：退还局数（game_pnl 恰为 0），产出「n_push」——DNB 的直接证据
              SUM(CASE WHEN game_pnl = 0 THEN 1 ELSE 0 END) * 1.0                                         -- 除法或乘法计算：退还局占比起算
                / NULLIF(COUNT(*), 0)                        AS push_rate,                                -- 续行：除以总局数，产出「push_rate」——庄闲应约 9.5%，押和与边注应近 0
              SUM(CASE WHEN game_pnl > 0 THEN 1 ELSE 0 END) * 1.0                                         -- 除法或乘法计算：决胜局基准起算
                / NULLIF(SUM(CASE WHEN game_pnl <> 0 THEN 1 ELSE 0 END), 0) AS p_base_dnb,                -- 续行：分母取决胜局，产出「p_base_dnb」——报告 Z 分数所用之基，庄应约 50.7%
              SUM(CASE WHEN game_pnl > 0 THEN 1 ELSE 0 END) * 1.0                                         -- 除法或乘法计算：无条件基准起算
                / NULLIF(COUNT(*), 0)                        AS p_base_incl                               -- 续行：分母取全部局，产出「p_base_incl」——庄家优势所用之基，庄应约 45.9%
      FROM    pr                                                                                          -- 取数来源：取自本条自建的中间结果集 pr
      GROUP BY UPPER(TRIM(bet_side))                                                                      -- 分组：按投注产品汇总——23 种各出一行
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds AS n_rounds_xagg,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`bet_side` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  73. R03inv_pair_census.csv   [总包行 4745~4796 · 原 6 列 ＋六层约 60 列]
--     典型学：M07 荷官关联　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`eligibility_status`, w.`n_bucket`, w.`n_pairs`, w.`n_members`, w.`n_dealers`, w.`n_rounds_total`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ranked AS (                                                                                    -- 公共表表达式：开启中间结果集 ranked——口径与 §R03 一字不差（§R03-inv）
        SELECT b.bet01, b.bet03, b.bet04, b.bet39, b.bet05, b.bet09, b.bet11,                             -- 取列：起始取列子句，涉 bet03（靴号）、bet04（局内序号）、bet39（桌号）、bet05（会员号）
               b.bet13, b.bet14, b.bet38, b.eid,                                                          -- 续行：接续上一取列子句，涉 bet13（下注金额）、bet14（派彩金额）、bet38（重對）
               ROW_NUMBER() OVER (PARTITION BY b.bet01                                                    -- 行号窗口表达式：按注单号开窗取最终态
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：取最新一版，产出「rn」
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定 E1 全局窗，涉 dt（营业日）
          AND b.bet02 = '101' AND b.category = '1'                                                        -- 并列条件：限定百家乐产品大类，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03-inv）
      base AS (                                                                                           -- 公共表表达式：开启中间结果集 base——有效注单，哨兵**标注不删**（§R03-inv）
        SELECT r.bet05 AS member_id, r.eid AS dealer_id,                                                  -- 取列：起始取列子句，涉 bet05（会员号）、eid（荷官工号）
               CASE WHEN TRIM(r.eid) IN ('-1','0') THEN 1 ELSE 0 END AS is_sentinel_dealer,               -- 取值表达式：哨兵标记，产出「is_sentinel_dealer」
               CONCAT_WS('|', r.bet03, r.bet04, r.bet39) AS round_key,                                    -- 取值表达式：三键拼物理局键，产出「round_key」
               (CAST(NULLIF(TRIM(r.bet14),'') AS DECIMAL(20,4))                                           -- 取值表达式：派彩额起算，涉 bet14（派彩金额）
                - CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4)))                                        -- 续行：减投注额得游戏净输赢，涉 bet13（下注金额）
                / CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) AS game_pnl                             -- 续行：折汇率归一，产出「game_pnl」
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        WHERE r.rn = 1 AND UPPER(TRIM(r.bet38)) = 'N'                                                     -- 过滤条件：只取每注最终态且非测试单，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：剔除无效会员号，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：汇率须为正，涉 bet11（汇率）
          AND NULLIF(TRIM(r.eid),'') IS NOT NULL                                                          -- 并列条件：荷官号非空——空号无从归属，不构成关系
          AND UPPER(TRIM(r.bet09)) NOT LIKE 'TIP\\_1\\_%'                                                 -- 并列条件：排除小费单，涉 bet09（玩法）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03-inv）
      pr AS (                                                                                             -- 公共表表达式：开启中间结果集 pr——同局多注折成一局（§R03-inv）
        SELECT member_id, dealer_id, is_sentinel_dealer, round_key,                                       -- 取列：起始取列子句，本行先列四键
               SUM(game_pnl) AS game_pnl                                                                  -- 聚合：局内合计游戏净输赢，产出「game_pnl」
        FROM base GROUP BY member_id, dealer_id, is_sentinel_dealer, round_key                            -- 分组：按会员×荷官×哨兵标记×物理局汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§R03-inv）
      pd AS (                                                                                             -- 公共表表达式：开启中间结果集 pd——玩家×荷官关系级（§R03-inv）
        SELECT member_id, dealer_id, is_sentinel_dealer,                                                  -- 取列：起始取列子句，本行先列三键
               COUNT(*)                                          AS n_rounds_eff,                         -- 计数表达式：有效局数，产出「n_rounds_eff」
               SUM(CASE WHEN game_pnl <> 0 THEN 1 ELSE 0 END)    AS n_dec                                 -- 聚合：决胜局数（退还局不入），产出「n_dec」
        FROM pr GROUP BY member_id, dealer_id, is_sentinel_dealer                                         -- 分组：按会员×荷官×哨兵标记汇总
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§R03-inv）
      SELECT  CASE WHEN is_sentinel_dealer = 1 THEN 'SENTINEL_DEALER'                                     -- 取值表达式：资格分档起算——与 §R03 的 eligibility_status 逐字同口径
                   WHEN n_dec = 0              THEN 'NO_DECISIVE_ROUND'                                   -- 续行：全为退还局
                   WHEN n_rounds_eff < 30      THEN 'INSUFFICIENT_N'                                      -- 续行：局数不足 30
                   ELSE 'ELIGIBLE' END                           AS eligibility_status,                   -- 续行：产出「eligibility_status」
              CASE WHEN n_rounds_eff = 1 THEN '01_n=1'                                                    -- 取值表达式：稀疏分层起算——长尾的形状此前被 WHERE 藏起，今逐层点清
                   WHEN n_rounds_eff <= 2  THEN '02_n=2'                                                  -- 续行：n 等于 2
                   WHEN n_rounds_eff <= 5  THEN '03_n=3~5'                                                -- 续行：n 介于 3 至 5
                   WHEN n_rounds_eff <= 10 THEN '04_n=6~10'                                               -- 续行：n 介于 6 至 10
                   WHEN n_rounds_eff <= 29 THEN '05_n=11~29'                                              -- 续行：n 介于 11 至 29
                   WHEN n_rounds_eff <= 100 THEN '06_n=30~100'                                            -- 续行：n 介于 30 至 100
                   ELSE '07_n>100' END                           AS n_bucket,                             -- 续行：产出「n_bucket」
              COUNT(*)                                          AS n_pairs,                               -- 计数表达式：该格的玩家×荷官关系数，产出「n_pairs」
              COUNT(DISTINCT member_id)                         AS n_members,                             -- 计数表达式：涉及会员数（去重），产出「n_members」
              COUNT(DISTINCT dealer_id)                         AS n_dealers,                             -- 计数表达式：涉及荷官数（去重），产出「n_dealers」
              SUM(n_rounds_eff)                                 AS n_rounds_total                         -- 聚合：该格的有效局合计，产出「n_rounds_total」
      FROM    pd                                                                                          -- 取数来源：取自本条自建的中间结果集 pd
      GROUP BY 1, 2                                                                                       -- 分组：按资格分档与稀疏分层汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  74. R_rebate_dist.csv   [总包行 1489~1492 · 原 3 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`rebate_rate`, w.`n_member`, w.`pct`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT mem003 AS rebate_rate, COUNT(*) AS n_member,                                                 -- 取列：起始取列子句，本行先取「n_member」，涉 mem003（会员编号）、n_member（会员数）
             COUNT(*)*1.0/SUM(COUNT(*)) OVER() AS pct                                                     -- 计数表达式：全局窗口，用作占比之分母，产出「pct」
      FROM ods_mariadb_2b.ods_a168_member_dtl                                                             -- 取数来源：取自会员主档明细（注册与账户属性）
      GROUP BY mem003
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  75. S01_player_score.csv   [总包行 2198~2320 · 原 11 列 ＋六层约 60 列]
--     典型学：M15 活跃周期　分区：recent（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`流水贡献`, w.`游戏输赢贡献`, w.`活跃稳定`, w.`退水支出`, w.`n_ip`, w.`n_chain`, w.`人工标记史`, w.`投注产品结构熵`, w.`索提诺稳定性`, w.`生命周期阶段`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ta AS (            -- 公司测试线代理（214 条，跨五级）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S01_player_score.csv）
      rk AS (                  -- 需求 §3.2：同注单号取最新版本（三级排序去重）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet09，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet11, b.bet13, b.bet14, b.bet16, b.bet17,                                               -- 续行：接续上一取列子句，续列 b.bet11, b.bet13, b.bet14, b.bet16, b.bet17，涉 bet11（汇率）、bet13（下注金额）、bet14（派彩金额）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet38, b.bet39, b.category, b.eid, b.ip,                                                 -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category, b.eid, b.ip，涉 bet38（重對）、bet39（桌号）、eid（荷官工号）
               b.validbet,                                                                                -- 续行：接续上一取列子句，续列 b.validbet，涉 validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S01_player_score.csv）
      vd AS (                  -- 有效注单：非测试线、非重对、一般注单
        SELECT r.*                                                                                        -- 取列：整体承接上游结果集的全部字段，不再逐列列举
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S01_player_score.csv）
      bs AS (                  -- 金额正名：本金/洗码量/游戏输赢/退水/净输赢（一律÷汇率）
        SELECT v.bet05 AS member_id, v.eid AS dealer_id, v.ip AS bet_ip,                                  -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、eid（荷官工号）、bet_ip（下注 IP）
               v.bet20 AS lv3, v.bet09 AS bet_side, v.dt AS bet_date,                                     -- 取值表达式：取用 bet09（玩法）、bet20（三级代理线）、dt（营业日），产出「bet_date」
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.bet08),'') AS DATETIME) AS t_bet,                                       -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「t_bet」
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake,                              -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               CAST(NULLIF(TRIM(v.validbet),'') AS DECIMAL(20,4))                                         -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS valid_bet,                          -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「valid_bet」
               (CAST(NULLIF(TRIM(v.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS game_pnl,                           -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               CAST(NULLIF(TRIM(v.bet16),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS rebate,                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
               CAST(NULLIF(TRIM(v.bet17),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS net_pnl                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_pnl」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S01_player_score.csv）
      lab AS (SELECT bet05 AS member_id,                                                                  -- 取值表达式：取用 bet05（会员号）、member_id（会员号），产出「member_id」
                     SUM(CASE WHEN risk='1' THEN 1 ELSE 0 END) AS n_risk_days,                            -- 汇总表达式：产出「n_risk_days」
                     SUM(CASE WHEN orders='1' THEN 1 ELSE 0 END) AS n_order_days                          -- 汇总表达式：产出「n_order_days」
              FROM ods_mariadb_2b.ods_a168_dailyreport_member GROUP BY bet05),                            -- 取数来源：取自会员日结报表（日粒度业绩与风控标记）
      /* ── 扩编维度① 投注产品结构熵 ─────────────────────────────────────
         香农熵：把该会员的投注额按 23 种投注产品拆开，看分散还是集中。
         熵高 = 庄闲和边注都押一点，是娱乐型客人的常态；
         熵低 = 火力集中在少数高赔率边注，是技术型画像的特征之一。
         赏罚方向为「赏」——熵高的客人对平台更安全。 */
      psplit AS (                                                                                         -- 公共表表达式：开启中间结果集 psplit，其后各行为其定义体（交付件 S01_player_score.csv）
        SELECT member_id, bet_side,                                                                       -- 取列：起始取列子句，本行先列 member_id, bet_side，涉 member_id（会员号）
               SUM(stake) AS s_side,                                                                      -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「s_side」
               SUM(SUM(stake)) OVER (PARTITION BY member_id) AS s_all                                     -- 汇总表达式：取用 stake（下注额（经汇率归一化））、member_id（会员号），产出「s_all」
        FROM bs GROUP BY member_id, bet_side                                                              -- 取数来源：取自本条自建的中间结果集 bs
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S01_player_score.csv）
      ent AS (                                                                                            -- 公共表表达式：开启中间结果集 ent，其后各行为其定义体（交付件 S01_player_score.csv）
        SELECT member_id,                                                                                 -- 取列：起始取列子句，本行先列 member_id，涉 member_id（会员号）
               -SUM((s_side/s_all) * LN(s_side/s_all)) AS 投注产品结构熵                                         -- 加减计算：香农熵定义式之前置负号，与 GGR 无涉，产出「投注产品结构熵」
        FROM psplit WHERE s_all > 0 AND s_side > 0                                                        -- 取数来源：取自本条自建的中间结果集 psplit
        GROUP BY member_id                                                                                -- 分组：按 member_id 汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S01_player_score.csv）
      /* ── 扩编维度② 索提诺稳定性（会员视角的下行风险调整收益）──────────
         先把注单压成「会员×日」的日 ROI，再算 日ROI均值 ÷ 下行标准差。
         只统计低于 0 那一侧的波动——赢钱时的波动不是风险。
         赏罚方向为「罚」：赢得稳的客人正是 T2 技术型风险用户，
         赢得多但忽输忽赢的反而是正常娱乐客。**不要把方向搞反。** */
      dly AS (                                                                                            -- 公共表表达式：开启中间结果集 dly，其后各行为其定义体（交付件 S01_player_score.csv）
        SELECT member_id, bet_date,                                                                       -- 取列：起始取列子句，本行先列 member_id, bet_date，涉 member_id（会员号）、bet_date（营业日）
               SUM(game_pnl) AS pnl_d, SUM(stake) AS stake_d                                              -- 汇总表达式：取用 stake（下注额（经汇率归一化））、game_pnl（游戏净输赢），产出「stake_d」
        FROM bs GROUP BY member_id, bet_date                                                              -- 取数来源：取自本条自建的中间结果集 bs
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S01_player_score.csv）
      srt AS (                                                                                            -- 公共表表达式：开启中间结果集 srt，其后各行为其定义体（交付件 S01_player_score.csv）
        SELECT member_id,                                                                                 -- 取列：起始取列子句，本行先列 member_id，涉 member_id（会员号）
               AVG(pnl_d / NULLIF(stake_d,0))                                  AS mu_roi_d,               -- 求均值表达式：产出「mu_roi_d」
               SQRT(AVG(POW(LEAST(pnl_d / NULLIF(stake_d,0), 0), 2)))          AS dd_roi_d,               -- 取值表达式：求均值，产出「dd_roi_d」
               AVG(pnl_d / NULLIF(stake_d,0))                                                             -- 求均值表达式：承接上一子句，构成完整语句，属 交付件 S01_player_score.csv 之取数
                 / NULLIF(SQRT(AVG(POW(LEAST(pnl_d / NULLIF(stake_d,0), 0), 2))), 0)                      -- 除法或乘法计算：求均值
                                                                                AS 索提诺稳定性                 -- 取值表达式：取用索提诺稳定性（玩家雷达罚侧维），产出「索提诺稳定性」
        FROM dly GROUP BY member_id                                                                       -- 取数来源：取自本条自建的中间结果集 dly
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S01_player_score.csv）
      /* ── 扩编维度③ 生命周期阶段 ──────────────────────────────────────
         首末注单跨度 × 近期活跃衰减，取值越大代表越处于成熟稳定期。
         算法：活跃跨度天数 × (1 − 距窗口右端的静默天数 / 窗口总天数)，
         新客跨度短、流失前兆者静默久，两端都会被压低。窗口 139 天为字面量。
         赏罚方向为「赏」。 */
      lc AS (                                                                                             -- 公共表表达式：开启中间结果集 lc，其后各行为其定义体（交付件 S01_player_score.csv）
        SELECT member_id,                                                                                 -- 取列：起始取列子句，本行先列 member_id，涉 member_id（会员号）
               DATEDIFF(MAX(bet_date), MIN(bet_date)) + 1 AS span_days,                                   -- 取值表达式：取最大值、取最小值，取用 bet_date（营业日），产出「span_days」
               DATEDIFF(DATE '2026-08-06', MAX(bet_date)) AS silent_days,                                 -- 取值表达式：取最大值，取用 bet_date（营业日），产出「silent_days」
               (DATEDIFF(MAX(bet_date), MIN(bet_date)) + 1)                                               -- 续行：取最大值、取最小值，取用 bet_date（营业日）
                 * (1 - DATEDIFF(DATE '2026-08-06', MAX(bet_date)) / 139.0) AS 生命周期阶段                     -- 除法或乘法计算：取最大值，取用 bet_date（营业日）、生命周期阶段（玩家雷达赏侧维），产出「生命周期阶段」
        FROM bs GROUP BY member_id                                                                        -- 取数来源：取自本条自建的中间结果集 bs
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（交付件 S01_player_score.csv）
      SELECT b.member_id,                                                                                 -- 取列：起始取列子句，本行先列 b.member_id，涉 member_id（会员号）
        SUM(b.valid_bet) AS 流水贡献,                                                                         -- 汇总表达式：取用 valid_bet（有效投注（洗码量））、流水贡献（玩家雷达赏侧维），产出「流水贡献」
        -SUM(b.game_pnl) AS 游戏输赢贡献,          -- 会员输=平台赢，取负号
        COUNT(DISTINCT b.bet_date) AS 活跃稳定,                                                               -- 计数表达式：统计去重个数，产出「活跃稳定」
        SUM(b.rebate) AS 退水支出,                                                                            -- 汇总表达式：取用 rebate（退水）、退水支出（退水成本），产出「退水支出」
        COUNT(DISTINCT b.bet_ip) AS n_ip,                                                                 -- 计数表达式：统计去重个数，产出「n_ip」
        COUNT(DISTINCT b.lv3) AS n_chain,                                                                 -- 计数表达式：统计去重个数，产出「n_chain」
        COALESCE(MAX(l.n_risk_days),0)+COALESCE(MAX(l.n_order_days),0) AS 人工标记史,                          -- 取值表达式：取最大值，取用人工标记史（玩家雷达罚侧维），产出「人工标记史」
        -- ★ 扩编三列：报告的玩家雷达按列名取用，列名一字不可改
        MAX(e.投注产品结构熵)  AS 投注产品结构熵,                                                                       -- 取最大值表达式：取用投注产品结构熵（玩家雷达赏侧维），产出「投注产品结构熵」
        MAX(sr.索提诺稳定性)   AS 索提诺稳定性,                                                                       -- 取最大值表达式：取用索提诺稳定性（玩家雷达罚侧维），产出「索提诺稳定性」
        MAX(lcx.生命周期阶段)  AS 生命周期阶段                                                                        -- 取最大值表达式：取用生命周期阶段（玩家雷达赏侧维），产出「生命周期阶段」
      FROM bs b                                                                                           -- 取数来源：取自本条自建的中间结果集 bs
      LEFT JOIN lab l   ON l.member_id  = b.member_id                                                     -- 左连接：取自本条自建的中间结果集 lab，连接键为 member_id（会员号）
      LEFT JOIN ent e   ON e.member_id  = b.member_id                                                     -- 左连接：取自本条自建的中间结果集 ent，连接键为 member_id（会员号）
      LEFT JOIN srt sr  ON sr.member_id = b.member_id                                                     -- 左连接：取自本条自建的中间结果集 srt，连接键为 member_id（会员号）
      LEFT JOIN lc  lcx ON lcx.member_id = b.member_id                                                    -- 左连接：取自本条自建的中间结果集 lc，连接键为 member_id（会员号）
      GROUP BY b.member_id                                                                                -- 分组：按 b.member_id 汇总
      HAVING COUNT(DISTINCT b.round_key) >= 30                                                            -- 分组后过滤：限定 b.round_key 的去重个数不少于 30，涉 round_key（局键）
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  76. S02_dealer_score.csv   [总包行 2339~2458 · 原 9 列 ＋六层约 60 列]
--     典型学：M07 荷官关联　分区：recent（s.x_rec = 1）　连接：q.`dealer_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`dealer_id`, w.`在桌洗码量`, w.`客群广度`, w.`n_rounds`, w.`n_tables`, w.`桌面输赢`, w.`当值时长`, w.`节奏稳定度`, w.`特定玩家复现率`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_dealer                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_dealer IS NOT NULL AND TRIM(CAST(s.x_dealer AS STRING)) <> ''
    GROUP BY s.x_dealer
  ),
    q AS (
      WITH ta AS (            -- 公司测试线代理（214 条，跨五级）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S02_dealer_score.csv）
      rk AS (                  -- 需求 §3.2：同注单号取最新版本（三级排序去重）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt,                                                  -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt，涉 dt（营业日）
               b.category, b.bet38, b.bet05, b.bet11, b.bet08,                                            -- 续行：接续上一取列子句，续列 b.category, b.bet38, b.bet05, b.bet11, b.bet08，涉 bet05（会员号）、bet08（下注时间）、bet11（汇率）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet03, b.bet04, b.bet39, b.eid,                                                          -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet39, b.eid，涉 bet03（靴号）、bet04（局内序号）、bet39（桌号）
               b.validbet, b.bet13, b.bet14,                                                              -- 续行：接续上一取列子句，续列 b.validbet, b.bet13, b.bet14，涉 bet13（下注金额）、bet14（派彩金额）、validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S02_dealer_score.csv）
      vd AS (                  -- 有效注单：非测试线、非重对、一般注单（谓词与原版一字不差）
        SELECT r.*                                                                                        -- 取列：整体承接上游结果集的全部字段，不再逐列列举
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S02_dealer_score.csv）
      bs AS (                  -- 金额正名（只保留本查询实际使用的量）
        SELECT v.bet05 AS member_id, v.eid AS dealer_id,                                                  -- 取列：起始取列子句，本行先取「dealer_id」，涉 bet05（会员号）、eid（荷官工号）、member_id（会员号）
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.validbet),'') AS DECIMAL(20,4))                                         -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS valid_bet,                          -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「valid_bet」
               (CAST(NULLIF(TRIM(v.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS game_pnl                            -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
        WHERE NULLIF(TRIM(v.eid),'') IS NOT NULL   -- 空荷官行本就不进任何输出组，前置过滤不改输出
          AND TRIM(v.eid) NOT IN ('-1', '0')       -- 哨兵荷官号一并剔除：-1／0 非真实荷官，不得入评分（2026-08-11 增）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S02_dealer_score.csv）
      bd AS (                  -- ★ 一次坍缩：荷官×桌×会员×局。bs 全文只被此处引用一次
        SELECT dealer_id, table_id, member_id, round_key,                                                 -- 取列：起始取列子句，本行先列 dealer_id, table_id, member_id, round_key，涉 round_key（局键）、member_id（会员号）、dealer_id（荷官工号）
               COUNT(*)       AS n_bet_rows,     -- 该组合的注单行数（pace 的权重）
               SUM(valid_bet) AS v_sum,                                                                   -- 汇总表达式：取用 valid_bet（有效投注（洗码量）），产出「v_sum」
               SUM(game_pnl)  AS g_sum                                                                    -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「g_sum」
        FROM bs                                                                                           -- 取数来源：取自本条自建的中间结果集 bs
        GROUP BY dealer_id, table_id, member_id, round_key                                                -- 分组：按 dealer_id, table_id, member_id, round_key 汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S02_dealer_score.csv）
      /* ── 扩编维度① 当值时长 ─────────────────────────────────────────
         以「在册局数」为分层基准。不做这一层分层，裸排名的榜首永远是
         上班天数最少的新人——他们样本少、波动大，任何比率指标都会虚高。
         赏罚方向为「赏」。 */
      /* ── 扩编维度② 节奏稳定度：加权矩坍缩版 ─────────────────────────
         原式 = 对「荷官的每笔注单 × 同桌每一局的时长」求 STDDEV_SAMP。
         等价于：每桌局时长以该荷官在该桌的注单行数加权。
         样本标准差只需三个矩：N=Σw·n、S1=Σw·Σx、S2=Σw·Σx²，
         var = (S2 − S1²/N)/(N−1)。桌级统计仅数百行，荷官×桌仅数千行。
         节奏忽快忽慢是操作异常的先兆，也会影响玩家体验。赏罚方向为「赏」。 */
      gi AS (                                                                                             -- 公共表表达式：开启中间结果集 gi，其后各行为其定义体（交付件 S02_dealer_score.csv）
        SELECT gi011 AS table_id,                                                                         -- 取列：起始取列子句，本行先取「table_id」，涉 gi011（桌号）
               UNIX_TIMESTAMP(gi006) - UNIX_TIMESTAMP(gi004) AS sec_round                                 -- 取值表达式：取用 gi004（局开始时刻）、gi006（开牌时刻），产出「sec_round」
        FROM ods_mariadb_2b.ods_a168_game_info                                                            -- 取数来源：取自局信息表（靴局、开牌与桌号的时序基准）
        WHERE gi001 = '101' AND gi013 = '1'                                                               -- 过滤条件：限定 gi001等于 '101'、gi013等于 '1'，涉 gi001（局主键）
          AND gi004 >= '2026-03-21' AND gi004 < '2026-08-07'                                              -- 并列条件：限定 gi004不少于 '2026-03-21'、gi004小于 '2026-08-07'，涉 gi004（局开始时刻）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S02_dealer_score.csv）
      git AS (                 -- 每桌三矩（COUNT/SUM 天然忽略 NULL，与 STDDEV_SAMP 同语义）
        SELECT table_id,                                                                                  -- 取列：起始取列子句，本行先列 table_id
               COUNT(sec_round)                                            AS n_g,                        -- 计数表达式：产出「n_g」
               SUM(CAST(sec_round AS DOUBLE))                              AS s1,                         -- 汇总表达式：产出「s1」
               SUM(CAST(sec_round AS DOUBLE) * CAST(sec_round AS DOUBLE))  AS s2                          -- 汇总表达式：产出「s2」
        FROM gi GROUP BY table_id                                                                         -- 取数来源：取自本条自建的中间结果集 gi
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S02_dealer_score.csv）
      dtc AS (                 -- 荷官×桌的注单行数（加权权重）
        SELECT dealer_id, table_id, SUM(n_bet_rows) AS n_rows                                             -- 取列：汇总，取用 dealer_id（荷官工号），产出「n_rows」
        FROM bd GROUP BY dealer_id, table_id                                                              -- 取数来源：取自本条自建的中间结果集 bd
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S02_dealer_score.csv）
      pace AS (                                                                                           -- 公共表表达式：开启中间结果集 pace，其后各行为其定义体（交付件 S02_dealer_score.csv）
        SELECT d.dealer_id,                                                                               -- 取列：起始取列子句，本行先列 d.dealer_id，涉 dealer_id（荷官工号）
               CASE                                                                                       -- 条件分支：开启分支判定，其后逐行列出条件与取值（交付件 S02_dealer_score.csv）
                 WHEN SUM(d.n_rows * g.n_g) >= 2 THEN                                                     -- 分支判定：汇总
                   1.0 / (1.0 + COALESCE(SQRT(GREATEST(                                                   -- 取值表达式：承接上一子句，构成完整语句，属 交付件 S02_dealer_score.csv 之取数
                     ( SUM(d.n_rows * g.s2)                                                               -- 续行：汇总
                       - SUM(d.n_rows * g.s1) * SUM(d.n_rows * g.s1)                                      -- 加减计算：二阶矩减一阶矩平方 ＝ 方差，与 GGR 无涉
                         / SUM(d.n_rows * CAST(g.n_g AS DOUBLE)) )                                        -- 除法或乘法计算：汇总
                     / (SUM(d.n_rows * CAST(g.n_g AS DOUBLE)) - 1), 0)), 0))                              -- 除法或乘法计算：汇总
                 ELSE 1.0     -- 原版：样本≤1 时 STDDEV_SAMP=NULL → COALESCE 0 → 稳定度 1.0
               END AS 节奏稳定度                                                                               -- 分支收束：结束条件分支并收敛为单一取值
        FROM dtc d JOIN git g ON g.table_id = d.table_id                                                  -- 取数来源：取自本条自建的中间结果集 dtc，连接键为 g.table_id = d.table_id
        GROUP BY d.dealer_id                                                                              -- 分组：按 d.dealer_id 汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S02_dealer_score.csv）
      /* ── 扩编维度③ 特定玩家复现率：改从 bd 派生，语义不变 ────────────
         该荷官桌上「出现最频繁的那一位会员」占其总局数的比例。
         正常荷官服务的是流动客群，比例低；比例畸高说明有固定的人一直跟着他，
         这是玩家—荷官关联的第一道筛。赏罚方向为「罚」。 */
      mrep AS (                                                                                           -- 公共表表达式：开启中间结果集 mrep，其后各行为其定义体（交付件 S02_dealer_score.csv）
        SELECT dealer_id, member_id, COUNT(DISTINCT round_key) AS n_r                                     -- 取列：统计去重个数，产出「n_r」
        FROM bd GROUP BY dealer_id, member_id                                                             -- 取数来源：取自本条自建的中间结果集 bd
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S02_dealer_score.csv）
      top1 AS (                                                                                           -- 公共表表达式：开启中间结果集 top1，其后各行为其定义体（交付件 S02_dealer_score.csv）
        SELECT dealer_id, MAX(n_r) AS max_member_rounds                                                   -- 取列：取最大值，取用 dealer_id（荷官工号），产出「max_member_rounds」
        FROM mrep GROUP BY dealer_id                                                                      -- 取数来源：取自本条自建的中间结果集 mrep
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（交付件 S02_dealer_score.csv）
      SELECT b.dealer_id,                                                                                 -- 取列：起始取列子句，本行先列 b.dealer_id，涉 dealer_id（荷官工号）
        SUM(b.v_sum) AS 在桌洗码量,                                                                            -- 汇总表达式：取用在桌洗码量（荷官雷达赏侧维），产出「在桌洗码量」
        COUNT(DISTINCT b.member_id) AS 客群广度,                                                              -- 计数表达式：统计去重个数，产出「客群广度」
        COUNT(DISTINCT b.round_key) AS n_rounds,                                                          -- 计数表达式：统计去重个数，产出「n_rounds」
        COUNT(DISTINCT b.table_id) AS n_tables,                                                           -- 计数表达式：统计去重个数，产出「n_tables」
        SUM(b.g_sum) AS 桌面输赢,                                                                             -- 汇总表达式：产出「桌面输赢」
        -- ★ 扩编三列：报告的荷官雷达按列名取用，列名一字不可改
        COUNT(DISTINCT b.round_key) AS 当值时长,                                                              -- 计数表达式：统计去重个数，产出「当值时长」
        MAX(p.节奏稳定度) AS 节奏稳定度,                                                                            -- 取最大值表达式：取用节奏稳定度（荷官雷达赏侧维），产出「节奏稳定度」
        MAX(t.max_member_rounds) * 1.0                                                                    -- 取值表达式：比率之分子，乘 1.0 以避整数除法截断
          / NULLIF(COUNT(DISTINCT b.round_key), 0) AS 特定玩家复现率                                             -- 除法或乘法计算：统计去重个数，产出「特定玩家复现率」
      FROM bd b                                                                                           -- 取数来源：取自本条自建的中间结果集 bd
      LEFT JOIN pace p ON p.dealer_id = b.dealer_id                                                       -- 左连接：取自本条自建的中间结果集 pace，连接键为 dealer_id（荷官工号）
      LEFT JOIN top1 t ON t.dealer_id = b.dealer_id                                                       -- 左连接：取自本条自建的中间结果集 top1，连接键为 dealer_id（荷官工号）
      GROUP BY b.dealer_id
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds AS n_rounds_xagg,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`dealer_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  77. S02b_dealer_pace.csv   [总包行 6390~6447 · 原 3 列 ＋六层约 60 列]
--     典型学：M07 荷官关联　分区：recent（s.x_rec = 1）　连接：q.`dealer_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`dealer_id`, w.`平均每局秒数`, w.`局速`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_dealer                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_dealer IS NOT NULL AND TRIM(CAST(s.x_dealer AS STRING)) <> ''
    GROUP BY s.x_dealer
  ),
    q AS (
      WITH ta AS (                                                                                        -- 公共表表达式：开启中间结果集 ta，其后各行为其定义体（§EX-05）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§EX-05）
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk，其后各行为其定义体（§EX-05）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt,                                                  -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt，涉 dt（营业日）
               b.category, b.bet38, b.bet05, b.bet11, b.bet08,                                            -- 续行：接续上一取列子句，续列 b.category, b.bet38, b.bet05, b.bet11, b.bet08，涉 bet05（会员号）、bet08（下注时间）、bet11（汇率）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet39, b.eid,                                                                            -- 续行：接续上一取列子句，续列 b.bet39, b.eid，涉 bet39（桌号）、eid（荷官工号）
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§EX-05）
      vd AS (                                                                                             -- 公共表表达式：开启中间结果集 vd，其后各行为其定义体（§EX-05）
        SELECT r.*                                                                                        -- 取列：整体承接上游结果集的全部字段，不再逐列列举
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
          AND NULLIF(TRIM(r.eid),'') IS NOT NULL                                                          -- 并列条件：限定该值非空，涉 eid（荷官工号）
          AND TRIM(r.eid) NOT IN ('-1', '0')                                                              -- 并列条件：剔除哨兵荷官号——-1／0 非真实荷官，入检即污染榜单与多重比较总体（2026-08-11 增）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§EX-05）
      dtc AS (                     -- 荷官×桌的注单行数（与 §S-02 的权重同源）
        SELECT eid AS dealer_id, bet39 AS table_id, COUNT(*) AS n_rows                                    -- 取列：计数，取用 bet39（桌号）、eid（荷官工号）、dealer_id（荷官工号），产出「n_rows」
        FROM vd GROUP BY eid, bet39                                                                       -- 取数来源：取自本条自建的中间结果集 vd
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§EX-05）
      gi AS (                      -- 每局秒数，脏值以 BETWEEN 剔除
        SELECT gi011 AS table_id,                                                                         -- 取列：起始取列子句，本行先取「table_id」，涉 gi011（桌号）
               UNIX_TIMESTAMP(gi006) - UNIX_TIMESTAMP(gi004) AS sec_round                                 -- 取值表达式：取用 gi004（局开始时刻）、gi006（开牌时刻），产出「sec_round」
        FROM ods_mariadb_2b.ods_a168_game_info                                                            -- 取数来源：取自局信息表（靴局、开牌与桌号的时序基准）
        WHERE gi001 = '101' AND gi013 = '1'                                                               -- 过滤条件：限定 gi001等于 '101'、gi013等于 '1'，涉 gi001（局主键）
          AND gi004 >= '2026-03-21' AND gi004 < '2026-08-07'                                              -- 并列条件：限定 gi004不少于 '2026-03-21'、gi004小于 '2026-08-07'，涉 gi004（局开始时刻）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§EX-05）
      git AS (                     -- 每桌两矩：局数与总秒数
        SELECT table_id,                                                                                  -- 取列：起始取列子句，本行先列 table_id
               COUNT(sec_round)               AS n_g,                                                     -- 计数表达式：产出「n_g」
               SUM(CAST(sec_round AS DOUBLE)) AS s1                                                       -- 汇总表达式：产出「s1」
        FROM gi                                                                                           -- 取数来源：取自本条自建的中间结果集 gi
        WHERE sec_round BETWEEN 5 AND 600                                                                 -- 过滤条件：限定落入闭区间
        GROUP BY table_id                                                                                 -- 分组：按 table_id 汇总
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§EX-05）
      SELECT d.dealer_id,                                                                                 -- 取列：起始取列子句，本行先列 d.dealer_id，涉 dealer_id（荷官工号）
             SUM(d.n_rows * g.s1) / NULLIF(SUM(d.n_rows * CAST(g.n_g AS DOUBLE)), 0)                      -- 汇总表达式：承接上一子句，构成完整语句，属 §EX-05 之取数
               AS 平均每局秒数,                                                                                 -- 取值表达式：产出「平均每局秒数」
             3600.0 * SUM(d.n_rows * CAST(g.n_g AS DOUBLE))                                               -- 取值表达式：汇总
               / NULLIF(SUM(d.n_rows * g.s1), 0)                        AS 局速                             -- 除法或乘法计算：汇总，产出「局速」
      FROM dtc d JOIN git g ON g.table_id = d.table_id                                                    -- 取数来源：取自本条自建的中间结果集 dtc，连接键为 g.table_id = d.table_id
      GROUP BY d.dealer_id                                                                                -- 分组：按 d.dealer_id 汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`dealer_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  78. S03_agent_score.csv   [总包行 2467~2566 · 原 9 列 ＋六层约 60 列]
--     典型学：M06 代理线　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`lv3`, w.`线下规模`, w.`真实流水`, w.`退水支出`, w.`链下游戏输赢`, w.`退水消耗比`, w.`下线留存`, w.`新增会员质量`, w.`新增会员数`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ta AS (            -- 公司测试线代理（214 条，跨五级）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S03_agent_score.csv）
      rk AS (                  -- 需求 §3.2：同注单号取最新版本（三级排序去重）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet09，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet11, b.bet13, b.bet14, b.bet16, b.bet17,                                               -- 续行：接续上一取列子句，续列 b.bet11, b.bet13, b.bet14, b.bet16, b.bet17，涉 bet11（汇率）、bet13（下注金额）、bet14（派彩金额）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet38, b.bet39, b.category, b.eid, b.ip,                                                 -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category, b.eid, b.ip，涉 bet38（重對）、bet39（桌号）、eid（荷官工号）
               b.validbet,                                                                                -- 续行：接续上一取列子句，续列 b.validbet，涉 validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S03_agent_score.csv）
      vd AS (                  -- 有效注单：非测试线、非重对、一般注单
        SELECT r.*                                                                                        -- 取列：整体承接上游结果集的全部字段，不再逐列列举
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S03_agent_score.csv）
      bs AS (                  -- 金额正名：本金/洗码量/游戏输赢/退水/净输赢（一律÷汇率）
        SELECT v.bet05 AS member_id, v.eid AS dealer_id, v.ip AS bet_ip,                                  -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、eid（荷官工号）、bet_ip（下注 IP）
               v.bet20 AS lv3, v.bet09 AS bet_side, v.dt AS bet_date,                                     -- 取值表达式：取用 bet09（玩法）、bet20（三级代理线）、dt（营业日），产出「bet_date」
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.bet08),'') AS DATETIME) AS t_bet,                                       -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「t_bet」
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake,                              -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               CAST(NULLIF(TRIM(v.validbet),'') AS DECIMAL(20,4))                                         -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS valid_bet,                          -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「valid_bet」
               (CAST(NULLIF(TRIM(v.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS game_pnl,                           -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               CAST(NULLIF(TRIM(v.bet16),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS rebate,                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
               CAST(NULLIF(TRIM(v.bet17),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS net_pnl                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_pnl」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S03_agent_score.csv）
      /* ── 扩编维度① 下线留存 ─────────────────────────────────────────
         把 139 天窗口一切两半（前 70 天 / 后 69 天），算「两段都活跃的会员
         占前段活跃会员的比例」。留存高说明这条线在做真实经营；
         留存趋零而流水不低，多半是一批号刷完就换。赏罚方向为「赏」。 */
      half AS (                                                                                           -- 公共表表达式：开启中间结果集 half，其后各行为其定义体（交付件 S03_agent_score.csv）
        SELECT lv3, member_id,                                                                            -- 取列：起始取列子句，本行先列 lv3, member_id，涉 member_id（会员号）、lv3（三级代理线）
               MAX(CASE WHEN bet_date <  '2026-05-30' THEN 1 ELSE 0 END) AS in_h1,                        -- 取最大值表达式：取用 bet_date（营业日），产出「in_h1」
               MAX(CASE WHEN bet_date >= '2026-05-30' THEN 1 ELSE 0 END) AS in_h2                         -- 取最大值表达式：取用 bet_date（营业日），产出「in_h2」
        FROM bs WHERE NULLIF(TRIM(lv3),'') IS NOT NULL                                                    -- 取数来源：取自本条自建的中间结果集 bs
        GROUP BY lv3, member_id                                                                           -- 分组：按 lv3, member_id 汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S03_agent_score.csv）
      ret AS (                                                                                            -- 公共表表达式：开启中间结果集 ret，其后各行为其定义体（交付件 S03_agent_score.csv）
        SELECT lv3,                                                                                       -- 取列：起始取列子句，本行先列 lv3，涉 lv3（三级代理线）
               SUM(CASE WHEN in_h1 = 1 AND in_h2 = 1 THEN 1 ELSE 0 END) * 1.0                             -- 取值表达式：比率之分子，乘 1.0 以避整数除法截断
                 / NULLIF(SUM(in_h1), 0) AS 下线留存                                                          -- 除法或乘法计算：汇总，取用下线留存（代理雷达赏侧维），产出「下线留存」
        FROM half GROUP BY lv3                                                                            -- 取数来源：取自本条自建的中间结果集 half
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S03_agent_score.csv）
      /* ── 扩编维度② 新增会员质量 ─────────────────────────────────────
         只看「窗口内才首次下注」的会员，算其人均净贡献（平台口径，取负号）。
         这一维把「拉人头」与「拉客人」区分开：新增数量高但人均净贡献接近零
         甚至为负，说明拉来的是刷返水的号，不是客人。赏罚方向为「赏」。 */
      newm AS (                                                                                           -- 公共表表达式：开启中间结果集 newm，其后各行为其定义体（交付件 S03_agent_score.csv）
        SELECT lv3, member_id,                                                                            -- 取列：起始取列子句，本行先列 lv3, member_id，涉 member_id（会员号）、lv3（三级代理线）
               MIN(bet_date) AS first_date,                                                               -- 取最小值表达式：取用 bet_date（营业日），产出「first_date」
               -SUM(net_pnl) AS 平台净得                                                                      -- 加减计算：汇总后取负号——会员净输赢取负即平台毛利（GGR），产出「平台净得」
        FROM bs WHERE NULLIF(TRIM(lv3),'') IS NOT NULL                                                    -- 取数来源：取自本条自建的中间结果集 bs
        GROUP BY lv3, member_id                                                                           -- 分组：按 lv3, member_id 汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S03_agent_score.csv）
      nq AS (                                                                                             -- 公共表表达式：开启中间结果集 nq，其后各行为其定义体（交付件 S03_agent_score.csv）
        SELECT lv3,                                                                                       -- 取列：起始取列子句，本行先列 lv3，涉 lv3（三级代理线）
               AVG(平台净得) AS 新增会员质量,                                                                       -- 求均值表达式：取用新增会员质量（代理雷达赏侧维），产出「新增会员质量」
               COUNT(*)      AS n_new_member                                                              -- 计数表达式：产出「n_new_member」
        FROM newm WHERE first_date >= '2026-04-20'   -- 窗口首 30 天之后才首注，视为新增
        GROUP BY lv3                                                                                      -- 分组：按 lv3 汇总
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（交付件 S03_agent_score.csv）
      SELECT b.lv3,                                                                                       -- 取列：起始取列子句，本行先列 b.lv3，涉 lv3（三级代理线）
        COUNT(DISTINCT b.member_id) AS 线下规模,                                                              -- 计数表达式：统计去重个数，产出「线下规模」
        SUM(b.valid_bet) AS 真实流水,                                                                         -- 汇总表达式：取用 valid_bet（有效投注（洗码量））、真实流水（代理雷达赏侧维），产出「真实流水」
        SUM(b.rebate)    AS 退水支出,                                                                         -- 汇总表达式：取用 rebate（退水）、退水支出（退水成本），产出「退水支出」
        SUM(b.game_pnl)  AS 链下游戏输赢,                                                                       -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「链下游戏输赢」
        SUM(b.rebate)/NULLIF(-SUM(b.game_pnl),0) AS 退水消耗比,  -- >1 = 退水吃光赢利
        -- ★ 扩编两列：报告的代理雷达按列名取用，列名一字不可改
        MAX(r.下线留存)      AS 下线留存,                                                                         -- 取最大值表达式：取用下线留存（代理雷达赏侧维），产出「下线留存」
        MAX(n.新增会员质量)  AS 新增会员质量,                                                                         -- 取最大值表达式：取用新增会员质量（代理雷达赏侧维），产出「新增会员质量」
        MAX(n.n_new_member)  AS 新增会员数                                                                     -- 取最大值表达式：产出「新增会员数」
      FROM bs b                                                                                           -- 取数来源：取自本条自建的中间结果集 bs
      LEFT JOIN ret r ON r.lv3 = b.lv3                                                                    -- 左连接：取自本条自建的中间结果集 ret，连接键为 lv3（三级代理线）
      LEFT JOIN nq  n ON n.lv3 = b.lv3                                                                    -- 左连接：取自本条自建的中间结果集 nq，连接键为 lv3（三级代理线）
      WHERE NULLIF(TRIM(b.lv3),'') IS NOT NULL                                                            -- 过滤条件：限定该值非空，涉 lv3（三级代理线）
      GROUP BY b.lv3
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  79. S04_analyst_score.csv   [总包行 6277~6302 · 原 7 列 ＋六层约 60 列]
--     典型学：R01 风控专员　分区：recent（s.x_rec = 1）　连接：q.`entity_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`entity_id`, w.`标注产量`, w.`覆盖IP广度`, w.`判定详尽度`, w.`理由空白数`, w.`首次登记`, w.`最近登记`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT                                                                                              -- 续行：接续上一取列子句，续列 SELECT
        COALESCE(NULLIF(TRIM(creator), ''), '未署名')      AS entity_id,                                     -- 取值表达式：产出「entity_id」
        -- ★ 下面三个列名与报告的风控专员雷达字典严格对应，一字不可改
        COUNT(*)                                            AS 标注产量,                                      -- 计数表达式：产出「标注产量」
        COUNT(DISTINCT TRIM(ip))                            AS 覆盖IP广度,                                    -- 计数表达式：统计去重个数，产出「覆盖IP广度」
        AVG(LENGTH(COALESCE(remarks, '')))                  AS 判定详尽度,                                     -- 求均值表达式：产出「判定详尽度」
        -- ★ 2026-08-14 增列（S1 斧正 · 同名异义之根治）：
        --   「理由空白率」此前在报表侧以代理口径推算（1 − 判定详尽度 ÷ 全体中位详尽度），
        --   该式量的是**理由字数的相对长短**，而非**有没有写理由**——
        --   一名写五百字套话者判为 0，一名写二十字要害者判近 1，名实不符。
        --   今补出真口径之分子。表达式逐字取自 §S-04b（本包第 2,229 行），同表同源，
        --   不新增扫描、不新增连接。报表侧「理由空白率 = 理由空白数 ÷ 标注产量」。
        SUM(CASE WHEN NULLIF(TRIM(remarks),'') IS NULL
                 THEN 1 ELSE 0 END)                         AS 理由空白数,                                     -- 汇总表达式：产出「理由空白数」（理由空白率之分子）
        MIN(addtime)                                        AS 首次登记,                                      -- 取最小值表达式：产出「首次登记」
        MAX(addtime)                                        AS 最近登记                                       -- 取最大值表达式：产出「最近登记」
      FROM ods_mariadb_2b.ods_a168_alert_ip_setting                                                       -- 取数来源：取自风控 IP 名单配置表（人工标记史料源）
      -- ★ 2026-08-14 增补窗口上界（S1 斧正 · 时间一致性）：
      --   实测查出 creator = livegame 之最近登记为 2026-08-09 20:48:43，
      --   晚于本方案分析窗口截止日 2026-08-07——属未来信息。
      --   凡该维参与与窗内结果之对照判定者，未加此界即构成时间一致性违规。
      --   注：alert_ip_setting 为配置类表、无 dt 分区列（见 §EX-00 判据），
      --   故此处以 addtime 施界，不写 dt。
      WHERE addtime < '2026-08-07'                                                                        -- 过滤条件：限定 addtime 小于窗口截止日，杜绝未来信息
      GROUP BY COALESCE(NULLIF(TRIM(creator), ''), '未署名')                                                 -- 分组：按 COALESCE(NULLIF(TRIM(creator), ''), '未署名') 汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`entity_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  80. S05_member_month_panel.csv   [总包行 2614~2678 · 原 10 列 ＋六层约 60 列]
--     典型学：M15 活跃周期　分区：recent（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`bet_date`, w.`流水贡献`, w.`游戏输赢贡献`, w.`活跃稳定`, w.`退水支出`, w.`n_ip`, w.`n_chain`, w.`round_key`, w.`risk_label`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ta AS (            -- 公司测试线代理（214 条，跨五级）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S05_member_month_panel.csv）
      rk AS (                  -- 需求 §3.2：同注单号取最新版本（三级排序去重）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet09，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet11, b.bet13, b.bet14, b.bet16, b.bet17,                                               -- 续行：接续上一取列子句，续列 b.bet11, b.bet13, b.bet14, b.bet16, b.bet17，涉 bet11（汇率）、bet13（下注金额）、bet14（派彩金额）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet38, b.bet39, b.category, b.eid, b.ip,                                                 -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category, b.eid, b.ip，涉 bet38（重對）、bet39（桌号）、eid（荷官工号）
               b.validbet,                                                                                -- 续行：接续上一取列子句，续列 b.validbet，涉 validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S05_member_month_panel.csv）
      vd AS (                  -- 有效注单：非测试线、非重对、一般注单
        SELECT r.*                                                                                        -- 取列：整体承接上游结果集的全部字段，不再逐列列举
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S05_member_month_panel.csv）
      bs AS (                  -- 金额正名：本金/洗码量/游戏输赢/退水/净输赢（一律÷汇率）
        SELECT v.bet05 AS member_id, v.eid AS dealer_id, v.ip AS bet_ip,                                  -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、eid（荷官工号）、bet_ip（下注 IP）
               v.bet20 AS lv3, v.bet09 AS bet_side, v.dt AS bet_date,                                     -- 取值表达式：取用 bet09（玩法）、bet20（三级代理线）、dt（营业日），产出「bet_date」
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.bet08),'') AS DATETIME) AS t_bet,                                       -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「t_bet」
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake,                              -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               CAST(NULLIF(TRIM(v.validbet),'') AS DECIMAL(20,4))                                         -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS valid_bet,                          -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「valid_bet」
               (CAST(NULLIF(TRIM(v.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS game_pnl,                           -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               CAST(NULLIF(TRIM(v.bet16),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS rebate,                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
               CAST(NULLIF(TRIM(v.bet17),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS net_pnl                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_pnl」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S05_member_month_panel.csv）
      lab AS (SELECT bet05 AS member_id, DATE_TRUNC('month', dt) AS ym,                                   -- 取值表达式：取用 bet05（会员号）、dt（营业日）、member_id（会员号），产出「ym」
                     MAX(CASE WHEN risk='1' THEN 1 ELSE 0 END) AS risk_label                              -- 取最大值表达式：产出「risk_label」
              FROM ods_mariadb_2b.ods_a168_dailyreport_member                                             -- 取数来源：取自会员日结报表（日粒度业绩与风控标记）
              GROUP BY bet05, DATE_TRUNC('month', dt))                                                    -- 分组：按 bet05, DATE_TRUNC('month', dt)) 汇总
      SELECT b.member_id, DATE_TRUNC('month', b.bet_date) AS bet_date,                                    -- 取列：起始取列子句，本行先取「bet_date」，涉 member_id（会员号）、bet_date（营业日）
        SUM(b.valid_bet) AS 流水贡献, -SUM(b.game_pnl) AS 游戏输赢贡献,                                             -- 汇总表达式：汇总后取负号——会员净输赢取负即平台毛利（GGR），产出「游戏输赢贡献」
        COUNT(DISTINCT b.bet_date) AS 活跃稳定, SUM(b.rebate) AS 退水支出,                                        -- 计数表达式：统计去重个数，产出「退水支出」
        COUNT(DISTINCT b.bet_ip) AS n_ip, COUNT(DISTINCT b.lv3) AS n_chain,                               -- 计数表达式：统计去重个数，产出「n_chain」
        MIN(b.round_key) AS round_key,                                                                    -- 取最小值表达式：取用 round_key（局键），产出「round_key」
        COALESCE(MAX(l.risk_label),0) AS risk_label                                                       -- 取值表达式：取最大值，产出「risk_label」
      FROM bs b LEFT JOIN lab l                                                                           -- 取数来源：取自本条自建的中间结果集 bs
        ON l.member_id=b.member_id AND l.ym=DATE_TRUNC('month', b.bet_date)                               -- 连接键：限定 l.member_id等于 b.member_id，涉 member_id（会员号）、bet_date（营业日）
      GROUP BY b.member_id, DATE_TRUNC('month', b.bet_date)                                               -- 分组：按 b.member_id, DATE_TRUNC('month', b.bet_date) 汇总
      HAVING COUNT(*) >= 30                                                                               -- 分组后过滤：限定行数不少于 30
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  81. S_player_tail.csv   [总包行 2077~2189 · 原 18 列 ＋六层约 60 列]
--     典型学：M15 活跃周期　分区：recent（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`n_rounds_all`, w.`n_rounds_tail`, w.`tail_share`, w.`tail_win`, w.`tail_lose`, w.`tail_winrate`, w.`norm_winrate`, w.`winrate_diff`, w.`tail_stake`, w.`tail_valid_bet`, w.`tail_game_pnl`, w.`tail_rebate`, w.`tail_net_pnl`, w.`tail_roi`, w.`norm_game_pnl`, w.`norm_roi`, w.`tail_profit_share`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ta AS (                                                                                        -- 公共表表达式：开启中间结果集 ta，其后各行为其定义体（交付件 S_player_tail.csv）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S_player_tail.csv）
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk，其后各行为其定义体（交付件 S_player_tail.csv）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet11,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet11，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet13, b.bet14, b.bet16, b.bet17, b.bet18,                                               -- 续行：接续上一取列子句，续列 b.bet13, b.bet14, b.bet16, b.bet17, b.bet18，涉 bet13（下注金额）、bet14（派彩金额）、bet16（退水金额）
               b.bet19, b.bet20, b.bet21, b.bet22, b.bet38,                                               -- 续行：接续上一取列子句，续列 b.bet19, b.bet20, b.bet21, b.bet22, b.bet38，涉 bet19（二级代理线）、bet20（三级代理线）、bet21（四级代理线）
               b.bet39, b.category, b.validbet,                                                           -- 续行：接续上一取列子句，续列 b.bet39, b.category, b.validbet，涉 bet39（桌号）、validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S_player_tail.csv）
      vd AS (                                                                                             -- 公共表表达式：开启中间结果集 vd，其后各行为其定义体（交付件 S_player_tail.csv）
        SELECT r.*                                                                                        -- 取列：整体承接上游结果集的全部字段，不再逐列列举
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S_player_tail.csv）
      bs AS (                                                                                             -- 公共表表达式：开启中间结果集 bs，其后各行为其定义体（交付件 S_player_tail.csv）
        SELECT v.bet05 AS member_id,                                                                      -- 取列：起始取列子句，本行先取「member_id」，涉 bet05（会员号）、member_id（会员号）
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
                                                                                                          -- ⚠ 局键完整性（2026-08-17 实测 · Z08_roundkey_integrity.csv）：曾疑本键缺日期
                                                                                                          --   成分致跨日塌缩（S1 候选）。今实测结案：跨桌局键 0、bet03 跨桌 0、
                                                                                                          --   注单/局键 = 20.73 注/局（塌缩则应达数千）、含哨兵值 103 行（7.8e-7）。
                                                                                                          --   →【塌缩未发生，S1 候选解除】。附：bet03 已单独决定桌号，bet39 于本键属冗余。
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.bet08),'') AS DATETIME) AS t_bet,                                       -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「t_bet」
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake,                              -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               CAST(NULLIF(TRIM(v.validbet),'') AS DECIMAL(20,4))                                         -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS valid_bet,                          -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「valid_bet」
               (CAST(NULLIF(TRIM(v.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS game_pnl,                           -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               CAST(NULLIF(TRIM(v.bet16),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS rebate,                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
               CAST(NULLIF(TRIM(v.bet17),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS net_pnl                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_pnl」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S_player_tail.csv）
      ord AS (                                                                                            -- 公共表表达式：开启中间结果集 ord，其后各行为其定义体（交付件 S_player_tail.csv）
        SELECT b.*, PERCENT_RANK() OVER (PARTITION BY round_key ORDER BY t_bet) AS pr_in_round,           -- 取列：整体承接上游结果集的全部字段，不再逐列列举
                    COUNT(*)      OVER (PARTITION BY round_key)                 AS n_in_round             -- 计数表达式：取用 round_key（局键），产出「n_in_round」
        FROM bs b                                                                                         -- 取数来源：取自本条自建的中间结果集 bs
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S_player_tail.csv）
      prd AS (                                                                                            -- 公共表表达式：开启中间结果集 prd，其后各行为其定义体（交付件 S_player_tail.csv）
        SELECT member_id, round_key, table_id,                                                            -- 取列：起始取列子句，本行先列 member_id, round_key, table_id，涉 round_key（局键）、member_id（会员号）
               MAX(pr_in_round) AS max_pr,                                                                -- 取最大值表达式：产出「max_pr」
               MAX(n_in_round)  AS n_in_round,                                                            -- 取最大值表达式：产出「n_in_round」
               SUM(stake) AS stake, SUM(valid_bet) AS valid_bet,                                          -- 汇总表达式：取用 stake（下注额（经汇率归一化））、valid_bet（有效投注（洗码量）），产出「valid_bet」
               SUM(game_pnl) AS game_pnl, SUM(rebate) AS rebate, SUM(net_pnl) AS net_pnl,                 -- 汇总表达式：取用 net_pnl（会员净输赢）、game_pnl（游戏净输赢）、rebate（退水），产出「net_pnl」
               CASE WHEN SUM(game_pnl) > 0 THEN 1 ELSE 0 END AS is_win,                                   -- 条件分支：汇总，取用 game_pnl（游戏净输赢），产出「is_win」
               CASE WHEN SUM(game_pnl) < 0 THEN 1 ELSE 0 END AS is_lose                                   -- 条件分支：汇总，取用 game_pnl（游戏净输赢），产出「is_lose」
        FROM ord WHERE n_in_round >= 5                                                                    -- 取数来源：取自本条自建的中间结果集 ord
        GROUP BY member_id, round_key, table_id                                                           -- 分组：按 member_id, round_key, table_id 汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S_player_tail.csv）
      fl AS (                                                                                             -- 公共表表达式：开启中间结果集 fl，其后各行为其定义体（交付件 S_player_tail.csv）
        SELECT p.*, CASE WHEN p.max_pr >= 0.80 THEN 1 ELSE 0 END AS is_tail                               -- 取列：整体承接上游结果集的全部字段，不再逐列列举
                                                                                                          -- ⚠ 阈值溯源（2026-08-17）：【基准】0.80（现行代码），业务启发式非统计推导。
                                                                                                          --   【建议默认】暂无——本阈值当前不具可优化性：① 尾段假说已证伪（p=0.5847，
                                                                                                          --   个体层 winrate_diff>0 仅 49.7%），无有效目标函数，网格搜索即在噪音上过拟合；
                                                                                                          --   ② PERCENT_RANK 离散，曝光率随 n_in_round 在 20.0%~33.3% 振荡，目标函数不连续；
                                                                                                          --   ③ 与 L6 闸 n_in_round>=5 强耦合（n=5 时退化为「严格最后一注」），不可分别调参。
        FROM prd p                                                                                        -- 取数来源：取自本条自建的中间结果集 prd
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（交付件 S_player_tail.csv）
      SELECT member_id,                                                                                   -- 取列：起始取列子句，本行先列 member_id，涉 member_id（会员号）
        COUNT(*)                      AS n_rounds_all,                                                    -- 计数表达式：产出「n_rounds_all」
        SUM(is_tail)                  AS n_rounds_tail,                                                   -- 汇总表达式：产出「n_rounds_tail」
        SUM(is_tail) * 1.0 / COUNT(*) AS tail_share,                                                      -- 汇总表达式：计数，产出「tail_share」
        SUM(is_tail * is_win)  AS tail_win,                                                               -- 汇总表达式：产出「tail_win」
        SUM(is_tail * is_lose) AS tail_lose,                                                              -- 汇总表达式：产出「tail_lose」
        SUM(is_tail * is_win) * 1.0                                                                       -- 取值表达式：比率之分子，乘 1.0 以避整数除法截断
          / NULLIF(SUM(is_tail * is_win) + SUM(is_tail * is_lose), 0) AS tail_winrate,                    -- 除法或乘法计算：汇总，产出「tail_winrate」
        SUM((1 - is_tail) * is_win) * 1.0                                                                 -- 取值表达式：比率之分子，乘 1.0 以避整数除法截断
          / NULLIF(SUM((1 - is_tail) * is_win) + SUM((1 - is_tail) * is_lose), 0) AS norm_winrate,        -- 除法或乘法计算：汇总，产出「norm_winrate」
        SUM(is_tail * is_win) * 1.0                                                                       -- 取值表达式：比率之分子，乘 1.0 以避整数除法截断
          / NULLIF(SUM(is_tail * is_win) + SUM(is_tail * is_lose), 0)                                     -- 除法或乘法计算：汇总
          - SUM((1 - is_tail) * is_win) * 1.0                                                             -- 取值表达式：比率之分子，乘 1.0 以避整数除法截断
          / NULLIF(SUM((1 - is_tail) * is_win) + SUM((1 - is_tail) * is_lose), 0) AS winrate_diff,        -- 除法或乘法计算：汇总，产出「winrate_diff」
        SUM(is_tail * stake)     AS tail_stake,                                                           -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「tail_stake」
        SUM(is_tail * valid_bet) AS tail_valid_bet,                                                       -- 汇总表达式：取用 valid_bet（有效投注（洗码量）），产出「tail_valid_bet」
        SUM(is_tail * game_pnl)  AS tail_game_pnl,                                                        -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「tail_game_pnl」
        SUM(is_tail * rebate)    AS tail_rebate,                                                          -- 汇总表达式：取用 rebate（退水），产出「tail_rebate」
        SUM(is_tail * net_pnl)   AS tail_net_pnl,                                                         -- 汇总表达式：取用 net_pnl（会员净输赢），产出「tail_net_pnl」
        SUM(is_tail * game_pnl) / NULLIF(SUM(is_tail * valid_bet), 0) AS tail_roi,                        -- 汇总表达式：取用 valid_bet（有效投注（洗码量））、game_pnl（游戏净输赢），产出「tail_roi」
        SUM((1 - is_tail) * game_pnl) AS norm_game_pnl,                                                   -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「norm_game_pnl」
        SUM((1 - is_tail) * game_pnl)                                                                     -- 汇总表达式：取用 game_pnl（游戏净输赢）
          / NULLIF(SUM((1 - is_tail) * valid_bet), 0) AS norm_roi,                                        -- 除法或乘法计算：汇总，取用 valid_bet（有效投注（洗码量）），产出「norm_roi」
        SUM(CASE WHEN is_tail = 1 AND game_pnl > 0 THEN game_pnl ELSE 0 END)                              -- 汇总表达式：取用 game_pnl（游戏净输赢）
          / NULLIF(SUM(CASE WHEN game_pnl > 0 THEN game_pnl ELSE 0 END), 0) AS tail_profit_share          -- 除法或乘法计算：汇总，取用 game_pnl（游戏净输赢），产出「tail_profit_share」
      FROM fl                                                                                             -- 取数来源：取自本条自建的中间结果集 fl
      GROUP BY member_id                                                                                  -- 分组：按 member_id 汇总
      HAVING COUNT(*) >= 30                                                                               -- 分组后过滤：限定行数不少于 30
                                                                                                          -- ⚠ 阈值溯源（2026-08-17 实测 · S_player_tail.csv）：
                                                                                                          --   【基准】30（现行代码）—— 实测 Wilson 95% 宽度 0.3369，为目标 0.15 的 2.25 倍。
                                                                                                          --   原注「低于此样本量宽度 > 0.15」字面为真但具误导：30 并未使宽度达标，
                                                                                                          --   它是工程折中而非统计闸门。
                                                                                                          --   【建议默认】171 —— 宽度 0.1482 首次达标，业务代价极小：会员 159,603→57,676
                                                                                                          --   （留存 36.1%），但洗码量仅损失 6.09%（留存 93.91%），流失者皆低活跃会员。
                                                                                                          --   各档实测：50→0.2671｜100→0.1923｜171→0.1482。代码改动须裁定，现以基准运行。
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  82. S_second_dist.csv   [总包行 1914~1982 · 原 8 列 ＋六层约 60 列]
--     典型学：M15 活跃周期　分区：recent（s.x_rec = 1）　连接：q.`table_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`table_id`, w.`n_orders`, w.`p50`, w.`p75`, w.`p90`, w.`p95`, w.`p99`, w.`max_progress`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_table                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_table IS NOT NULL AND TRIM(CAST(s.x_table AS STRING)) <> ''
    GROUP BY s.x_table
  ),
    q AS (
      WITH ta AS (            -- 公司测试线代理（214 条，跨五级）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S_second_dist.csv）
      rk AS (                  -- 需求 §3.2：同注单号取最新版本（三级排序去重）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet09，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet11, b.bet13, b.bet14, b.bet16, b.bet17,                                               -- 续行：接续上一取列子句，续列 b.bet11, b.bet13, b.bet14, b.bet16, b.bet17，涉 bet11（汇率）、bet13（下注金额）、bet14（派彩金额）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet38, b.bet39, b.category, b.eid, b.ip,                                                 -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category, b.eid, b.ip，涉 bet38（重對）、bet39（桌号）、eid（荷官工号）
               b.validbet,                                                                                -- 续行：接续上一取列子句，续列 b.validbet，涉 validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S_second_dist.csv）
      vd AS (                  -- 有效注单：非测试线、非重对、一般注单
        SELECT r.*                                                                                        -- 取列：整体承接上游结果集的全部字段，不再逐列列举
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S_second_dist.csv）
      bs AS (                  -- 金额正名：本金/洗码量/游戏输赢/退水/净输赢（一律÷汇率）
        SELECT v.bet05 AS member_id, v.eid AS dealer_id, v.ip AS bet_ip,                                  -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、eid（荷官工号）、bet_ip（下注 IP）
               v.bet20 AS lv3, v.bet09 AS bet_side, v.dt AS bet_date,                                     -- 取值表达式：取用 bet09（玩法）、bet20（三级代理线）、dt（营业日），产出「bet_date」
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.bet08),'') AS DATETIME) AS t_bet,                                       -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「t_bet」
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake,                              -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               CAST(NULLIF(TRIM(v.validbet),'') AS DECIMAL(20,4))                                         -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS valid_bet,                          -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「valid_bet」
               (CAST(NULLIF(TRIM(v.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS game_pnl,                           -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               CAST(NULLIF(TRIM(v.bet16),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS rebate,                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
               CAST(NULLIF(TRIM(v.bet17),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS net_pnl                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_pnl」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 S_second_dist.csv）
      gi AS (SELECT gi011 AS table_id,                                                                    -- 取值表达式：取用 gi011（桌号），产出「table_id」
                    CONCAT_WS('|', gi002, gi003, gi011) AS round_key,                                     -- 取值表达式：取用 gi011（桌号）、round_key（局键），产出「round_key」
                    gi004 AS t_open, gi006 AS t_reveal                                                    -- 取值表达式：取用 gi004（局开始时刻）、gi006（开牌时刻），产出「t_reveal」
             FROM ods_mariadb_2b.ods_a168_game_info                                                       -- 取数来源：取自局信息表（靴局、开牌与桌号的时序基准）
             WHERE gi001='101' AND gi013='1' AND is_lock='N'                                              -- 过滤条件：限定 gi001等于 '101'、gi013等于 '1'、is_lock等于 'N'，涉 gi001（局主键）
               AND gi004>='2026-03-21' AND gi004<'2026-08-07'),                                           -- 并列条件：限定 gi004不少于 '2026-03-21'，涉 gi004（局开始时刻）
      pr AS (SELECT b.table_id, b.round_key,                                                              -- 取值表达式：取用 round_key（局键）
                    (UNIX_TIMESTAMP(b.t_bet)-UNIX_TIMESTAMP(g.t_open))*1.0                                -- 取值表达式：比率之分子，乘 1.0 以避整数除法截断
                    /NULLIF(UNIX_TIMESTAMP(g.t_reveal)-UNIX_TIMESTAMP(g.t_open),0) AS progress            -- 除法或乘法计算：产出「progress」
             FROM bs b JOIN gi g ON g.round_key=b.round_key)                                              -- 取数来源：取自本条自建的中间结果集 bs，连接键为 round_key（局键）
      SELECT table_id, COUNT(*) AS n_orders,                                                              -- 取列：起始取列子句，本行先取「n_orders」
             PERCENTILE_APPROX(progress,0.5)  AS p50,                                                     -- 取近似分位数表达式：产出「p50」
             PERCENTILE_APPROX(progress,0.75) AS p75,                                                     -- 取近似分位数表达式：产出「p75」
             PERCENTILE_APPROX(progress,0.9)  AS p90,                                                     -- 取近似分位数表达式：产出「p90」
             PERCENTILE_APPROX(progress,0.95) AS p95,                                                     -- 取近似分位数表达式：产出「p95」
             PERCENTILE_APPROX(progress,0.99) AS p99,                                                     -- 取近似分位数表达式：产出「p99」
             MAX(progress) AS max_progress                                                                -- 取最大值表达式：产出「max_progress」
      FROM pr WHERE progress BETWEEN 0 AND 1.5                                                            -- 取数来源：取自本条自建的中间结果集 pr
      GROUP BY table_id
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`table_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  83. T02_daily_roi.csv   [总包行 5146~5190 · 原 5 列 ＋六层约 60 列]
--     典型学：M15 活跃周期　分区：recent（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`bet_date`, w.`stake`, w.`game_pnl`, w.`n_rounds`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH test_agents AS (                                                                               -- 公共表表达式：开启中间结果集 test_agents，其后各行为其定义体（§T02）
        SELECT age001 AS agent_id FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                   -- 取列：取用代理主档（五级代理线归属来源），产出「agent_id」
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§T02）
      ranked AS (                                                                                         -- 公共表表达式：开启中间结果集 ranked，其后各行为其定义体（§T02）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet11, b.bet13,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet11, b.bet13，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet14, b.bet18, b.bet19, b.bet20, b.bet21,                                               -- 续行：接续上一取列子句，续列 b.bet14, b.bet18, b.bet19, b.bet20, b.bet21，涉 bet14（派彩金额）、bet18（一级代理线）、bet19（二级代理线）
               b.bet22, b.bet38, b.bet39, b.category,                                                     -- 续行：接续上一取列子句，续列 b.bet22, b.bet38, b.bet39, b.category，涉 bet22（五级代理线）、bet38（重對）、bet39（桌号）
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b            -- ★ 表名开关
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'，涉 dt（营业日）
          AND b.bet02 = '101'                                                                             -- 并列条件：限定 b.bet02等于 '101'，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§T02）
      ord AS (                                                                                            -- 公共表表达式：开启中间结果集 ord，其后各行为其定义体（§T02）
        SELECT r.bet05 AS member_id, r.dt AS bet_date,                                                    -- 取列：起始取列子句，本行先取「bet_date」，涉 bet05（会员号）、dt（营业日）、member_id（会员号）
               CONCAT_WS('|', r.bet03, r.bet04, r.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)),0) AS stake,                    -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               (CAST(NULLIF(TRIM(r.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)),0) AS game_pnl                  -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet18（一级代理线）
        LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet19（二级代理线）
        LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet20（三级代理线）
        LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet21（四级代理线）
        LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND COALESCE(t1.agent_id,t2.agent_id,t3.agent_id,t4.agent_id,t5.agent_id) IS NULL               -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§T02）
      act AS (   -- 报告里索提诺要求「活跃日 ≥ 20」，少于 20 天的会员导出来也用不上
        SELECT member_id FROM ord                                                                         -- 取列：取用 member_id（会员号）
        GROUP BY member_id HAVING COUNT(DISTINCT bet_date) >= 20                                          -- 分组：统计去重个数
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§T02）
      SELECT o.member_id, o.bet_date,                                                                     -- 取列：起始取列子句，本行先列 o.member_id, o.bet_date，涉 member_id（会员号）、bet_date（营业日）
             SUM(o.stake)                     AS stake,                                                   -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake」
             SUM(o.game_pnl)                  AS game_pnl,                                                -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「game_pnl」
             COUNT(DISTINCT o.round_key)      AS n_rounds                                                 -- 计数表达式：统计去重个数，产出「n_rounds」
      FROM ord o JOIN act a ON a.member_id = o.member_id                                                  -- 取数来源：取自本条自建的中间结果集 ord，连接键为 member_id（会员号）
      GROUP BY o.member_id, o.bet_date                                                                    -- 分组：按 o.member_id, o.bet_date 汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake_xagg, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds AS n_rounds_xagg,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  84. T03_arbitrage.csv   [总包行 5204~5275 · 原 10 列 ＋六层约 60 列]
--     典型学：M02 对冲对打　分区：hedge（h.n_side >= 2）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`stake`, w.`validbet`, w.`game_pnl`, w.`net_pnl`, w.`rebate`, w.`hedge_rate`, w.`stake_cv`, w.`n_rounds`, w.`n_days`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_hg AS (SELECT x_round, COUNT(DISTINCT x_product) AS n_side FROM x_bs GROUP BY x_round),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN h.n_side >= 2 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN h.n_side >= 2 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN h.n_side >= 2 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (h.n_side >= 2) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (h.n_side >= 2) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (h.n_side >= 2) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    JOIN x_hg h ON h.x_round = s.x_round
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH test_agents AS (                                                                               -- 公共表表达式：开启中间结果集 test_agents，其后各行为其定义体（§T03）
        SELECT age001 AS agent_id FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                   -- 取列：取用代理主档（五级代理线归属来源），产出「agent_id」
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§T03）
      ranked AS (                                                                                         -- 公共表表达式：开启中间结果集 ranked，其后各行为其定义体（§T03）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet09, b.bet11,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet09, b.bet11，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet13, b.bet14, b.bet16, b.bet17, b.bet18,                                               -- 续行：接续上一取列子句，续列 b.bet13, b.bet14, b.bet16, b.bet17, b.bet18，涉 bet13（下注金额）、bet14（派彩金额）、bet16（退水金额）
               b.bet19, b.bet20, b.bet21, b.bet22, b.bet38,                                               -- 续行：接续上一取列子句，续列 b.bet19, b.bet20, b.bet21, b.bet22, b.bet38，涉 bet19（二级代理线）、bet20（三级代理线）、bet21（四级代理线）
               b.bet39, b.category, b.validbet,                                                           -- 续行：接续上一取列子句，续列 b.bet39, b.category, b.validbet，涉 bet39（桌号）、validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b            -- ★ 表名开关
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'，涉 dt（营业日）
          AND b.bet02 = '101'                                                                             -- 并列条件：限定 b.bet02等于 '101'，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§T03）
      ord AS (                                                                                            -- 公共表表达式：开启中间结果集 ord，其后各行为其定义体（§T03）
        SELECT r.bet05 AS member_id, r.dt AS bet_date, r.bet09 AS bet_side,                               -- 取列：起始取列子句，本行先取「bet_side」，涉 bet05（会员号）、bet09（玩法）、dt（营业日）
               CONCAT_WS('|', r.bet03, r.bet04, r.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) AS fx,                                     -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「fx」
               CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4)) AS stake_raw,                              -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake_raw」
               CAST(NULLIF(TRIM(r.bet14),'') AS DECIMAL(20,4)) AS payout_raw,                             -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「payout_raw」
               CAST(NULLIF(TRIM(r.bet16),'') AS DECIMAL(20,4)) AS rebate_raw,                             -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate_raw」
               CAST(NULLIF(TRIM(r.bet17),'') AS DECIMAL(20,4)) AS net_raw,                                -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_raw」
               CAST(NULLIF(TRIM(r.validbet),'') AS DECIMAL(20,4)) AS vb_raw                               -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「vb_raw」
        FROM ranked r                                                                                     -- 取数来源：取自本条自建的中间结果集 ranked
        LEFT JOIN test_agents t1 ON t1.agent_id = r.bet18                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet18（一级代理线）
        LEFT JOIN test_agents t2 ON t2.agent_id = r.bet19                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet19（二级代理线）
        LEFT JOIN test_agents t3 ON t3.agent_id = r.bet20                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet20（三级代理线）
        LEFT JOIN test_agents t4 ON t4.agent_id = r.bet21                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet21（四级代理线）
        LEFT JOIN test_agents t5 ON t5.agent_id = r.bet22                                                 -- 左连接：取自本条自建的中间结果集 test_agents，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND COALESCE(t1.agent_id,t2.agent_id,t3.agent_id,t4.agent_id,t5.agent_id) IS NULL               -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§T03）
      o2 AS (                                                                                             -- 公共表表达式：开启中间结果集 o2，其后各行为其定义体（§T03）
        SELECT member_id, bet_date, round_key,                                                            -- 取列：起始取列子句，本行先列 member_id, bet_date, round_key，涉 round_key（局键）、member_id（会员号）、bet_date（营业日）
               stake_raw / fx                AS stake,                                                    -- 取值表达式：取用 stake（下注额（经汇率归一化）），产出「stake」
               COALESCE(vb_raw,stake_raw)/fx AS validbet,                                                 -- 取值表达式：取用 validbet（有效投注（洗码量）），产出「validbet」
               (payout_raw - stake_raw)/fx   AS game_pnl,                                                 -- 续行：取用 game_pnl（游戏净输赢），产出「game_pnl」
               rebate_raw / fx               AS rebate,                                                   -- 取值表达式：取用 rebate（退水），产出「rebate」
               net_raw / fx                  AS net_pnl,                                                  -- 取值表达式：取用 net_pnl（会员净输赢），产出「net_pnl」
               /* ★ 2026-08-07 DX-01 实测校准：bet09 为英文玩法名。
                  必须用等值匹配，不能用 LIKE '%BANK%' —— 后者会把
                  BankerDragonBonus(20.7万) 与 BankerNatural(6.2万) 一并算成庄，
                  而那是边注，与主线庄闲不构成对冲关系。 */
               CASE WHEN TRIM(bet_side) = 'Banker' THEN 1 ELSE 0 END AS is_b,                             -- 条件分支：产出「is_b」
               CASE WHEN TRIM(bet_side) = 'Player' THEN 1 ELSE 0 END AS is_p                              -- 条件分支：产出「is_p」
        FROM ord                                                                                          -- 取数来源：取自本条自建的中间结果集 ord
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§T03）
      rk AS (      -- 局级：该会员本局是否同时押了庄与闲（自对押）
        SELECT member_id, bet_date, round_key,                                                            -- 取列：起始取列子句，本行先列 member_id, bet_date, round_key，涉 round_key（局键）、member_id（会员号）、bet_date（营业日）
               SUM(stake) AS stake, SUM(validbet) AS validbet,                                            -- 汇总表达式：取用 stake（下注额（经汇率归一化））、validbet（有效投注（洗码量）），产出「validbet」
               SUM(game_pnl) AS game_pnl, SUM(rebate) AS rebate, SUM(net_pnl) AS net_pnl,                 -- 汇总表达式：取用 net_pnl（会员净输赢）、game_pnl（游戏净输赢）、rebate（退水），产出「net_pnl」
               CASE WHEN MAX(is_b)=1 AND MAX(is_p)=1 THEN 1 ELSE 0 END AS is_self_hedge                   -- 条件分支：取最大值，产出「is_self_hedge」
        FROM o2 GROUP BY member_id, bet_date, round_key                                                   -- 取数来源：取自本条自建的中间结果集 o2
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§T03）
      SELECT member_id,                                                                                   -- 取列：起始取列子句，本行先列 member_id，涉 member_id（会员号）
             SUM(stake)                          AS stake,                                                -- 汇总表达式：取用 stake（下注额（经汇率归一化）），产出「stake」
             SUM(validbet)                       AS validbet,                                             -- 汇总表达式：取用 validbet（有效投注（洗码量）），产出「validbet」
             SUM(game_pnl)                       AS game_pnl,                                             -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「game_pnl」
             SUM(net_pnl)                        AS net_pnl,                                              -- 汇总表达式：取用 net_pnl（会员净输赢），产出「net_pnl」
             SUM(rebate)                         AS rebate,                                               -- 汇总表达式：取用 rebate（退水），产出「rebate」
             SUM(is_self_hedge) * 1.0 / COUNT(*) AS hedge_rate,     -- 自对押率
             STDDEV_SAMP(stake) / NULLIF(AVG(stake),0) AS stake_cv, -- 注额变异系数（打水者极低）
             COUNT(*)                            AS n_rounds,                                             -- 计数表达式：产出「n_rounds」
             COUNT(DISTINCT bet_date)            AS n_days                                                -- 计数表达式：统计去重个数，产出「n_days」
      FROM rk                                                                                             -- 取数来源：取自本条自建的中间结果集 rk
      GROUP BY member_id                                                                                  -- 分组：按 member_id 汇总
      HAVING COUNT(*) >= 100                       -- 局数太少算不出稳定的比率
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake_xagg, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds AS n_rounds_xagg,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hedge,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hedge,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hedge,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hedge,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hedge,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hedge,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hedge,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hedge,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  85. TL00_table_inventory.csv   [总包行 8168~8215 · 原 5 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`TABLE_NAME`, w.`列数`, w.`时间列数`, w.`处置类列数`, w.`定日类列数`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT  c.TABLE_NAME,
              COUNT(*)                                                   AS 列数,
              SUM(CASE WHEN c.DATA_TYPE IN ('date','datetime') THEN 1 ELSE 0 END) AS 时间列数,
              SUM(CASE WHEN c.COLUMN_COMMENT LIKE '%限額%'
                         OR c.COLUMN_COMMENT LIKE '%限红%'
                         OR c.COLUMN_COMMENT LIKE '%限紅%'
                         OR c.COLUMN_COMMENT LIKE '%退水%'
                         OR c.COLUMN_COMMENT LIKE '%凍結%'
                         OR c.COLUMN_COMMENT LIKE '%冻结%'
                         OR c.COLUMN_COMMENT LIKE '%停用%'
                         OR c.COLUMN_COMMENT LIKE '%狀態%'
                         OR c.COLUMN_COMMENT LIKE '%状态%'
                         OR c.COLUMN_COMMENT LIKE '%審核%'
                         OR c.COLUMN_COMMENT LIKE '%审核%'
                         OR c.COLUMN_COMMENT LIKE '%處理%'
                         OR c.COLUMN_COMMENT LIKE '%处理%'
                         OR c.COLUMN_COMMENT LIKE '%操作%'
                         OR c.COLUMN_COMMENT LIKE '%備註%'
                         OR c.COLUMN_COMMENT LIKE '%备注%'
                       THEN 1 ELSE 0 END)                                AS 处置类列数,
              SUM(CASE WHEN c.COLUMN_COMMENT LIKE '%時間%'
                         OR c.COLUMN_COMMENT LIKE '%时间%'
                         OR c.COLUMN_COMMENT LIKE '%日期%'
                         OR c.COLUMN_NAME   LIKE '%time%'
                         OR c.COLUMN_NAME   LIKE '%date%'
                       THEN 1 ELSE 0 END)                                AS 定日类列数
      FROM    information_schema.columns c
      WHERE   c.TABLE_SCHEMA = 'ods_mariadb_2b'
        AND   c.TABLE_NAME LIKE 'ods_a168_%'
      GROUP BY c.TABLE_NAME
      HAVING  SUM(CASE WHEN c.COLUMN_COMMENT LIKE '%限額%'
                         OR c.COLUMN_COMMENT LIKE '%限红%'
                         OR c.COLUMN_COMMENT LIKE '%限紅%'
                         OR c.COLUMN_COMMENT LIKE '%退水%'
                         OR c.COLUMN_COMMENT LIKE '%凍結%'
                         OR c.COLUMN_COMMENT LIKE '%冻结%'
                         OR c.COLUMN_COMMENT LIKE '%停用%'
                         OR c.COLUMN_COMMENT LIKE '%狀態%'
                         OR c.COLUMN_COMMENT LIKE '%状态%'
                         OR c.COLUMN_COMMENT LIKE '%審核%'
                         OR c.COLUMN_COMMENT LIKE '%审核%'
                         OR c.COLUMN_COMMENT LIKE '%處理%'
                         OR c.COLUMN_COMMENT LIKE '%处理%'
                         OR c.COLUMN_COMMENT LIKE '%操作%'
                         OR c.COLUMN_COMMENT LIKE '%備註%'
                         OR c.COLUMN_COMMENT LIKE '%备注%'
                       THEN 1 ELSE 0 END) > 0
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  86. TL02_config_change.csv   [总包行 8238~8269 · 原 4 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`变更列`, w.`发生变更的会员数`, w.`在册会员数`, w.`变更率`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT  变更列,
              SUM(CASE WHEN 取值数 > 1 THEN 1 ELSE 0 END)        AS 发生变更的会员数,
              COUNT(*)                                          AS 在册会员数,
              SUM(CASE WHEN 取值数 > 1 THEN 1 ELSE 0 END) * 1.0 / COUNT(*) AS 变更率
      FROM (
          SELECT  '退水 mem003' AS 变更列, CAST(mem001 AS STRING) AS mem,
                  COUNT(DISTINCT CAST(mem003 AS STRING)) AS 取值数
          FROM ods_mariadb_2b.ods_a168_member_dtl
          WHERE dt >= '2026-03-21' AND dt < '2026-08-07' GROUP BY 1, 2
          UNION ALL
          SELECT  '最大押分 mem009', CAST(mem001 AS STRING),
                  COUNT(DISTINCT CAST(mem009 AS STRING))
          FROM ods_mariadb_2b.ods_a168_member_dtl
          WHERE dt >= '2026-03-21' AND dt < '2026-08-07' GROUP BY 1, 2
          UNION ALL
          SELECT  '新版限額 mem015', CAST(mem001 AS STRING),
                  COUNT(DISTINCT CAST(mem015 AS STRING))
          FROM ods_mariadb_2b.ods_a168_member_dtl
          WHERE dt >= '2026-03-21' AND dt < '2026-08-07' GROUP BY 1, 2
          UNION ALL
          SELECT  '最大可贏 mem012', CAST(mem001 AS STRING),
                  COUNT(DISTINCT CAST(mem012 AS STRING))
          FROM ods_mariadb_2b.ods_a168_member_dtl
          WHERE dt >= '2026-03-21' AND dt < '2026-08-07' GROUP BY 1, 2
          UNION ALL
          SELECT  '可贏可輸起算 mem013', CAST(mem001 AS STRING),
                  COUNT(DISTINCT CAST(mem013 AS STRING))
          FROM ods_mariadb_2b.ods_a168_member_dtl
          WHERE dt >= '2026-03-21' AND dt < '2026-08-07' GROUP BY 1, 2
      ) t
      GROUP BY 变更列
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  87. TL03_pseudo_treatment.csv   [总包行 8276~8324 · 原 6 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`change_date`, w.`action_type`, w.`value_before`, w.`value_after`, w.`direction`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH snap AS (
          SELECT  CAST(mem001 AS STRING)                        AS member_id,
                  dt,
                  CAST(NULLIF(TRIM(mem003), '') AS DOUBLE)      AS rebate,
                  CAST(NULLIF(TRIM(mem009), '') AS DOUBLE)      AS maxbet,
                  CAST(NULLIF(TRIM(mem012), '') AS DOUBLE)      AS maxwin,
                  CAST(NULLIF(TRIM(mem014), '') AS DOUBLE)      AS maxloss
          FROM    ods_mariadb_2b.ods_a168_member_dtl
          WHERE   dt >= '2026-03-21'
            AND   dt <  '2026-08-07'
            AND   CAST(mem002 AS STRING) = '1'
      ),
      lagged AS (
          SELECT  member_id, dt, rebate, maxbet, maxwin, maxloss,
                  LAG(rebate)  OVER (PARTITION BY member_id ORDER BY dt) AS rebate_prev,
                  LAG(maxbet)  OVER (PARTITION BY member_id ORDER BY dt) AS maxbet_prev,
                  LAG(maxwin)  OVER (PARTITION BY member_id ORDER BY dt) AS maxwin_prev,
                  LAG(maxloss) OVER (PARTITION BY member_id ORDER BY dt) AS maxloss_prev
          FROM    snap
      )
      SELECT  member_id,
              dt                                                AS change_date,
              CASE WHEN rebate <> rebate_prev THEN '退水'
                   WHEN maxbet <> maxbet_prev THEN '最大押分'
                   WHEN maxwin <> maxwin_prev THEN '最大可贏'
                   ELSE '最大可輸' END                           AS action_type,
              CASE WHEN rebate <> rebate_prev THEN rebate_prev
                   WHEN maxbet <> maxbet_prev THEN maxbet_prev
                   WHEN maxwin <> maxwin_prev THEN maxwin_prev
                   ELSE maxloss_prev END                         AS value_before,
              CASE WHEN rebate <> rebate_prev THEN rebate
                   WHEN maxbet <> maxbet_prev THEN maxbet
                   WHEN maxwin <> maxwin_prev THEN maxwin
                   ELSE maxloss END                              AS value_after,
              CASE WHEN rebate <> rebate_prev THEN
                        CASE WHEN rebate < rebate_prev THEN '收紧' ELSE '放宽' END
                   WHEN maxbet <> maxbet_prev THEN
                        CASE WHEN maxbet < maxbet_prev THEN '收紧' ELSE '放宽' END
                   WHEN maxwin <> maxwin_prev THEN
                        CASE WHEN maxwin < maxwin_prev THEN '收紧' ELSE '放宽' END
                   ELSE CASE WHEN maxloss < maxloss_prev THEN '收紧' ELSE '放宽' END
              END                                                AS direction
      FROM    lagged
      WHERE   rebate_prev IS NOT NULL
        AND   (rebate  <> rebate_prev
           OR  maxbet  <> maxbet_prev
           OR  maxwin  <> maxwin_prev
           OR  maxloss <> maxloss_prev)
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  88. TL03b_pseudo_treatment.csv   [总包行 8417~8466 · 原 8 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`cate`, w.`prev_date`, w.`change_date`, w.`action_type`, w.`value_before`, w.`value_after`, w.`direction`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH snap AS (
          SELECT  CAST(mem001 AS STRING)                        AS member_id,
                  CAST(mem002 AS STRING)                        AS cate,
                  dt,
                  COALESCE(CAST(NULLIF(TRIM(mem003), '') AS DOUBLE), -999999) AS rebate,
                  COALESCE(CAST(NULLIF(TRIM(mem009), '') AS DOUBLE), -999999) AS maxbet,
                  COALESCE(CAST(NULLIF(TRIM(mem012), '') AS DOUBLE), -999999) AS maxwin,
                  COALESCE(CAST(NULLIF(TRIM(mem014), '') AS DOUBLE), -999999) AS maxloss
          FROM    ods_mariadb_2b.ods_a168_member_dtl
          WHERE   dt >= '2026-03-21'
            AND   dt <= '2026-07-18'
            AND   dt NOT IN ('2026-05-21', '2026-07-18')
      ),
      lagged AS (
          SELECT  member_id, cate, dt, rebate, maxbet, maxwin, maxloss,
                  LAG(rebate)  OVER (PARTITION BY member_id, cate ORDER BY dt) AS rebate_p,
                  LAG(maxbet)  OVER (PARTITION BY member_id, cate ORDER BY dt) AS maxbet_p,
                  LAG(maxwin)  OVER (PARTITION BY member_id, cate ORDER BY dt) AS maxwin_p,
                  LAG(maxloss) OVER (PARTITION BY member_id, cate ORDER BY dt) AS maxloss_p,
                  LAG(dt)      OVER (PARTITION BY member_id, cate ORDER BY dt) AS dt_p
          FROM    snap
      )
      SELECT  member_id,
              cate,
              dt_p                                              AS prev_date,
              dt                                                AS change_date,
              CASE WHEN rebate  <> rebate_p  THEN '退水'
                   WHEN maxbet  <> maxbet_p  THEN '最大押分'
                   WHEN maxwin  <> maxwin_p  THEN '最大可贏'
                   ELSE '最大可輸' END                           AS action_type,
              CASE WHEN rebate  <> rebate_p  THEN rebate_p
                   WHEN maxbet  <> maxbet_p  THEN maxbet_p
                   WHEN maxwin  <> maxwin_p  THEN maxwin_p
                   ELSE maxloss_p END                            AS value_before,
              CASE WHEN rebate  <> rebate_p  THEN rebate
                   WHEN maxbet  <> maxbet_p  THEN maxbet
                   WHEN maxwin  <> maxwin_p  THEN maxwin
                   ELSE maxloss END                              AS value_after,
              CASE WHEN rebate  <> rebate_p  THEN CASE WHEN rebate  < rebate_p  THEN '收紧' ELSE '放宽' END
                   WHEN maxbet  <> maxbet_p  THEN CASE WHEN maxbet  < maxbet_p  THEN '收紧' ELSE '放宽' END
                   WHEN maxwin  <> maxwin_p  THEN CASE WHEN maxwin  < maxwin_p  THEN '收紧' ELSE '放宽' END
                   ELSE CASE WHEN maxloss < maxloss_p THEN '收紧' ELSE '放宽' END
              END                                                AS direction
      FROM    lagged
      WHERE   dt_p IS NOT NULL
        AND  (rebate  <> rebate_p
           OR maxbet  <> maxbet_p
           OR maxwin  <> maxwin_p
           OR maxloss <> maxloss_p)
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  89. TL04_log_schema.csv   [总包行 8383~8389 · 原 5 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`TABLE_NAME`, w.`ORDINAL_POSITION`, w.`COLUMN_NAME`, w.`DATA_TYPE`, w.`COLUMN_COMMENT`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT  TABLE_NAME, ORDINAL_POSITION, COLUMN_NAME, DATA_TYPE, COLUMN_COMMENT
      FROM    information_schema.columns
      WHERE   TABLE_SCHEMA = 'ods_mariadb_2b'
        AND   TABLE_NAME IN ('ods_a168_log_mem_change',
                             'ods_a168_change_log',
                             'ods_a168_log_age_cash_change')
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  90. TL06_log_mem_change.csv   [总包行 8473~8479 · 原 2 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：q.`dt`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`dt`, w.`行数`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_date                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_date IS NOT NULL AND TRIM(CAST(s.x_date AS STRING)) <> ''
    GROUP BY s.x_date
  ),
    q AS (
      SELECT  dt,
              COUNT(*)                                          AS 行数
      FROM    ods_mariadb_2b.ods_a168_log_mem_change
      WHERE   dt >= '2026-03-21'
        AND   dt <  '2026-08-07'
      GROUP BY dt
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`dt` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  91. TL07_mem_change_shape.csv   [总包行 8519~8533 · 原 10 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`类别`, w.`条数`, w.`涉及会员数`, w.`操作者数`, w.`最早异动`, w.`最晚异动`, w.`内容平均长度`, w.`内容样例`, w.`SQL平均长度`, w.`SQL样例`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT  CAST(lmc04 AS STRING)                             AS 类别,
              COUNT(*)                                          AS 条数,
              COUNT(DISTINCT CAST(lmc02 AS STRING))             AS 涉及会员数,
              COUNT(DISTINCT CAST(lmc06 AS STRING))             AS 操作者数,
              MIN(CAST(lmc08 AS STRING))                        AS 最早异动,
              MAX(CAST(lmc08 AS STRING))                        AS 最晚异动,
              AVG(LENGTH(CAST(lmc05 AS STRING)))                AS 内容平均长度,
              MAX(CAST(lmc05 AS STRING))                        AS 内容样例,
              AVG(LENGTH(CAST(lmc09 AS STRING)))                AS SQL平均长度,
              MAX(CAST(lmc09 AS STRING))                        AS SQL样例
      FROM    ods_mariadb_2b.ods_a168_log_mem_change
      WHERE   dt >= '2026-03-21'
        AND   dt <  '2026-08-07'
      GROUP BY 1
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  92. TL08_treatment_events.csv   [总包行 8541~8572 · 原 9 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`action_date`, w.`action_time`, w.`action_class`, w.`operator_id`, w.`operator_lv`, w.`content`, w.`raw_sql`, w.`is_baccarat_member`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ev AS (
          SELECT  CAST(lmc02 AS STRING)                         AS member_id,
                  CAST(lmc04 AS STRING)                         AS action_class,
                  SUBSTR(CAST(lmc08 AS STRING), 1, 10)          AS action_date,
                  CAST(lmc08 AS STRING)                         AS action_time,
                  CAST(lmc06 AS STRING)                         AS operator_id,
                  CAST(lmc07 AS STRING)                         AS operator_lv,
                  CAST(lmc05 AS STRING)                         AS content,
                  CAST(lmc09 AS STRING)                         AS raw_sql
          FROM    ods_mariadb_2b.ods_a168_log_mem_change
          WHERE   dt >= '2026-03-21'
            AND   dt <  '2026-08-07'
      ),
      bl AS (
          SELECT  DISTINCT CAST(bet05 AS STRING)                AS member_id
          FROM    ods_mariadb_2b.ods_a168_bet02
          WHERE   dt >= '2026-03-21'
            AND   dt <  '2026-08-07'
            AND   CAST(bet02 AS STRING) = '101'
      )
      SELECT  ev.member_id,
              ev.action_date,
              ev.action_time,
              ev.action_class,
              ev.operator_id,
              ev.operator_lv,
              ev.content,
              ev.raw_sql,
              CASE WHEN bl.member_id IS NULL THEN 0 ELSE 1 END  AS is_baccarat_member
      FROM        ev
      LEFT JOIN   bl ON bl.member_id = ev.member_id
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  93. TL09_member_schema.csv   [总包行 8603~8607 · 原 4 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`ORDINAL_POSITION`, w.`COLUMN_NAME`, w.`DATA_TYPE`, w.`COLUMN_COMMENT`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT  ORDINAL_POSITION, COLUMN_NAME, DATA_TYPE, COLUMN_COMMENT
      FROM    information_schema.columns
      WHERE   TABLE_SCHEMA = 'ods_mariadb_2b'
        AND   TABLE_NAME   = 'ods_a168_member'
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  94. TL10_treatment_ledger.csv   [总包行 8615~8675 · 原 10 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`action_date`, w.`action_time`, w.`action_class`, w.`field_name`, w.`value_before`, w.`value_after`, w.`operator_id`, w.`operator_lv`, w.`is_baccarat_member`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ev AS (
          SELECT  CAST(lmc02 AS STRING)                         AS member_id,
                  CAST(lmc04 AS STRING)                         AS action_class,
                  SUBSTR(CAST(lmc08 AS STRING), 1, 10)          AS action_date,
                  CAST(lmc08 AS STRING)                         AS action_time,
                  CAST(lmc06 AS STRING)                         AS operator_id,
                  CAST(lmc07 AS STRING)                         AS operator_lv,
                  CAST(lmc05 AS STRING)                         AS content
          FROM    ods_mariadb_2b.ods_a168_log_mem_change
          WHERE   dt >= '2026-03-21'
            AND   dt <  '2026-08-07'
            AND   CAST(lmc04 AS STRING) IN ('edit', 'changestatus')
      ),
      seg AS (
          SELECT  e.member_id, e.action_class, e.action_date, e.action_time,
                  e.operator_id, e.operator_lv,
                  TRIM(s.piece)                                 AS piece
          FROM    ev e, unnest(split(e.content, ';')) AS s(piece)
      ),
      parsed AS (
          SELECT  member_id, action_class, action_date, action_time,
                  operator_id, operator_lv, piece,
                  TRIM(SPLIT_PART(piece, ':', 1))               AS field_name,
                  TRIM(SPLIT_PART(SPLIT_PART(piece, ':', 2), '=>', 1)) AS value_before,
                  TRIM(SPLIT_PART(SPLIT_PART(piece, ':', 2), '=>', 2)) AS value_after
          FROM    seg
          WHERE   piece LIKE '%=>%'
            AND   piece LIKE '%:%'
      ),
      guarded AS (
          -- 护栏：另有一类 edit 内容形如「skyname=>…, Gateway_url=>https://…」，
          -- 其 URL 里的冒号会骗过上面的两个 LIKE，解析出一整串垃圾作列名。
          -- 故加数道限制——列名须短、须无空格、须不含箭头与斜杠；实测所见列名皆形如 memNNN 或 tip。
          SELECT  *
          FROM    parsed
          WHERE   LENGTH(field_name) BETWEEN 2 AND 12
            AND   field_name NOT LIKE '% %'
            AND   field_name NOT LIKE '%=>%'
            AND   field_name NOT LIKE '%/%'
            AND   value_after NOT LIKE '%//%'
      ),
      bl AS (
          SELECT  DISTINCT CAST(bet05 AS STRING)                AS member_id
          FROM    ods_mariadb_2b.ods_a168_bet02
          WHERE   dt >= '2026-03-21'
            AND   dt <  '2026-08-07'
            AND   CAST(bet02 AS STRING) = '101'
      )
      SELECT  p.member_id,
              p.action_date,
              p.action_time,
              p.action_class,
              p.field_name,
              p.value_before,
              p.value_after,
              p.operator_id,
              p.operator_lv,
              CASE WHEN bl.member_id IS NULL THEN 0 ELSE 1 END  AS is_baccarat_member
      FROM        guarded p
      LEFT JOIN   bl ON bl.member_id = p.member_id
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  95. TL11_treatment_episode.csv   [总包行 8706~8832 · 原 24 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`field_name`, w.`business_field`, w.`enforcement_type`, w.`is_baccarat_member`, w.`episode_id`, w.`start_date`, w.`start_time`, w.`value_before`, w.`value_after`, w.`start_operator`, w.`start_operator_lv`, w.`parse_sep`, w.`parse_status`, w.`release_date`, w.`censored`, w.`last_bet_date`, w.`n_bets_in_window`, w.`last_config_time`, w.`days_since_last_bet`, w.`censor_cutoff_days`, w.`censor_reason`, w.`duration_days`, w.`episode_seq`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ev AS (                                                                                        -- 公共表表达式：开启中间结果集 ev——变更日志原始事件，口径与 §TL-10 一致（§TL-11）
          SELECT  CAST(lmc02 AS STRING)                         AS member_id,                             -- 取值表达式：取用 lmc02（被改会员号），产出「member_id」
                  SUBSTR(CAST(lmc08 AS STRING), 1, 10)          AS action_date,                           -- 取值表达式：取用 lmc08（操作时间）之日期段，产出「action_date」
                  CAST(lmc08 AS STRING)                         AS action_time,                           -- 取值表达式：取用 lmc08（操作时间），产出「action_time」
                  CAST(lmc06 AS STRING)                         AS operator_id,                           -- 取值表达式：取用 lmc06（操作人账号），产出「operator_id」——审计轨迹之「谁」
                  CAST(lmc07 AS STRING)                         AS operator_lv,                           -- 取值表达式：取用 lmc07（操作人层级），产出「operator_lv」
                  CAST(lmc05 AS STRING)                         AS content                                -- 取值表达式：取用 lmc05（变更内容串），产出「content」
          FROM    ods_mariadb_2b.ods_a168_log_mem_change                                                  -- 取数来源：取自会员变更日志表（处置痕迹的正主）
          WHERE   dt >= '2026-03-21'                                                                      -- 过滤条件：限定 dt不少于 '2026-03-21'，涉 dt（营业日）——E1 全局窗起点
            AND   dt <  '2026-08-07'                                                                      -- 并列条件：限定 dt小于 '2026-08-07'——E1 全局窗终点（冻结字面量）
            AND   CAST(lmc04 AS STRING) IN ('edit', 'changestatus')                                       -- 并列条件：只留配置修改与状态变更；add 系新增账户与处置无涉
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-11）
      seg AS (                                                                                            -- 公共表表达式：开启中间结果集 seg——把多段 content 逐段拆开（§TL-11）
          SELECT  e.member_id, e.action_date, e.action_time,                                              -- 取列：起始取列子句，本行先列 member_id, action_date, action_time
                  e.operator_id, e.operator_lv,                                                           -- 续行：接续上一取列子句，续列 operator_id, operator_lv
                  TRIM(s.piece)                                 AS piece                                  -- 取值表达式：逐段去空白，产出「piece」
          FROM ev e, unnest(split(e.content, ';')) AS s(piece)                                            -- 行展开：以 unnest(split(…)) 把分号分隔的多段变更逐段成行（StarRocks 已验语法）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-11）
      norm AS (                                                                                           -- 公共表表达式：开启中间结果集 norm——两式并收：冒号式与空格式统一为「列|旧|新」（§TL-11）
          SELECT  member_id, action_date, action_time, operator_id, operator_lv, piece,                   -- 取列：起始取列子句，透传六要素与原始片段
                  CASE WHEN piece LIKE '%:%' THEN 'COLON' ELSE 'SPACE' END AS parse_sep,                  -- 取值表达式：记明所用分隔符，产出「parse_sep」——★ 使丢弃永不静默
                  CASE WHEN piece LIKE '%:%'                                                              -- 取值表达式：列名解析起算——冒号式取冒号前段
                       THEN TRIM(SPLIT_PART(piece, ':', 1))                                               -- 续行：冒号式列名
                       ELSE TRIM(SPLIT_PART(piece, ' ', 1)) END      AS field_name,                       -- 续行：空格式取首个空格前段（如「mem020 Y=>N」），产出「field_name」
                  CASE WHEN piece LIKE '%:%'                                                              -- 取值表达式：旧值解析起算
                       THEN TRIM(SPLIT_PART(SPLIT_PART(piece, ':', 2), '=>', 1))                          -- 续行：冒号式旧值
                       ELSE TRIM(SPLIT_PART(SPLIT_PART(piece, ' ', 2), '=>', 1)) END AS value_before,     -- 续行：空格式旧值，产出「value_before」
                  TRIM(SPLIT_PART(piece, '=>', 2))              AS value_after                            -- 取值表达式：新值一律取箭头后段（两式同形），产出「value_after」
          FROM    seg                                                                                     -- 取数来源：取自本条自建的中间结果集 seg
          WHERE   piece LIKE '%=>%'                                                                       -- 过滤条件：须含箭头方为有效变更段——两式共有的唯一必要标志
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-11）
      enf AS (                                                                                            -- 公共表表达式：开启中间结果集 enf——只留已译定的处置字段并判方向与类别（§TL-11）
          SELECT  member_id, field_name, action_date, action_time,                                        -- 取列：起始取列子句，透传节要素之前四
                  value_before, value_after, operator_id, operator_lv, parse_sep,                         -- 续行：接续上一取列子句，续列前后值、操作人与分隔符
                  CASE WHEN field_name = 'mem017' THEN 'CANBET_下注权限'                                      -- 取值表达式：处置类别起算——mem017 系下注权限（§TL-09 列义）
                       WHEN field_name = 'mem016' THEN 'ENABLE_账户启停'                                      -- 续行：mem016 系账户启停
                       ELSE 'OTHER' END                     AS enforcement_type,                          -- 续行：产出「enforcement_type」——外部审计所要的处置类别列
                  CASE WHEN field_name = 'mem017' THEN 'canbet'                                           -- 取值表达式：业务列名起算——代号译为业务名，免下游再查字典
                       WHEN field_name = 'mem016' THEN 'enable'                                           -- 续行：mem016 之业务名
                       ELSE 'unknown' END                   AS business_field,                            -- 续行：产出「business_field」
                  CASE WHEN TRIM(value_after) IN ('N', '0') THEN 'TIGHTEN'                                -- 取值表达式：新值为 N／0 判收紧——禁投或停用
                       WHEN TRIM(value_after) IN ('Y', '1') THEN 'RELEASE'                                -- 续行：新值为 Y／1 判解除
                       ELSE 'OTHER' END                     AS direction,                                 -- 续行：其余取值不明记 OTHER，不入节（宁缺毋滥），产出「direction」
                  'OK'                                     AS parse_status                                -- 取值表达式：解析状态，产出「parse_status」——本 CTE 所留者皆已解析成功
          FROM    norm                                                                                    -- 取数来源：取自本条自建的中间结果集 norm
          WHERE   field_name IN ('mem016', 'mem017')                                                      -- 过滤条件：只认已译定的处置字段——mem015 已证为 login_error，非处置，一律不入
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-11）
      dedup AS (                                                                                          -- 公共表表达式：开启中间结果集 dedup——同刻重复写入只计一次（去重键四元组）
          SELECT  *,                                                                                      -- 取列：整体承接上游结果集的全部字段，不再逐列列举
                  ROW_NUMBER() OVER (PARTITION BY member_id, field_name, action_time,                     -- 行号窗口表达式：按去重键 member×field×时刻×前后值 分组编号
                                     value_before, value_after ORDER BY operator_id) AS rn                -- 续行：键内任取其一（按操作人序），产出「rn」
          FROM    enf                                                                                     -- 取数来源：取自本条自建的中间结果集 enf
          WHERE   direction <> 'OTHER'                                                                    -- 过滤条件：方向不明者不入节
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-11）
      tight AS (                                                                                          -- 公共表表达式：开启中间结果集 tight——全部收紧事件，每行是一节的起点（§TL-11）
          SELECT * FROM dedup WHERE rn = 1 AND direction = 'TIGHTEN'                                      -- 取数来源：去重后的收紧事件
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-11）
      rel AS (                                                                                            -- 公共表表达式：开启中间结果集 rel——全部解除事件，用以为每节寻其后首个解除（§TL-11）
          SELECT * FROM dedup WHERE rn = 1 AND direction = 'RELEASE'                                      -- 取数来源：去重后的解除事件
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-11）
      bl AS (                                                                                             -- 公共表表达式：开启中间结果集 bl——本方案口径的百家乐投注会员名单（§TL-11 补列所需）
          SELECT  DISTINCT CAST(bet05 AS STRING)                AS member_id                              -- 取值表达式：取用 bet05（会员号）去重，产出「member_id」
          FROM    ods_mariadb_2b.ods_a168_bet02                                                           -- 取数来源：取自注单明细表
          WHERE   dt >= '2026-03-21' AND dt < '2026-08-07'                                                -- 过滤条件：限定 E1 全局窗，涉 dt（营业日）
            AND   CAST(bet02 AS STRING) = '101'                                                           -- 并列条件：限定百家乐产品大类，涉 bet02（游戏类别）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-11）
      la AS (                                                                                             -- 公共表表达式：开启中间结果集 la——每会员窗内最末一次**投注**，供删失成因分类（§TL-11）
          SELECT  CAST(bet05 AS STRING)                         AS member_id,                             -- 取值表达式：取用 bet05（会员号），产出「member_id」
                  MAX(dt)                                       AS last_bet_date,                         -- 聚合：该会员窗内最末一次投注之营业日，产出「last_bet_date」
                  COUNT(*)                                      AS n_bets_in_window                       -- 计数表达式：窗内注单数，产出「n_bets_in_window」——覆盖率证据，供删失分类追溯
          FROM    ods_mariadb_2b.ods_a168_bet02                                                           -- 取数来源：取自注单明细表——★ 2026-08-11 斧正：旧版以「最末一次出现于变更日志」为凭
          WHERE   dt >= '2026-03-21' AND dt < '2026-08-07'                                                -- 过滤条件：限定 E1 全局窗——而变更日志之缺席只说明未再改配置，不说明人已不在
          GROUP BY CAST(bet05 AS STRING)                                                                  -- 分组：按会员汇总——投注活动方是「仍在观察」的正当凭据
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-11）
      lc AS (                                                                                             -- 公共表表达式：开启中间结果集 lc——每会员末次配置变更时刻（§TL-11）
          SELECT  member_id, MAX(action_time)                   AS last_config_time                       -- 聚合：末次出现于变更日志之时刻，产出「last_config_time」
          FROM    norm GROUP BY member_id                                                                 -- 取数来源：取自本条自建的中间结果集 norm——★ 此量仅作证据留档，**不再用作删失判据**
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-11）
      ep AS (                                                                                             -- 公共表表达式：开启中间结果集 ep——为每次收紧配其后首个解除（§TL-11）
          SELECT  t.member_id, t.field_name, t.business_field, t.enforcement_type,                        -- 取列：起始取列子句，节起点要素与两枚新增类别列
                  t.action_date, t.action_time, t.value_before, t.value_after,                            -- 续行：接续上一取列子句，续列时点与前后值
                  t.operator_id, t.operator_lv, t.parse_sep, t.parse_status,                              -- 续行：续列操作人、层级与解析两列
                  MIN(r.action_time)                            AS release_time                           -- 聚合：其后（同人同字段）首个解除时刻，无则空，产出「release_time」
          FROM        tight t                                                                             -- 取数来源：取自本条自建的中间结果集 tight
          LEFT JOIN   rel   r                                                                             -- 左连接：取自本条自建的中间结果集 rel——解除可缺席（右删失），故用左连接
                 ON   r.member_id  = t.member_id                                                          -- 连接键：同一会员
                AND   r.field_name = t.field_name                                                         -- 并列键：同一处置字段——canbet 之节不由 enable 之解除关闭
                AND   r.action_time > t.action_time                                                       -- 并列键：解除须晚于收紧——只认其后者
          GROUP BY t.member_id, t.field_name, t.business_field, t.enforcement_type,                       -- 分组：按节起点全键汇总，使 MIN 取到「首个」解除
                   t.action_date, t.action_time, t.value_before, t.value_after,                           -- 续行：分组键补齐
                   t.operator_id, t.operator_lv, t.parse_sep, t.parse_status                              -- 续行：分组键补齐
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§TL-11）
      SELECT  p.member_id,                                                                                -- 取列：起始取列子句，本行先取「member_id」——节属谁
              p.field_name,                                                                               -- 取值表达式：处置字段代号
              p.business_field,                                                                           -- 取值表达式：业务列名（canbet／enable）——★ 补列，免下游再查字典
              p.enforcement_type,                                                                         -- 取值表达式：处置类别——★ 补列，区分下注权限与账户启停
              CASE WHEN bl.member_id IS NULL THEN 0 ELSE 1 END  AS is_baccarat_member,                    -- 取值表达式：是否本方案口径的百家乐会员——★ 补列，准实验总体由此界定
              CONCAT_WS('#', p.member_id, p.field_name, p.action_time) AS episode_id,                     -- 取值表达式：三键拼节号，产出「episode_id」——审计轨迹与准实验共用的主键
              p.action_date                                 AS start_date,                                -- 取值表达式：节起日，产出「start_date」
              p.action_time                                 AS start_time,                                -- 取值表达式：节起时刻，产出「start_time」
              p.value_before,                                                                             -- 取值表达式：收紧前取值——留痕以备申诉复核
              p.value_after,                                                                              -- 取值表达式：收紧后取值
              p.operator_id                                 AS start_operator,                            -- 取值表达式：收紧操作人，产出「start_operator」——审计轨迹之「谁」
              p.operator_lv                                 AS start_operator_lv,                         -- 取值表达式：操作人层级，产出「start_operator_lv」
              p.parse_sep,                                                                                -- 取值表达式：本行所用分隔符（COLON／SPACE）——★ 补列，两式并收之凭据
              p.parse_status,                                                                             -- 取值表达式：解析状态——★ 补列，恒为 OK；丢弃者见 §TL-11b 对账
              COALESCE(SUBSTR(p.release_time, 1, 10), '2026-08-07') AS release_date,                      -- 取值表达式：解除日；缺席者以窗末顶替（配合下行 censored 判读），产出「release_date」
              CASE WHEN p.release_time IS NULL THEN 1 ELSE 0 END    AS censored,                          -- 取值表达式：右删失标志——1 谓至窗末仍收紧，产出「censored」
              la.last_bet_date,                                                                           -- 取值表达式：末次投注营业日——★ 删失分类之原始证据，供逐节追溯
              la.n_bets_in_window,                                                                        -- 取值表达式：窗内注单数——★ 覆盖率证据
              lc.last_config_time,                                                                        -- 取值表达式：末次配置变更时刻——★ 第二重证据（旧判据留档，已不作分类依据）
              DATEDIFF(DATE '2026-08-06', CAST(la.last_bet_date AS DATE)) AS days_since_last_bet,         -- 取值表达式：末次投注距窗末天数——★ **连续原始量**，供 3D／7D／14D 敏感性，免回访原表
              7                                             AS censor_cutoff_days,                        -- 取值表达式：本次分类所用的运营切点（日）——★ 显式化：7 日是**工程约定**，非统计真理
              CASE WHEN p.release_time IS NOT NULL THEN 'NOT_CENSORED'                                    -- 取值表达式：删失成因分类起算——★ KM 无偏性之前提
                   WHEN la.last_bet_date IS NULL THEN 'NO_BET_ACTIVITY'                                   -- 续行：窗内全无投注 → 无从判其去留（先判此项，免 NULL 落入比较而误分）
                   WHEN DATEDIFF(DATE '2026-08-06', CAST(la.last_bet_date AS DATE)) <= 7                  -- 续行：末次投注距窗末不逾切点 → 人尚在、处置尚在
                        THEN 'WINDOW_END'                                                                 -- 续行：属真窗口右删失
                   ELSE 'INACTIVE_BEFORE_END' END                    AS censor_reason,                    -- 续行：投注早止 → 疑为流失或账户终止型**假删失**，产出「censor_reason」
              DATEDIFF(CAST(COALESCE(SUBSTR(p.release_time, 1, 10), '2026-08-07') AS DATE),               -- 取值表达式：节时长起算——解除日（或窗末）
                       CAST(p.action_date AS DATE))         AS duration_days,                             -- 续行：减节起日得持续天数，产出「duration_days」——生存分析之时间轴
              ROW_NUMBER() OVER (PARTITION BY p.member_id                                                 -- 行号窗口表达式：按会员为其节编序
                                 ORDER BY p.action_time)    AS episode_seq                                -- 续行：产出「episode_seq」——主分析取 1（首次处置设计），其余入敏感性
      FROM        ep p                                                                                    -- 取数来源：取自本条自建的中间结果集 ep
      LEFT JOIN   bl ON bl.member_id = p.member_id                                                        -- 左连接：取自本条自建的中间结果集 bl，连接键为 member_id（会员号）——标注百家乐口径
      LEFT JOIN   la ON la.member_id = p.member_id                                                        -- 左连接：取自本条自建的中间结果集 la（末次投注日），连接键为 member_id（会员号）
      LEFT JOIN   lc ON lc.member_id = p.member_id                                                        -- 左连接：取自本条自建的中间结果集 lc（末次配置变更），连接键为 member_id（会员号）
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  96. TL11c_population_reconcile.csv   [总包行 8969~9055 · 原 7 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ev AS (                                                                                        -- 公共表表达式：开启中间结果集 ev——与 §TL-11 同源同窗（§TL-11c）
          SELECT  CAST(lmc02 AS STRING) AS member_id, CAST(lmc05 AS STRING) AS content                    -- 取值表达式：取用 lmc02（被改会员号）与 lmc05（变更内容串）
          FROM    ods_mariadb_2b.ods_a168_log_mem_change                                                  -- 取数来源：取自会员变更日志表
          WHERE   dt >= '2026-03-21' AND dt < '2026-08-07'                                                -- 过滤条件：限定 E1 全局窗，涉 dt（营业日）
            AND   CAST(lmc04 AS STRING) IN ('edit', 'changestatus')                                       -- 并列条件：只留配置修改与状态变更两类
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-11c）
      pieces AS (                                                                                         -- 公共表表达式：开启中间结果集 pieces——第一级：原始片段（§TL-11c）
          SELECT  e.member_id, TRIM(s.piece) AS piece                                                     -- 取值表达式：逐段去空白，产出「piece」
          FROM ev e, unnest(split(e.content, ';')) AS s(piece)                                            -- 行展开：以 unnest(split(…)) 把分号分隔的多段逐段成行
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-11c）
      enf AS (                                                                                            -- 公共表表达式：开启中间结果集 enf——第二级：已确认处置的解析事件（§TL-11c）
          SELECT  member_id, piece,                                                                       -- 取列：起始取列子句，本行先列 member_id, piece
                  CASE WHEN TRIM(SPLIT_PART(piece, '=>', 2)) IN ('N','0') THEN 'TIGHTEN'                  -- 取值表达式：方向判定起算——新值 N／0 为收紧
                       WHEN TRIM(SPLIT_PART(piece, '=>', 2)) IN ('Y','1') THEN 'RELEASE'                  -- 续行：新值 Y／1 为解除
                       ELSE 'OTHER' END                          AS direction                             -- 续行：产出「direction」
          FROM    pieces                                                                                  -- 取数来源：取自本条自建的中间结果集 pieces
          WHERE   piece LIKE '%=>%'                                                                       -- 过滤条件：须含箭头方为有效变更段
            AND ( piece LIKE 'mem016%' OR piece LIKE 'mem017%' )                                          -- 并列条件：只认语义已确认的两枚处置字段（mem015 已证非处置，封案）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-11c）
      bl AS (                                                                                             -- 公共表表达式：开启中间结果集 bl——本方案口径的百家乐投注会员名单（§TL-11c）
          SELECT  DISTINCT CAST(bet05 AS STRING) AS member_id                                             -- 取值表达式：取用 bet05（会员号）去重，产出「member_id」
          FROM    ods_mariadb_2b.ods_a168_bet02                                                           -- 取数来源：取自注单明细表
          WHERE   dt >= '2026-03-21' AND dt < '2026-08-07'                                                -- 过滤条件：限定 E1 全局窗，涉 dt（营业日）
            AND   CAST(bet02 AS STRING) = '101'                                                           -- 并列条件：限定百家乐产品大类，涉 bet02（游戏类别）
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§TL-11c）
      SELECT  1 AS lvl, '① 原始片段（raw mutation pieces）' AS level_name,                                      -- 取值表达式：第一级序号与名称，产出「lvl」「level_name」
              COUNT(*) AS n_all,                                                                          -- 计数表达式：全体计数，产出「n_all」
              SUM(CASE WHEN bl.member_id IS NULL THEN 0 ELSE 1 END) AS n_baccarat,                        -- 聚合：其中百家乐会员之计数，产出「n_baccarat」
              COUNT(DISTINCT p.member_id) AS n_member_all,                                                -- 计数表达式：涉及会员数（去重），产出「n_member_all」
              COUNT(DISTINCT CASE WHEN bl.member_id IS NOT NULL THEN p.member_id END) AS n_member_bac,    -- 计数表达式：其中百家乐会员数，产出「n_member_bac」
              '变更日志 content 按分号拆开后的每一段' AS definition                                                     -- 取值表达式：本级定义，产出「definition」——逐级递减须由定义解释得通
      FROM        pieces p                                                                                -- 取数来源：取自本条自建的中间结果集 pieces
      LEFT JOIN   bl ON bl.member_id = p.member_id                                                        -- 左连接：取自本条自建的中间结果集 bl，连接键为 member_id（会员号）
      UNION ALL                                                                                           -- 集合运算：纵向拼接下一级
      SELECT  2, '② 处置事件（enforcement events）',                                                            -- 取值表达式：第二级序号与名称
              COUNT(*),                                                                                   -- 计数表达式：全体计数
              SUM(CASE WHEN bl.member_id IS NULL THEN 0 ELSE 1 END),                                      -- 聚合：其中百家乐会员之计数
              COUNT(DISTINCT e.member_id),                                                                -- 计数表达式：涉及会员数（去重）
              COUNT(DISTINCT CASE WHEN bl.member_id IS NOT NULL THEN e.member_id END),                    -- 计数表达式：其中百家乐会员数
              '解析成功且语义经字典确认为处置者（mem016／mem017），含收紧与解除两向'                                                  -- 取值表达式：本级定义
      FROM        enf e                                                                                   -- 取数来源：取自本条自建的中间结果集 enf
      LEFT JOIN   bl ON bl.member_id = e.member_id                                                        -- 左连接：取自本条自建的中间结果集 bl，连接键为 member_id（会员号）
      UNION ALL                                                                                           -- 集合运算：纵向拼接下一级
      SELECT  3, '③ 收紧事件（episode starts）',                                                                -- 取值表达式：第三级序号与名称
              COUNT(*),                                                                                   -- 计数表达式：全体计数
              SUM(CASE WHEN bl.member_id IS NULL THEN 0 ELSE 1 END),                                      -- 聚合：其中百家乐会员之计数
              COUNT(DISTINCT e.member_id),                                                                -- 计数表达式：涉及会员数（去重）
              COUNT(DISTINCT CASE WHEN bl.member_id IS NOT NULL THEN e.member_id END),                    -- 计数表达式：其中百家乐会员数——★ 此即处置节口径的唯一处置会员数
              '处置事件中方向为收紧者；每一条起一个处置节，故其数即处置节数'                                                            -- 取值表达式：本级定义
      FROM        enf e                                                                                   -- 取数来源：取自本条自建的中间结果集 enf
      LEFT JOIN   bl ON bl.member_id = e.member_id                                                        -- 左连接：取自本条自建的中间结果集 bl，连接键为 member_id（会员号）
      WHERE   e.direction = 'TIGHTEN'                                                                     -- 过滤条件：只留收紧方向
      UNION ALL                                                                                           -- 集合运算：纵向拼接下一级
      SELECT  4, '④ 解除事件（episode ends）',                                                                  -- 取值表达式：第四级序号与名称
              COUNT(*),                                                                                   -- 计数表达式：全体计数
              SUM(CASE WHEN bl.member_id IS NULL THEN 0 ELSE 1 END),                                      -- 聚合：其中百家乐会员之计数
              COUNT(DISTINCT e.member_id),                                                                -- 计数表达式：涉及会员数（去重）
              COUNT(DISTINCT CASE WHEN bl.member_id IS NOT NULL THEN e.member_id END),                    -- 计数表达式：其中百家乐会员数
              '处置事件中方向为解除者；未必与收紧一一对应，故删失由此而生'                                                             -- 取值表达式：本级定义
      FROM        enf e                                                                                   -- 取数来源：取自本条自建的中间结果集 enf
      LEFT JOIN   bl ON bl.member_id = e.member_id                                                        -- 左连接：取自本条自建的中间结果集 bl，连接键为 member_id（会员号）
      WHERE   e.direction = 'RELEASE'                                                                     -- 过滤条件：只留解除方向
      UNION ALL                                                                                           -- 集合运算：纵向拼接下一级
      SELECT  5, '⑤ 左截断：只见解除而未见收紧之会员',                                                                    -- 取值表达式：第五级——★ 2026-08-11 增，解释 ④ 多于 ③ 之差
              0, 0,                                                                                       -- 计数表达式：本级只计会员，条数置零以免与事件级混淆
              COUNT(DISTINCT r.member_id),                                                                -- 计数表达式：全体——其收紧发生于窗口之前，本窗只见尾巴
              COUNT(DISTINCT CASE WHEN bl.member_id IS NOT NULL THEN r.member_id END),                    -- 计数表达式：其中百家乐会员数
              '窗内有解除而无收紧者：处置起于观察窗之前（左截断），无 T0 故不入处置节，亦不入 KM'                                              -- 取值表达式：本级定义——**须明写，否则 ④ 与 ③ 之差无从解释**
      FROM        ( SELECT DISTINCT member_id FROM enf WHERE direction = 'RELEASE' ) r                    -- 取数来源：解除方会员集合
      LEFT JOIN   ( SELECT DISTINCT member_id FROM enf WHERE direction = 'TIGHTEN' ) t                    -- 左连接：收紧方会员集合
             ON   t.member_id = r.member_id                                                               -- 连接键：同一会员
      LEFT JOIN   bl ON bl.member_id = r.member_id                                                        -- 左连接：取自本条自建的中间结果集 bl，连接键为 member_id（会员号）
      WHERE   t.member_id IS NULL                                                                         -- 过滤条件：只留在收紧集合中查无此人者——即左截断
      UNION ALL                                                                                           -- 集合运算：纵向拼接下一级
      SELECT  6, '⑥ 语义待定：解析成功而列义未经字典确认',                                                                  -- 取值表达式：第六级——★ 2026-08-11 增，与 §TL-11b 的 semantic_status 对齐
              COUNT(*),                                                                                   -- 计数表达式：全体计数
              SUM(CASE WHEN bl.member_id IS NULL THEN 0 ELSE 1 END),                                      -- 聚合：其中百家乐会员之计数
              COUNT(DISTINCT q.member_id),                                                                -- 计数表达式：涉及会员数（去重）
              COUNT(DISTINCT CASE WHEN bl.member_id IS NOT NULL THEN q.member_id END),                    -- 计数表达式：其中百家乐会员数
              '含箭头而非 mem015／mem016／mem017 者，如 mem020 与 101-mem015——PENDING，不入处置总体'                        -- 取值表达式：本级定义
      FROM        pieces q                                                                                -- 取数来源：取自本条自建的中间结果集 pieces
      LEFT JOIN   bl ON bl.member_id = q.member_id                                                        -- 左连接：取自本条自建的中间结果集 bl，连接键为 member_id（会员号）
      WHERE   q.piece LIKE '%=>%'                                                                         -- 过滤条件：须为有效变更段
        AND   q.piece NOT LIKE 'mem015%'                                                                  -- 并列条件：mem015 已封案（login_error），另计
        AND   q.piece NOT LIKE 'mem016%'                                                                  -- 并列条件：mem016 已确认为处置，另计
        AND   q.piece NOT LIKE 'mem017%'                                                                  -- 并列条件：mem017 已确认为处置，另计
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  97. TL14_limit_treatment.csv   [总包行 5058~5136 · 原 15 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`action_date`, w.`action_time`, w.`operator_id`, w.`operator_lv`, w.`grp_before`, w.`grp_after`, w.`max_hi_before`, w.`max_hi_after`, w.`n_grp_before`, w.`n_grp_after`, w.`direction`, w.`has_zero_group`, w.`is_baccarat_member`, w.`filter_rule_version`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH dict AS (                                                                                      -- 公共表表达式：开启中间结果集 dict——百家乐限额组字典（§TL-14）
          SELECT  CAST(id AS STRING)                            AS grp_id,                                -- 取值表达式：取用组号并转字符，产出「grp_id」——与变更日志内的字面值同型方能连上
                  CAST(NULLIF(TRIM(SPLIT_PART(set01, ',', 1)),'') AS DECIMAL(20,2)) AS lo,                -- 取值表达式：set01 逗号前段为下限，产出「lo」
                  CAST(NULLIF(TRIM(SPLIT_PART(set01, ',', 2)),'') AS DECIMAL(20,2)) AS hi                 -- 取值表达式：set01 逗号后段为上限，产出「hi」——方向判定之凭据
          FROM    ods_mariadb_2b.ods_a168_bet_limit_default                                               -- 取数来源：取自限额组定义表（§TL-13 查得）
          WHERE   gtype = '101'                                                                           -- 过滤条件：限定百家乐，与本方案 bet02 = '101' 同口径
            AND   dt = ( SELECT MAX(dt) FROM ods_mariadb_2b.ods_a168_bet_limit_default )                  -- 并列条件：只取最新一版快照，免同组多版重复
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-14）
      ev AS (                                                                                             -- 公共表表达式：开启中间结果集 ev——变更日志原始事件，口径与 §TL-11 一致（§TL-14）
          SELECT  CAST(lmc02 AS STRING)                         AS member_id,                             -- 取值表达式：取用 lmc02（被改会员号），产出「member_id」
                  SUBSTR(CAST(lmc08 AS STRING), 1, 10)          AS action_date,                           -- 取值表达式：取用 lmc08（操作时间）之日期段，产出「action_date」
                  CAST(lmc08 AS STRING)                         AS action_time,                           -- 取值表达式：取用 lmc08（操作时间），产出「action_time」
                  CAST(lmc06 AS STRING)                         AS operator_id,                           -- 取值表达式：取用 lmc06（操作人账号），产出「operator_id」
                  CAST(lmc07 AS STRING)                         AS operator_lv,                           -- 取值表达式：取用 lmc07（操作人层级），产出「operator_lv」
                  CAST(lmc05 AS STRING)                         AS content                                -- 取值表达式：取用 lmc05（变更内容串），产出「content」
          FROM    ods_mariadb_2b.ods_a168_log_mem_change                                                  -- 取数来源：取自会员变更日志表
          WHERE   dt >= '2026-03-21' AND dt < '2026-08-07'                                                -- 过滤条件：限定 E1 全局窗，涉 dt（营业日）
            AND   CAST(lmc04 AS STRING) IN ('edit', 'changestatus')                                       -- 并列条件：只留配置修改与状态变更两类
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-14）
      seg AS (                                                                                            -- 公共表表达式：开启中间结果集 seg——把多段 content 逐段拆开（§TL-14）
          SELECT  e.member_id, e.action_date, e.action_time, e.operator_id, e.operator_lv,                -- 取列：起始取列子句，透传五要素
                  TRIM(s.piece)                                 AS piece                                  -- 取值表达式：逐段去空白，产出「piece」
          FROM ev e, unnest(split(e.content, ';')) AS s(piece)                                            -- 行展开：以 unnest(split(…)) 把分号分隔的多段逐段成行
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-14）
      lim AS (                                                                                            -- 公共表表达式：开启中间结果集 lim——只留百家乐限额变更段（§TL-14）
          SELECT  member_id, action_date, action_time, operator_id, operator_lv,                          -- 取列：起始取列子句，透传五要素
                  TRIM(SPLIT_PART(SPLIT_PART(piece, ':', 2), '=>', 1)) AS grp_before,                     -- 取值表达式：箭头前段为旧组清单，产出「grp_before」
                  TRIM(SPLIT_PART(piece, '=>', 2))              AS grp_after,                             -- 取值表达式：箭头后段为新组清单，产出「grp_after」
                  CONCAT_WS('#', member_id, action_time)        AS ev_key                                 -- 取值表达式：会员×时刻拼事件键，产出「ev_key」——供两侧展开后回接
          FROM    seg                                                                                     -- 取数来源：取自本条自建的中间结果集 seg
          WHERE   piece LIKE '101-mem015:%'                                                               -- 过滤条件：只认百家乐的新版限額段——★ 不得与 member.mem015（login_error）相混
            AND   piece LIKE '%=>%'                                                                       -- 并列条件：须含箭头方为有效变更段
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-14）
      bfr AS (                                                                                            -- 公共表表达式：开启中间结果集 bfr——旧组清单逐组展开后取最高上限（§TL-14）
          SELECT  l.ev_key,                                                                               -- 取列：起始取列子句，本行先取事件键
                  MAX(d.hi)                                     AS max_hi_before,                         -- 聚合：旧清单之最高上限，产出「max_hi_before」
                  SUM(CASE WHEN d.hi = 0 THEN 1 ELSE 0 END)     AS zero_before,                           -- 聚合：旧清单含「0,0」组之个数，产出「zero_before」——歧义标记之料
                  COUNT(d.grp_id)                               AS n_grp_before                           -- 计数表达式：旧清单中能连上字典的组数，产出「n_grp_before」
          FROM        lim l                                                                               -- 取数来源：取自本条自建的中间结果集 lim
          CROSS JOIN  unnest(split(l.grp_before, ',')) AS g(grp)                                          -- 行展开：把逗号分隔的组清单逐组成行
          LEFT JOIN   dict d ON d.grp_id = TRIM(g.grp)                                                    -- 左连接：取自本条自建的中间结果集 dict，连接键为组号——连不上者留空，不以 0 顶替
          GROUP BY l.ev_key                                                                               -- 分组：按事件键汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-14）
      aft AS (                                                                                            -- 公共表表达式：开启中间结果集 aft——新组清单同法（§TL-14）
          SELECT  l.ev_key,                                                                               -- 取列：起始取列子句，本行先取事件键
                  MAX(d.hi)                                     AS max_hi_after,                          -- 聚合：新清单之最高上限，产出「max_hi_after」
                  SUM(CASE WHEN d.hi = 0 THEN 1 ELSE 0 END)     AS zero_after,                            -- 聚合：新清单含「0,0」组之个数，产出「zero_after」
                  COUNT(d.grp_id)                               AS n_grp_after                            -- 计数表达式：新清单中能连上字典的组数，产出「n_grp_after」
          FROM        lim l                                                                               -- 取数来源：取自本条自建的中间结果集 lim
          CROSS JOIN  unnest(split(l.grp_after, ',')) AS g(grp)                                           -- 行展开：把逗号分隔的组清单逐组成行
          LEFT JOIN   dict d ON d.grp_id = TRIM(g.grp)                                                    -- 左连接：取自本条自建的中间结果集 dict，连接键为组号
          GROUP BY l.ev_key                                                                               -- 分组：按事件键汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（§TL-14）
      bl AS (                                                                                             -- 公共表表达式：开启中间结果集 bl——本方案口径的百家乐投注会员名单（§TL-14）
          SELECT  DISTINCT CAST(bet05 AS STRING)                AS member_id                              -- 取值表达式：取用 bet05（会员号）去重，产出「member_id」
          FROM    ods_mariadb_2b.ods_a168_bet02                                                           -- 取数来源：取自注单明细表
          WHERE   dt >= '2026-03-21' AND dt < '2026-08-07'                                                -- 过滤条件：限定 E1 全局窗，涉 dt（营业日）
            AND   CAST(bet02 AS STRING) = '101'                                                           -- 并列条件：限定百家乐产品大类，涉 bet02（游戏类别）
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（§TL-14）
      SELECT  l.member_id,                                                                                -- 取列：起始取列子句，本行先取「member_id」
              l.action_date, l.action_time,                                                               -- 取值表达式：事件之日与时刻
              l.operator_id, l.operator_lv,                                                               -- 取值表达式：操作人与其层级——审计轨迹之「谁」
              l.grp_before, l.grp_after,                                                                  -- 取值表达式：新旧限额组清单原文，留档以备复核
              b.max_hi_before, f.max_hi_after,                                                            -- 取值表达式：新旧清单之最高上限——方向判定之两端
              b.n_grp_before, f.n_grp_after,                                                              -- 取值表达式：两侧能连上字典的组数——连不上者即字典外之组，须查
              CASE WHEN b.max_hi_before IS NULL OR f.max_hi_after IS NULL                                 -- 取值表达式：方向判定起算——任一端无从取上限即判不明
                   THEN 'UNKNOWN_NO_DICT'                                                                 -- 续行：组号不在字典内，方向不明
                   WHEN f.max_hi_after < b.max_hi_before THEN 'TIGHTEN'                                   -- 续行：上限调低即收紧——**限红处置**
                   WHEN f.max_hi_after > b.max_hi_before THEN 'RELEASE'                                   -- 续行：上限调高即放宽
                   ELSE 'FLAT' END                              AS direction,                             -- 续行：持平——清单变而上限未变，不入处置节，产出「direction」
              CASE WHEN COALESCE(b.zero_before,0) + COALESCE(f.zero_after,0) > 0                          -- 取值表达式：歧义标记起算——含「0,0」组者
                   THEN 1 ELSE 0 END                            AS has_zero_group,                        -- 续行：产出「has_zero_group」——★ 为 1 者方向判定须人工复核，不得径入处置总体
              CASE WHEN bl.member_id IS NULL THEN 0 ELSE 1 END  AS is_baccarat_member,                    -- 取值表达式：是否本方案口径的百家乐会员
              'v2026-08-11'                                     AS filter_rule_version                    -- 取值表达式：规则版本号——改判定规则须改版本并记入变更日志
      FROM        lim l                                                                                   -- 取数来源：取自本条自建的中间结果集 lim
      LEFT JOIN   bfr b  ON b.ev_key = l.ev_key                                                           -- 左连接：取自本条自建的中间结果集 bfr，连接键为事件键
      LEFT JOIN   aft f  ON f.ev_key = l.ev_key                                                           -- 左连接：取自本条自建的中间结果集 aft，连接键为事件键
      LEFT JOIN   bl     ON bl.member_id = l.member_id                                                    -- 左连接：取自本条自建的中间结果集 bl，连接键为 member_id（会员号）
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  98. T_table_span.csv   [总包行 1447~1455 · 原 6 列 ＋六层约 60 列]
--     典型学：M15 活跃周期　分区：recent（s.x_rec = 1）　连接：q.`table_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`table_id`, w.`n_rounds`, w.`mean_sec`, w.`p50`, w.`p90`, w.`p99`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_table                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_table IS NOT NULL AND TRIM(CAST(s.x_table AS STRING)) <> ''
    GROUP BY s.x_table
  ),
    q AS (
      SELECT gi011 AS table_id, COUNT(*) AS n_rounds,                                                     -- 取列：起始取列子句，本行先取「n_rounds」，涉 gi011（桌号）
             AVG(UNIX_TIMESTAMP(gi006)-UNIX_TIMESTAMP(gi004))  AS mean_sec,                               -- 求均值表达式：取用 gi004（局开始时刻）、gi006（开牌时刻），产出「mean_sec」
             PERCENTILE_APPROX(UNIX_TIMESTAMP(gi006)-UNIX_TIMESTAMP(gi004),0.5) AS p50,                   -- 取近似分位数表达式：取用 gi004（局开始时刻）、gi006（开牌时刻），产出「p50」
             PERCENTILE_APPROX(UNIX_TIMESTAMP(gi006)-UNIX_TIMESTAMP(gi004),0.9) AS p90,                   -- 取近似分位数表达式：取用 gi004（局开始时刻）、gi006（开牌时刻），产出「p90」
             PERCENTILE_APPROX(UNIX_TIMESTAMP(gi006)-UNIX_TIMESTAMP(gi004),0.99) AS p99                   -- 取近似分位数表达式：取用 gi004（局开始时刻）、gi006（开牌时刻），产出「p99」
      FROM ods_mariadb_2b.ods_a168_game_info                                                              -- 取数来源：取自局信息表（靴局、开牌与桌号的时序基准）
      WHERE gi001='101' AND gi013='1' AND is_lock='N'                                                     -- 过滤条件：限定 gi001等于 '101'、gi013等于 '1'、is_lock等于 'N'，涉 gi001（局主键）
        AND gi004>='2026-03-21' AND gi004<'2026-08-07'                                                    -- 并列条件：限定 gi004不少于 '2026-03-21'、gi004小于 '2026-08-07'，涉 gi004（局开始时刻）
      GROUP BY gi011
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds AS n_rounds_xagg,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`table_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
--  99. VB_00_validbet_contract.csv   [总包行 10675~10697 · 原 13 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`scope`, w.`n_rows`, w.`n_vb_null`, w.`n_vb_blank`, w.`n_vb_zero`, w.`n_vb_pos`, w.`n_vb_neg`, w.`null_rate_pct`, w.`n_fallback_also_null`, w.`vb_sum`, w.`b13_sum`, w.`b41_sum`, w.`verdict_contract`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        'validbet 契约普查（全窗 · 百家乐）'                                    AS scope,                  -- 字面取值：受检范围
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：总行数
        SUM(CASE WHEN validbet IS NULL THEN 1 ELSE 0 END)                      AS n_vb_null,              -- 条件计数：raw 为空值
        SUM(CASE WHEN validbet IS NOT NULL AND TRIM(validbet) = '' THEN 1 ELSE 0 END) AS n_vb_blank,      -- 条件计数：raw 为空串
        SUM(CASE WHEN CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8)) = 0 THEN 1 ELSE 0 END) AS n_vb_zero, -- 条件计数：raw 为零
        SUM(CASE WHEN CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8)) > 0 THEN 1 ELSE 0 END) AS n_vb_pos,  -- 条件计数：raw 为正
        SUM(CASE WHEN CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8)) < 0 THEN 1 ELSE 0 END) AS n_vb_neg,  -- 条件计数：★ raw 为负——若非零须追成因
        ROUND(SUM(CASE WHEN NULLIF(TRIM(validbet), '') IS NULL THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 8)                                        AS null_rate_pct,          -- 比率表达式：★★ 回退触发率——二式分歧之量
        SUM(CASE WHEN NULLIF(TRIM(validbet), '') IS NULL
                   AND NULLIF(TRIM(bet13), '') IS NULL THEN 1 ELSE 0 END)      AS n_fallback_also_null,   -- 条件计数：★ 回退源 bet13 亦空——契约在此失效
        ROUND(SUM(CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))), 2)       AS vb_sum,                 -- 汇总表达式：raw 总额
        ROUND(SUM(CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))), 2)          AS b13_sum,                -- 汇总表达式：本金总额——与 vb 对照
        ROUND(SUM(CAST(NULLIF(TRIM(bet41), '') AS DECIMAL(20,8))), 2)          AS b41_sum,                -- 汇总表达式：bet41 总额
        CASE                                                                                              -- 条件分支：起始分支——契约裁定
          WHEN SUM(CASE WHEN NULLIF(TRIM(validbet), '') IS NULL THEN 1 ELSE 0 END) = 0
            THEN '◐ 本窗口内 null_rate = 0 —— 直取与 COALESCE 二式**数据条件等价**（★ 非设计等价，换窗须重验）' -- 分支判定：暂时等价
          ELSE '❌ 二式分歧 —— 须立唯一契约 validbet_effective，禁各章各自为政'                              -- 分支判定：须立契约
        END                                                                    AS verdict_contract        -- 条件分支：收束上方判词，产出「verdict_contract」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                -- 并列条件：限定百家乐
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 100. VB_00b_zero_five_rates.csv   [总包行 10801~10869 · 原 18 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`scope`, w.`n_base_rows`, w.`n_active_member`, w.`n_zero_rows`, w.`n_zero_member`, w.`zero_record_rate_pct`, w.`zero_amount_rate_pct`, w.`zero_member_rate_pct`, w.`zero_profit_rate_pct`, w.`zero_active_rate_pct`, w.`zero_stake_sum`, w.`zero_ggr_sum`, w.`all_stake_sum`, w.`all_ggr_sum`, w.`zero_mu0_pct`, w.`base_mu0_pct`, w.`n_zero_both`, w.`verdict_zero`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ta AS (                                                                                        -- 公共表表达式：开启中间结果集 ta——公司测试线代理
        SELECT DISTINCT age001 AS aid FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'               -- 取列与过滤：铁律④b——公司测试线
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk——同注单号取最新版本
        SELECT b.bet05, b.bet09, b.bet13, b.bet14, b.bet11, b.validbet, b.bet41,                          -- 取列：会员、注型、本金、派彩、汇率、洗码、bet41
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22, b.category, b.bet38,                          -- 续行：五级代理、类别、重对标记
               ROW_NUMBER() OVER (PARTITION BY b.bet01
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC)      AS rn                      -- 行号窗口表达式：三级排序取最新（§M0-6 已证唯一）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单主表
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'                                                -- 过滤条件：限定分析窗
          AND CAST(b.bet02 AS STRING) = '101'                                                             -- 并列条件：限定百家乐
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      base AS (                                                                                           -- 公共表表达式：开启中间结果集 base——过基础闸之母体
        SELECT TRIM(r.bet05)                                                   AS mid,                    -- 取列：会员编号
               TRIM(r.bet09)                                                   AS g9,                     -- 取列：注型
               CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8))
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)), 0) AS stake,                  -- 取值表达式：本金归一
               (CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8))
              - CAST(NULLIF(TRIM(r.bet14), '') AS DECIMAL(20,8)))
                 / NULLIF(CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)), 0) AS ggr,                    -- 取值表达式：毛利归一（本金减派彩）
               CAST(NULLIF(TRIM(r.validbet), '') AS DECIMAL(20,8))             AS vb,                     -- 取列：洗码量原值
               CAST(NULLIF(TRIM(r.bet41), '')    AS DECIMAL(20,8))             AS v41                     -- 取列：bet41 原值
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：一级代理是否测试线
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：二级代理是否测试线
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：三级代理是否测试线
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：四级代理是否测试线
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：五级代理是否测试线
        WHERE r.rn = 1                                                                                    -- 过滤条件：基础闸①——最新版本
          AND r.category = '1'                                                                            -- 并列条件：基础闸②——一般注单
          AND UPPER(TRIM(r.bet38)) = 'N'                                                                  -- 并列条件：基础闸③——非重对
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：基础闸④——非测试线
          AND CAST(NULLIF(TRIM(r.bet11), '') AS DECIMAL(20,8)) > 0                                        -- 并列条件：基础闸⑤——汇率为正
          AND CAST(NULLIF(TRIM(r.bet13), '') AS DECIMAL(20,8)) > 0                                        -- 并列条件：基础闸⑥——本金为正
      )                                                                                                   -- 续行：收束上方的子查询或函数括号
      SELECT                                                                                              -- 取列：起始取列子句
        'validbet 零值 · 五类平行分母（过基础闸之母体）'                          AS scope,                  -- 字面取值：受检范围
        COUNT(*)                                                               AS n_base_rows,            -- 计数表达式：母体记录数
        COUNT(DISTINCT mid)                                                    AS n_active_member,        -- 计数表达式：★ active_member——本件所立之定义
        SUM(CASE WHEN vb = 0 THEN 1 ELSE 0 END)                                AS n_zero_rows,            -- 条件计数：零值记录数
        COUNT(DISTINCT CASE WHEN vb = 0 THEN mid END)                          AS n_zero_member,          -- 条件去重计数：曾现零值之会员数
        ROUND(SUM(CASE WHEN vb = 0 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 6)                                        AS zero_record_rate_pct,   -- 比率表达式：①记录率——分母＝全部记录
        ROUND(SUM(CASE WHEN vb = 0 THEN stake ELSE 0 END) * 100.0
              / NULLIF(SUM(stake), 0), 6)                                      AS zero_amount_rate_pct,   -- 比率表达式：②金额率——分母＝全部本金
        ROUND(COUNT(DISTINCT CASE WHEN vb = 0 THEN mid END) * 100.0
              / NULLIF(COUNT(DISTINCT mid), 0), 6)                             AS zero_member_rate_pct,   -- 比率表达式：③会员率——分母＝有效会员
        ROUND(SUM(CASE WHEN vb = 0 THEN ABS(ggr) ELSE 0 END) * 100.0
              / NULLIF(SUM(ABS(ggr)), 0), 6)                                   AS zero_profit_rate_pct,   -- 比率表达式：④毛利率——分母＝全部毛利绝对值
        ROUND(COUNT(DISTINCT CASE WHEN vb = 0 THEN mid END) * 100.0
              / NULLIF(COUNT(DISTINCT mid), 0), 6)                             AS zero_active_rate_pct,   -- 比率表达式：⑤★ 活跃会员率——分母＝active_member，禁用全会员
        ROUND(SUM(CASE WHEN vb = 0 THEN stake ELSE 0 END), 2)                  AS zero_stake_sum,         -- 条件汇总：零值群之本金总额
        ROUND(SUM(CASE WHEN vb = 0 THEN ggr   ELSE 0 END), 2)                  AS zero_ggr_sum,           -- 条件汇总：零值群之毛利总额
        ROUND(SUM(stake), 2)                                                   AS all_stake_sum,          -- 汇总表达式：母体本金总额
        ROUND(SUM(ggr), 2)                                                     AS all_ggr_sum,            -- 汇总表达式：母体毛利总额
        ROUND(-1 * SUM(CASE WHEN vb = 0 THEN ggr ELSE 0 END) * 100.0
              / NULLIF(SUM(CASE WHEN vb = 0 THEN stake ELSE 0 END), 0), 6)     AS zero_mu0_pct,           -- 比率表达式：★ 零值群之 μ0——与母体 μ0 对照可判其性质
        ROUND(-1 * SUM(ggr) * 100.0 / NULLIF(SUM(stake), 0), 6)                AS base_mu0_pct,           -- 比率表达式：母体 μ0——基准
        SUM(CASE WHEN vb = 0 AND v41 = 0 THEN 1 ELSE 0 END)                    AS n_zero_both,            -- 条件计数：★ 二列皆零者——与 964 笔之交集判别
        CASE                                                                                              -- 条件分支：起始分支——五率一致性裁定
          WHEN ABS(SUM(CASE WHEN vb = 0 THEN 1 ELSE 0 END) * 100.0 / NULLIF(COUNT(*), 0)
                 - SUM(CASE WHEN vb = 0 THEN stake ELSE 0 END) * 100.0 / NULLIF(SUM(stake), 0)) > 5
            THEN '★ 记录率与金额率相差逾 5pp —— 零值集中于特定金额段，须按金额分层复验'                       -- 分支判定：金额偏斜
          WHEN ABS(SUM(CASE WHEN vb = 0 THEN 1 ELSE 0 END) * 100.0 / NULLIF(COUNT(*), 0)
                 - COUNT(DISTINCT CASE WHEN vb = 0 THEN mid END) * 100.0 / NULLIF(COUNT(DISTINCT mid), 0)) > 20
            THEN '★ 记录率与会员率相差逾 20pp —— 零值集中于少数会员或普及于多数会员，须按会员分层复验'          -- 分支判定：会员偏斜
          ELSE '◐ 五率量级相近 —— 零值形似结构性均匀分布（★ 仍须按注型复验，未可径断）'                        -- 分支判定：形似均匀
        END                                                                    AS verdict_zero            -- 条件分支：收束上方判词，产出「verdict_zero」
      FROM base                                                                                          -- 取数来源：取自本条自建的中间结果集 base
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 101. VB_41_identity_global.csv   [总包行 10716~10750 · 原 17 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`test`, w.`n_rows`, w.`n_both_null`, w.`n_null_asym`, w.`n_exact_equal`, w.`n_value_diff`, w.`pct_exact_equal`, w.`max_abs_diff`, w.`sum_abs_diff`, w.`d_min`, w.`d_p01`, w.`d_p05`, w.`d_p50`, w.`d_p95`, w.`d_p99`, w.`d_max`, w.`verdict_identity`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH vb AS (                                                                                        -- 公共表表达式：开启中间结果集 vb——两列并置与差值
        SELECT CAST(NULLIF(TRIM(bet41), '')    AS DECIMAL(20,8))               AS v41,                    -- 取列：bet41 转数值
               CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))               AS vvb,                    -- 取列：validbet 转数值
               CAST(NULLIF(TRIM(bet41), '')    AS DECIMAL(20,8))
                 - CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))           AS d                       -- 取值表达式：★ 差值 D ＝ bet41 − validbet
        FROM ods_mariadb_2b.ods_a168_bet02                                                                -- 取数来源：取自注单主表
        WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                    -- 过滤条件：限定分析窗
          AND CAST(bet02 AS STRING) = '101'                                                               -- 并列条件：限定百家乐
      )                                                                                                   -- 续行：收束上方的子查询或函数括号
      SELECT                                                                                              -- 取列：起始取列子句
        'bet41 ≡ validbet（全窗 · 百家乐）'                                    AS test,                   -- 字面取值：受检命题
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：总行数
        SUM(CASE WHEN v41 IS NULL AND vvb IS NULL THEN 1 ELSE 0 END)           AS n_both_null,            -- 条件计数：★ 象限一——二者皆空
        SUM(CASE WHEN (v41 IS NULL) <> (vvb IS NULL) THEN 1 ELSE 0 END)        AS n_null_asym,            -- 条件计数：★★ 象限二——空值非对称，最危险
        SUM(CASE WHEN v41 IS NOT NULL AND vvb IS NOT NULL AND v41 = vvb THEN 1 ELSE 0 END) AS n_exact_equal, -- 条件计数：★ 象限三——皆非空且相等
        SUM(CASE WHEN v41 IS NOT NULL AND vvb IS NOT NULL AND v41 <> vvb THEN 1 ELSE 0 END) AS n_value_diff, -- 条件计数：★ 象限四——皆非空而不等
        ROUND(SUM(CASE WHEN v41 IS NOT NULL AND vvb IS NOT NULL AND v41 = vvb THEN 1 ELSE 0 END) * 100.0
              / NULLIF(SUM(CASE WHEN v41 IS NOT NULL AND vvb IS NOT NULL THEN 1 ELSE 0 END), 0), 8) AS pct_exact_equal, -- 比率表达式：★★ 皆非空者中之相等率
        ROUND(MAX(ABS(d)), 8)                                                  AS max_abs_diff,           -- 聚合取值：★ 最大绝对差——判是否仅浮点误差
        ROUND(SUM(ABS(d)), 8)                                                  AS sum_abs_diff,           -- 汇总表达式：绝对差之和
        ROUND(MIN(d), 8)                                                       AS d_min,                  -- 聚合取值：差值下界
        ROUND(PERCENTILE_APPROX(d, 0.01), 8)                                   AS d_p01,                  -- 取近似分位数表达式：差值 1 分位
        ROUND(PERCENTILE_APPROX(d, 0.05), 8)                                   AS d_p05,                  -- 取近似分位数表达式：差值 5 分位
        ROUND(PERCENTILE_APPROX(d, 0.50), 8)                                   AS d_p50,                  -- 取近似分位数表达式：差值中位
        ROUND(PERCENTILE_APPROX(d, 0.95), 8)                                   AS d_p95,                  -- 取近似分位数表达式：差值 95 分位
        ROUND(PERCENTILE_APPROX(d, 0.99), 8)                                   AS d_p99,                  -- 取近似分位数表达式：差值 99 分位
        ROUND(MAX(d), 8)                                                       AS d_max,                  -- 聚合取值：差值上界
        CASE                                                                                              -- 条件分支：起始分支——恒等裁定（三态）
          WHEN SUM(CASE WHEN (v41 IS NULL) <> (vvb IS NULL) THEN 1 ELSE 0 END) > 0
            THEN '❌ NOT_IDENTICAL —— 空值语义不同（非对称空），二列不可互代'                                 -- 分支判定：空值非对称
          WHEN SUM(CASE WHEN v41 IS NOT NULL AND vvb IS NOT NULL AND v41 <> vvb THEN 1 ELSE 0 END) > 0
            THEN '◐ CONDITIONAL —— 存在数值差异，须并读 max_abs_diff 判其系浮点抑或量纲之别'                 -- 分支判定：有数值差
          ELSE '✅ IDENTICAL（★ 措辞锁：只得书「本窗口本产品下二列取值相同」，禁书「bet41 就是 validbet」）'     -- 分支判定：本窗口相同
        END                                                                    AS verdict_identity        -- 条件分支：收束上方判词，产出「verdict_identity」
      FROM vb                                                                                            -- 取数来源：取自本条自建的中间结果集 vb
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 102. VB_41b_identity_strata.csv   [总包行 10761~10792 · 原 10 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：q.`bet09_value`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`bet09_value`, w.`commission_value`, w.`n_rows`, w.`n_both_null`, w.`n_null_asym`, w.`n_value_diff`, w.`max_abs_diff`, w.`b41_sum`, w.`vb_sum`, w.`verdict_stratum`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_product                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_product IS NOT NULL AND TRIM(CAST(s.x_product AS STRING)) <> ''
    GROUP BY s.x_product
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        TRIM(bet09)                                                            AS bet09_value,            -- 取列：注型组
        TRIM(commission)                                                       AS commission_value,       -- 取列：佣金标记
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：该层行数
        SUM(CASE WHEN NULLIF(TRIM(bet41), '') IS NULL
                   AND NULLIF(TRIM(validbet), '') IS NULL THEN 1 ELSE 0 END)   AS n_both_null,            -- 条件计数：象限一
        SUM(CASE WHEN (NULLIF(TRIM(bet41), '') IS NULL)
                  <> (NULLIF(TRIM(validbet), '') IS NULL) THEN 1 ELSE 0 END)   AS n_null_asym,            -- 条件计数：★ 象限二
        SUM(CASE WHEN NULLIF(TRIM(bet41), '') IS NOT NULL
                   AND NULLIF(TRIM(validbet), '') IS NOT NULL
                   AND CAST(TRIM(bet41) AS DECIMAL(20,8))
                     <> CAST(TRIM(validbet) AS DECIMAL(20,8)) THEN 1 ELSE 0 END) AS n_value_diff,         -- 条件计数：★ 象限四
        ROUND(MAX(ABS(CAST(NULLIF(TRIM(bet41), '') AS DECIMAL(20,8))
                      - CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8)))), 8) AS max_abs_diff,          -- 聚合取值：该层最大绝对差
        ROUND(SUM(CAST(NULLIF(TRIM(bet41), '') AS DECIMAL(20,8))), 2)          AS b41_sum,                -- 汇总表达式：该层 bet41 总额
        ROUND(SUM(CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))), 2)       AS vb_sum,                 -- 汇总表达式：该层 validbet 总额
        CASE                                                                                              -- 条件分支：起始分支——逐层裁定
          WHEN SUM(CASE WHEN (NULLIF(TRIM(bet41), '') IS NULL)
                         <> (NULLIF(TRIM(validbet), '') IS NULL) THEN 1 ELSE 0 END) > 0
            THEN '❌ 本层空值非对称'                                                                       -- 分支判定：空值非对称
          WHEN SUM(CASE WHEN NULLIF(TRIM(bet41), '') IS NOT NULL
                          AND NULLIF(TRIM(validbet), '') IS NOT NULL
                          AND CAST(TRIM(bet41) AS DECIMAL(20,8))
                            <> CAST(TRIM(validbet) AS DECIMAL(20,8)) THEN 1 ELSE 0 END) > 0
            THEN '◐ 本层有数值差'                                                                         -- 分支判定：有数值差
          ELSE '✅ 本层相同'                                                                              -- 分支判定：本层相同
        END                                                                    AS verdict_stratum         -- 条件分支：收束上方判词，产出「verdict_stratum」
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
      GROUP BY TRIM(bet09), TRIM(commission)                                                              -- 分组：按注型与佣金制分层
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`bet09_value` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 103. VB_41b_identity_strata_v18b.csv   [总包行 10876~10930 · 原 18 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：q.`bet09_value`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`bet09_value`, w.`commission_value`, w.`n_rows`, w.`n_both_null`, w.`n_null_asym`, w.`n_value_diff`, w.`pct_value_diff`, w.`max_abs_diff`, w.`d_min`, w.`d_max`, w.`sum_abs_diff`, w.`n_both_zero`, w.`n_b41_zero_vb_pos`, w.`n_b41_pos_vb_zero`, w.`d_sign_uniform`, w.`b41_sum`, w.`vb_sum`, w.`verdict_stratum`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_product                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_product IS NOT NULL AND TRIM(CAST(s.x_product AS STRING)) <> ''
    GROUP BY s.x_product
  ),
    q AS (
      WITH src AS (                                                                                       -- 公共表表达式：开启中间结果集 src——逐行取二列与差值
        SELECT TRIM(bet09)                                                     AS g,                      -- 取列：注型组
               TRIM(commission)                                                AS comm,                   -- 取列：佣金标记
               CAST(NULLIF(TRIM(bet41), '')    AS DECIMAL(20,8))               AS v41,                    -- 取列：bet41 转数值
               CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))               AS vvb,                    -- 取列：validbet 转数值
               CAST(NULLIF(TRIM(bet41), '')    AS DECIMAL(20,8))
                 - CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))           AS d                       -- 取值表达式：差值 D ＝ bet41 − validbet
        FROM ods_mariadb_2b.ods_a168_bet02                                                                -- 取数来源：取自注单主表
        WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                    -- 过滤条件：限定分析窗
          AND CAST(bet02 AS STRING) = '101'                                                               -- 并列条件：限定百家乐
      )                                                                                                   -- 续行：收束上方的子查询或函数括号
      SELECT                                                                                              -- 取列：起始取列子句
        g                                                                      AS bet09_value,            -- 取列：〔01〕注型
        comm                                                                   AS commission_value,       -- 取列：〔02〕佣金制
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：〔03〕该层行数
        SUM(CASE WHEN v41 IS NULL AND vvb IS NULL THEN 1 ELSE 0 END)           AS n_both_null,            -- 条件计数：〔04〕二者皆空
        SUM(CASE WHEN (v41 IS NULL) <> (vvb IS NULL) THEN 1 ELSE 0 END)        AS n_null_asym,            -- 条件计数：〔05〕空值非对称
        SUM(CASE WHEN v41 IS NOT NULL AND vvb IS NOT NULL AND v41 <> vvb THEN 1 ELSE 0 END) AS n_value_diff, -- 条件计数：〔06〕数值不等
        ROUND(SUM(CASE WHEN v41 IS NOT NULL AND vvb IS NOT NULL AND v41 <> vvb THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 8)                                        AS pct_value_diff,         -- 比率表达式：〔07〕差异占该层之比
        ROUND(MAX(ABS(d)), 8)                                                  AS max_abs_diff,           -- 聚合取值：〔08〕最大绝对差
        ROUND(MIN(d), 8)                                                       AS d_min,                  -- 聚合取值：〔09〕差值下界
        ROUND(MAX(d), 8)                                                       AS d_max,                  -- 聚合取值：〔10〕差值上界
        ROUND(SUM(ABS(d)), 8)                                                  AS sum_abs_diff,           -- 汇总表达式：〔11〕绝对差之和
        SUM(CASE WHEN v41 = 0 AND vvb = 0 THEN 1 ELSE 0 END)                   AS n_both_zero,            -- 条件计数：〔12〕零对零
        SUM(CASE WHEN v41 = 0 AND vvb > 0 THEN 1 ELSE 0 END)                   AS n_b41_zero_vb_pos,      -- 条件计数：〔13〕bet41 零而 validbet 正
        SUM(CASE WHEN v41 > 0 AND vvb = 0 THEN 1 ELSE 0 END)                   AS n_b41_pos_vb_zero,      -- 条件计数：〔14〕反向
        CASE                                                                                              -- 条件分支：〔15〕起始分支——★ 六态穷尽（本版之核心）
          WHEN MIN(d) IS NULL OR MAX(d) IS NULL                       THEN 'NO_DATA'                      -- 分支判定：该层无可比之行
          WHEN MIN(d) = 0 AND MAX(d) = 0                              THEN 'NO_DIFFERENCE'                -- 分支判定：a=0 ∧ b=0——差值恒零
          WHEN MIN(d) < 0 AND MAX(d) < 0 AND MIN(d) = MAX(d)          THEN 'B41_LE_VB_CONSTANT'           -- 分支判定：★ a=b<0——恒定负差
          WHEN MIN(d) > 0 AND MAX(d) > 0 AND MIN(d) = MAX(d)          THEN 'B41_GE_VB_CONSTANT'           -- 分支判定：★ a=b>0——恒定正差
          WHEN MIN(d) < 0 AND MAX(d) <= 0                             THEN 'B41_LE_VB_UNIFORM'            -- 分支判定：a<0 ∧ b≤0——单向非正
          WHEN MIN(d) >= 0 AND MAX(d) > 0                             THEN 'B41_GE_VB_UNIFORM'            -- 分支判定：a≥0 ∧ b>0——单向非负
          WHEN MIN(d) < 0 AND MAX(d) > 0                              THEN 'BIDIRECTIONAL'                -- 分支判定：a<0 ∧ b>0——双向
          ELSE 'UNCLASSIFIED'                                                                             -- 分支判定：★★ 穷举锁——跑毕须为 0，非零即判词空间仍有缺口
        END                                                                    AS d_sign_uniform,         -- 条件分支：收束上方判词，产出「d_sign_uniform」
        ROUND(SUM(v41), 2)                                                     AS b41_sum,                -- 汇总表达式：〔16〕该层 bet41 总额
        ROUND(SUM(vvb), 2)                                                     AS vb_sum,                 -- 汇总表达式：〔17〕该层 validbet 总额
        CASE                                                                                              -- 条件分支：〔18〕起始分支——五态裁定（承 v1.0.2，不改）
          WHEN SUM(CASE WHEN (v41 IS NULL) <> (vvb IS NULL) THEN 1 ELSE 0 END) > 0
            THEN 'FAIL'                                                                                   -- 分支判定：空值语义不同
          WHEN COUNT(*) = SUM(CASE WHEN v41 = 0 AND vvb = 0 THEN 1 ELSE 0 END)
            THEN 'NO_COMPARABLE_VALUE'                                                                    -- 分支判定：二列皆恒为零，无可比之量
          WHEN SUM(CASE WHEN v41 IS NOT NULL AND vvb IS NOT NULL AND v41 <> vvb THEN 1 ELSE 0 END) = 0
           AND (SUM(v41) > 0 OR SUM(vvb) > 0)
            THEN 'IDENTICAL_NONZERO'                                                                      -- 分支判定：无差异且有正值
          WHEN SUM(CASE WHEN v41 IS NOT NULL AND vvb IS NOT NULL AND v41 <> vvb THEN 1 ELSE 0 END) = 0
           AND COALESCE(SUM(v41), 0) = 0 AND COALESCE(SUM(vvb), 0) = 0
            THEN 'IDENTICAL_ZERO_ONLY'                                                                    -- 分支判定：无差异惟总额皆零
          ELSE 'CONDITIONAL'                                                                              -- 分支判定：有数值差异
        END                                                                    AS verdict_stratum         -- 条件分支：收束上方判词，产出「verdict_stratum」
      FROM src                                                                                            -- 取数来源：取自本条自建的中间结果集 src
      GROUP BY g, comm                                                                                    -- 分组：按注型与佣金制分层
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`bet09_value` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 104. VB_41c1_964_detail.csv   [总包行 10939~10970 · 原 22 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`grp`, w.`bet01`, w.`member_id`, w.`dt`, w.`bet_time`, w.`table_id`, w.`shoe_id`, w.`bet09`, w.`commission`, w.`category`, w.`rebet_flag`, w.`agent_lv1`, w.`stake_raw`, w.`fx_rate`, w.`payout_raw`, w.`rebate_raw`, w.`net_raw`, w.`bet41`, w.`validbet`, w.`diff_vb_minus_b41`, w.`updatetime`, w.`sync_time`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        CASE WHEN TRIM(bet09) IN ('Bear8', 'Lucky7TwoCard', 'SmallTiger',
                                  'SuperTie4', 'SuperTie5', 'SuperTie8', 'SuperTie9')
             THEN 'B_上线测试型' ELSE 'A_主群' END                                AS grp,                    -- 条件取值：★ 证据分群，承锁二十六
        TRIM(bet01)                                                            AS bet01,                  -- 取列：注单唯一键
        TRIM(bet05)                                                            AS member_id,              -- 取列：会员编号
        dt                                                                     AS dt,                     -- 取列：分区日
        TRIM(bet08)                                                            AS bet_time,               -- 取列：投注时间
        TRIM(bet39)                                                            AS table_id,               -- 取列：桌号
        TRIM(bet03)                                                            AS shoe_id,                -- 取列：靴号
        TRIM(bet09)                                                            AS bet09,                  -- 取列：注型
        TRIM(commission)                                                       AS commission,             -- 取列：佣金标记
        TRIM(category)                                                         AS category,               -- 取列：注单类别
        TRIM(bet38)                                                            AS rebet_flag,             -- 取列：重对标记
        TRIM(bet18)                                                            AS agent_lv1,              -- 取列：一级代理——判是否测试线
        CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))                         AS stake_raw,              -- 取列：本金（原币）
        CAST(NULLIF(TRIM(bet11), '') AS DECIMAL(20,8))                         AS fx_rate,                -- 取列：汇率
        CAST(NULLIF(TRIM(bet14), '') AS DECIMAL(20,8))                         AS payout_raw,             -- 取列：派彩（原币）
        CAST(NULLIF(TRIM(bet16), '') AS DECIMAL(20,8))                         AS rebate_raw,             -- 取列：退水（原币）
        CAST(NULLIF(TRIM(bet17), '') AS DECIMAL(20,8))                         AS net_raw,                -- 取列：会员净额（原币）
        CAST(NULLIF(TRIM(bet41), '') AS DECIMAL(20,8))                         AS bet41,                  -- 取列：★ 恒为 0
        CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))                      AS validbet,               -- 取列：★ 恒为正
        CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))
          - CAST(NULLIF(TRIM(bet41), '') AS DECIMAL(20,8))                     AS diff_vb_minus_b41,      -- 取值表达式：★ 缺失额 ＝ validbet − bet41
        TRIM(updatetime)                                                       AS updatetime,             -- 取列：★ 更新时间——判是否结算后方写入（候选成因③）
        TRIM(sync_time)                                                        AS sync_time               -- 取列：★ 同步时间——判是否 ETL 环节所致（候选成因⑤）
      FROM ods_mariadb_2b.ods_a168_bet02                                                                  -- 取数来源：取自注单主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
        AND CAST(bet02 AS STRING) = '101'                                                                 -- 并列条件：限定百家乐
        AND CAST(NULLIF(TRIM(bet41), '')    AS DECIMAL(20,8)) = 0                                         -- 并列条件：★ bet41 恰为零
        AND CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8)) > 0                                         -- 并列条件：★ validbet 为正
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 105. VB_41c2_concentration.csv   [总包行 10982~11045 · 原 20 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`grp`, w.`n_rows`, w.`n_members`, w.`top1_member_share_pct`, w.`top1_member_n`, w.`max_member_diff_sum`, w.`n_dates`, w.`min_date`, w.`max_date`, w.`top_date_n`, w.`top_date_share_pct`, w.`n_tables`, w.`top_table_share_pct`, w.`n_bet09`, w.`n_commission`, w.`sum_stake`, w.`sum_validbet`, w.`min_stake`, w.`max_stake`, w.`verdict_concentration`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH tgt AS (                                                                                       -- 公共表表达式：开启中间结果集 tgt——964 笔之目标集
        SELECT CASE WHEN TRIM(bet09) IN ('Bear8', 'Lucky7TwoCard', 'SmallTiger',
                                         'SuperTie4', 'SuperTie5', 'SuperTie8', 'SuperTie9')
                    THEN 'B_上线测试型' ELSE 'A_主群' END                       AS grp,                    -- 条件取值：证据分群
               TRIM(bet05)                                                     AS mid,                    -- 取列：会员编号
               dt                                                              AS dt,                     -- 取列：分区日
               TRIM(bet04)                                                     AS tbl,                    -- 取列：桌号
               TRIM(bet09)                                                     AS g9,                     -- 取列：注型
               TRIM(commission)                                                AS comm,                   -- 取列：佣金标记
               CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))                  AS stake,                  -- 取列：本金
               CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8))               AS vvb                     -- 取列：validbet
        FROM ods_mariadb_2b.ods_a168_bet02                                                                -- 取数来源：取自注单主表
        WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                    -- 过滤条件：限定分析窗
          AND CAST(bet02 AS STRING) = '101'                                                               -- 并列条件：限定百家乐
          AND CAST(NULLIF(TRIM(bet41), '')    AS DECIMAL(20,8)) = 0                                       -- 并列条件：bet41 恰为零
          AND CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8)) > 0                                       -- 并列条件：validbet 为正
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      by_mid AS (                                                                                         -- 公共表表达式：开启中间结果集 by_mid——逐会员计数
        SELECT grp, mid, COUNT(*) AS n, SUM(vvb) AS s                                                     -- 取列与汇总：会员、笔数、缺失额
        FROM tgt GROUP BY grp, mid                                                                        -- 分组：按群与会员汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      by_dt AS (                                                                                          -- 公共表表达式：开启中间结果集 by_dt——逐日计数
        SELECT grp, dt, COUNT(*) AS n FROM tgt GROUP BY grp, dt                                           -- 取列与汇总：群、日、笔数
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      by_tbl AS (                                                                                         -- 公共表表达式：开启中间结果集 by_tbl——逐桌计数
        SELECT grp, tbl, COUNT(*) AS n FROM tgt GROUP BY grp, tbl                                         -- 取列与汇总：群、桌、笔数
      )                                                                                                   -- 续行：收束上方的子查询或函数括号
      SELECT                                                                                              -- 取列：起始取列子句
        t.grp                                                                  AS grp,                    -- 取列：证据群
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：该群笔数
        COUNT(DISTINCT t.mid)                                                  AS n_members,              -- 计数表达式：★ A 维——涉及会员数
        ROUND((SELECT MAX(n) FROM by_mid m WHERE m.grp = t.grp) * 100.0
              / NULLIF(COUNT(*), 0), 4)                                        AS top1_member_share_pct,  -- 比率表达式：★★ 首位会员占比——集中度之判据
        (SELECT MAX(n) FROM by_mid m WHERE m.grp = t.grp)                      AS top1_member_n,          -- 标量子查询：首位会员之笔数
        ROUND((SELECT MAX(s) FROM by_mid m WHERE m.grp = t.grp), 2)            AS max_member_diff_sum,    -- 标量子查询：单一会员之最大缺失额
        COUNT(DISTINCT t.dt)                                                   AS n_dates,                -- 计数表达式：★ B 维——涉及天数（对照全窗 139）
        MIN(t.dt)                                                              AS min_date,               -- 聚合取值：首现日
        MAX(t.dt)                                                              AS max_date,               -- 聚合取值：末现日
        (SELECT MAX(n) FROM by_dt d WHERE d.grp = t.grp)                       AS top_date_n,             -- 标量子查询：单日最多笔数
        ROUND((SELECT MAX(n) FROM by_dt d WHERE d.grp = t.grp) * 100.0
              / NULLIF(COUNT(*), 0), 4)                                        AS top_date_share_pct,     -- 比率表达式：★ 首位日期占比
        COUNT(DISTINCT t.tbl)                                                  AS n_tables,               -- 计数表达式：★ C 维——涉及桌数
        ROUND((SELECT MAX(n) FROM by_tbl b WHERE b.grp = t.grp) * 100.0
              / NULLIF(COUNT(*), 0), 4)                                        AS top_table_share_pct,    -- 比率表达式：首位桌台占比
        COUNT(DISTINCT t.g9)                                                   AS n_bet09,                -- 计数表达式：★ E 维——涉及注型数
        COUNT(DISTINCT t.comm)                                                 AS n_commission,           -- 计数表达式：涉及佣金制数
        ROUND(SUM(t.stake), 2)                                                 AS sum_stake,              -- 汇总表达式：★ D 维——本金总额
        ROUND(SUM(t.vvb), 2)                                                   AS sum_validbet,           -- 汇总表达式：validbet 总额（＝ 缺失额，因 bet41 恒零）
        ROUND(MIN(t.stake), 2)                                                 AS min_stake,              -- 聚合取值：最小本金
        ROUND(MAX(t.stake), 2)                                                 AS max_stake,              -- 聚合取值：最大本金
        CASE                                                                                              -- 条件分支：起始分支——集中度裁定
          WHEN (SELECT MAX(n) FROM by_mid m WHERE m.grp = t.grp) * 1.0
               / NULLIF(COUNT(*), 0) >= 0.30
            THEN '★ 高度集中于少数会员 —— 属风控线索，须逐会员追'                                             -- 分支判定：会员集中
          WHEN COUNT(DISTINCT t.dt) * 1.0 / 139 >= 0.50
            THEN '◐ 散布全窗过半天数 —— 倾向系统性规则或持续漏记，非单次事件'                                   -- 分支判定：时间散布
          WHEN (SELECT MAX(n) FROM by_dt d WHERE d.grp = t.grp) * 1.0
               / NULLIF(COUNT(*), 0) >= 0.30
            THEN '◐ 集中于少数日期 —— 须核该期是否有变更、上线或重同步'                                        -- 分支判定：时间集中
          ELSE '◐ 无单一维度显著集中 —— 须并读五维方可判'                                                    -- 分支判定：无显著集中
        END                                                                    AS verdict_concentration   -- 条件分支：收束上方判词，产出「verdict_concentration」
      FROM tgt t                                                                                          -- 取数来源：取自本条自建的中间结果集 tgt
      GROUP BY t.grp                                                                                      -- 分组：按证据群汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 106. VB_41c2_concentration_v2.csv   [总包行 11052~11140 · 原 20 列 ＋六层约 60 列]
--     典型学：M05 退水套利　分区：hiturn（s.x_valid > s.x_stake）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`grp`, w.`n_rows`, w.`n_members`, w.`n_dates`, w.`n_tables`, w.`n_bet09`, w.`top1_member_share_pct`, w.`top_date_share_pct`, w.`top_table_share_pct`, w.`top_bet09_share_pct`, w.`top_stake_share_pct`, w.`st_member`, w.`st_date`, w.`st_table`, w.`st_bet09`, w.`st_stake`, w.`n_dims_high`, w.`n_dims_near_or_high`, w.`verdict_concentration`, w.`sum_stake`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_valid > s.x_stake THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_valid > s.x_stake) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH tgt AS (                                                                                       -- 公共表表达式：开启中间结果集 tgt——964 笔之目标集
        SELECT CASE WHEN TRIM(bet09) IN ('Bear8', 'Lucky7TwoCard', 'SmallTiger',
                                         'SuperTie4', 'SuperTie5', 'SuperTie8', 'SuperTie9')
                    THEN 'B_上线测试型' ELSE 'A_主群' END                       AS grp,                    -- 条件取值：证据分群，禁合并解释
               TRIM(bet05)                                                     AS mid,                    -- 取列：会员编号
               dt                                                              AS dt,                     -- 取列：分区日
               TRIM(bet04)                                                     AS tbl,                    -- 取列：桌号
               TRIM(bet09)                                                     AS g9,                     -- 取列：注型
               CAST(NULLIF(TRIM(bet13), '') AS DECIMAL(20,8))                  AS stake                   -- 取列：本金
        FROM ods_mariadb_2b.ods_a168_bet02                                                                -- 取数来源：取自注单主表
        WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                    -- 过滤条件：限定分析窗
          AND CAST(bet02 AS STRING) = '101'                                                               -- 并列条件：限定百家乐
          AND CAST(NULLIF(TRIM(bet41), '')    AS DECIMAL(20,8)) = 0                                       -- 并列条件：bet41 恰为零
          AND CAST(NULLIF(TRIM(validbet), '') AS DECIMAL(20,8)) > 0                                       -- 并列条件：validbet 为正
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      tot AS (                                                                                            -- 公共表表达式：开启中间结果集 tot——各群之总量
        SELECT grp, COUNT(*) AS n, SUM(stake) AS s FROM tgt GROUP BY grp                                  -- 取列与汇总：群、笔数、本金总额
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号
      mx AS (                                                                                             -- 公共表表达式：开启中间结果集 mx——五维之首位占量
        SELECT t.grp                                                           AS grp,                    -- 取列：证据群
               (SELECT MAX(c) FROM (SELECT grp AS g2, mid, COUNT(*) AS c FROM tgt GROUP BY grp, mid) x
                 WHERE x.g2 = t.grp)                                           AS top_member_n,           -- 标量子查询：首位会员之笔数
               (SELECT MAX(c) FROM (SELECT grp AS g2, dt, COUNT(*) AS c FROM tgt GROUP BY grp, dt) x
                 WHERE x.g2 = t.grp)                                           AS top_date_n,             -- 标量子查询：首位日期之笔数
               (SELECT MAX(c) FROM (SELECT grp AS g2, tbl, COUNT(*) AS c FROM tgt GROUP BY grp, tbl) x
                 WHERE x.g2 = t.grp)                                           AS top_table_n,            -- 标量子查询：首位桌台之笔数
               (SELECT MAX(c) FROM (SELECT grp AS g2, g9, COUNT(*) AS c FROM tgt GROUP BY grp, g9) x
                 WHERE x.g2 = t.grp)                                           AS top_bet09_n,            -- 标量子查询：首位注型之笔数
               (SELECT MAX(c) FROM (SELECT grp AS g2, mid, SUM(stake) AS c FROM tgt GROUP BY grp, mid) x
                 WHERE x.g2 = t.grp)                                           AS top_stake_amt           -- 标量子查询：★ 单一会员之最大本金和——金额维
        FROM (SELECT DISTINCT grp FROM tgt) t                                                             -- 取数来源：各群一行
      )                                                                                                   -- 续行：收束上方的子查询或函数括号
      SELECT                                                                                              -- 取列：起始取列子句
        o.grp                                                                  AS grp,                    -- 取列：证据群
        o.n                                                                    AS n_rows,                 -- 取列：该群笔数
        (SELECT COUNT(DISTINCT mid) FROM tgt z WHERE z.grp = o.grp)            AS n_members,              -- 标量子查询：涉及会员数
        (SELECT COUNT(DISTINCT dt)  FROM tgt z WHERE z.grp = o.grp)            AS n_dates,                -- 标量子查询：涉及天数
        (SELECT COUNT(DISTINCT tbl) FROM tgt z WHERE z.grp = o.grp)            AS n_tables,               -- 标量子查询：涉及桌数
        (SELECT COUNT(DISTINCT g9)  FROM tgt z WHERE z.grp = o.grp)            AS n_bet09,                -- 标量子查询：涉及注型数
        ROUND(m.top_member_n * 100.0 / NULLIF(o.n, 0), 4)                      AS top1_member_share_pct,  -- 比率表达式：〔一层〕首位会员占比
        ROUND(m.top_date_n   * 100.0 / NULLIF(o.n, 0), 4)                      AS top_date_share_pct,     -- 比率表达式：〔一层〕首位日期占比
        ROUND(m.top_table_n  * 100.0 / NULLIF(o.n, 0), 4)                      AS top_table_share_pct,    -- 比率表达式：〔一层〕首位桌台占比
        ROUND(m.top_bet09_n  * 100.0 / NULLIF(o.n, 0), 4)                      AS top_bet09_share_pct,    -- 比率表达式：〔一层〕首位注型占比
        ROUND(m.top_stake_amt * 100.0 / NULLIF(o.s, 0), 4)                     AS top_stake_share_pct,    -- 比率表达式：〔一层〕★ 首位会员之本金占比——金额维
        CASE WHEN m.top_member_n  * 100.0 / NULLIF(o.n, 0) >= 30 THEN 'HIGH'
             WHEN m.top_member_n  * 100.0 / NULLIF(o.n, 0) >= 29 THEN 'NEAR' ELSE 'NORMAL' END AS st_member, -- 条件分支：〔二层〕会员维之三态
        CASE WHEN m.top_date_n    * 100.0 / NULLIF(o.n, 0) >= 30 THEN 'HIGH'
             WHEN m.top_date_n    * 100.0 / NULLIF(o.n, 0) >= 29 THEN 'NEAR' ELSE 'NORMAL' END AS st_date,   -- 条件分支：〔二层〕日期维之三态
        CASE WHEN m.top_table_n   * 100.0 / NULLIF(o.n, 0) >= 30 THEN 'HIGH'
             WHEN m.top_table_n   * 100.0 / NULLIF(o.n, 0) >= 29 THEN 'NEAR' ELSE 'NORMAL' END AS st_table,  -- 条件分支：〔二层〕桌台维之三态
        CASE WHEN m.top_bet09_n   * 100.0 / NULLIF(o.n, 0) >= 30 THEN 'HIGH'
             WHEN m.top_bet09_n   * 100.0 / NULLIF(o.n, 0) >= 29 THEN 'NEAR' ELSE 'NORMAL' END AS st_bet09,  -- 条件分支：〔二层〕注型维之三态
        CASE WHEN m.top_stake_amt * 100.0 / NULLIF(o.s, 0) >= 30 THEN 'HIGH'
             WHEN m.top_stake_amt * 100.0 / NULLIF(o.s, 0) >= 29 THEN 'NEAR' ELSE 'NORMAL' END AS st_stake,  -- 条件分支：〔二层〕金额维之三态
        (CASE WHEN m.top_member_n  * 100.0 / NULLIF(o.n, 0) >= 30 THEN 1 ELSE 0 END
       + CASE WHEN m.top_date_n    * 100.0 / NULLIF(o.n, 0) >= 30 THEN 1 ELSE 0 END
       + CASE WHEN m.top_table_n   * 100.0 / NULLIF(o.n, 0) >= 30 THEN 1 ELSE 0 END
       + CASE WHEN m.top_bet09_n   * 100.0 / NULLIF(o.n, 0) >= 30 THEN 1 ELSE 0 END
       + CASE WHEN m.top_stake_amt * 100.0 / NULLIF(o.s, 0) >= 30 THEN 1 ELSE 0 END) AS n_dims_high,      -- 计算表达式：〔三层〕★ 高集中之维数
        (CASE WHEN m.top_member_n  * 100.0 / NULLIF(o.n, 0) >= 29 THEN 1 ELSE 0 END
       + CASE WHEN m.top_date_n    * 100.0 / NULLIF(o.n, 0) >= 29 THEN 1 ELSE 0 END
       + CASE WHEN m.top_table_n   * 100.0 / NULLIF(o.n, 0) >= 29 THEN 1 ELSE 0 END
       + CASE WHEN m.top_bet09_n   * 100.0 / NULLIF(o.n, 0) >= 29 THEN 1 ELSE 0 END
       + CASE WHEN m.top_stake_amt * 100.0 / NULLIF(o.s, 0) >= 29 THEN 1 ELSE 0 END) AS n_dims_near_or_high, -- 计算表达式：〔三层〕★ 高或近之维数
        CASE                                                                                              -- 条件分支：〔三层〕起始分支——四态联合判词
          WHEN (CASE WHEN m.top_member_n  * 100.0 / NULLIF(o.n, 0) >= 30 THEN 1 ELSE 0 END
              + CASE WHEN m.top_date_n    * 100.0 / NULLIF(o.n, 0) >= 30 THEN 1 ELSE 0 END
              + CASE WHEN m.top_table_n   * 100.0 / NULLIF(o.n, 0) >= 30 THEN 1 ELSE 0 END
              + CASE WHEN m.top_bet09_n   * 100.0 / NULLIF(o.n, 0) >= 30 THEN 1 ELSE 0 END
              + CASE WHEN m.top_stake_amt * 100.0 / NULLIF(o.s, 0) >= 30 THEN 1 ELSE 0 END) >= 2
            THEN 'MULTI_DIMENSION_SIGNAL'                                                                 -- 分支判定：二维以上为高
          WHEN (CASE WHEN m.top_member_n  * 100.0 / NULLIF(o.n, 0) >= 29 THEN 1 ELSE 0 END
              + CASE WHEN m.top_date_n    * 100.0 / NULLIF(o.n, 0) >= 29 THEN 1 ELSE 0 END
              + CASE WHEN m.top_table_n   * 100.0 / NULLIF(o.n, 0) >= 29 THEN 1 ELSE 0 END
              + CASE WHEN m.top_bet09_n   * 100.0 / NULLIF(o.n, 0) >= 29 THEN 1 ELSE 0 END
              + CASE WHEN m.top_stake_amt * 100.0 / NULLIF(o.s, 0) >= 29 THEN 1 ELSE 0 END) >= 2
            THEN 'MULTI_DIMENSION_NEAR_THRESHOLD'                                                         -- 分支判定：★ 二维以上为近——本版所补之缺口
          WHEN (CASE WHEN m.top_member_n  * 100.0 / NULLIF(o.n, 0) >= 30 THEN 1 ELSE 0 END
              + CASE WHEN m.top_date_n    * 100.0 / NULLIF(o.n, 0) >= 30 THEN 1 ELSE 0 END
              + CASE WHEN m.top_table_n   * 100.0 / NULLIF(o.n, 0) >= 30 THEN 1 ELSE 0 END
              + CASE WHEN m.top_bet09_n   * 100.0 / NULLIF(o.n, 0) >= 30 THEN 1 ELSE 0 END
              + CASE WHEN m.top_stake_amt * 100.0 / NULLIF(o.s, 0) >= 30 THEN 1 ELSE 0 END) = 1
            THEN 'SINGLE_DIMENSION_SIGNAL'                                                                -- 分支判定：恰一维为高
          ELSE 'NO_CONCENTRATION_SIGNAL'                                                                  -- 分支判定：无高无近
        END                                                                    AS verdict_concentration,  -- 条件分支：收束上方判词，产出「verdict_concentration」
        ROUND(o.s, 2)                                                          AS sum_stake               -- 取列：该群本金总额
      FROM tot o                                                                                          -- 取数来源：取自本条自建的中间结果集 tot
      JOIN mx m ON m.grp = o.grp                                                                          -- 连接：按证据群接首位占量
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_hiturn,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_hiturn,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_hiturn,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_hiturn,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_hiturn,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_hiturn,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_hiturn,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_hiturn,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 107. V_ipmatch.csv   [总包行 3017~3029 · 原 5 列 ＋六层约 60 列]
--     典型学：M04 IP 聚集　分区：recent（s.x_rec = 1）　连接：q.`ip`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`ip`, w.`n_member_log`, w.`country`, w.`city`, w.`in_bet_ip`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_ip                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_ip IS NOT NULL AND TRIM(CAST(s.x_ip AS STRING)) <> ''
    GROUP BY s.x_ip
  ),
    q AS (
      WITH a AS (SELECT DISTINCT TRIM(ip) AS ip FROM ods_mariadb_2b.ods_a168_bet02                        -- 公共表表达式：取用注单明细表（金额与行为口径的第一料源），产出「ip」
                 WHERE dt>='2026-03-21' AND dt<'2026-08-07' AND bet02='101'                               -- 过滤条件：限定 dt不少于 '2026-03-21'、dt小于 '2026-08-07'、bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
                   AND NULLIF(TRIM(ip),'') IS NOT NULL),                                                  -- 并列条件：限定该值非空
           b AS (SELECT TRIM(ip) AS ip, COUNT(DISTINCT mid) AS n_member_log,                              -- 取值表达式：统计去重个数，产出「n_member_log」
                        MAX(country_code) AS country, MAX(city) AS city                                   -- 取最大值表达式：产出「city」
                 FROM ods_mariadb_2b.ods_a168_game_log                                                    -- 取数来源：取自局流水日志（局级过程记录）
                 WHERE dt>='2026-03-21' AND dt<'2026-08-07'                                               -- 过滤条件：限定 dt不少于 '2026-03-21'、dt小于 '2026-08-07'，涉 dt（营业日）
                   AND NULLIF(TRIM(ip),'') IS NOT NULL                                                    -- 并列条件：限定该值非空
                 GROUP BY TRIM(ip))                                                                       -- 分组：按 TRIM(ip)) 汇总
      SELECT b.ip, b.n_member_log, b.country, b.city,                                                     -- 取列：起始取列子句，本行先列 b.ip, b.n_member_log, b.country, b.city
             CASE WHEN a.ip IS NULL THEN 0 ELSE 1 END AS in_bet_ip                                        -- 条件分支：产出「in_bet_ip」
      FROM b LEFT JOIN a ON a.ip=b.ip                                                                     -- 取数来源：取自本条自建的中间结果集 b，连接键为 a.ip=b.ip
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`ip` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 108. V_recon.csv   [总包行 1506~1576 · 原 12 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`n_matched_round`, w.`mae_turnover`, w.`mae_rebate`, w.`mae_net`, w.`scale_turnover`, w.`scale_rebate`, w.`scale_net`, w.`mase_turnover`, w.`mase_rebate`, w.`mase_net`, w.`pct_exact_turnover`, w.`pct_exact_net`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ta AS (                                                                                        -- 公共表表达式：开启中间结果集 ta，其后各行为其定义体（交付件 V_recon.csv）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 V_recon.csv）
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk，其后各行为其定义体（交付件 V_recon.csv）
        SELECT b.bet01, b.bet03, b.bet04, b.bet05, b.bet11, b.bet13, b.bet16, b.bet17,                    -- 取列：起始取列子句，本行先列 b.bet01, b.bet03, b.bet04, b.bet05, b.bet11, b.bet13, b.bet16, b.bet17，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet38, b.bet39, b.category,                                                              -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category，涉 bet38（重對）、bet39（桌号）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 V_recon.csv）
      br AS (                                                                                             -- 公共表表达式：开启中间结果集 br，其后各行为其定义体（交付件 V_recon.csv）
        SELECT r.bet03 AS sh, r.bet04 AS rd, r.bet39 AS tb,                                               -- 取列：起始取列子句，本行先取「tb」，涉 bet03（靴号）、bet04（局内序号）、bet39（桌号）
          SUM(CAST(NULLIF(TRIM(r.bet13),'') AS DECIMAL(20,4))                                             -- 汇总表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
              / CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))) AS turnover,                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「turnover」
          SUM(CAST(NULLIF(TRIM(r.bet16),'') AS DECIMAL(20,4))                                             -- 汇总表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
              / CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))) AS rebate,                               -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
          SUM(CAST(NULLIF(TRIM(r.bet17),'') AS DECIMAL(20,4))                                             -- 汇总表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
              / CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))) AS net_pnl                               -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_pnl」
        FROM rk r                                                                                         -- 取数来源：取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
        GROUP BY r.bet03, r.bet04, r.bet39                                                                -- 分组：按 r.bet03, r.bet04, r.bet39 汇总
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 V_recon.csv）
      gi AS (                                                                                             -- 公共表表达式：开启中间结果集 gi，其后各行为其定义体（交付件 V_recon.csv）
        SELECT gi002 AS sh, gi003 AS rd, gi011 AS tb,                                                     -- 取列：起始取列子句，本行先取「tb」，涉 gi011（桌号）
               CAST(NULLIF(TRIM(gi004),'') AS DATETIME)     AS t_open,                                    -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「t_open」
               CAST(NULLIF(TRIM(gi005),'') AS DECIMAL(20,4)) AS gi_turnover,                              -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「gi_turnover」
               CAST(NULLIF(TRIM(gi009),'') AS DECIMAL(20,4)) AS gi_rebate,                                -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「gi_rebate」
               CAST(NULLIF(TRIM(gi010),'') AS DECIMAL(20,4)) AS gi_net                                    -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「gi_net」
        FROM ods_mariadb_2b.ods_a168_game_info                                                            -- 取数来源：取自局信息表（靴局、开牌与桌号的时序基准）
        WHERE dt >= '2026-03-21' AND dt < '2026-08-07' AND gi001 = '101'                                  -- 过滤条件：限定 dt不少于 '2026-03-21'、dt小于 '2026-08-07'、gi001等于 '101'，涉 dt（营业日）、gi001（局主键）
          AND gi013 = '1' AND is_lock = 'N'                                                               -- 并列条件：限定 gi013等于 '1'、is_lock等于 'N'
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 V_recon.csv）
      j AS (                                                                                              -- 公共表表达式：开启中间结果集 j，其后各行为其定义体（交付件 V_recon.csv）
        SELECT g.tb, g.t_open,                                                                            -- 取列：起始取列子句，本行先列 g.tb, g.t_open
               ABS(b.turnover - g.gi_turnover) AS ae_turnover,                                            -- 取值表达式：取用 turnover（洗码量），产出「ae_turnover」
               ABS(b.rebate   - g.gi_rebate)   AS ae_rebate,                                              -- 取值表达式：取用 rebate（退水），产出「ae_rebate」
               ABS(b.net_pnl  - g.gi_net)      AS ae_net,                                                 -- 取值表达式：取用 net_pnl（会员净输赢），产出「ae_net」
               g.gi_turnover, g.gi_rebate, g.gi_net,                                                      -- 续行：接续上一取列子句，续列 g.gi_turnover, g.gi_rebate, g.gi_net
               LAG(g.gi_turnover) OVER (PARTITION BY g.tb ORDER BY g.t_open) AS lag_turnover,             -- 取前一行表达式：产出「lag_turnover」
               LAG(g.gi_rebate)   OVER (PARTITION BY g.tb ORDER BY g.t_open) AS lag_rebate,               -- 取前一行表达式：产出「lag_rebate」
               LAG(g.gi_net)      OVER (PARTITION BY g.tb ORDER BY g.t_open) AS lag_net                   -- 取前一行表达式：产出「lag_net」
        FROM br b                                                                                         -- 取数来源：取自本条自建的中间结果集 br
        JOIN gi g ON b.sh = g.sh AND b.rd = g.rd AND b.tb = g.tb                                          -- 连接：取自本条自建的中间结果集 gi，连接键为 b.sh = g.sh AND b.rd = g.rd AND b.tb = g.tb
      )                                                                                                   -- 续行：收束上方的子查询或函数括号（交付件 V_recon.csv）
      SELECT                                                                                              -- 续行：接续上一取列子句，续列 SELECT
        COUNT(*)                                            AS n_matched_round,                           -- 计数表达式：产出「n_matched_round」
        AVG(ae_turnover)                                    AS mae_turnover,                              -- 求均值表达式：产出「mae_turnover」
        AVG(ae_rebate)                                      AS mae_rebate,                                -- 求均值表达式：产出「mae_rebate」
        AVG(ae_net)                                         AS mae_net,                                   -- 求均值表达式：产出「mae_net」
        AVG(ABS(gi_turnover - lag_turnover))                AS scale_turnover,                            -- 求均值表达式：产出「scale_turnover」
        AVG(ABS(gi_rebate   - lag_rebate))                  AS scale_rebate,                              -- 求均值表达式：产出「scale_rebate」
        AVG(ABS(gi_net      - lag_net))                     AS scale_net,                                 -- 求均值表达式：产出「scale_net」
        AVG(ae_turnover) / NULLIF(AVG(ABS(gi_turnover - lag_turnover)), 0) AS mase_turnover,              -- 求均值表达式：产出「mase_turnover」
        AVG(ae_rebate)   / NULLIF(AVG(ABS(gi_rebate   - lag_rebate)),   0) AS mase_rebate,                -- 求均值表达式：产出「mase_rebate」
        AVG(ae_net)      / NULLIF(AVG(ABS(gi_net      - lag_net)),      0) AS mase_net,                   -- 求均值表达式：产出「mase_net」
        SUM(CASE WHEN ae_turnover <= 0.01 THEN 1 ELSE 0 END) * 1.0 / COUNT(*) AS pct_exact_turnover,      -- 汇总表达式：计数，产出「pct_exact_turnover」
        SUM(CASE WHEN ae_net      <= 0.01 THEN 1 ELSE 0 END) * 1.0 / COUNT(*) AS pct_exact_net            -- 汇总表达式：计数，产出「pct_exact_net」
      FROM j                                                                                              -- 取数来源：取自本条自建的中间结果集 j
      WHERE lag_turnover IS NOT NULL                                                                     -- 过滤条件：限定该值非空
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 109. V_table_counts.csv   [总包行 6092~6101 · 原 2 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT 'ods_a168_bet02'              AS 表名, COUNT(*) AS 行数 FROM ods_mariadb_2b.ods_a168_bet02       -- 取列：计数，取用注单明细表（金额与行为口径的第一料源），产出「行数」
      UNION ALL SELECT 'ods_a168_bet01',              COUNT(*) FROM ods_mariadb_2b.ods_a168_bet01         -- 结果集合并：计数，取用注单主表（仅供比对，不作主用）
      UNION ALL SELECT 'ods_a168_game_info',          COUNT(*) FROM ods_mariadb_2b.ods_a168_game_info     -- 结果集合并：计数，取用局信息表（靴局、开牌与桌号的时序基准）
      UNION ALL SELECT 'ods_a168_agent',              COUNT(*) FROM ods_mariadb_2b.ods_a168_agent         -- 结果集合并：计数，取用代理主档（五级代理线归属来源）
      UNION ALL SELECT 'ods_a168_member_dtl',         COUNT(*) FROM ods_mariadb_2b.ods_a168_member_dtl    -- 结果集合并：计数，取用会员主档明细（注册与账户属性）
      UNION ALL SELECT 'ods_a168_dailyreport_member', COUNT(*) FROM ods_mariadb_2b.ods_a168_dailyreport_member  -- 结果集合并：计数，取用会员日结报表（日粒度业绩与风控标记）
      UNION ALL SELECT 'ods_a168_alert_ip_setting',   COUNT(*) FROM ods_mariadb_2b.ods_a168_alert_ip_setting  -- 结果集合并：计数，取用风控 IP 名单配置表（人工标记史料源）
      UNION ALL SELECT 'ods_a168_employee',           COUNT(*) FROM ods_mariadb_2b.ods_a168_employee      -- 结果集合并：计数，取用员工主档（荷官工号与在职状态）
      UNION ALL SELECT 'ods_a168_game_No',            COUNT(*) FROM ods_mariadb_2b.ods_a168_game_No       -- 结果集合并：计数，取用局号编码表（局键位数核验之用）
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 110. X_combo.csv   [总包行 2776~2857 · 原 6 列 ＋六层约 60 列]
--     典型学：M06 代理线　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi_xagg` DESC, w.`flag_ip`, w.`flag_tail_p90`, w.`n_member`, w.`valid_bet`, w.`game_pnl`, w.`roi`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ta AS (            -- 公司测试线代理（214 条，跨五级）
        SELECT DISTINCT age001 AS aid                                                                     -- 取列：产出「aid」
        FROM ods_mariadb_2b.ods_a168_agent WHERE age022 = '1'                                             -- 取数来源：取自代理主档（五级代理线归属来源）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 X_combo.csv）
      rk AS (                                                                                             -- 公共表表达式：开启中间结果集 rk，其后各行为其定义体（交付件 X_combo.csv）
        SELECT b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02,                                         -- 取列：起始取列子句，本行先列 b.bet01, b.updatetime, b.sync_time, b.dt, b.bet02，涉 bet02（游戏类别）、dt（营业日）
               b.bet03, b.bet04, b.bet05, b.bet08, b.bet09,                                               -- 续行：接续上一取列子句，续列 b.bet03, b.bet04, b.bet05, b.bet08, b.bet09，涉 bet03（靴号）、bet04（局内序号）、bet05（会员号）
               b.bet11, b.bet13, b.bet14, b.bet16, b.bet17,                                               -- 续行：接续上一取列子句，续列 b.bet11, b.bet13, b.bet14, b.bet16, b.bet17，涉 bet11（汇率）、bet13（下注金额）、bet14（派彩金额）
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,                                               -- 续行：接续上一取列子句，续列 b.bet18, b.bet19, b.bet20, b.bet21, b.bet22，涉 bet18（一级代理线）、bet19（二级代理线）、bet20（三级代理线）
               b.bet38, b.bet39, b.category, b.eid, b.ip,                                                 -- 续行：接续上一取列子句，续列 b.bet38, b.bet39, b.category, b.eid, b.ip，涉 bet38（重對）、bet39（桌号）、eid（荷官工号）
               b.validbet,                                                                                -- 续行：接续上一取列子句，续列 b.validbet，涉 validbet（有效投注（洗码量））
               ROW_NUMBER() OVER (                                                                        -- 行号窗口表达式：以行号窗口取每组头部或去重，免出重复行
                 PARTITION BY b.bet01                                                                     -- 窗口分区：按 b.bet01 分组开窗，组内各自编号或排名
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn                           -- 排序：按 b.updatetime（降序）, b.sync_time（降序）, b.dt（降序）) AS rn 排列；导出必带排序，否则分页无稳定序（曾致 36.49% 重复行）
        FROM ods_mariadb_2b.ods_a168_bet02 b                                                              -- 取数来源：取自注单明细表（金额与行为口径的第一料源）
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07' AND b.bet02 = '101'                            -- 过滤条件：限定 b.dt不少于 '2026-03-21'、b.dt小于 '2026-08-07'、b.bet02等于 '101'，涉 bet02（游戏类别）、dt（营业日）
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 X_combo.csv）
      vd AS (                                                                                             -- 公共表表达式：开启中间结果集 vd，其后各行为其定义体（交付件 X_combo.csv）
        SELECT r.* FROM rk r                                                                              -- 取列：整体承接上游结果集的全部字段，不再逐列列举，取自本条自建的中间结果集 rk
        LEFT JOIN ta t1 ON t1.aid = r.bet18                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet18（一级代理线）
        LEFT JOIN ta t2 ON t2.aid = r.bet19                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet19（二级代理线）
        LEFT JOIN ta t3 ON t3.aid = r.bet20                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet20（三级代理线）
        LEFT JOIN ta t4 ON t4.aid = r.bet21                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet21（四级代理线）
        LEFT JOIN ta t5 ON t5.aid = r.bet22                                                               -- 左连接：取自本条自建的中间结果集 ta，连接键为 bet22（五级代理线）
        WHERE r.rn = 1 AND r.category = '1' AND UPPER(TRIM(r.bet38)) = 'N'                                -- 过滤条件：限定 r.rn等于 1、r.category等于 '1'、UPPER(TRIM(r.bet38))等于 'N'，涉 bet38（重對）
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0                                                -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)大于 0，涉 bet05（会员号）
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0                                         -- 并列条件：限定 CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8))大于 0，涉 bet11（汇率）
          AND NULLIF(TRIM(r.bet08),'') IS NOT NULL                                                        -- 并列条件：限定该值非空，涉 bet08（下注时间）
          AND COALESCE(t1.aid, t2.aid, t3.aid, t4.aid, t5.aid) IS NULL                                    -- 并列条件：限定该值为空——本包以左连接加空值判定替代 EXISTS，因 StarRocks 不支持 EXISTS 配多列 IN
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 X_combo.csv）
      bs AS (                                                                                             -- 公共表表达式：开启中间结果集 bs，其后各行为其定义体（交付件 X_combo.csv）
        SELECT v.bet05 AS member_id, v.eid AS dealer_id, v.ip AS bet_ip,                                  -- 取列：起始取列子句，本行先取「bet_ip」，涉 bet05（会员号）、eid（荷官工号）、bet_ip（下注 IP）
               v.bet20 AS lv3, v.bet09 AS bet_side, v.dt AS bet_date,                                     -- 取值表达式：取用 bet09（玩法）、bet20（三级代理线）、dt（营业日），产出「bet_date」
               CONCAT_WS('|', v.bet03, v.bet04, v.bet39) AS round_key,                                    -- 取值表达式：取用 bet03（靴号）、bet04（局内序号）、bet39（桌号），产出「round_key」
               v.bet39 AS table_id,                                                                       -- 取值表达式：取用 bet39（桌号），产出「table_id」
               CAST(NULLIF(TRIM(v.bet08),'') AS DATETIME) AS t_bet,                                       -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「t_bet」
               CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS stake,                              -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「stake」
               CAST(NULLIF(TRIM(v.validbet),'') AS DECIMAL(20,4))                                         -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS valid_bet,                          -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「valid_bet」
               (CAST(NULLIF(TRIM(v.bet14),'') AS DECIMAL(20,4))                                           -- 续行：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                - CAST(NULLIF(TRIM(v.bet13),'') AS DECIMAL(20,4)))                                        -- 加减计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS game_pnl,                           -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「game_pnl」
               CAST(NULLIF(TRIM(v.bet16),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS rebate,                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「rebate」
               CAST(NULLIF(TRIM(v.bet17),'') AS DECIMAL(20,4))                                            -- 取值表达式：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真
                 / CAST(NULLIF(TRIM(v.bet11),'') AS DECIMAL(20,8)) AS net_pnl                             -- 除法或乘法计算：先去空白、空串归 NULL，再显式转型——本库字段多为 varchar，不转型即比较失真，产出「net_pnl」
        FROM vd v                                                                                         -- 取数来源：取自本条自建的中间结果集 vd
      ),                                                                                                  -- 续行：收束上方的子查询或函数括号（交付件 X_combo.csv）
      gi AS (SELECT CONCAT_WS('|', gi002, gi003, gi011) AS round_key,                                     -- 取值表达式：取用 gi011（桌号）、round_key（局键），产出「round_key」
                    gi004 AS t_open, gi006 AS t_reveal                                                    -- 取值表达式：取用 gi004（局开始时刻）、gi006（开牌时刻），产出「t_reveal」
             FROM ods_mariadb_2b.ods_a168_game_info                                                       -- 取数来源：取自局信息表（靴局、开牌与桌号的时序基准）
             WHERE gi001='101' AND gi013='1' AND is_lock='N'                                              -- 过滤条件：限定 gi001等于 '101'、gi013等于 '1'、is_lock等于 'N'，涉 gi001（局主键）
               AND gi004>='2026-03-21' AND gi004<'2026-08-07'),                                           -- 并列条件：限定 gi004不少于 '2026-03-21'，涉 gi004（局开始时刻）
      pr AS (SELECT b.member_id, b.bet_ip, b.valid_bet, b.game_pnl,                                       -- 取值表达式：取用 valid_bet（有效投注（洗码量））、bet_ip（下注 IP）、game_pnl（游戏净输赢）
                    (UNIX_TIMESTAMP(b.t_bet)-UNIX_TIMESTAMP(g.t_open))*1.0                                -- 取值表达式：比率之分子，乘 1.0 以避整数除法截断
                    /NULLIF(UNIX_TIMESTAMP(g.t_reveal)-UNIX_TIMESTAMP(g.t_open),0) AS progress            -- 除法或乘法计算：产出「progress」
             FROM bs b JOIN gi g ON g.round_key=b.round_key),                                             -- 取数来源：取自本条自建的中间结果集 bs，连接键为 round_key（局键）
      ipn AS (SELECT bet_ip, COUNT(DISTINCT member_id) AS n_member_ip                                     -- 取值表达式：统计去重个数，产出「n_member_ip」
              FROM bs WHERE NULLIF(TRIM(bet_ip),'') IS NOT NULL GROUP BY bet_ip),                         -- 取数来源：取自本条自建的中间结果集 bs
      mk AS (SELECT p.member_id,                                                                          -- 取值表达式：取用 member_id（会员号）
               MAX(CASE WHEN i.n_member_ip >= 20 THEN 1 ELSE 0 END) AS flag_ip,                           -- 取最大值表达式：产出「flag_ip」
               SUM(CASE WHEN p.progress>=0.90 THEN 1 ELSE 0 END)*1.0/COUNT(*) AS tail_share,              -- 汇总表达式：计数，产出「tail_share」
                                                                                                          -- ⚠ 同名异义警示（2026-08-17）：本处 tail_share 用 progress（物理时间进度）口径，
                                                                                                          --   与 §D-06 用 max_pr（局内注单时序分位）口径的同名字段【不可比、不可互换】；
                                                                                                          --   两者分母亦异：本处为注单数，D-06 为玩家-局数。建议日后正名区分。
               SUM(p.valid_bet) AS valid_bet, SUM(p.game_pnl) AS game_pnl                                 -- 汇总表达式：取用 valid_bet（有效投注（洗码量））、game_pnl（游戏净输赢），产出「game_pnl」
             FROM pr p LEFT JOIN ipn i ON i.bet_ip=p.bet_ip                                               -- 取数来源：取自本条自建的中间结果集 pr，连接键为 bet_ip（下注 IP）
             WHERE p.progress BETWEEN 0 AND 1.5                                                           -- 过滤条件：限定落入闭区间
             GROUP BY p.member_id HAVING COUNT(*) >= 30)                                                  -- 分组：按 p.member_id HAVING COUNT(*) >= 30) 汇总
      SELECT flag_ip,                                                                                     -- 取列：起始取列子句，本行先列 flag_ip
             CASE WHEN tail_share>=0.47 THEN 1 ELSE 0 END AS flag_tail_p90,                               -- 条件分支：产出「flag_tail_p90」
                                                                                                          -- ⚠ 阈值溯源（2026-08-17 实测）：0.47 为硬编码，别名虽称 p90，但实测
                                                                                                          --   tail_share 的 P90 = 0.4156（S_player_tail.csv 159,603 行），偏差 -11.6%。
                                                                                                          --   按 0.47 切命中 10,763 人（6.74%）；按真 P90 切 15,961 人（10.00%）。
                                                                                                          --   【基准】0.47（现行代码）｜【建议默认】改为渲染时实算 P90，勿写死。
                                                                                                          --   另：tail_share>0.6 命中 3,590 人（2.25%），而实测分布【单峰右偏、无双峰】，
                                                                                                          --   0.6 不对应任何自然分界，作为「极度偏好压哨」判据无统计依据，建议撤销。
             COUNT(*) AS n_member, SUM(valid_bet) AS valid_bet,                                           -- 计数表达式：汇总，取用 valid_bet（有效投注（洗码量））、n_member（会员数），产出「valid_bet」
             SUM(game_pnl) AS game_pnl,                                                                   -- 汇总表达式：取用 game_pnl（游戏净输赢），产出「game_pnl」
             SUM(game_pnl)/NULLIF(SUM(valid_bet),0) AS roi                                                -- 汇总表达式：取用 valid_bet（有效投注（洗码量））、game_pnl（游戏净输赢），产出「roi」
      FROM mk GROUP BY 1,2
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet_xagg,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi_xagg,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 111. Z01_table_inventory.csv   [总包行 561~579 · 原 5 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`表名`, w.`表类型`, w.`估计行数`, w.`表注释`, w.`疑似性质`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT
          TABLE_NAME                                                   AS 表名,
          TABLE_TYPE                                                   AS 表类型,
          TABLE_ROWS                                                   AS 估计行数,
          TABLE_COMMENT                                                AS 表注释,
          CASE
            WHEN LOWER(TABLE_NAME) LIKE '%_bak%'  THEN '备份'
            WHEN LOWER(TABLE_NAME) LIKE '%bak2%'  THEN '备份'
            WHEN LOWER(TABLE_NAME) LIKE '%_test%' THEN '测试'
            WHEN LOWER(TABLE_NAME) LIKE '%_tmp%'  THEN '临时'
            WHEN LOWER(TABLE_NAME) LIKE '%_old%'  THEN '旧版'
            WHEN LOWER(TABLE_NAME) LIKE '%_copy%' THEN '副本'
            WHEN LOWER(TABLE_NAME) LIKE '%_v1%'   THEN '疑似旧版'
            WHEN LOWER(TABLE_NAME) LIKE '%demo%'  THEN '疑似演示'
            ELSE '生产'
          END                                                          AS 疑似性质
      FROM INFORMATION_SCHEMA.TABLES
      WHERE TABLE_SCHEMA = 'ods_mariadb_2b'
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 112. Z02_excluded_tables.csv   [总包行 589~608 · 原 4 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`表名`, w.`估计行数`, w.`表注释`, w.`处置`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT
          TABLE_NAME                          AS 表名,
          TABLE_ROWS                          AS 估计行数,
          TABLE_COMMENT                       AS 表注释,
          CASE
            WHEN LOWER(TABLE_NAME) LIKE '%_bak%'  OR LOWER(TABLE_NAME) LIKE '%bak2%' THEN '备份表·禁用'
            WHEN LOWER(TABLE_NAME) LIKE '%_test%' THEN '测试表·禁用'
            WHEN LOWER(TABLE_NAME) LIKE '%_tmp%'  THEN '临时表·禁用'
            WHEN LOWER(TABLE_NAME) LIKE '%_old%'  THEN '旧版表·禁用'
            WHEN LOWER(TABLE_NAME) LIKE '%_copy%' THEN '副本表·禁用'
            WHEN LOWER(TABLE_NAME) LIKE '%demo%'  THEN '演示表·须确认'
            ELSE '其他·须确认'
          END                                 AS 处置
      FROM INFORMATION_SCHEMA.TABLES
      WHERE TABLE_SCHEMA = 'ods_mariadb_2b'
        AND (   LOWER(TABLE_NAME) LIKE '%_bak%'  OR LOWER(TABLE_NAME) LIKE '%bak2%'
             OR LOWER(TABLE_NAME) LIKE '%_test%' OR LOWER(TABLE_NAME) LIKE '%_tmp%'
             OR LOWER(TABLE_NAME) LIKE '%_old%'  OR LOWER(TABLE_NAME) LIKE '%_copy%'
             OR LOWER(TABLE_NAME) LIKE '%demo%')
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 113. Z03_column_dictionary.csv   [总包行 619~630 · 原 8 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`表名`, w.`列序`, w.`列名`, w.`存储类型`, w.`可空`, w.`原始类型`, w.`业务含义`, w.`原始注释`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT
          TABLE_NAME                                                        AS 表名,
          ORDINAL_POSITION                                                  AS 列序,
          COLUMN_NAME                                                       AS 列名,
          DATA_TYPE                                                         AS 存储类型,
          IS_NULLABLE                                                       AS 可空,
          REGEXP_EXTRACT(COLUMN_COMMENT, 'source_type=([^;]+)', 1)          AS 原始类型,
          TRIM(REGEXP_REPLACE(COLUMN_COMMENT, 'source_type=[^;]+;', ''))    AS 业务含义,
          COLUMN_COMMENT                                                    AS 原始注释
      FROM INFORMATION_SCHEMA.COLUMNS
      WHERE TABLE_SCHEMA = 'ods_mariadb_2b'
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 114. Z04_name_collision.csv   [总包行 644~667 · 原 6 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`列名`, w.`释义数`, w.`类型数`, w.`出现表数`, w.`风险`, w.`明细`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH d AS (
        SELECT
            COLUMN_NAME,
            TABLE_NAME,
            TRIM(REGEXP_REPLACE(COLUMN_COMMENT, 'source_type=[^;]+;', ''))  AS 含义,
            REGEXP_EXTRACT(COLUMN_COMMENT, 'source_type=([^;]+)', 1)        AS 类型
        FROM INFORMATION_SCHEMA.COLUMNS
        WHERE TABLE_SCHEMA = 'ods_mariadb_2b'
          AND COLUMN_COMMENT IS NOT NULL
          AND TRIM(COLUMN_COMMENT) <> ''
      )
      SELECT
          COLUMN_NAME                                  AS 列名,
          COUNT(DISTINCT 含义)                          AS 释义数,
          COUNT(DISTINCT 类型)                          AS 类型数,
          COUNT(DISTINCT TABLE_NAME)                   AS 出现表数,
          CASE WHEN COUNT(DISTINCT 类型) >= 2 THEN '🔴 义异且型异'
               ELSE '🟠 义异型同' END                   AS 风险,
          GROUP_CONCAT(DISTINCT CONCAT(TABLE_NAME, ' → ', 含义, ' [', 类型, ']')
                       SEPARATOR '  ||  ')             AS 明细
      FROM d
      GROUP BY COLUMN_NAME
      HAVING COUNT(DISTINCT 含义) >= 2
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 115. Z05_missing_typehint.csv   [总包行 676~695 · 原 6 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`表名`, w.`列序`, w.`列名`, w.`存储类型`, w.`原始注释`, w.`状态`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT
          TABLE_NAME                        AS 表名,
          ORDINAL_POSITION                  AS 列序,
          COLUMN_NAME                       AS 列名,
          DATA_TYPE                         AS 存储类型,
          COLUMN_COMMENT                    AS 原始注释,
          CASE
            WHEN COLUMN_COMMENT IS NULL OR TRIM(COLUMN_COMMENT) = ''      THEN '🔴 无任何注释'
            WHEN COLUMN_COMMENT NOT LIKE '%source_type=%'                 THEN '🟠 缺原始类型'
            WHEN TRIM(REGEXP_REPLACE(COLUMN_COMMENT,'source_type=[^;]+;','')) = ''
                                                                          THEN '🟠 有类型无含义'
            ELSE '🟢 完整'
          END                               AS 状态
      FROM INFORMATION_SCHEMA.COLUMNS
      WHERE TABLE_SCHEMA = 'ods_mariadb_2b'
        AND (COLUMN_COMMENT IS NULL
             OR TRIM(COLUMN_COMMENT) = ''
             OR COLUMN_COMMENT NOT LIKE '%source_type=%'
             OR TRIM(REGEXP_REPLACE(COLUMN_COMMENT,'source_type=[^;]+;','')) = '')
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 116. Z06_generated_probe_sql.csv   [总包行 709~730 · 原 5 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`表名`, w.`列序`, w.`列名`, w.`估计行数`, w.`待执行SQL`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT
          c.TABLE_NAME                                          AS 表名,
          c.ORDINAL_POSITION                                    AS 列序,
          c.COLUMN_NAME                                         AS 列名,
          t.TABLE_ROWS                                          AS 估计行数,
          CONCAT(
            'SELECT ''', c.TABLE_NAME, ''' AS t, ''', c.COLUMN_NAME, ''' AS c, ',
            'CAST(', c.COLUMN_NAME, ' AS VARCHAR) AS v, COUNT(*) AS n, ',
            'COUNT(*) * 1.0 / SUM(COUNT(*)) OVER () AS pct ',
            'FROM ods_mariadb_2b.', c.TABLE_NAME, ' ',
            'GROUP BY 3 ORDER BY n DESC LIMIT 20;'
          )                                                     AS 待执行SQL
      FROM INFORMATION_SCHEMA.COLUMNS c
      LEFT JOIN INFORMATION_SCHEMA.TABLES t
             ON t.TABLE_SCHEMA = c.TABLE_SCHEMA AND t.TABLE_NAME = c.TABLE_NAME
      WHERE c.TABLE_SCHEMA = 'ods_mariadb_2b'
        AND c.COLUMN_NAME NOT IN ('__source_pk','source_db','source_table',
                                  'ods_table_name','sync_time')
        AND NOT (   LOWER(c.TABLE_NAME) LIKE '%_bak%'  OR LOWER(c.TABLE_NAME) LIKE '%bak2%'
                 OR LOWER(c.TABLE_NAME) LIKE '%_test%' OR LOWER(c.TABLE_NAME) LIKE '%_tmp%'
                 OR LOWER(c.TABLE_NAME) LIKE '%_old%'  OR LOWER(c.TABLE_NAME) LIKE '%_copy%')
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 117. Z07_probe_bet02.csv   [总包行 743~782 · 原 5 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT 'ods_a168_bet02' AS t, 'bet03' AS c, CAST(bet03 AS VARCHAR) AS v,
             COUNT(*) AS n, COUNT(*) * 1.0 / SUM(COUNT(*)) OVER () AS pct
      FROM ods_mariadb_2b.ods_a168_bet02 GROUP BY 3
      UNION ALL
      SELECT 'ods_a168_bet02', 'bet04', CAST(bet04 AS VARCHAR),
             COUNT(*), COUNT(*) * 1.0 / SUM(COUNT(*)) OVER ()
      FROM ods_mariadb_2b.ods_a168_bet02 GROUP BY 3
      UNION ALL
      SELECT 'ods_a168_bet02', 'bet39', CAST(bet39 AS VARCHAR),
             COUNT(*), COUNT(*) * 1.0 / SUM(COUNT(*)) OVER ()
      FROM ods_mariadb_2b.ods_a168_bet02 GROUP BY 3
      UNION ALL
      SELECT 'ods_a168_bet02', 'eid', CAST(eid AS VARCHAR),
             COUNT(*), COUNT(*) * 1.0 / SUM(COUNT(*)) OVER ()
      FROM ods_mariadb_2b.ods_a168_bet02 GROUP BY 3
      UNION ALL
      SELECT 'ods_a168_bet02', 'gametype', CAST(gametype AS VARCHAR),
             COUNT(*), COUNT(*) * 1.0 / SUM(COUNT(*)) OVER ()
      FROM ods_mariadb_2b.ods_a168_bet02 GROUP BY 3
      UNION ALL
      SELECT 'ods_a168_bet02', 'commission', CAST(commission AS VARCHAR),
             COUNT(*), COUNT(*) * 1.0 / SUM(COUNT(*)) OVER ()
      FROM ods_mariadb_2b.ods_a168_bet02 GROUP BY 3
      UNION ALL
      SELECT 'ods_a168_bet02', 'category', CAST(category AS VARCHAR),
             COUNT(*), COUNT(*) * 1.0 / SUM(COUNT(*)) OVER ()
      FROM ods_mariadb_2b.ods_a168_bet02 GROUP BY 3
      UNION ALL
      SELECT 'ods_a168_bet02', 'bet09', CAST(bet09 AS VARCHAR),
             COUNT(*), COUNT(*) * 1.0 / SUM(COUNT(*)) OVER ()
      FROM ods_mariadb_2b.ods_a168_bet02 GROUP BY 3
      UNION ALL
      SELECT 'ods_a168_bet02', 'bet10', CAST(bet10 AS VARCHAR),
             COUNT(*), COUNT(*) * 1.0 / SUM(COUNT(*)) OVER ()
      FROM ods_mariadb_2b.ods_a168_bet02 GROUP BY 3
      UNION ALL
      SELECT 'ods_a168_bet02', 'bet11', CAST(bet11 AS VARCHAR),
             COUNT(*), COUNT(*) * 1.0 / SUM(COUNT(*)) OVER ()
      FROM ods_mariadb_2b.ods_a168_bet02 GROUP BY 3
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 118. Z08_roundkey_integrity.csv   [总包行 796~833 · 原 9 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`注单行数`, w.`局键数`, w.`单局最大注单行数`, w.`单局最大会员数`, w.`单局会员数P999`, w.`跨桌局键数_应为0`, w.`bet03跨桌数`, w.`bet03唯一值数`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH v AS (
        SELECT
            CAST(bet03 AS VARCHAR) AS k3,
            CAST(bet04 AS VARCHAR) AS k4,
            CAST(bet39 AS VARCHAR) AS k39,
            CAST(bet05 AS VARCHAR) AS member_id,
            CONCAT_WS('|', CAST(bet03 AS VARCHAR),
                           CAST(bet04 AS VARCHAR),
                           CAST(bet39 AS VARCHAR)) AS round_key
        FROM ods_mariadb_2b.ods_a168_bet02
        WHERE bet02 = '101'
      ),
      per_key AS (
        SELECT round_key,
               COUNT(*)                  AS n_rows,
               COUNT(DISTINCT member_id) AS n_member,
               COUNT(DISTINCT k39)       AS n_table
        FROM v GROUP BY round_key
      ),
      per_k3 AS (
        SELECT k3, COUNT(DISTINCT k39) AS n_table_per_k3,
                   COUNT(DISTINCT k4)  AS n_k4_per_k3
        FROM v GROUP BY k3
      )
      SELECT
          (SELECT COUNT(*) FROM v)                                            AS 注单行数,
          (SELECT COUNT(*) FROM per_key)                                      AS 局键数,
          (SELECT MAX(n_rows)   FROM per_key)                                 AS 单局最大注单行数,
          (SELECT MAX(n_member) FROM per_key)                                 AS 单局最大会员数,
          (SELECT PERCENTILE_APPROX(CAST(n_member AS DOUBLE),0.999) FROM per_key)
                                                                              AS 单局会员数P999,
          (SELECT COUNT(*) FROM per_key WHERE n_table > 1)                    AS 跨桌局键数_应为0,
          (SELECT COUNT(*) FROM per_k3 WHERE n_table_per_k3 > 1)              AS bet03跨桌数,
          (SELECT COUNT(*) FROM per_k3)                                       AS bet03唯一值数,
          (SELECT SUM(CASE WHEN k3 IN ('-1','0','') OR k3 IS NULL
                             OR k4 IN ('-1','0','') OR k4 IS NULL
                             OR k39 IN ('-1','0','') OR k39 IS NULL
                           THEN 1 ELSE 0 END) FROM v)                         AS 局键含哨兵值行数
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 119. Z09_time_axis_recon.csv   [总包行 854~885 · 原 17 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`行数`, w.`dt等于账务日`, w.`dt等于下注日`, w.`账务日等于下注日`, w.`账务日不可用`, w.`开局时间不可用`, w.`下注时间不可用`, w.`下注时间纪元哨兵`, w.`开局时间纪元哨兵`, w.`账务日最小`, w.`账务日最大`, w.`下注早于开局_时序异常`, w.`下注距开局秒_最小`, w.`下注距开局秒_P50`, w.`下注距开局秒_P99`, w.`下注距开局秒_P999`, w.`下注距开局秒_最大`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH v AS (
        SELECT
            dt,
            CAST(NULLIF(TRIM(bet07),'') AS DATE)      AS d_acct,
            CAST(NULLIF(TRIM(bet06),'') AS DATETIME)  AS t_open,
            CAST(NULLIF(TRIM(bet08),'') AS DATETIME)  AS t_bet
        FROM ods_mariadb_2b.ods_a168_bet02
        WHERE bet02 = '101'
      )
      SELECT
          COUNT(*)                                                        AS 行数,
          SUM(CASE WHEN dt = d_acct THEN 1 ELSE 0 END)                    AS dt等于账务日,
          SUM(CASE WHEN dt = DATE(t_bet) THEN 1 ELSE 0 END)               AS dt等于下注日,
          SUM(CASE WHEN d_acct = DATE(t_bet) THEN 1 ELSE 0 END)           AS 账务日等于下注日,
          SUM(CASE WHEN d_acct IS NULL THEN 1 ELSE 0 END)                 AS 账务日不可用,
          SUM(CASE WHEN t_open IS NULL THEN 1 ELSE 0 END)                 AS 开局时间不可用,
          SUM(CASE WHEN t_bet  IS NULL THEN 1 ELSE 0 END)                 AS 下注时间不可用,
          SUM(CASE WHEN t_bet <= '1971-01-01 00:00:00' THEN 1 ELSE 0 END) AS 下注时间纪元哨兵,
          SUM(CASE WHEN t_open <= '1971-01-01 00:00:00' THEN 1 ELSE 0 END) AS 开局时间纪元哨兵,
          MIN(d_acct)                                                     AS 账务日最小,
          MAX(d_acct)                                                     AS 账务日最大,
          SUM(CASE WHEN TIMESTAMPDIFF(SECOND, t_open, t_bet) <  0 THEN 1 ELSE 0 END)
                                                                          AS 下注早于开局_时序异常,
          MIN(TIMESTAMPDIFF(SECOND, t_open, t_bet))                       AS 下注距开局秒_最小,
          PERCENTILE_APPROX(CAST(TIMESTAMPDIFF(SECOND, t_open, t_bet) AS DOUBLE), 0.50)
                                                                          AS 下注距开局秒_P50,
          PERCENTILE_APPROX(CAST(TIMESTAMPDIFF(SECOND, t_open, t_bet) AS DOUBLE), 0.99)
                                                                          AS 下注距开局秒_P99,
          PERCENTILE_APPROX(CAST(TIMESTAMPDIFF(SECOND, t_open, t_bet) AS DOUBLE), 0.999)
                                                                          AS 下注距开局秒_P999,
          MAX(TIMESTAMPDIFF(SECOND, t_open, t_bet))                       AS 下注距开局秒_最大
      FROM v
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 120. Z11_sentinel_impact.csv   [总包行 919~957 · 原 13 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`未处置_注单行数`, w.`已处置_注单行数`, w.`未处置_局键数`, w.`已处置_局键数`, w.`未处置_会员数`, w.`已处置_会员数`, w.`未处置_IP数`, w.`已处置_IP数`, w.`未处置_单局最大会员数`, w.`已处置_单局最大会员数`, w.`单局最大会员数_差额`, w.`剔除行数`, w.`剔除占比`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH v AS (
        SELECT
            CAST(bet05 AS VARCHAR) AS member_id,
            CAST(ip    AS VARCHAR) AS ip_addr,
            CONCAT_WS('|', CAST(bet03 AS VARCHAR),
                           CAST(bet04 AS VARCHAR),
                           CAST(bet39 AS VARCHAR)) AS round_key,
            CASE WHEN CAST(bet03 AS VARCHAR) IN ('-1','0','') OR bet03 IS NULL
                      OR CAST(bet04 AS VARCHAR) IN ('-1','0','') OR bet04 IS NULL
                      OR CAST(bet39 AS VARCHAR) IN ('-1','0','') OR bet39 IS NULL
                 THEN 1 ELSE 0 END AS is_sentinel_key
        FROM ods_mariadb_2b.ods_a168_bet02
        WHERE bet02 = '101'
      ),
      a AS (
        SELECT COUNT(*) AS n_rows, COUNT(DISTINCT round_key) AS n_key,
               COUNT(DISTINCT member_id) AS n_member, COUNT(DISTINCT ip_addr) AS n_ip
        FROM v
      ),
      b AS (
        SELECT COUNT(*) AS n_rows, COUNT(DISTINCT round_key) AS n_key,
               COUNT(DISTINCT member_id) AS n_member, COUNT(DISTINCT ip_addr) AS n_ip
        FROM v WHERE is_sentinel_key = 0
      ),
      ma AS (SELECT MAX(c) AS m FROM (SELECT round_key, COUNT(DISTINCT member_id) AS c
                                      FROM v GROUP BY round_key) x),
      mb AS (SELECT MAX(c) AS m FROM (SELECT round_key, COUNT(DISTINCT member_id) AS c
                                      FROM v WHERE is_sentinel_key = 0 GROUP BY round_key) y)
      SELECT
          a.n_rows   AS 未处置_注单行数,  b.n_rows   AS 已处置_注单行数,
          a.n_key    AS 未处置_局键数,    b.n_key    AS 已处置_局键数,
          a.n_member AS 未处置_会员数,    b.n_member AS 已处置_会员数,
          a.n_ip     AS 未处置_IP数,      b.n_ip     AS 已处置_IP数,
          ma.m       AS 未处置_单局最大会员数,
          mb.m       AS 已处置_单局最大会员数,
          ma.m - mb.m                                       AS 单局最大会员数_差额,
          a.n_rows - b.n_rows                               AS 剔除行数,
          (a.n_rows - b.n_rows) * 1.0 / NULLIF(a.n_rows, 0) AS 剔除占比
      FROM a CROSS JOIN b CROSS JOIN ma CROSS JOIN mb
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 121. Z14_zero_month_diagnosis.csv   [总包行 1026~1130 · 原 23 列 ＋六层约 60 列]
--     典型学：M12 资金轨迹　分区：recent（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`bet_month`, w.`zero_kind`, w.`vb_sum_state`, w.`n_orders`, w.`n_rounds_month`, w.`n_days_month`, w.`n_tables_month`, w.`n_vb_null`, w.`n_vb_blank`, w.`n_vb_true_zero`, w.`n_vb_positive`, w.`vb_null_share`, w.`sum_valid_bet`, w.`sum_stake`, w.`sum_game_pnl`, w.`sum_rebate`, w.`n_order_nocomm`, w.`n_hedge_rounds`, w.`n_hedge_eq`, w.`n_zero_pnl_rounds`, w.`hedge_eq_share`, w.`verdict_hint`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH ta AS (
        SELECT DISTINCT CAST(NULLIF(TRIM(age001),'') AS BIGINT) AS agent_id
        FROM ods_mariadb_2b.ods_a168_agent
        WHERE age022 = '1'
      ),
      ranked AS (
        SELECT b.bet01, b.bet03, b.bet04, b.bet05, b.bet09, b.bet11,
               b.bet13, b.bet14, b.bet16, b.bet17, b.bet38, b.bet39,
               b.bet18, b.bet19, b.bet20, b.bet21, b.bet22,
               b.validbet, b.commission, b.category, b.dt,
               ROW_NUMBER() OVER (PARTITION BY b.bet01
                 ORDER BY b.updatetime DESC, b.sync_time DESC, b.dt DESC) AS rn
        FROM ods_mariadb_2b.ods_a168_bet02 b
        WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
          AND b.bet02 = '101'
      ),
      base AS (
        SELECT CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT)            AS member_id,
               DATE_TRUNC('month', r.dt)                           AS bet_month,
               r.dt                                                AS dt,
               r.bet39                                             AS table_id,
               CONCAT_WS('|', r.bet03, r.bet04, r.bet39)           AS round_key,
               TRIM(r.bet09)                                       AS bet_side,
               TRIM(r.commission)                                  AS commission,
               r.validbet                                          AS vb_txt,
               CAST(NULLIF(TRIM(r.validbet),'') AS DECIMAL(20,4))  AS vb_raw,
               CAST(NULLIF(TRIM(r.bet13),'')    AS DECIMAL(20,4))  AS stake_raw,
               CAST(NULLIF(TRIM(r.bet14),'')    AS DECIMAL(20,4))  AS payout_raw,
               CAST(NULLIF(TRIM(r.bet16),'')    AS DECIMAL(20,4))  AS rebate_raw,
               CAST(NULLIF(TRIM(r.bet11),'')    AS DECIMAL(20,8))  AS fx
        FROM ranked r
        LEFT JOIN ta t1 ON t1.agent_id = CAST(NULLIF(TRIM(r.bet18),'') AS BIGINT)
        LEFT JOIN ta t2 ON t2.agent_id = CAST(NULLIF(TRIM(r.bet19),'') AS BIGINT)
        LEFT JOIN ta t3 ON t3.agent_id = CAST(NULLIF(TRIM(r.bet20),'') AS BIGINT)
        LEFT JOIN ta t4 ON t4.agent_id = CAST(NULLIF(TRIM(r.bet21),'') AS BIGINT)
        LEFT JOIN ta t5 ON t5.agent_id = CAST(NULLIF(TRIM(r.bet22),'') AS BIGINT)
        WHERE r.rn = 1
          AND r.category = '1'
          AND UPPER(TRIM(r.bet38)) = 'N'
          AND CAST(NULLIF(TRIM(r.bet05),'') AS BIGINT) > 0
          AND CAST(NULLIF(TRIM(r.bet11),'') AS DECIMAL(20,8)) > 0
          AND COALESCE(t1.agent_id,t2.agent_id,t3.agent_id,t4.agent_id,t5.agent_id) IS NULL
      ),
      rnd AS (
        SELECT member_id, bet_month, round_key,
               MAX(CASE WHEN bet_side = 'Banker' THEN 1 ELSE 0 END) AS has_b,
               MAX(CASE WHEN bet_side = 'Player' THEN 1 ELSE 0 END) AS has_p,
               SUM(CASE WHEN bet_side = 'Banker' THEN stake_raw/fx ELSE 0 END) AS stk_b,
               SUM(CASE WHEN bet_side = 'Player' THEN stake_raw/fx ELSE 0 END) AS stk_p,
               SUM((payout_raw - stake_raw)/fx)                                AS pnl_rnd
        FROM base
        GROUP BY member_id, bet_month, round_key
      ),
      mm AS (
        SELECT member_id, bet_month,
               COUNT(*)                                                          AS n_orders,
               COUNT(DISTINCT round_key)                                         AS n_rounds_month,
               COUNT(DISTINCT dt)                                                AS n_days_month,
               COUNT(DISTINCT table_id)                                          AS n_tables_month,
               SUM(CASE WHEN vb_txt IS NULL THEN 1 ELSE 0 END)                   AS n_vb_null,
               SUM(CASE WHEN vb_txt IS NOT NULL AND TRIM(vb_txt) = '' THEN 1 ELSE 0 END)
                                                                                 AS n_vb_blank,
               SUM(CASE WHEN vb_raw = 0 THEN 1 ELSE 0 END)                       AS n_vb_true_zero,
               SUM(CASE WHEN vb_raw > 0 THEN 1 ELSE 0 END)                       AS n_vb_positive,
               SUM(vb_raw / fx)                                                  AS sum_valid_bet,
               SUM(stake_raw / fx)                                               AS sum_stake,
               SUM((payout_raw - stake_raw) / fx)                                AS sum_game_pnl,
               SUM(rebate_raw / fx)                                              AS sum_rebate,
               SUM(CASE WHEN commission = '1' THEN 1 ELSE 0 END)                 AS n_order_nocomm
        FROM base
        GROUP BY member_id, bet_month
      ),
      hg AS (
        SELECT member_id, bet_month,
               SUM(CASE WHEN has_b = 1 AND has_p = 1 THEN 1 ELSE 0 END)          AS n_hedge_rounds,
               SUM(CASE WHEN has_b = 1 AND has_p = 1 AND stk_b = stk_p
                        THEN 1 ELSE 0 END)                                       AS n_hedge_eq,
               SUM(CASE WHEN pnl_rnd = 0 THEN 1 ELSE 0 END)                       AS n_zero_pnl_rounds
        FROM rnd
        GROUP BY member_id, bet_month
      )
      SELECT m.member_id                                                         AS member_id,
             m.bet_month                                                         AS bet_month,
             CASE WHEN COALESCE(m.sum_valid_bet,0) = 0 AND COALESCE(m.sum_game_pnl,0) = 0
                       THEN '洗码与输赢皆为零'
                  WHEN COALESCE(m.sum_valid_bet,0) = 0 THEN '洗码贡献为零'
                  ELSE '游戏输赢为零' END                                          AS zero_kind,
             CASE WHEN m.sum_valid_bet IS NULL THEN 'NULL' ELSE '有值' END        AS vb_sum_state,
             m.n_orders, m.n_rounds_month, m.n_days_month, m.n_tables_month,
             m.n_vb_null, m.n_vb_blank, m.n_vb_true_zero, m.n_vb_positive,
             m.n_vb_null * 1.0 / NULLIF(m.n_orders,0)                            AS vb_null_share,
             m.sum_valid_bet, m.sum_stake, m.sum_game_pnl, m.sum_rebate,
             m.n_order_nocomm,
             h.n_hedge_rounds, h.n_hedge_eq, h.n_zero_pnl_rounds,
             h.n_hedge_eq * 1.0 / NULLIF(m.n_rounds_month,0)                     AS hedge_eq_share,
             CASE WHEN COALESCE(m.n_vb_null,0) + COALESCE(m.n_vb_blank,0) > 0 THEN '支持①NULL抹平'
                  WHEN COALESCE(h.n_hedge_eq,0) > 0                              THEN '支持②自对押真实零'
                  ELSE '两者皆低·判UNKNOWN' END                                    AS verdict_hint
      FROM mm m
      JOIN hg h ON h.member_id = m.member_id AND h.bet_month = m.bet_month
      WHERE COALESCE(m.sum_valid_bet,0) = 0
         OR m.sum_valid_bet IS NULL
         OR m.sum_game_pnl = 0
         OR m.sum_game_pnl IS NULL
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 122. _P12b-pre.csv   [总包行 9791~9799 · 原 4 列 ＋六层约 60 列]
--     典型学：M09 处置限额　分区：post（s.x_rec = 1）　连接：q.`member_id`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`member_id`, w.`action`, w.`content`, w.`action_time`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT  CAST(lmc02 AS STRING)                AS member_id,                                          -- 取值表达式：取用 lmc02（被改会员号），产出「member_id」
              CAST(lmc04 AS STRING)                AS action,                                             -- 取值表达式：取用 lmc04（动作类别），产出「action」
              CAST(lmc05 AS STRING)                AS content,                                            -- 取值表达式：取用 lmc05（变更内容串），产出「content」——目检对象
              CAST(lmc08 AS STRING)                AS action_time                                         -- 取值表达式：取用 lmc08（操作时间），产出「action_time」
      FROM    ods_mariadb_2b.ods_a168_log_mem_change                                                      -- 取数来源：取自会员变更日志表（与 §TL-14 同源）
      WHERE   dt >= '2026-03-21' AND dt < '2026-08-07'                                                    -- 过滤条件：限定全局窗，涉 dt（营业日）
        AND   ( CAST(lmc05 AS STRING) LIKE '%mem016%'                                                     -- 并列条件：粗筛含 mem016（enable）之变更串
             OR CAST(lmc05 AS STRING) LIKE '%mem017%' )                                                   -- 并列条件：或含 mem017（canbet）之变更串
      LIMIT 20                                                                                           -- 行数上限：只看屏幕 20 行，目检段式后再定 §P12b 谓词
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_post,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_post,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_post,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_post,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_post,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_post,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_post,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_post,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`member_id` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 123. _PH0_1b_table_inventory.csv   [总包行 12393~12445 · 原 10 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`table_name`, w.`h_src`, w.`has_uid`, w.`has_time`, w.`has_amount`, w.`n_cols`, w.`table_nature`, w.`scope_db`, w.`scope_pattern`, w.`scope_scanned_at`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH col_flag AS (                                                                                  -- 公共表表达式：开启中间结果集 col_flag——逐表之三类列存在性
        SELECT c.TABLE_NAME                                                            AS table_name,     -- 取列：表名
               MAX(CASE WHEN LOWER(c.COLUMN_NAME) REGEXP                                                  -- 条件分支：UID 型列——含编码式（bet05／memNNN／ageNNN）
                          '^(uid|member_id|user_id|username|account|mem[0-9]{2,3}|bet05|age[0-9]{3}|lmc[0-9]{2})$'
                         OR c.COLUMN_COMMENT REGEXP '會員|会员|帳號|账号|用戶|用户'                        -- 并列条件：中文列义命中会员键
                         THEN 1 ELSE 0 END)                                            AS has_uid,        -- 条件分支：产出「has_uid」
               MAX(CASE WHEN LOWER(c.DATA_TYPE) REGEXP 'date|time'                                        -- 条件分支：时间型列——先认类型，再认名
                         OR LOWER(c.COLUMN_NAME) REGEXP 'time|date|_at$|_dt$|^dt$|created|updated'        -- 并列条件：列名命中时间族
                         OR c.COLUMN_COMMENT REGEXP '時間|时间|日期'                                       -- 并列条件：中文列义命中
                         THEN 1 ELSE 0 END)                                            AS has_time,       -- 条件分支：产出「has_time」
               MAX(CASE WHEN LOWER(c.DATA_TYPE) REGEXP 'decimal|double|float'                             -- 条件分支：金额型列——先认类型
                         OR LOWER(c.COLUMN_NAME) REGEXP 'amount|money|balance|sum|fee|cash'               -- 并列条件：列名命中金额族
                         OR c.COLUMN_COMMENT REGEXP '金額|金额|餘額|余额|款'                               -- 并列条件：中文列义命中
                         THEN 1 ELSE 0 END)                                            AS has_amount,     -- 条件分支：产出「has_amount」
               COUNT(*)                                                                AS n_cols          -- 计数表达式：列数，配置表通常列少
        FROM information_schema.columns c                                                                 -- 取数来源：取自元数据字典（列清单）
        WHERE c.TABLE_SCHEMA = 'ods_mariadb_2b'                                                           -- 过滤条件：限定 TABLE_SCHEMA等于 'ods_mariadb_2b'
        GROUP BY c.TABLE_NAME                                                                             -- 分组：按表名汇总
      )                                                                                                   -- 续行：收束上方的子查询或函数括号
      SELECT                                                                                              -- 取列：起始取列子句
        t.TABLE_NAME                                                                   AS table_name,     -- 取列：表名
        CASE                                                                                              -- 条件分支：起始分支——六源归类，责任博彩优先判以免被状态族吞并
          WHEN LOWER(t.TABLE_NAME) REGEXP 'self_?exclu|cooling|cool_?off|cooldown|responsible|_rg$|^rg_'   -- 分支判定：责任博彩族（★ 已排除 response 之误撞）
            THEN 'H-SRC-05 自我排除／冷静期／责任博彩'                                                     -- 分支判定：H 轴最高价值之源
          WHEN LOWER(t.TABLE_NAME) REGEXP 'wallet|pay|deposit|withdraw|cashier|recharge|transact|transation|fund|remit|balance|cash|order' -- 分支判定：支付族（★ v1.0.1 补 wallet／balance／cash）
            THEN 'H-SRC-01 支付／资金'                                                                    -- 分支判定：归支付源
          WHEN LOWER(t.TABLE_NAME) REGEXP 'session|login|logout|online|heartbeat|device|token|visit'       -- 分支判定：会话族
            THEN 'H-SRC-02 会话／登录'                                                                    -- 分支判定：归会话源
          WHEN LOWER(t.TABLE_NAME) REGEXP 'mem_change|member_log|suspend|close|reopen|freeze|restrict|limit|level|vip|status' -- 分支判定：会员状态族
            THEN 'H-SRC-03 会员状态变更'                                                                  -- 分支判定：归状态源
          WHEN LOWER(t.TABLE_NAME) REGEXP 'ticket|case|complain|dispute|service|support|feedback|chat|message|msg|alarm|alert' -- 分支判定：工单／消息族
            THEN 'H-SRC-04 客服工单／消息'                                                                -- 分支判定：归工单源（★ 告警与工单须于 §P-H0-3 再辨）
          WHEN LOWER(t.TABLE_NAME) REGEXP '^ods_a168_(log_|change_log)|_log$|history|journal|record|trace' -- 分支判定：日志族（★ v1.0.1 新增，变更留痕之通称）
            THEN 'H-SRC-06 日志／变更留痕'                                                                -- 分支判定：归日志源
          ELSE '（未命中六源）'                                                                           -- 分支判定：余者照登，以备扩关键词后翻查
        END                                                                            AS h_src,         -- 条件分支：收束上方判词，产出「h_src」
        f.has_uid,                                                                                        -- 取列：是否具会员键
        f.has_time,                                                                                       -- 取列：是否具时间列
        f.has_amount,                                                                                     -- 取列：是否具金额列
        f.n_cols,                                                                                         -- 取列：列数
        CASE                                                                                              -- 条件分支：起始分支——表性质判别（D-5）
          WHEN f.has_uid = 1 AND f.has_time = 1 AND f.has_amount = 1 THEN '★ 事务表候选（三列俱备）'      -- 分支判定：可作 H 轴资金证据源之候选
          WHEN f.has_uid = 1 AND f.has_time = 1                      THEN '事件表候选（无金额）'          -- 分支判定：可作行为事件源
          WHEN f.has_uid = 0 AND f.has_time = 0                      THEN '配置表（无会员键无时间）'      -- 分支判定：无会员级事件，不可作证据源
          ELSE '无从判（须逐表验货）'                                                                     -- 分支判定：余者待 §P-H0-3
        END                                                                            AS table_nature,  -- 条件分支：收束上方判词，产出「table_nature」
        'ods_mariadb_2b'                                                               AS scope_db,      -- 字面取值：检索范围之库——NO_TRACE 之锚
        'v1.0.1 六族表名正则 ＋ 三列性质判别（含编码式列名 betNN／memNNN／ageNNN／lmcNN）' AS scope_pattern, -- 字面取值：检索范围之模式
        NOW()                                                                          AS scope_scanned_at -- 取值表达式：检索时刻，六态判读之时间锚
      FROM information_schema.tables t                                                                    -- 取数来源：取自元数据字典（表清单）
      LEFT JOIN col_flag f ON f.table_name = t.TABLE_NAME                                                 -- 左连接：取自本条自建的中间结果集 col_flag，连接键为 table_name
      WHERE t.TABLE_SCHEMA = 'ods_mariadb_2b'                                                             -- 过滤条件：限定 TABLE_SCHEMA等于 'ods_mariadb_2b'
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 124. _PH0_2b_column_inventory.csv   [总包行 12457~12505 · 原 11 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`table_name`, w.`col_pos`, w.`column_name`, w.`data_type`, w.`column_comment`, w.`name_style`, w.`col_family`, w.`fp_flag`, w.`scope_db`, w.`scope_pattern`, w.`scope_scanned_at`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH cand AS (                                                                                      -- 公共表表达式：开启中间结果集 cand——六源命中之候选表
        SELECT t.TABLE_NAME                                                            AS table_name      -- 取列：表名
        FROM information_schema.tables t                                                                  -- 取数来源：取自元数据字典（表清单）
        WHERE t.TABLE_SCHEMA = 'ods_mariadb_2b'                                                           -- 过滤条件：限定 TABLE_SCHEMA等于 'ods_mariadb_2b'
          AND LOWER(t.TABLE_NAME) REGEXP                                                                  -- 并列条件：六族任一命中即入候选
            'self_?exclu|cooling|cool_?off|cooldown|responsible|_rg$|^rg_|wallet|pay|deposit|withdraw|cashier|recharge|transact|transation|fund|remit|balance|cash|order|session|login|logout|online|heartbeat|device|token|visit|mem_change|member_log|suspend|close|reopen|freeze|restrict|limit|level|vip|status|ticket|case|complain|dispute|service|support|feedback|chat|message|msg|alarm|alert|^ods_a168_(log_|change_log)|_log$|history|journal|record|trace'
      )                                                                                                   -- 续行：收束上方的子查询或函数括号
      SELECT                                                                                              -- 取列：起始取列子句
        c.TABLE_NAME                                                                   AS table_name,     -- 取列：表名
        c.ORDINAL_POSITION                                                             AS col_pos,        -- 取列：列序，供还原表结构
        c.COLUMN_NAME                                                                  AS column_name,    -- 取列：列名
        c.DATA_TYPE                                                                    AS data_type,      -- 取列：数据类型
        c.COLUMN_COMMENT                                                               AS column_comment, -- 取列：列注释——编码列唯一可读之语义线索
        CASE WHEN LOWER(c.COLUMN_NAME) REGEXP '^(bet|mem|age|lmc|emp|acc|lacc)[0-9]{2,3}$'                -- 分支判定：编码式列名
             THEN '★ 编码式列名（语义须读 COLUMN_COMMENT）' ELSE '普通列名' END          AS name_style,    -- 条件分支：产出「name_style」
        CASE                                                                                              -- 条件分支：起始分支——列义归族（★ 只作标记，不参与过滤）
          WHEN LOWER(c.COLUMN_NAME) REGEXP 'self_?exclu|cooling|cool_?off|cooldown'                       -- 分支判定：责任博彩族（★ 已去 respons，免撞 responseDate）
            OR c.COLUMN_COMMENT REGEXP '自我排除|冷靜期|冷静期|責任博彩|责任博彩|自我限制'                  -- 并列条件：中文列义命中
            THEN '★★ RG 责任博彩'                                                                        -- 分支判定：H 轴最高价值之列
          WHEN LOWER(c.COLUMN_NAME) REGEXP 'deposit|withdraw|recharge|refund|chargeback|reject|fail'       -- 分支判定：支付事件族
            OR c.COLUMN_COMMENT REGEXP '充值|入金|存款|提現|提现|出金|退款|拒付|失敗|失败'                  -- 并列条件：中文列义命中
            THEN '支付事件'                                                                               -- 分支判定：归支付事件
          WHEN LOWER(c.COLUMN_NAME) REGEXP 'amount|money|balance|fee|cash'                                -- 分支判定：金额族
            OR c.COLUMN_COMMENT REGEXP '金額|金额|餘額|余额'                                               -- 并列条件：中文列义命中
            THEN '金额'                                                                                   -- 分支判定：归金额
          WHEN LOWER(c.COLUMN_NAME) REGEXP 'login|logout|session|online|duration|heartbeat'                -- 分支判定：会话族
            OR c.COLUMN_COMMENT REGEXP '登入|登录|登出|會話|会话|在線|在线|時長|时长'                       -- 并列条件：中文列义命中
            THEN '会话'                                                                                   -- 分支判定：归会话
          WHEN LOWER(c.COLUMN_NAME) REGEXP '^(old|new|before|after|prev|from|to)_|_(old|new|before|after)$' -- 分支判定：前后态族——变更留痕之指纹
            OR c.COLUMN_COMMENT REGEXP '變更前|变更前|變更後|变更后|原值|新值'                              -- 并列条件：中文列义命中
            THEN '★ 前后态（变更留痕指纹）'                                                               -- 分支判定：H-SRC-03/06 之核心
          WHEN LOWER(c.COLUMN_NAME) REGEXP '^(uid|member_id|user_id|username|account|mem[0-9]{2,3}|bet05|age[0-9]{3}|lmc[0-9]{2})$' -- 分支判定：会员键族（含编码式）
            OR c.COLUMN_COMMENT REGEXP '會員|会员|帳號|账号'                                               -- 并列条件：中文列义命中
            THEN '★ UID 候选键'                                                                           -- 分支判定：UID 可连接性之前提
          WHEN LOWER(c.DATA_TYPE) REGEXP 'date|time'                                                      -- 分支判定：时间型——先认类型，最可靠
            OR LOWER(c.COLUMN_NAME) REGEXP 'time|date|_at$|_dt$|^dt$|created|updated'                     -- 并列条件：列名命中
            OR c.COLUMN_COMMENT REGEXP '時間|时间|日期'                                                    -- 并列条件：中文列义命中
            THEN '★ 时间候选列'                                                                           -- 分支判定：时间覆盖之前提（★ D-1 之修复：本族已入输出）
          ELSE '（未标记）'                                                                               -- 分支判定：余者照登，不丢弃
        END                                                                            AS col_family,    -- 条件分支：收束上方判词，产出「col_family」
        CASE WHEN LOWER(c.COLUMN_NAME) REGEXP 'response|respond'                                          -- 分支判定：疑似假阳性——response 曾撞 responsible
             THEN '⚠ 疑似假阳性（response 非 responsible）' ELSE '—' END                AS fp_flag,       -- 条件分支：产出「fp_flag」，不静默丢弃
        'ods_mariadb_2b'                                                               AS scope_db,      -- 字面取值：检索范围之库
        'v1.0.1 候选表全列输出（关键词只作标记不作过滤）'                              AS scope_pattern, -- 字面取值：检索范围之模式
        NOW()                                                                          AS scope_scanned_at -- 取值表达式：检索时刻
      FROM information_schema.columns c                                                                   -- 取数来源：取自元数据字典（列清单）
      JOIN cand k ON k.table_name = c.TABLE_NAME                                                          -- 连接：取自本条自建的中间结果集 cand，连接键为 table_name
      WHERE c.TABLE_SCHEMA = 'ods_mariadb_2b'                                                             -- 过滤条件：限定 TABLE_SCHEMA等于 'ods_mariadb_2b'
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 125. _PH0_3_lmc_category.csv   [总包行 12598~12615 · 原 12 列 ＋六层约 60 列]
--     典型学：M10 责任博彩　分区：pre_rg（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`lmc04_category`, w.`n_events`, w.`n_target_id`, w.`n_operator`, w.`dt_min`, w.`dt_max`, w.`n_in_win`, w.`avg_len_content`, w.`avg_len_sql`, w.`n_content_hit_mem01617`, w.`n_sql_hit_mem01617`, w.`n_hit_rg_semantics`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        COALESCE(NULLIF(TRIM(lmc04), ''), '（空）')                            AS lmc04_category,         -- 取值表达式：變更類別，空值归一为「（空）」
        COUNT(*)                                                               AS n_events,               -- 计数表达式：事件数
        COUNT(DISTINCT TRIM(lmc02))                                            AS n_target_id,            -- 计数表达式：去重被改对象 ID（lmc02 = ID）
        COUNT(DISTINCT TRIM(lmc06))                                            AS n_operator,             -- 计数表达式：去重操作者数（lmc06 = 操作者）
        MIN(dt)                                                                AS dt_min,                 -- 聚合取值：分区日下界
        MAX(dt)                                                                AS dt_max,                 -- 聚合取值：分区日上界
        SUM(CASE WHEN dt >= '2026-03-21' AND dt < '2026-08-07' THEN 1 ELSE 0 END) AS n_in_win,            -- 条件计数：窗内事件数
        ROUND(AVG(LENGTH(COALESCE(lmc05, ''))), 2)                             AS avg_len_content,        -- 聚合取值：內容平均长度，判文本解析之工作量
        ROUND(AVG(LENGTH(COALESCE(lmc09, ''))), 2)                             AS avg_len_sql,            -- 聚合取值：SQL 平均长度
        SUM(CASE WHEN lmc05 REGEXP 'mem016|mem017' THEN 1 ELSE 0 END)          AS n_content_hit_mem01617, -- 条件计数：★ 內容文本命中 mem016/mem017 之行数
        SUM(CASE WHEN lmc09 REGEXP 'mem016|mem017' THEN 1 ELSE 0 END)          AS n_sql_hit_mem01617,     -- 条件计数：★ SQL 文本命中 mem016/mem017 之行数
        SUM(CASE WHEN CONCAT(COALESCE(lmc05, ''), COALESCE(lmc09, ''))
                      REGEXP '自我|排除|冷靜|冷静|責任|责任|限額|限额|凍結|冻结|停用|封鎖|封锁'
                 THEN 1 ELSE 0 END)                                            AS n_hit_rg_semantics      -- 条件计数：★★ 文本内是否含责任博彩语义——RG 之最后一线希望
      FROM ods_mariadb_2b.ods_a168_log_mem_change                                                         -- 取数来源：取自会员变更日志表
      GROUP BY COALESCE(NULLIF(TRIM(lmc04), ''), '（空）')                                                 -- 分组：按變更類別汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_pre_rg,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_pre_rg,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_pre_rg,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_pre_rg,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_pre_rg,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_pre_rg,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_pre_rg,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_pre_rg,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 126. _PH0_4L1_member_columns.csv   [总包行 12811~12820 · 原 5 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`table_name`, w.`col_pos`, w.`column_name`, w.`data_type`, w.`column_comment`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        c.TABLE_NAME                                                           AS table_name,             -- 取列：表名
        c.ORDINAL_POSITION                                                     AS col_pos,                -- 取列：列序
        c.COLUMN_NAME                                                          AS column_name,            -- 取列：列名
        c.DATA_TYPE                                                            AS data_type,              -- 取列：数据类型
        c.COLUMN_COMMENT                                                       AS column_comment          -- 取列：列注释——编码列唯一可读之语义线索
      FROM information_schema.columns c                                                                   -- 取数来源：取自元数据字典（列清单）
      WHERE c.TABLE_SCHEMA = 'ods_mariadb_2b'                                                             -- 过滤条件：限定 TABLE_SCHEMA等于 'ods_mariadb_2b'
        AND c.TABLE_NAME IN ('ods_a168_member', 'ods_a168_member_dtl', 'ods_a168_member_url')             -- 并列条件：限定三张会员主表
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 127. _PH0_4b_rg_keyword.csv   [总包行 12664~12694 · 原 11 列 ＋六层约 60 列]
--     典型学：M10 责任博彩　分区：pre_rg（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`lmc04_category`, w.`n_events`, w.`rg_self_exclusion`, w.`rg_cooling_off`, w.`rg_responsible`, w.`biz_limit`, w.`biz_freeze`, w.`biz_disable`, w.`biz_block`, w.`amb_exclude_any`, w.`n_in_win`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        COALESCE(NULLIF(TRIM(lmc04), ''), '（空）')                            AS lmc04_category,         -- 取值表达式：變更類別
        COUNT(*)                                                               AS n_events,               -- 计数表达式：该类事件总数
        SUM(CASE WHEN CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%自我排除%'
                  OR CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%self_exclu%'
                 THEN 1 ELSE 0 END)                                            AS rg_self_exclusion,      -- 条件计数：★★ 真 RG——自我排除
        SUM(CASE WHEN CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%冷靜%'
                  OR CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%冷静%'
                  OR CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%cooling%'
                 THEN 1 ELSE 0 END)                                            AS rg_cooling_off,         -- 条件计数：★★ 真 RG——冷静期
        SUM(CASE WHEN CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%責任博彩%'
                  OR CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%责任博彩%'
                  OR CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%responsible%'
                 THEN 1 ELSE 0 END)                                            AS rg_responsible,         -- 条件计数：★★ 真 RG——责任博彩
        SUM(CASE WHEN CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%限額%'
                  OR CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%限额%'
                 THEN 1 ELSE 0 END)                                            AS biz_limit,              -- 条件计数：商业管控——限额（★ 非 RG）
        SUM(CASE WHEN CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%凍結%'
                  OR CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%冻结%'
                 THEN 1 ELSE 0 END)                                            AS biz_freeze,             -- 条件计数：商业管控——冻结
        SUM(CASE WHEN CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%停用%'
                 THEN 1 ELSE 0 END)                                            AS biz_disable,            -- 条件计数：商业管控——停用
        SUM(CASE WHEN CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%封鎖%'
                  OR CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%封锁%'
                 THEN 1 ELSE 0 END)                                            AS biz_block,              -- 条件计数：商业管控——封锁
        SUM(CASE WHEN CONCAT(COALESCE(lmc05,''), COALESCE(lmc09,'')) LIKE '%排除%'
                 THEN 1 ELSE 0 END)                                            AS amb_exclude_any,        -- 条件计数：★ 歧义词——「排除」单独出现，须人工判其上下文
        SUM(CASE WHEN dt >= '2026-03-21' AND dt < '2026-08-07' THEN 1 ELSE 0 END) AS n_in_win             -- 条件计数：窗内事件数
      FROM ods_mariadb_2b.ods_a168_log_mem_change                                                         -- 取数来源：取自会员变更日志表
      GROUP BY COALESCE(NULLIF(TRIM(lmc04), ''), '（空）')                                                 -- 分组：按變更類別汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_pre_rg,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_pre_rg,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_pre_rg,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_pre_rg,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_pre_rg,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_pre_rg,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_pre_rg,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_pre_rg,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 128. _PH0_4w2_balance_points.csv   [总包行 12777~12799 · 原 12 列 ＋六层约 60 列]
--     典型学：M12 资金轨迹　分区：recent（s.x_rec = 1）　连接：全窗基线
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`dt_slice`, w.`n_user_active`, w.`n_pts_total`, w.`pts_avg`, w.`pts_min`, w.`pts_p50`, w.`pts_p90`, w.`pts_p99`, w.`pts_max`, w.`pts_ok_avg`, w.`n_user_single_pt`, w.`pct_single_pt`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_member                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_member IS NOT NULL AND TRIM(CAST(s.x_member AS STRING)) <> ''
    GROUP BY s.x_member
  ),
    q AS (
      WITH per_user AS (                                                                                  -- 公共表表达式：开启中间结果集 per_user——单日逐账号之点数
        SELECT TRIM(`user`)                                                    AS u,                      -- 取列：账号
               COUNT(*)                                                        AS n_pts,                  -- 计数表达式：该账号当日行数
               SUM(CASE WHEN TRIM(status) = '1' THEN 1 ELSE 0 END)             AS n_pts_ok                -- 条件计数：其中成功态行数
        FROM ods_mariadb_2b.ods_a168_wallet_dtl                                                           -- 取数来源：取自钱包明细表
        WHERE dt = '2026-06-15'                                                                           -- 过滤条件：限定单日切片（窗中段）
        GROUP BY TRIM(`user`)                                                                             -- 分组：按账号汇总
      )                                                                                                   -- 续行：收束上方的子查询或函数括号
      SELECT                                                                                              -- 取列：起始取列子句
        '2026-06-15'                                                           AS dt_slice,               -- 字面取值：切片日
        COUNT(*)                                                               AS n_user_active,          -- 计数表达式：当日活跃账号数
        SUM(n_pts)                                                             AS n_pts_total,            -- 聚合取值：当日总行数
        ROUND(AVG(n_pts), 2)                                                   AS pts_avg,                -- 聚合取值：人均点数
        MIN(n_pts)                                                             AS pts_min,                -- 聚合取值：点数下界
        PERCENTILE_APPROX(n_pts, 0.50)                                         AS pts_p50,                -- 聚合取值：★ 点数中位——决定日内轨迹可否建
        PERCENTILE_APPROX(n_pts, 0.90)                                         AS pts_p90,                -- 聚合取值：点数九十分位
        PERCENTILE_APPROX(n_pts, 0.99)                                         AS pts_p99,                -- 聚合取值：点数九九分位
        MAX(n_pts)                                                             AS pts_max,                -- 聚合取值：点数上界
        ROUND(AVG(n_pts_ok), 2)                                                AS pts_ok_avg,             -- 聚合取值：人均成功态点数
        SUM(CASE WHEN n_pts = 1 THEN 1 ELSE 0 END)                             AS n_user_single_pt,       -- 条件计数：★ 当日仅一个点之账号数——其轨迹不可建
        ROUND(SUM(CASE WHEN n_pts = 1 THEN 1 ELSE 0 END) * 100.0
              / NULLIF(COUNT(*), 0), 4)                                        AS pct_single_pt           -- 比率表达式：单点账号占比
      FROM per_user                                                                                      -- 取数来源：取自本条自建的中间结果集 per_user
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  CROSS JOIN x_agg e
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 129. _PH0_6b_member_daily.csv   [总包行 13015~13025 · 原 6 列 ＋六层约 60 列]
--     典型学：Z00 基建/元数据　分区：recent（s.x_rec = 1）　连接：q.`dt_day`
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- ── 原版审计版：六层商业指标 ＋ audit_rn（不切片，一次导全）──
SELECT w.*,
       ROW_NUMBER() OVER (ORDER BY w.`net_margin` ASC, w.`roi` DESC, w.`dt_day`, w.`n_rows`, w.`n_mem001`, w.`rows_per_key`, w.`n_enable_N`, w.`n_canbet_N`) AS audit_rn
FROM (
  WITH
    x_bs AS (
    SELECT b.bet05 AS x_member, b.eid AS x_dealer, b.bet39 AS x_table,
           b.bet03 AS x_shoe, b.ip AS x_ip, b.bet09 AS x_product, b.dt AS x_date,
           CONCAT_WS('|', b.bet03, b.bet04, b.bet39)                      AS x_round,
           CONCAT_WS('|', b.bet39, b.bet03)                               AS x_tblshoe,
           CAST(NULLIF(TRIM(b.bet04),'') AS INT)                          AS x_rno,
           CASE WHEN b.dt >= '2026-07-08' THEN 1 ELSE 0 END               AS x_rec,
           CASE WHEN TRIM(b.commission)='1' THEN 1 ELSE 0 END             AS x_freecomm,
           CAST(NULLIF(TRIM(b.bet13),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_stake,
           CAST(NULLIF(TRIM(b.validbet),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_valid,
           CAST(NULLIF(TRIM(b.bet16),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_rebate,
           CAST(NULLIF(TRIM(b.bet17),'') AS DECIMAL(20,4))
             / CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8))            AS x_net,
           CAST(NULLIF(TRIM(b.bet15),'') AS DECIMAL(20,6))                AS x_rbpct,
           COALESCE(CAST(NULLIF(TRIM(b.bet23),'') AS DECIMAL(20,6)),0)   AS x_agpct
    FROM ods_mariadb_2b.ods_a168_bet02 b
    WHERE b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
      AND b.bet02='101' AND b.category='1' AND UPPER(TRIM(b.bet38))='N'
      AND CAST(NULLIF(TRIM(b.bet11),'') AS DECIMAL(20,8)) > 0
  ),
  x_agg AS (
    SELECT s.x_date                                                        AS x_key,
           COUNT(*) AS n_bets, COUNT(DISTINCT s.x_round) AS n_rounds,
           COUNT(DISTINCT s.x_date) AS active_days, COUNT(DISTINCT s.x_tblshoe) AS active_shoes,
           COUNT(DISTINCT CONCAT_WS('|', s.x_date, s.x_table)) AS active_sessions,
           COUNT(DISTINCT s.x_table) AS n_tables, COUNT(DISTINCT s.x_dealer) AS n_dealers,
           COUNT(DISTINCT s.x_ip) AS n_ips, COUNT(DISTINCT s.x_product) AS n_products,
           MIN(s.x_date) AS first_seen_d, MAX(s.x_date) AS last_seen_d,
           SUM(s.x_stake) AS stake, SUM(s.x_valid) AS valid, SUM(s.x_rebate) AS rebate,
           SUM(s.x_net) AS net, SUM(-s.x_net *(100.0-s.x_agpct)/100.0) AS agent_cost,
           AVG(100.0-s.x_agpct)                   AS agent_pct,
           AVG(s.x_agpct)                         AS company_retain_pct,
           SUM(-s.x_net *s.x_agpct/100.0)         AS agent_cost_a_legacy,
           SUM(s.x_valid*s.x_agpct/100.0)         AS agent_cost_valid_base_legacy,
           AVG(s.x_rbpct) AS rbpct, AVG(s.x_freecomm*1.0) AS freecomm_rate,
           PERCENTILE_APPROX(s.x_stake,0.25) AS p25, PERCENTILE_APPROX(s.x_stake,0.50) AS p50,
           PERCENTILE_APPROX(s.x_stake,0.75) AS p75, PERCENTILE_APPROX(s.x_stake,0.90) AS p90,
           PERCENTILE_APPROX(s.x_stake,0.95) AS p95, PERCENTILE_APPROX(s.x_stake,0.99) AS p99,
           PERCENTILE_APPROX(s.x_valid,0.50) AS p50_valid,
           PERCENTILE_APPROX(s.x_net,0.50)   AS p50_profit,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_stake ELSE 0 END) AS st_in,
           SUM(CASE WHEN s.x_rec = 1 THEN 1 ELSE 0 END)         AS nb_in,
           SUM(CASE WHEN s.x_rec = 1 THEN s.x_net ELSE 0 END)   AS net_in,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_stake ELSE 0 END) AS st_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN 1 ELSE 0 END)         AS nb_out,
           SUM(CASE WHEN NOT (s.x_rec = 1) THEN s.x_net ELSE 0 END)   AS net_out,
           SUM(CASE WHEN s.x_rec=1 THEN s.x_stake ELSE 0 END) AS st_rec,
           SUM(CASE WHEN s.x_rec=1 THEN 1 ELSE 0 END)         AS nb_rec
    FROM x_bs s
    WHERE s.x_date IS NOT NULL AND TRIM(CAST(s.x_date AS STRING)) <> ''
    GROUP BY s.x_date
  ),
    q AS (
      SELECT                                                                                              -- 取列：起始取列子句
        dt                                                                     AS dt_day,                 -- 取列：分区日
        COUNT(*)                                                               AS n_rows,                 -- 计数表达式：当日行数
        COUNT(DISTINCT TRIM(mem001))                                           AS n_mem001,               -- 计数表达式：当日去重会员键数
        ROUND(COUNT(*) * 1.0 / NULLIF(COUNT(DISTINCT TRIM(mem001)), 0), 4)     AS rows_per_key,           -- 比率表达式：日内版本数
        SUM(CASE WHEN TRIM(mem016) = 'N' THEN 1 ELSE 0 END)                    AS n_enable_N,             -- 条件计数：当日停用数
        SUM(CASE WHEN TRIM(mem017) = 'N' THEN 1 ELSE 0 END)                    AS n_canbet_N              -- 条件计数：当日禁投注数
      FROM ods_mariadb_2b.ods_a168_member                                                                 -- 取数来源：取自会员主表
      WHERE dt >= '2026-03-21' AND dt < '2026-08-07'                                                      -- 过滤条件：限定分析窗
      GROUP BY dt                                                                                         -- 分组：按分区日汇总
    )
  SELECT q.*,
         -- 一·经济层
         ROUND(e.stake,4) AS stake, ROUND(e.valid,4) AS valid_bet,
         ROUND(-e.net,4) AS profit, e.n_bets, e.n_rounds,
         ROUND(e.net/NULLIF(e.stake,0),8) AS roi,
         ROUND(e.net_in/NULLIF(e.st_in,0),8) AS roi_recent,
         ROUND(e.net_out/NULLIF(e.st_out,0),8) AS roi_non_recent,
         ROUND((e.net-e.rebate)/NULLIF(e.stake,0),8) AS roi_ex_rebate,
         ROUND(e.net,4) AS roi_numerator, ROUND(e.stake,4) AS roi_denominator,
         CASE WHEN e.stake IS NULL THEN 'NULL_DENOM' WHEN e.stake<=0 THEN 'ZERO_DENOM'
              WHEN e.n_bets<30 THEN 'THIN_EXPOSURE' ELSE 'OK' END AS roi_denominator_status,
         ROUND(e.rebate,4) AS rebate_cost, ROUND(e.agent_cost,4)                           AS agent_cost,
       ROUND(e.agent_pct,4)                            AS agent_pct,
       ROUND(e.company_retain_pct,4)                   AS company_retain_pct,
       'AG03-B-v1'                                     AS agent_formula_version,
       ROUND(e.agent_cost_a_legacy,4)                  AS agent_cost_a_legacy,
       ROUND(e.agent_cost_valid_base_legacy,4)         AS agent_cost_valid_base_legacy,
         ROUND(-e.net-e.rebate,4)                        AS ngr,
       ROUND(-e.net-e.rebate-e.agent_cost,4)           AS net_margin,
       ROUND(-e.net-e.rebate-e.agent_cost_a_legacy,4)  AS net_margin_a_legacy,
         ROUND((-e.net-e.rebate)/NULLIF(e.stake,0),8)    AS ngr_rate,
       ROUND((-e.net-e.rebate-e.agent_cost)/NULLIF(e.stake,0),8)   AS net_margin_rate,
       ROUND((-e.net-e.rebate-e.agent_cost_a_legacy)/NULLIF(e.stake,0),8) AS net_margin_a_rate_legacy,
         -- 二·分布层
         ROUND(e.stake/NULLIF(e.n_bets,0),4) AS avg_stake_per_bet,
         ROUND(e.valid/NULLIF(e.n_rounds,0),4) AS avg_valid_bet_per_round,
         ROUND(-e.net/NULLIF(e.n_rounds,0),4) AS avg_profit_per_round,
         ROUND(-e.net/NULLIF(e.active_days,0),4) AS avg_profit_per_day,
         ROUND(-e.net/NULLIF(e.active_shoes,0),4) AS avg_profit_per_shoe,
         ROUND(e.stake/NULLIF(e.active_days,0),4) AS avg_stake_per_active_day,
         ROUND(e.n_rounds*1.0/NULLIF(e.active_days,0),4) AS avg_rounds_per_active_day,
         ROUND(e.st_in/NULLIF(e.nb_in,0),4) AS avg_stake_recent,
         ROUND(e.st_out/NULLIF(e.nb_out,0),4) AS avg_stake_non_recent,
         ROUND(e.p25,4) AS p25_stake, ROUND(e.p50,4) AS p50_stake, ROUND(e.p75,4) AS p75_stake,
         ROUND(e.p90,4) AS p90_stake, ROUND(e.p95,4) AS p95_stake, ROUND(e.p99,4) AS p99_stake,
         ROUND(e.p50_valid,4) AS p50_valid_bet, ROUND(e.p50_profit,4) AS p50_profit,
         ROUND(e.p95/NULLIF(e.p50,0),6) AS p95_over_p50,
         ROUND(e.p99/NULLIF(e.p50,0),6) AS p99_over_p50,
         ROUND((e.stake/NULLIF(e.n_bets,0))/NULLIF(e.p50,0),6) AS avg_over_p50,
         -- 三·关系层
         e.n_tables AS common_table_count, e.n_dealers AS common_dealer_count,
         e.n_ips AS common_ip_count, e.n_products AS common_product_count,
         ROUND(e.st_in/NULLIF(e.stake,0),8) AS share_recent,
         -- 四·rate 层
         ROUND(-e.net/NULLIF(e.stake,0),8) AS hold_rate,
         ROUND(-e.net_in/NULLIF(e.st_in,0),8) AS hold_rate_recent,
         ROUND(e.rebate/NULLIF(e.valid,0),8) AS rebate_rate_actual,
         ROUND(e.rbpct,6) AS rebate_pct_config,
         ROUND(e.valid/NULLIF(e.stake,0),6) AS turnover_multiple,
         ROUND(e.freecomm_rate,6) AS free_comm_rate,
         ROUND(e.net_in/NULLIF(e.st_in,0)-e.net_out/NULLIF(e.st_out,0),8) AS roi_delta_recent,
         -- 五·动态层
         e.first_seen_d AS first_seen, e.last_seen_d AS last_seen,
         e.active_days, e.active_shoes, e.active_sessions,
         DATEDIFF(e.last_seen_d, e.first_seen_d)+1 AS span_days,
         ROUND(e.active_days*1.0/NULLIF(DATEDIFF(e.last_seen_d,e.first_seen_d)+1,0),6) AS persistence,
         ROUND((e.st_in/NULLIF(e.nb_in,0))/NULLIF(e.st_out/NULLIF(e.nb_out,0),0),6) AS ramp_recent_vs_prior,
         ROUND((e.st_rec/NULLIF(e.nb_rec,0))/NULLIF(e.stake/NULLIF(e.n_bets,0),0),6) AS ramp_recent_vs_all,
         ROUND((e.st_rec/NULLIF(e.stake,0))/0.30,6) AS stake_ramp,
         -- 六·决策层
         NTILE(5) OVER (ORDER BY e.stake) AS vip_tier,
         PERCENT_RANK() OVER (ORDER BY -(-e.net-e.rebate-e.agent_cost)) AS economic_value,
         PERCENT_RANK() OVER (ORDER BY e.net/NULLIF(e.stake,0))         AS risk_value,
         PERCENT_RANK() OVER (ORDER BY e.stake)                         AS pr_stake,
         CASE WHEN e.stake IS NULL OR e.stake<=0 OR e.n_bets<30 THEN 'Z 证据不足 · 不动作'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)<=0.10
                THEN 'A 平台净亏首位 · 立即处置'
              WHEN PERCENT_RANK() OVER (ORDER BY -e.net-e.rebate-e.agent_cost)>=0.80
                THEN 'D VIP 优质 · 加码留存，严禁误伤'
              ELSE 'E 常规' END                                          AS action_priority
  FROM q
  LEFT JOIN x_agg e ON CAST(e.x_key AS STRING) = CAST(q.`dt_day` AS STRING)
) w
ORDER BY audit_rn;
--     ★ audit_rn 之排序键与分批作业版**逐字同一**（锁一），故两版可逐行对账。

