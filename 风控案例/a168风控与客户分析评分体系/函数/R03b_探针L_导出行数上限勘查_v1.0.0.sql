-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- R03b ＃071 · 验收探针 L · 导出行数上限勘查   v1.0.0   2026-09-05
-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 【何以要查】先生示知：查询十万行，只下得一万行。实测现档 R03b…_b1_to_b8.csv 亦证之——
--   该档含 80 个相异 batch_id、每个恰 10,000 行，系【80 次 10k 下载拼成】，
--   ⇒ 100k 版【从未成功下载过】。在下先前「改用 100k 版」之建议据此作废。
--
-- 【此事之分量 —— 已按 T_true = 18,139,550 实算】
--   上限维持 10,000：全量 1,814 批，已导 416，尚余 1,398 批
--       纯查询 18.7 小时；每批含改窗／点跑／下载／归档约 1.5~2 分钟 ⇒ 实际 35~47 小时
--   上限若能提至 100,000：全量 182 批，尚余约 141 批
--       ⇒ 作业次数【减少 89.9%】，省下 1,257 次人工操作；纯查询时长不变
--   ★ 故此项之价值，高于目前任何一项 SQL 优化。
--
-- 【Superset 之三重上限 —— 须分辨，否则会误判】
--   DISPLAY_MAX_ROW   预设 10,000  ← 只管【网格显示几行】
--   SQL_MAX_ROW       预设 100,000 ← Superset 追加之 LIMIT 之硬顶
--   DEFAULT_SQLLAB_LIMIT           ← LIMIT 下拉框之预设值
--   ⚑ 三者不同！**网格只显示 10,000 行，不等于 CSV 只能下 10,000 行。**
--      故【绝不可凭网格所示判定上限】，必须下载 CSV 后【数其行数】。
--   ⚑ 前番探针 M／M2 所见之 `Unexpected input 'LIMIT'`，正是 Superset 追加此 LIMIT 之铁证。
--
-- 【只读】纯 SELECT，单栏，仅取一个分区，代价极小。
-- ══════════════════════════════════════════════════════════════════════════════════════════════


-- ── 施测三步（务请依序）────────────────────────────────────────────────────
--   第一步 · 把 SQL Lab【Run 键旁之 LIMIT 下拉框】调至其【最大可选值】
--            （常见档位 100 / 1000 / 10000 / 100000；若有 100000 即选之）
--            ⚑ 顺手记下该下拉框【提供了哪些档位】——此即 SQL_MAX_ROW 之线索。
--   第二步 · 跑下方查询。网格若只显示 10,000 行，**勿据此下结论**。
--   第三步 · 点【Download to CSV】，把档下回来后【数其行数】。
--            此数方为【真正的导出上限】。
--
--   本查询只取一个营业日、一个栏位，故极轻（约 90 万行 × 一栏）。
SELECT b.bet01 AS x
FROM ods_mariadb_2b.ods_a168_bet02 b
WHERE b.dt = '2026-03-21'
  AND b.bet02 = '101';


-- ══════════════════════════════════════════════════════════════════════════════════════════════
-- 【判读】以下载之 CSV 行数为准（表头除外）：
--   ＝ 100,000  ⇒ ★★ 上限本就是 10 万，只是【下拉框先前设在 10,000】。
--                    ⇒ 改用 100k 版，作业次数立减 89.9%，无须劳动任何人。
--   ＝  10,000  ⇒ 上限确为 1 万。⇒ 须请 Superset 管理员调 SQL_MAX_ROW（预设本为 100000，
--                    此处若为 10000 即系被人调低过），或改走别的导出途径。
--   介于二者    ⇒ 即该值为真上限，按其大小重定批量（如 50,000 ⇒ 全量 363 批）。
--
-- 【若上限确不可动，尚有二法可议 —— 皆须先生裁夺】
--   甲　Superset 之【异步查询／Results backend】若已启用，其 CSV 导出常不受 DISPLAY_MAX_ROW 限，
--       且可导大结果集。须问管理员是否启用，及导出上限几何。
--   乙　改由 MySQL 协议直连 StarRocks（如 DBeaver／mysql 客户端）导出，绕开 Superset 一层。
--       ⚑ 此法须另有连线凭证与授权，且导出格式须与现档【逐字节一致】方可混用，
--          故须先以第 100 批验一次 MD5（应为 e1503f6e29f189f9676fd0f7a7ee580f），
--          不合即弃 —— 格式一旦有别，前 416 批与后续便不能拼接。
-- ══════════════════════════════════════════════════════════════════════════════════════════════
