SELECT b.bet01,
       COUNT(*)             AS n_versions,
       COUNT(DISTINCT b.dt) AS n_dt,
       MIN(b.dt)            AS min_dt,
       MAX(b.dt)            AS max_dt
FROM   ods_mariadb_2b.ods_a168_bet02 b
WHERE  b.dt >= '2026-03-21' AND b.dt < '2026-08-07'
  AND  b.bet02 = '101' AND b.category = '1'
GROUP  BY b.bet01
HAVING COUNT(DISTINCT b.dt) > 1
ORDER  BY n_dt DESC, b.bet01
LIMIT  100;

00:00:04.219

查询无结果